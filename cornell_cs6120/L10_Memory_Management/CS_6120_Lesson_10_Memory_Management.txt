Title: CS 6120: Lesson 10: Memory Management
Video ID: 1_21p8mjsw
Detected language: en
------------------------------------------------------------

Hi! Today we're going to talk about memory management, that is, automatic memory management, aka garbage collection. The reason that we care about automatically managing memory is because manually managing your memory in programs is extremely hard and very, very, very error-prone. This example on the left-hand side comes from the Brill memory extension, for instance. In Brill, you can allocate memory and you can free memory, but nothing really stops you from freeing the same memory twice, which is definitely a problem at runtime. And in languages that make you manually manage memory, this is the sort of thing that's really easy to do. And of course, it's not just double freeze. There are all sorts of other kinds of bugs, really, really bad ones, that happen when you are forced to manually reason about memory. You can, for example, use memory after it has been freed and perhaps reused for some other purpose. You can read uninitialized memory. You can forget to free memory. You can extend over the end of some memory you are supposed to use and read someone else's memory next door, aka a buffer overflow. All these problems cause real serious issues for software engineering, which motivates the need for some sort of automatic support in getting memory management right. Just for example, last year there was this very highly publicized finding from Microsoft, the security people at Microsoft who found that enumerating all of their CVEs, which are these reports of security problems within Microsoft, fully 70% of them stem from memory management bugs in languages that are unsafe with respect to memory. That is that let you write the kinds of bugs that are possible to write in the Braille memory extension. I think it's useful to ask ourselves, just in general, what the current landscape is with respect to memory management. That is, all those bugs that have to do with incorrect management of memory, those come from languages that make the programmer manage memory themselves. There are lots of other languages that automatically do it. That's what this lesson is going to be about. I think it's instructive to try to brainstorm languages that fall into these categories. That is, ones with those problems, that is, ones in the manual category. Then there's two different subcategories of automatic memory management I want to think about. One is when memory is managed dynamically. That is, something happens at runtime to help automatically reclaim memory. Then there's something where all that reasoning is done completely at compile time. I'm not going to make you pause the video or anything. I just want you to think for a moment, as I write these things down, like pick your favorite languages, pick your top 10 favorite languages, and try to categorize them along these columns. The standard barriers for manual memory management are definitely C and C++. There are others, of course, but these are the famous ones and the ones where the vast majority of those 70% of those bugs we're talking about come from. Then there is dynamic and automatic memory management. This, I would argue, is where the vast majority of languages that you probably use fall. For example, okay, I'm getting tired of writing, but you kind of get the idea. Languages from Haskell to JavaScript all use automatic management of memory. The final category, static but automatic management of memory, where all the decisions can be made at compile time, contains approximately one standard barrier in a mainstream language, and that is Rust. You can find other examples. They're mostly research-y, however. The vast majority of languages that attempt to automatically manage your memory for you and therefore avoid those scary memory bugs are automatic and dynamically managed, and that's what this lesson is going to be about. In particular, we want to create a programming model that looks more like this, that is taking off on that Brill memory extension. What we really want is to be able to allocate memory and then use that memory and not worry about freeing it. That is, never write down a free call and allow a combination of the compiler and a dynamic runtime system to free it whenever it deems necessary, whenever it decides to free it. Crucially, it should never free it before we actually need to use it again. Just as a quick reality check for why I'm emphasizing this dynamic management of memory, you can consider a little bit what it might look like if we tried to introduce completely static management of memory. That is, if we tried to guess when to free memory just by analyzing the program, instead of doing some sort of accounting at runtime. It's not that hard for this little program. That is, we can definitely tell we're done with this memory right at the end, if this is truly the end of the program, but let's consider a slightly more complicated example. This is like kind of pseudo Java syntax. We'll make a hash map, we'll make an object, and we're going to put it into the hash map, depending on taking input from the keyboard, for example. Put the thing into the hash map, and then we'll take input from the keyboard again to decide whether to overwrite the same key to store null. That is, to throw away the object something before finally iterating over the entire map and using the stuff. The question is, how will the compiler know at any given point, somewhere in the middle here, whether the something object is going to be used again in the future? That is, whether it's going to be used in one of these calls to dot use. Is it going to be used in the future here? Of course, that decision depends on two things that make it hard to decide. First, there is taking input from the user. We can never really be sure what input the user is going to provide by typing on the keyboard, so the compiler can't possibly know the answer. And the second is, it needs to know something about the semantics of hash map. That is, it needs to know that overwriting with the same key, which again might come from some unknown place, that that is going to destroy the object and we're no longer going to need it in the future. So all of these reasons about analyzing a whole bunch of code that seems irrelevant to the task at hand, and also dealing with dynamic behavior, mean that it's really, really hard to decide where to free memory if we have to do it completely statically. And that's why this lesson is primarily about the dynamic automatic memory management techniques, which are collectively typically called garbage collection. In garbage collection, that is, in automatically managing memory, the idea is that instead of doing what we really want, which is to free memory the moment that it is certain we're never going to use it again in the future, which that's too hard, instead we'll do something a little bit easier using the evidence actually available to the compiler and the runtime system, which is that we will free memory after sometime after it becomes unreachable. So we need a definition of unreachable. That depends on sort of viewing the data structures that exist in a program as a graph. And what we mean is the graph theory notion of unreachable in that graph. More intuitively, it just means that it's impossible for a program to diverse any pointers through its data structures to arrive at the point where it can refer to a thing. And if that's not possible, that is, it's not possible through any path through the program to get there, then that thing is classified as unreachable, and therefore can be freed by a garbage collector. So here's a picture of how the data structures in a program might look and how to view them as a graph. So there are variables involved here. There's like, I'm thinking of these as local variables. They can be normal integers, in which case the garbage collector doesn't really care about them, or they can be pointers, in which case they point to other stuff. There can also be global variables, aside from just the stack local variables. And then the actual objects involved can be things like objects with multiple fields, like in object-oriented languages, or there might be arrays that also contain pointers. But in any case, the values inside those objects can themselves be primitives, like they could just get an int, for example, or they can point to other objects, like this field of object B points to array A, and this field of array A points to itself. This, I hope you can see, is how you form a graph. That is, the values that can contain data, those are the vertices in the graph, and then where the pointers within those values point to, those are the edges in the graph, and self-loops are allowed. Broadly speaking, it's really helpful to divide up this memory graph into two main things. There is the heap, that is like the stuff that is allocated off of the stack, that this is the stuff we will eventually want to free, and then there are the variables that can refer to that, that can refer somewhere into the heap. In particular, these are the local variables from the stack and the global variables. Those can contain pointers to stuff within the heap, but we're never going to free a global variable by itself. That exists forever, and we're not tasked with freeing local variables. Those are going to go away when the stack frame returns. We're only considering those things because they can point into the heap, and that's where all the challenge exists. That is, that's where we need to decide when to free values. Those things on the left-hand side here, that is the local variables and the global variables, are typically called the roots, because that's where we'll start looking for live stuff. That is, the question is, is your value reachable from the roots? And then, of course, the rest of the stuff that we may or may not free, that's the heap. Here's a little bit of fun garbage collector terminology for you. People will often refer to the algorithm that's looking for stuff to free. That's the collector. That's the runtime system that decides when to free stuff. I think it's hilarious that in the garbage collection literature, the other stuff, that is the actual program you're trying to write gets this kind of diminutive name, the mutator, that is your program you're actually trying to run the whole reason we're doing this. That's the mutator to distinguish it from the collector. Then the terms live and dead are pretty similar to the terms that we used for local variables when doing our data flow analyses, for example. Live just means it's going to be used again, a thing is going to be used again sometime in the future, and dead means the other thing. So I want to introduce you to a big distinction within the world of automatic memory management. There are two main lenses through which you can see the task of trying to find unreachable values and calling free on them. I'm going to distinguish these two things along several different axes and try to explain them both at once. The two main categories here that I'm talking about are called reference counting and mark sweep or tracing. And very briefly as an overview, reference counting says I'm going to keep a number associated with every value that indicates how many references there are to it, and when that drops to zero I know I can free it. And mark sweep says I'm going to start at the roots and do a kind of graph search from there and everything I can find from it I will keep and everything else I will free. I'm going to go through these step by step and hopefully you get an idea of how these are kind of related but different from each other. All right, my cat Lefty got really excited about this distinction between reference counting and garbage collection, so she's going to be with us for the next little bit of the lesson. Lefty, say hello to 6120. Say hello to Lefty. Okay, so as I said the first difference between reference counting and tracing is that with metadata they have to keep along with every object that they might want to free. So reference counting tries to keep a value associated with every node in the graph, that is every value we might collect, that represents its in degree, that is how many references, how many pointers are pointing into that node, who might have a reference to that thing. Mark sweep or tracing or sometimes it's just called garbage collection doesn't have any extra metadata instead it just instead it's just going to analyze the graph. The second difference is when these two things run. Reference counting runs every time you change a pointer, that is every time you take a pointer that pointed somewhere, that you create a new pointer or you modify a field of an object, for example, that contains a pointer to point somewhere else, the reference counting has something to do, that is it needs to change some of those reference counts in some of the values in the variables. It needs to decrement the value of the thing it used to point to and increment the reference count of the thing it now points to. When mark sweep or tracing gc runs, it runs just periodically, that is like it kind of doesn't really have any bookkeeping to do so it's just like whenever you get around to it just call me back and I will run and try to find the garbage and delete it for you. At that point, however, it has to do something quite a bit more complicated, it needs to do some sort of a graph search, that is it needs to actually search, that is it needs to actually view the heap as a graph that you can run just a normal graph algorithm on it, like DFS or BFS or something, and it needs to create a data structure at that point where it keeps track of whether it has reached every single object to the entire heap. So it needs to like say I have reached this thing and this thing and this thing, but during the course of this BFS that I'm doing or whatever, I haven't yet reached this other object over here. And then finally, when that whole thing is done, it locates all the values that it has not reached, that is like where that bookkeeping bit is zero and it calls free on them. The next thing in these two columns is a little animation of what's going on in the heap when these two different algorithms run, that is when reference counting runs on a reference update and when mark sweep tracing GC runs periodically whenever it's invoked. So let's look at reference counting first. This scenario that I'm setting up is a tiny little heap that has two different roots pointing to two different objects that currently have a reference count of one. Then what the mutator does, that is what the actual program does, is it changes the value of one of those roots to instead of pointing to the second object, it now points to the first object. What reference counting now needs to do is of course count those references, that is on every pointer update it has to do this thing. It has to look at where the value where the pointer was previously pointing and take that count and mark it down as and decremented and it needs to look at where the reference is currently pointing and just take that value and increment it. So now that's two. The next thing it needs to do is look at where it previously just moved the the pointer from where it just did the decrement and see if it hit zero and if it did then we need to call free. So we go from this picture to this picture where that value that just became a reference count zero that now freed and we have one more thing to do which is to recursively call ourselves and do that for the value we just freed. So if our heap in fact looks like this, that is like this now zero count object is pointing to somewhere else and then after we delete it we have to do the exact same procedure for the pointers we just got rid of as part of this object. So recursively notice that this used to point to this object it now points nowhere so all we need to do is decrement the value on that object it becomes reference count zero and now we will free it. That's quite a bit different from the picture that that that tracing GC uses. Typically people talk about during the process of the of doing a tracing GC of nodes having different colors that is just to indicate the different state that they are in. The colors that people typically use are gray white and black where gray means we are currently working on visiting the thing that is like this just sort of implicitly stores the work list of the algorithm stuff we still have work to do on. The white vertices are values on the heap that are unvisited that is that they might be deleted if we end and things are still in the white state. Black means we are done visiting and we have no more work to do and also because we visited it of course we're not going to free that thing because we encountered it during during visiting the entire heap. So the heap starts out the the graph of memory starts out looking like this where the entire heap is colored white meaning we haven't gotten there yet and all the roots are colored gray meaning that they yes they are alive because their roots and roots are always alive but we haven't finished visiting them yet that is we still have work to do in order to traverse the references coming from these roots. The first step then is to visit those roots and then everything you do like during the search is just take a given node that was gray color it black and then color everything that it points to gray that's kind of the whole thing so we will color the roots black and say the stuff that they point to now that's gray the next frame just has those things are now black this one didn't have anything to point to so we're kind of done with that one this one points to here so this one's now gray and if you recursively do this over and over again until you have no more gray nodes then you will have the black nodes that were reachable from the roots and the white nodes that are not reachable from there and the only step that you have to do after this is just look for all the white nodes so you need to have a way to do that and you call free on all of them so that is the big distinction that i want to leave you with today the rest of this lesson is going to be further distinctions on the side of mark sweep or tracing garbage collection but this is the big difference when you start out and to add automatic memory management to your language do are you going to count references and do that kind of like eagerly on every reference update or are you going to do it kind of lazily and try to find the garbage by doing a big heavy graph search periodically instead of on every reference update like you to think for a little bit about whether the one of these is better than the other that is i would like you to think of as many reasons as you can to prefer reference counting or to prefer mark sweep pause the video now and write down your reasons that is really try to be positive about both of them i would love to hear what you came up with by posting in zulip especially if it doesn't match the like very classic uh the conventional wisdom that i'm about to spout for the strengths the relative strengths of these two techniques one nice thing about reference counting is that it is deterministic in the sense that it doesn't depend on someone else deciding when to run the garbage collector it runs immediately when the mutator changes a pointer so it's really easy to see from looking at your program what's going to happen and that's not really true with mark sweep which can run at any time and spend as long as it wants and find as much garbage as it can the second big advantage that i think reference counting has is that it is pretty damn simple that is it's pretty easy to see how to implement it it's pretty easy to understand how it works and there's not like a bunch of complicated graph traversal algorithms going on behind the scenes mark sweep on the other hand is going to be the subject of the rest of this lesson for a reason it creates a huge design space of possible choices and there's many different ways to do it all of them more complex than the last one reference counting at the very least is quite simple mark sweep garbage collection however has one really really really huge advantage over reference counting which is dealing with cycles if for example you have a situation where your your roots don't actually point to a data structure but everything in that data structure points to itself if you just have a cycle in the heap graph then it is definitely the case that that entire day structure all three of these nodes in this little example are unreachable and you should free them however in the reference counting world all of these have reference count one because they have in degree one so a perfectly good reference counter can't find this garbage this is a huge fundamental limitation to the way the reference counting works and it means that lots of languages out there that primarily depend on a reference counter like the main implementation of python for example also have a backup garbage collector that is a backup mark sweep collector that just works that is only there to try to find cycles and free those for you another really big advantage that that mark sweep collectors have is they make the the mutator fast by taking off bookkeeping from the critical path a different way to put this is that a big downside of reference counting is it has to do something on every pointer update that is every change every time you change a field with a pointer type every time you like delete a pointer or create a new pointer you have extra bookkeeping work to do and it's not really true with at least the purest definitions of mark sweep collection you can defer all that work to a different phase where you traverse the entire heap so those are the two big headline differences there's of course a lot of smaller ones and i'm really interested in what you wrote so i again want to emphasize that i am curious to see what you you wrote down as advantages and disadvantages on zulip if you can as i hinted at before the rest of this lesson is going to be about distinctions in the research about the ways to build mark sweep garbage collectors the first subcategory we will examine is conservative garbage collectors whereas what we saw so far in the example of a mark sweep collector is guaranteed to free all garbage that is to identify as white all those nodes that are unreachable conservative gcs do not do that they do guarantee as you would really really want that they don't free things that are still reachable however they don't guarantee that they free all the garbage that is all the uh the unreachable stuff it's worth noting that conservative gcs were invented later so we had to go back and retroactively classify normal gcs that is ones that actually free all the garbage as precise so the distinction is between precise and conservative gcs conservative gcs on the face sound really bad why would you ever want something to leave something in memory after it's no longer reachable that is to like to lose some data and never free it and indeed of course it's not a good thing to do that but there are real advantages that conservative gcs have the big one is that they can work on memory unsafe languages namely things like c and c plus plus where you don't know which things are pointers and which things are integers that is where it's possible to kind of like squirrel away pointers in data structures that the compiler has no hope of knowing whether they are pointers or not so the trick behind conservative gcs is they look at every word in memory and they try to decide just by looking at the memory whether it can be a pointer or not so you can see how you can do this in c and c plus plus even though it seems antithetical to the design of c plus plus that you should be able to do garbage collection all you have to do is scan over the stack that is where the roots might live and say does this word does this like a series of four or eight bytes depending on the machine that you're running on does it look like a pointer does that look like a pointer and the way that you can answer that is does it point to memory that is like addresses in the in the virtual address space of the process that first of all are live pages so if you just have like five for example you probably don't have a page mapped into your process that contains the address five usually they're much larger addresses than that where you have your actual pages and then secondly do they contain a value that do those addresses point to somewhere that we have previously mallocked so if you if you look at this this pointer it doesn't point to anywhere in the heap that is in point to any particular object that has been previously allocated but this one doesn't either it goes out of the range but this one if we have a object allocated at these addresses for example this thing seems to point here so conservative garbage collector says well this number looks like a pointer i must because i'm conservative i don't want to to free values that might still be reachable i have to assume that it's a pointer so i'll just assume that this is a heap object that i need to keep around that is that i need to mark as black instead of white and not free it so it's kind of amazing that it's possible to write a garbage collector that doesn't know what a pointer even is and just by looking at numbers it's possible to decide well what are all the things that i must keep around just to be sure that i'm not freeing memory that's going to be used in the future it is possible to create these things and people do actually use them in the cnc++ land there are actually other reasons to make conservative garbage collectors too for instance this is a really cool blog post from the developers of the javascript compiler at apple that is the one that lives in safari on macs and iphones the garbage collector for the javascript runtime uses a conservative garbage collector so here's the little sentence from their blog post where they say precisely that they're doing exactly this technique that is being used for cc and c++ where they can scan over the stack and registers conservatively just looking at everyone and deciding like does this look like a pointer that is without using extra metadata from the language to tell it whether it's a pointer or not my question for you is why would they do this that is in a language like javascript where you absolutely know because the compiler made them which things are pointers and which things are not which things are just normal integers how can you possibly why would you ever use a conservative collector you are free to actually read this blog post to find out but you're also free to just pause this video and think about it for a second what are some reasons that you might you might use a conservative collector in a language where it's not strictly necessary that is where the compiler can tell you which values are pointers and which are not i'm not going to tell you i would love you to post on azulip if you have any ideas about why they might do this or just to read the whole blog post and see if you can glean any thoughts about why the these developers of a javascript compiler used a conservative collector the next question you might have about conservative collectors is whether they would waste a ton of memory that is like sure you can do this but won't you be erroneously keeping around a whole bunch of data that is never pointed to just because the program happens to use an integer that looks like a pointer to that memory the answer surprisingly is no i'm pointing you here to a really cool paper you can read by hans bohm who's done a lot of the foundational work in this space who shows how common data structures are actually by their very interconnectedness robust to the kinds of errors that conservative gcs can make he defines this gc robustness criterion that says something about the shape of your data structure that if you satisfy this criterion then the extra space by a that can be taken up by a confused conservative gc is bounded by a constant factor and surprisingly most data structures in use today satisfy this criterion and this goes for a long way to explain why conservative gcs are actually practical despite sounding initially like a really weird idea one other especially weird fact about conservative collectors is they tend to work better on 64 bit machines than 32 bit machines fortunately we live in an era where most computers are 64 bit these days that is most of our desktops and laptops and phones and servers they're all running some sort of 64 bit architecture that is where pointers are 64 bit wide 64 bits wide and this in general makes conservative collectors work a lot better in the sense that they are much more precise than when they are on 32 bit the 32 bit machines for example i found this discussion on the the list serve about the go language super fascinating because it it has to do with like bug reports that people have been getting about retaining about their go programs using a whole bunch of more memory on when they're running on a 32 bit machine and then then they switch to a 64 bit machine all those problems go away and the reason is that go uses a conservative collector so i'd like you to pause the video now and ask yourself why this is that is why in general and this is definitely true empirically conservative collectors tend to be a much better idea on 64 bit machines than 32 bit machines the answer to this question shows up later in the same thread that is someone says that go uses a conservative collector and this is the key more values look like pointers in a 32 bit world that is when you have 32 bit integers and you have a like a world of data you've allocated that's just denser in a 32 bit address space than it is in a 64 bit address space where it's very sparse and it's unlikely for values to be confused for pointers so that's why this problem exists in go and why in general conservative collectors are better on 64 bit machines and which fortunately is the era we live in today so let's explore a few more distinctions in mark sweep garbage collectors one category is parallel collectors parallel garbage collectors are exactly how they sound they basically just use multiple threads that execute in parallel to do the mark sweep process this is a really straightforward argument we have multi-core cpus today why just run your collector that is the graph search algorithm that like marks things as white or gray or black why run that in a single threaded way why not use multiple cores to do it this is important but not all that interesting not as interesting as the next distinction which is a pretty ambitious idea called concurrent collectors whereas parallel collectors run the collector in parallel concurrent collectors run the collector and the mutator in parallel that is that those two things are running concurrently in separate threads or even on different cores as with conservative collectors when concurrent collectors were introduced we had to go back in time and rename every collector that existed in the past as distinct from that concurrent collectors are the opposite of what are called stop the world collectors stop the world collectors work exactly how you would expect the mutator runs for a while and then eventually the gc decides that it's time to run the mutator stops the gc runs and frees some stuff then the mutator runs for a little while longer and then we go through this sort of alternating process concurrent collector runs both at the same time again in different threads possibly on different cores so the mutator just keeps on running and the gc runs separately and runs at the same time as the mutator i'd like you to pause the video now and write down as many as you can the positives and negatives of running the collector concurrently with the mutator i think the advantages of a concurrent collector are fairly clear so i hope you got the same answer as me first is that you don't have to pause that is a really annoying thing about stop the world collectors is that they have to pause the execution of their program to do your work so for example if you're doing something real time like rendering frames to the screen in a video game it can be extremely annoying and perhaps even unworkable that occasionally just kind of out of nowhere you don't get to do your work anymore and you have to wait for the garbage collector to that's called a gc pause and it it can be damaging to many different types of applications other applications don't really care but ones that have to do with real time they really really care the second is it maybe they make the entire process go faster that is maybe there's some sort of end-to-end speed up by being able to use one core to do the gc and one for the mutator that may or may not be true but in an ideal world it would be because we have so much work to do and we can parallelize it across different cores the downsides are considerably more subtle and they are however substantial enough to prevent concurrent collectors from being the norm the first is that they need to synchronize that is you have to synchronize if we're whenever you're running two different things on two different threads perhaps on two different cores you need to have synchronization between the two you need to do some sort of hardware operation to make sure that they can see each other's updates and that's true of any parallel program you write including the parallel composition of a mutator and a collector this means that this whole bit about getting an end-to-end speed up is not so easy because you constantly have to be synchronizing you have to be doing all these synchronization and synchronization operations both in the collector and in the mutator so you might actually make both slower because they constantly have to synchronize whereas to stop the thread stop the world garbage collector just never needs to synchronize because it's running in the same thread it just gets to see a consistent snapshot of the memory state without doing any of these costly like mutex operations between the two contexts and the second and this is even more subtle is these are just ridiculously complex to implement that it's a pretty well received wisdom that concurrent collectors are incredibly complex to get right all these little bits of interaction with synchronization between the concurrent the collector and the mutator and having to do that out of way that works with the memory model of the underlying hardware make these extremely error-prone to get right they're a major target for formal verification for this reason because they're just so incredibly nuanced because of their interaction with parallelism which we all know is hard and hardware which is also hard here's yet another distinction incremental collectors are a different kind of collector that is a stop the world collector but it's kind of a less bad stop the world collector it tries to do not the entire tracing work in a single gc pause and instead tries to like sort of save its progress along the way and get more work done later so here's my little cartoon of what that looks like instead of having like big chunks of the mutator and then perhaps arbitrarily large chunks of collector time incremental collectors run a little bit and then a little bit more and then a little bit more so they kind of like advance the process of doing the tracing just a little bit in each in each gc pause which means they need to sort of like save the intermediate state of the processing so they can pick it up during the next gc pause and have that not be ruined by the mutator in the process but of course this can make a garbage collector much more predictable because it interrupts you for a small amount of time frequently instead of for a perhaps giant amount of time at seemingly random intervals a final big distinction i want to make is whether they use a technique that has lots of names it can be called compacting or copying or moving collectors and the main reason that these collectors exist is to deal with the problem of fragmentation in ordinary executions of programs you sort of start out with a completely empty heap eventually you fill it up and then you're going to do some garbage collection but that leaves you with a fragmented heap that is like there are free areas that you can allocate into but they might not be big enough for the thing that you want to allocate you may have like thousands and thousands of two byte holes in your memory but you can't use any of those to allocate a whole like one kilobyte chunk of memory even though there's in aggregate one kilobyte of memory available this can be a really big problem both for performance and for the overall memory usage of a program so a moving or a copying or a compacting collector tries to mitigate this by move as the name applies by like moving stuff around to compact things into as small places possible leaving you with a big contiguous area of memory in which to allocate large objects a compacting collector sounds really great but it has to deal with one enormous practical issue which is that it has to do something with the old pointers that are pointing to moved memory so i'm going to draw a little heap here say we've got like a few objects that point to each other this is a perfectly happy heap points all over the place these point to here and a moving collector comes along and says uh none of these nodes are dead however there's fragmentation so i would love to take this node that used to be there and move it somewhere else these pointers that used to point to that node are now wrong they point to memory that we have freed up for the purpose of getting rid of fragmentation so the question i have written in the notes here is what should compacting collectors do about this that is if you were a compacting collector how would you deal the problem with the problem of out-of-date pointers that come as a result of your moving memory around this is a terribly open-ended question but i think it's instructive to just ponder for a while so please pause the video go for a walk around the block and just think for a moment about what you would do to deal with this problem of dangling pointers i'm again going to refrain from giving my answer because i'm really interested to see what people say so if you do have a idea just about your first copying collector how it might deal with updating pointers i would love for you to write that down in a few sentences on zulip i want to show you a few strategies now for how to implement compacting collectors starting with one that i really have to emphasize because it's so simple it seems like a joke and it's not a joke it's actually something you can use and it is actually practical for a few reasons this kind of collector is called a semi-space collector because what it does is it takes all the memory that's available to a process and it immediately at the very beginning of the program divides it into two chunks and those chunks are called the from space and the to space with a semi-space collector the first phase is that you just let the mutator allocate things willy-nilly into the from space so you don't do any garbage collection at first you just sort of like let it whenever it makes a new thing you put it into the from space allocate allocate allocate make some big objects make some small objects nothing's being freed because we're not running garbage collection and eventually you run out of space so the moment that you can't allocate anything new in the from space it's time for the garbage collector to run and so you do the whole mark sweep thing you find all the stuff that is that you want to keep let's like mark some of these nodes as to be kept and a bunch of this stuff is now garbage and you do the following thing you find all the reachable objects that is the black nodes from the graph and you just copy them over to the to space which is completely empty at the moment you copy this over copy that over copy that over but we can leave behind all the white nodes all the stuff that is garbage that we didn't reach during the during the graph traversal and then here is the trick and here's what makes it sound like a joke all you do is switch the from space into the two and the two space so now this becomes the from space and it's where we're going to start allocating new objects as the mutator continues to run and the two space yes it contains a bunch of data but we don't have to worry about it anymore we don't have to do anything with that stuff we can just consider it garbage memory that we don't need anymore and the next time the garbage collector runs we will flip back the other way by copying things from the from space from the from space to the two space which was the old from space and we can keep on doing this forever every time we fill up half of memory semi-space collector is nice and elegant but i think you can probably see the problem which is that it requires you to double the amount of memory that you want to use in practice this is not so bad because all garbage collectors tend to have a significant memory overhead but as you might expect we can do a lot better than semi-space in the real world most compacting collectors today are generational generational collectors exploit something called in a very highfalutin way the generational hypothesis the generational hypothesis has this like very pithy but almost useless statement that says that most objects die young what the statement actually means is that and this is a lot less pithy but in my opinion a lot more useful is that recently at any given time when your garbage collector runs it is much more likely that you need to free recently allocated objects than ones that were allocated a long time ago this means that if you spend all your time looking over the entire heap looking for things to free you're wasting a bunch of time because most of the garbage you're going to find is among objects that were recently allocated so here's a kind of pictorial way of putting it that is like the the uh if we along the x-axis if we plot the age at the death of the object that is like when it becomes unreachable and will be collected and on the y-axis we're just doing a histogram so this is like the frequency of objects dying at that age for the most part things die relatively young and this kind of a long tail as we go out to things being older and older so maybe you can try to imagine why this might be true empirically it is born out this is true of most programs but like there's a lot of things that programs allocate and then they use for a little while and then they're done but if things survive for a while if they get out here they're much less likely to die in the next garbage collection because they've been around for a while they sort of like proved they're worse for the program they're going to hang around for a while you can probably imagine a lot of programs like have big data structures like this that is stuff that gets allocated and hangs around for a really long time and therefore if it wasn't freed last time and it wasn't free the time before that it seems pretty unlikely although possible that it needs to be freed in the next garbage collection and that the generational generational hypothesis is what a lot of collectors exploit in order to make their collection more efficient and to find more garbage faster one way to think of this is kind of generalization of semi-space collectors where there were two different regions in the heap and just copying things from one place to another and then back again generational collectors are like that except that they have a sort of a telescoping range of different areas of memory of different sizes and crucially different ages so you can view the heap pictorially as something like this where you have one small area where you do all your allocations and then once that fills up you move things into this next area generation one from generation zero and then as that fills up you move it on down to the next generation and then from there onto the next generation and the critical thing that makes this work is that we run collector collections at different frequencies on these different regions of memory so in this smallest fastest youngest generation generation zero which is sometimes cruelly called the nursery we will run collections very often because it is often possible to find a lot of garbage that has recently been allocated and then anything that survives out of the nursery we put into generation one and we run collections on that pretty fast but not quite as fast and so on and so on through as many generations as you like by tuning the number of generations you can kind of like categorize things by the probability that they need to be collected and then therefore tune the aggressiveness of your garbage collector accordingly that is like try to find the most garbage without spending a lot of time analyzing stuff that is unlikely to be free because it survived for longer so that's generational garbage collectors i want to take a moment to acknowledge that i've just thrown a whole bunch of distinctions at you so i thought it would be useful to kind of like go through these like telescoping distinctions in the way that we build memory management systems all the way from the idea of like well do you manually manage your memory or do you do something automatic at all all the way down to this fairly sophisticated notion of a generational collector so to recap the first distinction that i gave you was between doing manual and automatic memory management the standard manual memory management are c and c++ automatic memory management is most modern languages that aren't c and c and plus plus the next distinction is within automatic memory management are you going to do it dynamically at runtime or statically at compile time doing a complete job of automatic static memory management is an incredibly difficult problem that has broad range of implications for the design of the language itself which is why pretty much the only language that's even attempting to do this in a mainstream way is rust which is why most of this lesson is about the the predominant technique for automatic memory management which is dynamic aka garbage collection within dynamic automatic memory management we made a distinction between reference counting and tracing or mark sweep gc and i asked you to think deeply about what the advantages and disadvantages are of both a paper we'll discuss coming up very soon has to do with that kind of duality between reference counting and tracing gc i kind of went off onto an aside here within tracing gc there's several questions about like how you can implement a tracing gc including are you going to do it in parallel that is a multi-threaded collection is it going to be concurrent that is will the mutator and the collector run at the same time and will it be incremental that is will you leave work for yourself to do in the future within tracing i.e. mark sweep collectors the main distinction that we have is between precise and conservative collectors precise collectors are the normal ones conservative collectors are the strange ones that might leave some garbage around they sound very strange but they can work surprisingly well for memory unsafe languages like c and c++ and can even be useful for as an implementation technique even in safe languages within precise collectors there is the the big distinction is is whether you do compacting that is like whether you try to do move or copy data to avoid fragmentation i don't have a good name for non-compacting collectors within compacting collectors then finally we got to to the idea of doing a generational compacting collector so that's a big kind of decision tree here and i hope this gives you an overview into the wild world of a very very very large topic in the research on the implementation of programming languages
