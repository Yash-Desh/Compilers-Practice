Title: CS 6120: Lesson 3: Local Value Numbering
Video ID: 1_i2gnhw41
Detected language: en
------------------------------------------------------------

In this video, we're going to kill three local optimization birds with a single stone. The idea is going to be to shift our perspective on how we look at basic blocks so that several different forms of redundancy all become apparent at once and become easy to eliminate in a single pass. This technique is going to be called local value numbering. As you might guess, there is an equivalent called global value numbering that extends the same reasoning to work on entire functions. We're going to focus on single basic blocks for now, and then later you'll see how to extend this to deal with control flow graphs. To start off, I want to look at three different forms of redundancy that exist in programs, and I want to emphasize that each of these three things can be subject to optimization passes that remove them as their only purpose. That is, in some compiler courses, in some textbooks, you'll see each of these three things discussed as completely separate optimizations, and there are cases where it makes sense to think of them as totally separate optimizations. What we're going to do in this video, however, is try to see the underlying phenomenon that underpins all of them and does a pretty good job at resolving all of these forms of redundancy, and that's what local value numbering does. So let's look at these three programs. The first one demonstrates dead code elimination. That is, cases where there is code that has no effect, can't possibly have any effect on the output, and the optimization just consists of deleting the redundant instructions. The second one exhibits an optimization that is sometimes called copy propagation, which just says that if you have several instructions, several computations that reuse the same original value, it is often easier to just use that value directly. So in this function, we might eliminate all of these instructions and instead of using all those IDs, just print x instead. That optimization is sometimes called copy propagation. The third one, the third program here, demonstrates a different form of redundancy. This one, if you squinted it for a second, this is the long instruction-y way of saying a plus b times a plus b, and this represents an opportunity for what's called common sub-expression elimination, where these things are the sub-expressions that are common. That is, it's the same computation being done twice, and a better thing to do is just to do that addition once and then return the multiplication of that thing by itself. So we shouldn't need to do this sum two, but of course that variable is still referenced here. But if we were to just replace it with sum one, then we'd get the same answer. That's common sub-expression elimination, sometimes just abbreviated to CSE. So my deep philosophical question for you is to ponder what these three examples, what these three different forms of redundancy all have in common. That is, what is the fundamental underlying phenomenon that is making these programs redundant? There is no right answer to this question. I, of course, have an answer in mind to this philosophical imponderable, but I'd like you to think for a little bit just by yourself about what is the common thread between these forms of redundancy. I'll tell you my answer in a second. The reason that these programs all have redundancy is because they say to compute things using the variables, whereas it is equivalent to just look at the underlying values being computed. And if you do that, then these forms of redundancy kind of automatically disappear. So let's look at these programs again. A value numbering approach to optimizing this program will realize that the same variable is being used for two different values and try to disambiguate those two things. And then we'll end up with a program where there's two separate values, one of which is clearly unused. This makes it easy to eliminate that unused variable and get us down to a program that just assigns to A once. Looking at this second example, the one with copy propagation, the philosophy of value numbering, that again is trying to disentangle the values from the variables, says that there's only a single value in this program. That is the constant for, and it's being reused multiple times for multiple variables. And that's what looks like waste from a value numbering perspective. If we disentangle that single value from all those different variables, those other variables that can hold that value simply fall away. And we can see that the print statement should just be referring to the underlying value. Finally, let's look at the common sub-expression elimination example. In this one, the value numbering philosophy says that there are two variables, some one and some two, that hold the same value, that is A plus B. And of course, here we're getting into the tricky notion of how you define what a value even is. It's not just a constant. There's something underlying going on here about reflecting what computation is being reflected by the variable. But if you accept some definition, some careful definition of what a value is, then the value numbering optimization is going to look at this program and realize that the same value is being stored in two different variables, and that's redundant. And so in the computations that consume those variables, it suffices to just use a single one, and that will reduce and eliminate the redundancy. So let's see how the value numbering approach actually works. I'll give this idea to you at a very high level, and then we're going to walk through an example to see how it plays out. Finally, I'm going to leave it to you to develop the actual pseudocode and implement the algorithm. But the idea behind the value numbering as an optimization really revolves around one big data structure that we'll see here. There is a table, and this is where the name comes from, that's going to assign a bunch of numbers in the left-hand column, and by this I mean literal actual natural numbers, assigned to the values being computed. Values you can think of as expressions. I'll talk about the representation of values in a moment, but we're also going to maintain for every value a canonical home for where that value is being stored. So the very high level idea is that we will try to disentangle a notion of values, that is sort of expressions that are being computed, and also keep track of one place and one place only where that value is being stored. So for example, if we end up using the same value in two different locations, even if they're stored superficially in two completely different variables, this approach will be able to look through it and say, no that's actually the same underlying value, and instead of computing it again, we can just look at the canonical variable that's storing that thing already and just use that variable. One of the important things here is how to represent the computations, that is the expressions, the values that are being numbered, and as this table that I'm showing you indicates, we won't represent that in this kind of like funky form that refers to other rows in the table. So here we have this like number one plus number one expression. What that actually means is that this is the addition of value number one, which is currently stored in variable X, to value number one. So we're not going to, when we build up these values, we're going to be storing them as using these actual numbers that refer to rows in the table. So every expression that shows up in this column of the table will be referring to values by their numbers, rather than by their variables, and this sort of completes the disentanglement from between variables and the values that they're stored in. By referring sort of, by having self-references back to this table to say like, well this is one big value that's computed out of several smaller values, then we can live fully in the abstract realm of computed values and maintain a mapping to where those values are stored in variables. This all sounds fairly abstract, and I swear this is going to get more concrete in a second when we go through an example. However, there's one more piece to the data structure here. This is sort of the main table data structure, but there's one other thing that the algorithm is going to need, which is we're going to step through the basic block, instruction by instruction, instruction, keeping track of kind of the imaginary environment that would be happening if we were actually executing the block. So it's going to be like executing the program. We'll step through and sort of keep track of whatever variable holds, but instead of like in a normal interpreter where every variable will hold like an actual concrete value, like five, they'll hold this notion of a value that is one of these expressions. So this cloud I'm showing off to the left-hand side here is going to say that we'll have an environment so after we execute a statement that assigns into the variable A, we'll have this environment that says that, okay, A now currently holds a value number two. So it's going to work exactly like an interpreter, but instead of holding actual concrete runtime values, values here will just be the numbers, that is like the indices into this table. And this way we'll be able to keep track of a model of what computations go on in a basic block, just like an interpreter does, but using, but statically, that is without actually knowing what the inputs are. I hope you've had a moment to ingest that very high-level description. We're now going to go through a concrete example that will hopefully make these things a little bit more clear. And this is a pretty tricky and abstract way of thinking. So if you have any questions at all, I encourage you to either bring them up on the next Zoom iteration of this class or to ask questions in Zoom Up. You really, really got to ask questions if you have them because I know this is not easy to understand. So we're going to start out with this program on the right-hand side. So this is the common sub-expression elimination example. And we're going to start executing this program.
