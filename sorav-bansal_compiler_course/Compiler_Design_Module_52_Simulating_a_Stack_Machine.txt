Title: Compiler Design Module 52 : Simulating a Stack Machine
Video ID: QXKe0_q71V8
Detected language: en
------------------------------------------------------------

So last time we talked about a stack machine but now let's look at a typical hardware machine and as you may know a typical hardware machine has both a register file which is a set of registers for example a typical machine could have 8 16 32 or 64 or you know some some small number of tens to maybe hundreds of registers and then a memory space okay and the registers are basically you know you can name the registers and say I want to access register let's say 10 that makes sense but for memory you basically say that I want to access register let's say memory location 10 and and that's also fine just that memory has a much bigger size and it is possible to access memory using indirect mode so unlike registers where I have to explicitly say it's registered 10 I can't say tell me the register that is stored in give me the register contents of the register whose number is stored in another register where I can do that for memories I say give me the contents of memory at location x where x is stored in register r10 right so so memory has this extra advantage that it gives you indirect access on the other hand memory and memory is also larger on the other hand memory slower than registers so while registers can be accessed in a single cycle let's say the memory would typically require tens to hundreds of cycles to access now our first attempt to do code generation for a real hardware machine would be to simulate a one register stack machine on a hardware machine right so we're going to build this code generation algorithm step by step and our first attempt is just going to simulate a one register stack machine on a hardware machine so we're going to pretend that there are two layers one is the so first level is the high level programming language that we want to generate code for the second layer is the one register stack machine so we first lower the high level program into the one register stack machine and then we lower the one register stack machine to a hardware machine so in other words we simulate the one register stack machine on a hardware machine so we added one extra layer between the high level program and the actual hardware machine and and so and so this constrains the possibility so things that i could have done if i had directly generated code for the hardware machine are now not possible because i do first generate code for the one register stack machine for example i may not be able to use all the registers available in the hardware machine but we're going to come to that later let's first understand how code generation would typically work for for a high level language so what are some typical hardware machine architectures one of the prototypical architectures for a reduced instruction set computer or RISC is the MIPS architecture so what does the reduced instruction set computer mean it's basically you know there are two large categorizations of computers based on the instruction set that they support and this is the reduced instruction set the other one is called the complex instruction set or CISC so what does the MIPS architecture have so MIPS architecture is a reduced instruction set computer it has 32 general purpose registers each of them is 32 bit long there are explicit load and store op codes so if i want to read from memory there are explicit load and store op codes which you are going to read a word from memory and put them in a register and i can use direct or indirect mode of addressing to address memory but if i want to read memory i have to use these explicit op codes load and store i cannot just you know directly add to a memory location or something like that most of their op codes operate on registers so if i want to do any other operation for example if i want to do addition or multiplication or division it has to operate on registers in this in this architecture so if i wanted to do something for addition of two memory locations then i would first need to load those memory locations into registers and then do the operation on them so here are some examples load word so LW stands for load word load word loads into register one the contents of memory from this address and so this address basically represents reg2 plus offset all right so this is the offset mode of addressing the memory offset brackets reg2 and this represents reg2 plus offset and notice that this is you know you can use this way of addressing to do both direct and indirect addressing so for example if reg2 was nil which means i don't specify any register then it becomes a direct mode of addressing i'm just specifying the address that i want to access the memory using and if i if i use reg2 then it becomes an indirect access if i use offset equals zero then it's just indirect access otherwise it's offset in direct access here's another example of instruction add reg1 reg2 reg3 that adds reg2 and reg3 into reg1 notice that this opcode operates only on registers and cannot access memory locations directly in contrast to the MIPS architecture there is the x86 intel x86 architecture that's actually very popular architecture in terms of the market share that it captures it's a complex instruction set computer which is just the opposite of a reduced instruction set computer it has eight general purpose registers each of which is also 32 bits long just like the MIPS architecture but the important thing is that opcodes can operate typically both on memory and registers memory and registers for example i can just say add memory address to register which stands for reg gets reg plus memory address right so which means it actually does both a memory address and an addition in a single opcode if i were to do the same operation in a risk computer then i would have to have two instructions first to load and then to add as a result there are actually 400 plus opcodes in the x86 architecture whereas in something like MIPS there's much fewer of opcodes typically on the order of 40 to 100 opcodes that you would have so almost an order of magnitude difference between the number of opcodes you may think you may imagine that x86 would be a faster architecture because it can do more in a single opcode but it is also the case that simple opcodes allow fast allow simpler machines and and so they consume less power typically arm is an example of a risk computer x86 is an example of a sys computer sys computers have a lot of need to have a lot of circuitry to basically optimize their operations for example in this case a single opcode needs to do a lot of things and so while they can often have better performance than risk computers they also are very power hungry here are some relevant MIPS opcodes that we'll need for our course load word reg1 offset reg2 and here is a meaning represented in a c like syntax reg1 gets star reg2 plus offset this is load word we have already seen this before then here's another opcode add reg1 reg2 reg3 that this stands for reg1 gets reg2 plus reg3 notice that the destination operand is always the first operand and then the source operands are the other operands store word store word reg1 of reg2 this is an exception to that rule that the destination is the first operand here the destination is the second operand so this basically stores the value in reg1 to this address in memory reg2 plus offset so it says star reg2 plus off gets reg1 there's another opcode add image8 so unlike add which is just going to add the registers this one adds an image8 value to register this is add image8 and u stands for update so add image8 and update reg1 reg2 m so reg basically stands for reg1 gets reg2 plus the image8 value m and then load image8 which is load image8 reg1 m which stands for reg1 gets this image8 value m so once again summarizing lw stands for load word add stands for add store word add image8 and update and load image8 now how are we going to simulate a stack machine on a MIPS machine so we're going to use three registers sp for the stack pointer a0 for the accumulator and t1 for the temporary register recall that because we are using we are simulating only a single register stack machine one register stack machine only we only need three out of the 32 registers available on the MIPS architecture for for our code generation strategy so here's the invariant that we're going to follow the stack will be maintained in memory so recall that in the stack machine the stack was abstract quantity which to which we could push and pop but now we're going to assume how we're going to implement this stack on a real machine we're going to implement it in memory notice that in memory i can actually do random access whereas in the stack abstraction i can only do top of the stack access but in case you know memory is a more general abstraction and so a stack can be implemented in memory and the stack will grow downwards as has been our typical convention the register dollar sp will point to the next stack location all right so and dollar sp plus four would point to the current top of stack so i'm going to assume that all my values are four bytes long or 32 long and so i'm going to use four uh you know an offset so every value is four bytes long and sp plus four is going to point to the current top of the stack if i want to know what is the value at the current top of the stack then that's i can get that by dereferencing the address sp plus four on the other hand sp will point to the next stack location so if i want to push something then i would put it at sp and then maybe decrement sp by another four bytes the accumulator is maintained in the register a zero one of the registers on the mip machine and so this is what a simulating a stack machine on a mips machine would look like this this on the left hand side we i show the code for the stack machines for example accumulator is being assigned the value seven and pushing the accumulator to the stack accumulator being assigned the value five then you are doing executing the add-op code that's going to pop off the top two words of the stack and then push the result and then ultimately we pop off the top word of the stack to reduce the size of the stack to simulate this accumulator getting seven is just the load immediate instruction that's going to load the value seven into a zero which is what is emulating our accumulator push accumulator is just going to do a stored word of dollar a zero to zero dollar sp recall that zero dollar sp is basically the next thing that needs to be addressed at the next stack location and so we just store a zero into the next stack location and then we update the stack location by four to basically represent the fact that the stack has grown by four bytes so add immediate and update is being used here so here sp would get sp minus four similarly assigning to accumulator to five is just load immediate a zero five add is going to do load word of the top of the stack recall that the top of the stack is four plus sp so that's what four dollar sp represents but i'm going to put this in a temporary register i don't want to clobber my accumulator because accumulator already may have some useful information in fact in this program it does and then i'm going to say add a zero a zero t one that's going to add a zero and t1 and put it in a zero and that's exactly the emulation of the add opcode using our invariance and finally the pop stack opcode is going to just increment sp by four using the add immediate and update opcode
