Title: Compiler Design Module 161 : Introduction to Pipelining
Video ID: di_kFnEhSS4
Detected language: en
------------------------------------------------------------

Today I'm going to talk about a different parallelization scheme for a loop nest, program with a fine loop nest, which is very different from all the synchronization-free and synchronization-based parallelization that we have seen so far. Recall that when we talked about synchronization-based parallelization, we were talking about a constant amount of synchronization and we used barrier synchronization in that case. And barrier is a very tight form of synchronization because it requires that all the threads of the program synchronize at that particular barrier point before they start on the next segment of the other partition, the other side of the barrier. I'm going to talk about pipelining and you may have come across pipelining in your other courses such as computer architecture where we talk about a five-stage pipeline in a processor and the idea that we're going to use is similar except that we are going to do this at the software level, at the compiler level and the trade-offs and the kind of improvement that you can get by pipelining at the compiler level are very different from the kind of improvements that you get by pipelining at the hardware level but fundamentally speaking, theoretically speaking, it's the same concept. So let's look at an example to understand pipelining. Now this loop, I mean if I just basically use my synchronization-free algorithm, it is a synchronization-free parallelization algorithm, then it's very clear that the outer loop is already parallel and so I can pretty much parallelize the outer loop, different iterations of the outer loop can be executed in parallel on different processors but for a single iteration of the outer loop, the inner loop is data dependent because it is reading and writing to the same x i element. Now this is parallelizable except that if y is laid out in a row major order, so notice that the access to y is not i comma j but j comma i and this is deliberately done because the inner loop is iterating on j and it's not the innermost dimension of the array access for y which is j, so j is actually the outer access which basically means that if y is laid out in row major form then at every access of y in the inner loop, I'm going to have access a different cache line because the first time I'm going to access let's say y 0 comma 0, the second time I'm going to access y 1 comma 0, so y 0 comma 0 and y 1 comma 0 are significant distance apart in a row major layout of the array, 0 comma 0 and 0 comma 1 are adjacent but 0 comma 0 and 1 comma 0 are not adjacent, in fact they have a distance of order n and n assuming is a big number that basically means that they are actually very far apart and if 0 comma 0 and 1 comma 0 are very far apart basically both of them are going to cause cache misses because they cannot belong to the same cache line and so what happens is that every processor on every execution of the statement basically incurs a cache miss because of the access to y j i because j is the inner loop and yet j is the outer dimension of the array access and so there's a cache miss on every access for every processor and while it seems you know that we have parallelized the outer loop and you know we should be able to get order m speed up because we can create m different number of threads for the outer loop basically because of cache misses what can happen is there can be serialization at the memory because there are accesses to a different cache line on the each inner loop iteration they are cache misses and because there's no spatial locality and because their cache misses they're going all the way to memory and even though the processors are executing in parallel because all the requests from all the m different processors are get going to memory the memory get get overwhelmed another way to say this is that each of these different m processors had their own caches for example l1 cache l2 cache and so on let's just call that the l1 cache so each processor has the l1 cache but that cache has not been utilized because those caches are basically giving a miss on every iteration of the loop that each processor is executing ideally i would have lied that all those caches are also getting used and this depends on what is the cache miss cost so if the cache is you know if the memory and the cache are very close together then maybe it doesn't matter or if the memory already has a lot of parallelism maybe it doesn't matter but imagine a scenario where the memory is actually remote maybe the memories through a very thin link over the internet and so every cache miss is going to actually become really expensive and what i've done is basically every processor is going to make cache misses and i have actually increased the number of cache misses potentially because i have each processor basically has less has greater use distance and so if a cache miss is very expensive then parallelization may be counterproductive because consider this case where cpu 0 and cpu 1 are accessing and cpu 0 basically is accessing yji for different values of j then each time cpu 0 access yji then it causes a cache miss and similarly whenever cpu 1 accesses yji for another value of j then again it causes a cache miss and so all these cache misses are going to memory and the memory can get overwhelmed and all the computation get get bottlenecked by the memory speed and get serialized at the memory and once again it really depends on the underlying architecture and the hardware and the environment that we are working on but maybe you know on some machines outer loop parallelization would be better on other machines outer loop parallelization would be counterproductive and as a compiler developer our job is to basically come up with algorithms that given a cost function for the underlying architecture we are able to identify what is the best possible transformation for the program all right i'm not going to really worry too much about the cost function i'm going to assume that the cost function is given i'm also not going to worry about how to use the cost function to choose which algorithm to use but for now i'm just going to worry about what are the different algorithms so we have already seen one algorithm which is parallelizes as much as possible without any without paying without paying any heed to locality without paying any heed to how many cache misses each process is going to have but now we basically are going to try and see assuming that the cost function is such that you know cache misses are expensive then how else can i transform the program so that its parallelization is not counterproductive but parallelization is actually beneficial and the scheme that i'm going to discuss that the solution to this is going to be pipelining as i'm going to talk next so the question i want to really ask is is it possible that the jth processor where you know for let's say i just basically parallelized my program across k different processors and among them the you know the some jth processor accesses consecutive elements for example it accesses yj comma 0 yj comma 1 yj comma 2 and so on in which case i have very high spatial locality because yj comma 0 and yj comma 1 they are going to be belong to the same cache line assuming row major layout so and similarly yj2 so i'm going to basically be using my cache in a much better way my caches were designed to exploit spatial locality and if i am able to basically organize my program such that each cpu is basically accessing consecutive elements then i am basically utilizing the cache caching hardware available to me by increasing spatial locality whatever possibly the jth processor can access x0 x1 x2 x3 and so on across different iterations of the loop that is execute for example it can access it can read x yj0 and write to x0 on the 0th iteration read yj1 write to x1 on the 1th iteration and so on and overall if this was possible then i would have better cache usage for each cpu because i would have better spatial locality but there is an obvious problem here and the problem is that there are dependencies so there are dependencies for example from x0 to itself right so x0 to x0 so you know one iteration of x0 so cpu j is also accessing writing to x0 and cpu j plus one is also accessing writing to x0 and cpu j plus two is also accessing writing to x0 and so on and so you know it can't be completely synchronization free but the good thing is that you know after cpu 0 has written to x0 it doesn't really care i mean after that it doesn't really touch x0 after that you know cpu j plus one can write to x0 and after that cpu j plus two can write to x0 anytime it likes right so so that's the idea i'm going to basically pipeline these accesses to x0 by different processors so here's the here's the scheme let's say cpu 0 says x0 plus equals y00 now i want to point out that you know i'm using the plus operator and the equality operator and the you know xi these are i mean this particular program can be parallelized using other ways as well for example like you know i can exploit the commutativity and associativity property of the plus operator or i can basically exploit the fact that xi can be privatized and things like that but this is just you know i i mean this is just a placeholder so plus is just a placeholder for any operator right it could be some complicated operator and xi is just a placeholder for some memory access which is basically being accessed but in the which basically whose whose address depends on the index outer index and not the inner loop index okay so cpu 0 is going to access x0 plus equals y00 the second iteration of cpu 0 is going to do x1 plus equals y01 x2 plus equals y02 and so on notice that for cpu 0 there's very high cache locality because when i access y00 i bring the entire cache line in and now y01 would be present in the cache because y00 and y01 are adjacent elements similarly y02 would also likely be present in the cache and similarly if i look at x0 x1 x2 they are also adjacent elements and so they also exhibit high spatial locality but now the only problem is synchronization because if cpu 0 is touching all the elements of x then cpu 1 will also touch all the elements of x and and so i need to basically make sure that things remain consistent and and so cpu 1 will also do let's say x0 plus equals y10 instead of y00 x1 plus equals y11 x2 plus equals y12 and the only thing i require in this so this basically gives me better spatial locality but the only thing i require is that the first iteration of cpu 1 must execute after the first iteration of cpu 0 similarly the second iteration of cpu 1 should execute after the second iteration of cpu 0 and similarly the third iteration of cpu 1 should execute after the third iteration of cpu 0 and other than that i don't have any constraints and to basically represent these dependencies i have drawn these lines which basically say that this i mean x0 plus y0 must execute before x0 plus equals y00 must execute before x0 plus equals y10 and once again i want to emphasize that plus is not your you know your addition operator is just a placeholder for some operator which may not be commutative and associated and similarly cpu 2 would basically be similar except that it would be accessing just like this was accessing y00 one was accessing y10 so 2 would access y20 2122 all right and x0 x1 x2 so each of these processes is accessing both x and y but they are accessing it in a spatially local manner all right so this is pipelining right because what i have done is i have basically said that okay let's divide so let's say that the outer loop was one task right so the one iteration of the outer loop was basically one task and so i have divided the task into stages so x0 x1 x2 x0 plus equals y00 is x0 plus equals y10 x0 plus equals y20 and basically what i have done is so this whole thing is this is one outer loop iteration right so this whole thing here is one outer loop iteration and this is one outer loop iteration and what i've done is i have staged this outer loop iteration so the first inner loop iteration of the this outer loop iteration would execute on cpu 0 the first inner loop iteration the second inner loop iteration of the first outer loop iteration would execute on cpu 1 the third inner loop iteration of the first outer loop iteration would execute on cpu 2 and so on okay and so this is basically saying that let's assume that you know this whole thing was one task, I'm saying that the first stage of the task would execute on CPU 0, the second stage of the task would execute on CPU 1, the third stage of the task would execute on CPU 2 and so on. Similarly, the first stage of the second task would execute on CPU 0, the second stage of the second task would execute on CPU 1 and the third stage of the second task would execute on CPU 2. So what I'm doing is I'm basically dividing the whole task into different stages and basically saying that you know let's specialize CPU 0 for stage 1 let's specialize CPU 1 for stage 2 let's specialize CPU 2 for stage 3 and so on right and so basically CPU 0 is going to execute stage 0 of task 0 stage 1 or stage 0 of task 1 stage 0 of task 2 stage 0 of task 3 and so on and so CPU 0 is basically a stage 0 specialist which means it is it's a specialist for the case when j equals 0 similarly CPU 1 is a specialist for the case when j equals 1 and when I say specialist I'm basically talking about the fact that I've been able to bring that particular cache line into my CPU and so now I'm able to run with it in a faster manner and this first of all I want to point out that this requires order n synchronization because the number of these gray lines that I have drawn are order n and so that's the amount of synchronization I need although this is a loose form of synchronization unlike barrier I wanted all the threads to synchronize simultaneously here I'm basically saying okay CPU 0 signaling CPU 1 so that CPU 1 can execute any time later so if CPU 1 was slower then you know it can also execute later it doesn't really matter so it's a loser form of synchronization it's not like everything has to execute in lockstep things can happen in a more like a wave front so I can just say it's like passing the baton in a relay race as a CPU 0 said okay I'm done with stage one stage zero you can execute stage one whenever you like and so on so we're going to see how we're going to do this synchronization using weight signal primitives signal and weight condition variables if you were aware of them and but the point is that these this order n synchronization and the idea here is that I have one task which is basically this you know one task is basically it's computing x0 and it has n stages and I execute each stage on each processor and by executing each stage on each sub processor I'm specializing each processor for that particular stage and in some cases that specialization helps in this case the specialization makes better use of the locality available locality optimization available in the cache and this is no different from how a five stage pipeline works in a machine architecture so recall that in a machine architecture you have different instructions so each instruction can be thought of as a task and so each instruction can be thought of as the outer loop so maybe there are n instructions I first execute the zero instruction then the first instructions the second instruction and so on and in the architecture one way to organize things would have been okay I have m different processors and you know instruction one I have one CPU which basically does fetch decode execute memory and write back for the first instruction the second processor does everything for the second instruction and so on but that's not what we choose to do we instead choose to pipeline because that may give us better specialization because you know one part of the hardware is just specialized to do fetch the other part of the hardware is just specialized to do decode the third part of the hardware is just specialized to do execute and they do it in the fastest possible way and then what we do is we so these are the stages so for every task so every instruction is a task and every operation that needs to be executed for every instruction is a stage and so what I do is I basically pipeline the stages so for example for the first instruction fetch is executing on cpu0 decode is executing on cpu1 execute is executing on cpu2 and so on just as a metaphor right so when I say fetch I'm basically saying oh x0 is updated you know y00 is basically read in x0 y10 is read in d0 and y20 is read in e0 but basically you know each of these kind of cpus are specialized in that particular job for example cpu1 is specialized in the row that's at y1 cpu0 is specialized in the row that's y0 and so on and so this is you know it's a very similar kind of pattern fetch you know in the hardware pipeline is fetch decode execute in the compiler pipeline it's y00 y10 y20 because these are different tasks in terms of spatial locality different kind of specialization in terms of spatial locality you know basically we give them to different processors in this case and similarly for other things right so f1 d1 e1 f2 d2 e2 and so on this kind of specialization or for stages helps if you know stage specialization is going to give you better better answer and this is actually how also humans work if you go to a factory assembly line one human is a specialist in let's say bottling of let's say soda drink another human is specialized in packaging another so that is a better approach and that's the pipeline approach as opposed to i tell every human to do everything from beginning to end so that would be an you know that would require every human to learn a lot of things whereas humans basically prefer to be specialized even super specialized in one particular job and then that way that particular human is using his or her hardware which is you know his brain is the best possible way because you know he or she becomes an expert in that particular thing and so that's exactly what's happening here here expertise is basically based on spatial ability all right so the last thing i want to point out is this pipelining requires a loop depth of at least greater than equal to two so whenever i have a loop depth of greater than equal to two the iterations of the outer loop can be counted as tasks and the iteration of the inner loop can be counted as stages and now what i can do is i can pipeline based on stages and say okay this particular processor will specialize on stage zero this processor will specialize on stage one and so on and once again you know i'm not really talking about how to decide whether to pipeline or parallelize but let's say you know i wanted to figure out i wanted to try both options and just run the program and figure out which one is faster and choose that one but how does the compiler actually you know does it do this transformation that is going to be the subject of our discussion in the next few modules
