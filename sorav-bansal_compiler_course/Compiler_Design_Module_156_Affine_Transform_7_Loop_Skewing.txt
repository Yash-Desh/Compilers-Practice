Title: Compiler Design Module 156 : Affine Transform #7   Loop Skewing
Video ID: gPAjUHeX0BI
Detected language: en
------------------------------------------------------------

Today we are going to talk about our last and 7th primitive or fine transform which is called skewing and let's look at this example to understand skewing for i equals 0 to n i plus plus or j equals 0 to m j plus plus so this is a two-dimensional loop z i j equals z i minus 1 j minus 1 so it is once again we are making an axis into a two-dimensional array but this time the dependency order is different in our previous example when we were talking about permutation there was dependencies only on one of the dimensions for example the dependency was on the outer dimension i here the dependencies are basically on both dimensions in the sense that z i comma j equals z i minus 1 comma j minus 1 if I look at what iterations are dependent on which ones If I take the iteration 0 comma 0, 0 comma 0 is going to write z 0 0 and it's going to read z minus 1 comma minus 1 so let's look at the element that it wrote which is z 0 0 now let's look at which iteration is going to read z 0 0 and the answer actually is the iteration which is going to read z 0 0 is the iteration which will evaluate i minus 1 comma j minus 1 to 0 comma 0 in other words i and j should have been 1 right so the iteration 1 comma 1 will read 0 comma 0 and iteration 0 comma 0 would write z 0 comma 0 and so there is a data dependency from the 0 comma 0th iteration to the 1 comma 1th iteration and this is a read after write dependency because 1 comma 1 is going to read and 0 comma 0 is going to write and So I draw an edge from 0 comma 0 to 1 comma 1 and notice that the angle of this dependency edge is 45 degrees because you know both i and j are incremented by 1 and so basically we have an 45 degree angle and I can kind of generalize this for example 1 comma 1 will have a dependency on dependency 2 comma 2 similarly 3 comma 3 similarly the other elements in this row for example 0 comma 1 would have a dependency on 1 comma 2 on 2 comma 3 and so on and so I basically get a pictorial representation as I've shown in this figure with all the dependency edges and I would like to remind you that the order of execution of this this loop is basically ascending lexicographic that means first the first row is it Explode then the second row then the third row and so on okay Another because the inner loop is on J and J is the x-axis all right now I basically implement my space partitioning Constraints algorithm to basically find synchronization free parallelism on this particular loop and so what do I What do I expect before I even start with the mathematics? I would probably expect that all these data dependent iterations are basically grouped into separate processors and Across these kind of line, which is you know if I draw a 135 degrees line then all these strands that are kind of Touching this 135 degree line. They are all data independent for example this strand here that starts at 0 comma 0 is Independent of this strand here which stands starts at 0 comma 1 right and so We each of these strands can be paralyzed but within a strand They must be mapped to the same processor and executed sequentially now We're going to try and obtain a solution using our space partitioning constraints and the first thing we do is we represent the program in the polyhedron model and There is only one statement in this program and that's the statement z ij equals z i minus 1 j minus 1 And its iteration space is a two-dimensional iteration space 0 comma n for i and 0 comma m for j and so I take a Cartesian product between the two Spaces and so, you know, I have iterations like 0 0 0 1 1 0 0 2 2 and so on and so forth It's a total of n plus m 1 into m plus 1 points in my iteration space now I Specify my space partitioning constraint would say that if there is a data dependency between any two iterations Then the two iteration points must be mapped to the same processor and so that basically Boils down to saying that if the two iterations Points were i comma j and i prime comma j prime then for there to be a data dependence It must be true that i comma j equals i prime minus 1 comma j prime minus 1 in other words I prime comma j primeth iteration is reading the element That is being written by the i comma j Iteration and this is you know this this this constraint this data dependence constraints is coming from Looking at accesses across these two static accesses the one z ij and z i minus 1 j minus 1 There are actually two more pairs that have to be considered for example z ij with itself But there we are going to find that there is no data dependency and similarly z i minus 1 comma j minus 1 with Itself where again we are going to find that there is no data dependencies The only data dependencies are across these two static accesses and so and and the condition for data dependency is that the the iteration for the first axis which is i comma j should be equal to the I prime minus 1 comma j prime 1 minus 1 iteration of the second axis where i prime and j prime is the iteration Number of the second axis which is conflicting with i comma j and so whenever i comma j equals i prime minus j 1 comma j Minus 1 it must be true that they map to the same processor in other words P of i comma j should be equal to P of i prime minus minus 1 j prime minus 1 and notice that these functions P are actually I'm assuming that there's the same function in this because you know s1 is going to be totally mapped Together so whatever is the function the mapping function from iteration space to processor space for this static access is also the Mapping function for the other static access so we can assume that the P's are same Now if I solve for the maximum rank solution for these space partitioning constraints So these are my space partitioning constraints I want to find what is the function P then if I solve for the maximum rank solution I want to point out again that you know one trivial solution is that P ij equals 0 for all possible i comma j But that's not a maximum rank solution because that basically means everything is mapped to one processor So if I look at the maximum rank solution for this these space partitioning constraints I'm going to get the solution P equals i minus j and you can verify this for yourself that if P equals i minus j then i minus j equals i prime minus 1 minus Brackets j prime minus 1 and the ones would cancel out and so both i minus j and i prime minus j prime Would be identical and so P equals i minus j actually satisfies our space partitioning constraints now So So basically we have the solution which is the maximum rank solution and if I look at it pictorially it actually makes sense because What is the processor for this particular strand that starts at 0 comma 0? It is just 0 minus 0 or 1 minus 1 or 2 minus 2 or 3 minus 3 So all these points in this strand actually evaluate P to 0 because 0 minus 0 is also 0 1 minus 1 is also 0 2 minus 2 is also 0 and so on similarly if I look at this strand 0 comma 1 evaluates to minus 1 1 comma 2 also evaluates to minus 1 2 comma 3 also evaluates to minus 1 and so on so each of these strands is basically Mapping to the same all the points in a single strand are mapping to the same processor with this mapping function and so But but different Strands for example this strand is mapping to processor number 0 the one that starts at 0 comma 0 But this strand which starts at 0 comma 1 is mapping at 2 processor number minus 1 in other words the two process The two strands are being mapped to different processors and that's coming from a maximum rank solution So to get the maximum possible range of my P function so that I get the maximum amount of parallelism Now I look at what is the you know what is the minimum and the maximum value that P can take I already know that the Minimum value that I can take is 0 and the maximum value I can take is n Similarly for J the minimum and maximum are 0 and M respectively And so if I look at what is the minimum that I minus J can take well I minus J would be minimum and I is minimum and J is maximum so I is minimum It means 0 and then J is maximum. It's M. So the minimum value of I minus J is 0 minus M, which is minus M similarly What's the maximum value of I minus J when I is capital N and J is 0 because I is maximum and J is minimum Then the maximum value of P is capital N and so now I have the lower bound and upper bound of P In terms of M and N and now I can do my code generation My first attempt is going to be a naive code generation where I basically use an outer loop for P which goes from the lower bound of P which is minus M and to the upper bound of P which is capital N and Then I just paste the original program as it is except that I prefix or I predicate the statement S1 With the mapping function which which says if P equals I minus J So I should only execute the statement if I am on the correct processor And so this is my naive code generation strategy I Claim that in this code generation strategy the outer loop which is on P is data independent Which means different iterations of P are don't have Data dependencies among each other for example when P equals minus M and when P equals minus M plus 1 The two you know the two the sets of locations that the two iterations will access would be completely disjoint If one was a right, okay, so So this is my naive code generation strategy and my next step is to Tighten the loop bounds or eliminate the empty iterations. I'm going to do this in two steps My first step is going to be a tightening the loop bound for the innermost loop Which is the loop on J and if you look at the loop on J It basically says J equals 0 to M But it only looks at one value which satisfies P equals I minus J So actually I can just replace this whole loop from J to M by just saying that J should be equal to I minus P Right so instead of you know I just kind of rearrange the terms here with P equals I minus J Then it must be true that J equals I minus P So the only non empty iteration happens when J equals I minus P all the other iterations are empty Iterations and so I can just say that oh J equals I minus P and and then basically do it But even there it's not that you know for every value of I minus P This is empty because also I need the constraint and J should be greater than equal to 0 and J should be less than equal to M so not only is J equal to I minus P so I get these constraints if I look at my constraints I get J is Equal to I minus P J is Greater than equal to 0 and J is less than equal to M and now if I solve for these constraints I basically get that You know if I eliminate J using my Fourier mode scan method then I basically get that using my projection basically then what I get if is J should be greater than equal to 0 which basically means I minus P should be greater than equal to 0 J should be less than equal to M which means I minus P should be less than equal to M and there's only one Iteration that executes which is when P J equals I minus P So I just check whether I minus P is greater than equal to 0 and I minus P is less than equal to M in which case I just execute one iteration for J which is the iteration when I minus P equals J And I can just replace J with I minus P because I know that J equals I minus P I don't even need to have the variable J any longer And so if I look if you know by tightening the loop bounds of the innermost loop I get this if condition in other words if I minus P was greater than equal to 0 and I minus P was less than equal to M then one iteration will be executed of S1 if this condition is not met then zero iterations of S1 would get executed in other words There are still some empty iterations and those empty iterations are coming from the outer loop outer loop on I and my next step is going to be to tighten the loop bounds of the outer loop which is the for I equals 0 I is less than equal to N loop and basically I need to figure out that for what values of I is this condition going to get enabled and it's you know What are the minimum and the maximum values of I for which this condition I minus P is greater than equal to 0 and I minus P is less than equal to M is going to get enabled and I can actually see from here that only for those Conditions where I is greater than equal to P because only if I is greater than equal to P only then if and only if I is greater than equal to P would I minus P would be greater than equal to 0 and similarly the second constraint I minus P is less than equal to M can be reduced to I is less than equal to P plus M and so I have new lower bounds and upper bounds on I for which the iteration is going to be non-empty the lower bound is I is greater than equal to P and the upper bound is I is less than equal to M plus P and so now I can just you know Use my you know tighten my bounds using these lower bounds and upper bounds so instead of going from 0 to N I go from maximum of 0 comma P because P was my new lower bound of I to minimum of N comma P plus M because P plus M was an other upper bound for I so in other words I just incorporate the new lower bound and the new upper bound and once I do that I don't need the if condition anymore, and I can just have my statement as it is so my new program basically looks like for P equals minus M to N P plus plus for I equals maximum of 0 comma P to minimum of N comma P plus M I plus plus Z I comma I minus P so I minus P is just a replacement of J Equals Z I minus 1 comma I minus P minus 1 now I claim that this this new loop basically has better locality also if I it is possible to parallelize the outer loop because the iterations of the outer loop are data independent to see this pictorially Let's look at the two dependence graphs or the iteration spaces Recall that this was our original iteration space J is on the x-axis and I on the y-axis And there were these dependency edges that were going at 45 degree angles In my new Program the outer loop is on P and the inner loop is on I so now I becomes the x-axis and P becomes the y-axis Moreover the values of P go from minus M to N So they don't no longer go from 0 to N But they go from minus M to N and so this you know Let's say this point is somewhere near minus M And let's say the upper point is somewhere near plus 10 and so all these points are all these kind of our values that P Takes and then let's look at what are the values that I takes I takes Values from maximum of 0 comma P to minimum of N comma P plus M So notice that when P equals minus M then this becomes maximum of 0 comma minus M Minus M and assuming M is a positive number because we started with M to be kind of the upper bound So M must be a positive number so maximum of 0 comma minus M would be just 0 and Minimum of N comma minus M plus M because P I'm considering the case when P equals minus M So minimum of N comma minus M plus M is just minimum of N comma 0 and once again N is a positive number So minimum of N comma 0 is just 0 So the maximum the minimum value is also 0 the maximum value is also 0 for the first iteration of the inner loop For the first iteration space of the inner loop But if I as I increase M for example as I increase P for example when P equals somewhere in the middle Let's say P equals 0 then this becomes I equals max of 0 comma 0 So that's a 0 to min of N comma 0 plus M. So min of N comma M and so this now this iteration space has increased to From just one point which was 0 comma 0 for when P was equal to minus M to the iteration space from 0 to Min of N comma M So basically what I'm saying is that for different values of P the iteration space for I Has different lengths for example when P is minus M, then the iteration space of I is probably very small Maybe just one element. I'm just showing a strand of three elements here Just just as a no not to scale pictorial representation But as I increase P the value of this iteration space of I starts increasing Somewhere in the middle it becomes very large. For example, it becomes as large as either M or N And then it starts decreasing again because if I look at you know P as very high value So now if I look at P equals plus N then I equals max of 0 comma P becomes Max of 0 comma capital N which is just capital N because N is a positive number and then min of N comma P plus M just becomes min of N comma N plus M and that's just N because N plus M would always be greater than Equal to N assuming M is a positive number. So then again, we basically have a single Iteration single point iteration space for I when equals N which is from capital N to capital N And so that's what you know Basically will happen is that as P gets increases further the iteration space of I and the inner loop would also would start decreasing So from here we have basically converted the iteration space to this More intuitively what we are doing is we are basically traversing the iteration space it at a 135 degree angle, okay, so We first look at this strand at the very end Let's say and and that would just have the corner element one element Then the next strand which would have let's say three elements that the next strand which may have let's say five elements And so on until we hit the diagonal the longest diagonal of this iteration space Which is the middle element of value of P and then after that again these strands start becoming shorter and shorter and so our space Partitioning constraints has basically, you know Done done this resolution. We are basically it has figured out. What are those strands and those strands should be basically You know executed in parallel through the outer loop Even though the strands may be of different Lengths now I want to point out that because the strands are of different length if I just parallelize it Naively then it may happen that some iterations finish very quickly while some other iterations finish take longer to Finish but recall that when we started our whole discussion on synchronization free parallelism we said we're just going to assume that there are lots and lots of parallel processors and so You know and so in this case the number of parallel processor that we can potentially use at most is From minus m to n so that's nearly m plus n plus 1, right? So we can use m plus n plus 1 parallel processors But if we have smaller number of processes because capital N and capital N could be really big numbers Maybe in millions or something like that if we want if I have smaller number of processors We may need to do some scheduling for and we may need to have some intelligent scheduling strategy where we basically say that the longer strands are kind of not grouped but the smaller strands are grouped with each other so that each of the Physical processors has roughly an equal amount of work to do so that we are using the maximum amount of parallelism available at all possible program points during the execution Okay Finally this I mean this transformation from my original program to the new program is basically nothing but An example of changing the axes I change my axis from i comma j to i Minus j comma i all right. That's what this this this this space partitioning constraints formulation Achieved for me and if I basically was to write this as a matrix This is my skewing matrix that basically skews the axes You know, so the first axis becomes one i minus j and the second axis becomes i Notice that I'm calling I'm not calling this a permutation matrix a permutation matrix needs to have a special property that There's only one nonzero value on every row and every column and that value is a one This is not a permutation matrix because it's not just a permutation of the axes as it was for our affine transform number six It is actually a skewing transformation because we are basically traversing the data now at a 135 degree angle and it's not just about interchange of axes and loops Finally, I want to say a word about the geometrical interpretation that we may have developed an intuition about through all these primitive affine transforms Notice that we assume that you know, assuming a two-dimensional space and this can be generalized to higher dimensions as well But assuming a two-dimensional space if we use the x-axis as the inner dimension and the y-axis at the outer dimension So let's say the inner dimension is x1 and the outer dimension is x2 Then we are basically, you know Iterating the space in the ascending lexicographic order Which means I first exhaust all values of x1 for the first value of x2 and then you know And then all values of x1 for the second value of x2 and so on In other words, this is the direction of my traversal of the iteration space then there will be dependency edges from Iterations that read or write some data element to Iterations that read or write the same data element and I claim that you know in this set in this kind of representation graphical representation the dependency edges can no matter what program you write if you draw the Pictorial representation in this way where the inner loop is basically the x-axis and the outer loop is the y-axis Then the dependency edges can only have an angle Between 0 and 180 degrees which means I could have angles which look like this which look like this Which look like this which look like this which look like this, but I can't have an angle which looks like this All right, so this is not possible. So why is this not possible? Well, because if I have such an angle that basically means that this point executed before this point And that's not possible in our ascending lexicographic order in our ascending lexicographic order this point must execute, you know this point must execute before this point and so You know this that another way to say that is that the dependency edges can only Have an angle between 0 degrees and 180 degrees and this just comes from the ascending lexicographic order of the iteration space What do the space iteration space partitioning constraints for synchronization free parallelism by us? Space partitioning ensures that the outer loops are data independent So it tries to ensure that whatever as the direction of dependency edges it tries to find the maximum rank solution such that all those dependency edges basically are You know traversed in the inner loop iterations if possible and the outer loop iterations are basically data Independent and this is what we saw in us doing example as well the strands basically, you know each of the strands basically form the inner loop and The the iteration across the strands basically forms the outer loop in other words If I was able to find a rank minus a rank one solution for a two-dimensional space So if this is rank one solution, then the data dependencies would only only be horizontal in the new solution Just like they were in for us doing and permutation matrices Of course, I'm just you know doing this pictorial representation for a two-dimensional case You could potentially generalize this also for a three-dimensional case For example, I could now talk about either a rank one solution or a rank two solution Notice that if I get a rank zero solution, that's no good That's basically saying that everything is still executing on the same processor And if I do the code generation, I'm going to get the same program as the original program back So rank zero solution is no good rank one solution for a two-dimensional case ensures that all the dependency edges are only horizontal and there is no So all the dependency edges are basically zero degree edges basically theta equals zero for for the rank one solution Similarly for a three-dimensional case You could have an either a rank one solution or a rank two solution or even a rank three solution Even for a two-dimensional case I could have a rank two solution if I had a rank two solution basically means every point can be executed in parallel So there are no dependency edges for a if we got a rank two solution In a three-dimensional case if I get a rank one solution It basically means that all my dependency edges should be in the in the plane So if it's a three-dimensional case It should be on the XY plane where Z axis is the vertical axis the outermost axis and Similarly, you can also think about what will happen if it was a rank one solution in a three-dimensional case So I find this, you know pictorial thinking Of this of these kind of a fine transform very useful in trying to understand what's happening Apart from all the gory details related to mathematics
