Title: Compiler Design Module 155 : Affine Transform #6   Loop Interchange
Video ID: 1oUjj-537Mk
Detected language: en
------------------------------------------------------------

So we'll discuss our sixth primitive affine transform which is permutation. So to understand this transform, let's look at an example. For i equals 0 to n, for j equals 0 to m, z i j equals z i minus 1 j. So notice that in this example, we are accessing a two-dimensional array indexed by two indices, i, j in this case and i minus 1, j in this case. And so you can see that it's not the innermost dimension which has a dependency, it's the outer dimension which has a dependency within the statement. There's a dependency from i, jth iteration to i minus 1, jth iteration because in the i minus 1, jth iteration, z i minus 1, j would be written and in i, jth iteration, z i minus 1, j would be read. If I was to basically draw the pictorial representation in 2D and I draw the inner dimension as the x-axis, so the inner dimension is j and the outer dimension as the y-axis which is i and so the lexicographic order is basically shown by these green lines. So first I exhaust all values of j for the first value of i, then I exhaust all values of j for the second value of i and so on. Then the dependency edges are drawn in grey. For example, there's a dependency from 0, 0 to 1, 0 where this is i, j, so i is coming first but there's a dependency from 0, 0 to 1, 0 and this is exactly what our space, our data dependence constraints would also have told us because as I said before, in our 1, 0th iteration, 0, 0th element would have been read because of this i minus 1, j and in the 0, 0th iteration, 0, 0 would have been written. So in 0, 0 iteration, z 0, 0 would have been written and in 1, 0 iteration, z 0, 0 would have been read and so there is a read after write dependency as shown by this grey edge and in fact, this can be generalized to 1, 1 and 2, 1, 0 and 2, 0, 1, 2, 0 and 3, 0 and so on and similarly for other values of j. So this is our dependency graph and we can see that the outer loop iterations which are the iterations of i are not data independent, they are data dependent because you know, this is the lowest bottom row is basically for when i equals 0 and then the second row is basically when i equals 1. However, I cannot execute the iteration for i equals 0 in parallel with the iteration for i equals 1 because there are data dependencies between the iterations of i equals 0 and the iterations of i equals 1. So there is no data dependency between, there is, you know, there is data dependency between different iterations of the outer loop and so the outer loop is not parallelizable. Now it's a different matter that the inner loop for a given value of i, so for a given value of i, the inner loop may be parallelizable but that is, you know, that's like saying that the outer loop is not parallelizable and the inner loop doesn't have that much parallelism to have because inner loop only contains one iteration per point and so the inner loops parallelization is not going to be very helpful if the outer loop is basically sequential. So we are going to basically transform this program so that we can have better parallelism and we are going to use our well-known technique of space partitioning constraints. So what is the first step in doing so? We are going to represent this program as a polyhedron, as a set of polyhedral constraints. There's only one statement here which is z i j equals z i minus 1 j. I mean there are actually two static accesses but they basically have a dependency so we're going to consider this as one statement and so this statement has an iteration space of two-dimensional iteration space formed by the Cartesian product of 0 comma n which is for i and 0 comma m which is for j. So for example 0 comma 0, 0 comma 1, 0 comma 2, 1 comma 0, 1 comma 1, 1 comma 2 all the way till n comma n. So there are m plus 1 into n plus 1 kind of elements in my iteration space and now I basically want to set up the data dependence space partitioning constraints and so basically I want to say that whenever there are two iterations that have a data dependence then it must be true that they are mapped to the same processor and so this is what our data dependence constraints would look like. Let's say one of the iterations in points in the iteration space is i comma j and let's say the other point is i prime comma j prime. Then for there to be a dependence between i comma j and i prime comma j prime it must be true that you know if I look at these two static accesses then the two iterations should access the same location and so in other words the location access by i comma j as a right would be i comma j, z i comma j and the location access by i prime comma the i prime comma j prime iteration is z i prime minus 1 j prime. So for there to be a data dependency it must be true that i comma j equals i prime minus 1 comma j prime and notice that I'm only considering data dependence I'm only considering this access and this access as a pair. I could have also considered this access with itself in which case I would have said that there is no data dependency because every element is every iteration is accessing a different element of the matrix. Similarly I could have looked for data dependencies for this access with itself once again I would have found that they are all data independent so the only data dependencies are across this axis and this axis and that's what I have encoded in my space partitioning constraints. So when i comma j equals i prime minus 1 comma j prime then it must be true that they map to the same processor in other words the function from p from i comma j for this statement should be equal to the function from you know maybe I should say p prime for the different function for i prime minus 1 comma j prime. Now actually it's p prime should be equal to p because it's the same c statement right so there are anyway kind of static data dependencies so p prime would be equal to p and so I can just say p. Now when I look for the maximum rank solution for this function p which needs to be an affine function in our framework then I just get this solution that p must be equal to j. So notice that because there are dependency chains for i's for example there's a dependency from 0 comma j to 1 comma j 1 comma j to 2 comma j 2 comma j so along the dimension of i everything is data dependent but along the dimension of j things are independent and that's what we also saw in our pictorial representation and so basically what I'm saying is that you know just partition the space such that every different j value is given a different processor right so in our diagram it's every vertical strand or that is basically sent to a single processor and notice that in this particular example while my iteration space is two dimensional my processor space is one dimensional okay and this is the maximum rank solution that I could obtain using space partitioning constraints and this is not surprising because there are data dependencies on one of the dimensions and so the only dimension that is parallelizable is the other dimension which is j. So this is my function and now basically I can do my code generation. The outer loop is going to iterate on p and what are the minimum and maximum values for p they are the same as the minimum and maximum values for j because p equals j and so I get for p equals zero p is less than equal to m p plus plus and then I just paste the original code as it is except that I predicate all the statements with the corresponding processor mapping function so in this case it's so that basically encoding the fact that only those iterations should execute on this processor which are which are supposed to execute on this processor and in this case the mapping function is p equals j so I basically have a predicate it says if p equals j only then execute this statement which is my s1 statement so so that's you know that's our simple code generation strategy and our next step is to tighten the loop bounds and and in this case basically I can see that the inner loop is going to execute only once the statement and all the other kind of m minus all the so out of the m plus one iterations of the inner loop m iterations are going to be empty iterations and only one iteration is going to actually execute the statement and that iteration is going to be the iteration when j equals p and so what I can do is I can just replace I can just eliminate this whole loop and just replace this with j equals p and I am done right and because I have now said j equals p I don't even need the guard and now I can just you know just copy propagate j equals p so that I can just replace j with p everywhere and now this is what my new code looks like for p equals zero to m for i equals zero to n zip equals z i minus 1 p notice the difference the change that I have been able to make using the space partitioning constraints is that the outer loop on i has now become the inner loop on the same variable i whereas the outer loop has become a loop on the processor variable p and this is you know this is important because now what I have done is because you know the dimension on which there was a data dependency that dimension is now the inner loop and so I have decreased the reuse distance moreover the different iterations of the outer loop are now data independent and that's coming from our space partitioning constraints the way we have set it up because we had got the solution we can be sure that the outer loop iterations are data independent they don't have any data dependencies so if I was to look at this pictorially this is my original picture where there are data dependencies that are vertical but my traversal order is horizontal and so the outer loop didn't have the outer loop had data dependencies so the outer loop could not be parallelized but now I have changed it earlier my inner loop was on j and the outer loop was on i now my inner loop is on i so that's now our new x-axis and the outer loop is on p and p is just you know you can think of p as j so basically I've just switched you know I've just switched the loops i and j the inner and outer loops so that the inner loop has become the outer loop and the outer loop has become the inner loop and now in this kind of you know rotation of the axis what I see is that all my dependency edges are also horizontal and my traversal order of the loop is also in the same order as the dependency edges so clearly I have reduced my reuse distance because anything that I write is just read in the next iteration moreover different iterations of the outer loop are now data independent and they can be parallelized so each row can be given to a different processor and without any worry of incorrectness so this kind of parallelism would keep the program correct another way to say this is that I have changed the axis earlier my axes were p i comma j and so now my new axes are p and i and just to distinguish from the old i I say p and i prime and the relation between p and i prime and the original axis i and j is given by this matrix so p equals 0 into i plus 1 into j in other words p equals j and i prime equals 1 into i plus 0 into j in other words i prime equals i so i the i prime is just the same as i and p is now j and 0 1 1 0 represents our permutation matrix because which basically decides how we have permuted this permuted my axes so that I have better parallelism and locality so this particular matrix which basically dictates how the axes should be interchanged this is also called this transformation is also called loop interchange because I have interchanged the loops I have made the inner loop the outer loop and the outer loop the inner loop so this this matrix that dictates what this interchange should look like is also called the permutation matrix
