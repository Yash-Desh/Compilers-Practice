Title: Compiler Design Module 124 : Non linear Recurrences
Video ID: Uy4moXWJkzs
Detected language: en
------------------------------------------------------------

So, last time we discussed how induction variable analysis proceeds. It proceeds by first identifying loop invariant variables and basic induction variables and then representing other variables in the loop iteration as functions of either loop invariant variables or basic induction variables and then based on that identifying what those variables are as affine expressions on the loop iteration indices. But that is a little bit limited exposition of how the induction variable analysis works because we are limiting ourselves to basic induction variables and affine expressions and also loop invariant variables but in practice actually the induction variable analysis or similar kind of symbolic analysis has become significantly more sophisticated and is actually very useful in producing compiler transformations. So one of the analysis is related to loop trip count and we have discussed this in some in a brief in previous modules. So and how would this work? This would again be similar to the symbolic analysis or that is involved in identifying induction variables. So using the symbolic analysis first we identify all the loops, regions and stuff like that and then for every loop we are going to basically generate the data flow values using either data flow analysis or symbolic analysis and in this particular analysis we are going to have a map. So let's say the m0 x represents the map m0 represents the map at before the first iteration of the loop and x is the it maps x to a so m0 x equals a so the first before the first iteration the map of variables to expressions maps x to a constant a let's say and then if I look at the loop transfer function the summarized transfer function of the loop body I see that m1 x equals m0 x plus c so this is basically the summary for the transfer function for x of the loop body where c is also a constant. Now if we assume that and then we basically see what is the loop termination condition and so the loop termination condition can be just syntactically identified from the loop body basically figuring out what are the conditions under which the loop exit edges are taken edges that are going out of the loop recall that an exit edge is basically an edge whose source is part of the loop but the destination is not and once we are able to figure that and let's say it is of this form x is less than b then if a is less than b then loop trip count is b minus a by c right so if a b and c are constant if I am able to do the symbolic analysis to figure out what is the loop termination condition if I am able to do the induction variable analysis to figure out what is that you know x let's say is a is a basic induction variable with an increment of c then I can do the loop then I can basically claim or analyze using my symbolic analysis that the loop trip count is b minus a by c so when I say using symbolic analysis I mean that there is some kind of axiomatic structure in which I basically you know do some pattern matching and say okay if this is the pattern then I can say that the loop trip count is b minus a by c and here a d and c could be substituted with arbitrary constant expressions or constant values all right so this is the kind of I mean I'm just giving you a flavor of the kind of analysis that goes on in a modern compiler to identify the loop trip count all right why do I care about the loop trip count well if I am able so of course you know one conservative answer for the loop trip count is that it's bottom where bottom basically means I don't know how many iterations the loop is going to take but in cases where I'm able to identify the loop trip count precisely that really helps because now I can predict what are the values at the exit of the loop as potentially affine expressions on the values at the entry of the loop recall that the induction variable analysis gives you the values of every variable as a function of the loop iteration index and if I know the number of loop iterations then I also know what are the value of the affine variable at loop exit all right so this is another example of a symbolic analysis which uses induction variable analysis to identify the loop trip count here are some other types of recurrences so one of the very common themes in a compiler is canonicalization there are many ways that you can write the same logic for example we have seen strength reduction so inside the loop I could say x equals i into three or I could just say x equals x plus three and let's say x is initialized to zero before the start of the loop and both these things are going to produce the same set of values for x on every iteration it's going to be incremented by three every time and and so you know our induction variable analysis is able to canonicalize which means give a canonical representation of what is actually happening for x canonical way of representing how x is evolving as the loop is proceeding and so the canonical representation could be that let's just use a plus c into i and this is going to capture both the cases where x is getting incremented by three using the plus operator or where x is being explicitly computed using this particular expression a plus c into i on every iteration of the loop now here is one way of representing such canonical structures so a plus c into i this is I'm borrowing the notation from the scalar evolution analysis of LLVM this involves so let me just write it down so you can also study it on your own this is called the scalar evolution analysis in LLVM and this basically involves writing and variable x which is known to be an induction variable as three tuple the first one is first element says what is the start value before the entry of the first iteration the second says what is the operator that is with which it gets with which it evolves so in this case it says that it something gets added on every iteration so that's why it's a plus operator and then what is the value that gets added on every iteration that's c so a plus c a represents the start operate a start value c represents a stride and plus represents what is the operator with which it is striding right the significance of the operator is that I can now not only limit myself to affine expressions but maybe other types of expressions such as multiplication so I could say that it's getting multiplied by two on every iteration in which case I could replace plus with star also this notation has you know so they are curly braces for this three tuple but then it also has an annotation which says what is the loop or the loop name and this basically represents the innermost loops header with respect to which this tuple is has been written so basically this means that in the ith iteration of this particular loop the value of x would be a plus c into i so this notation here basically represents this sentence at iteration i of loop name so I should be very at iteration i of loop name x equals a plus c into i and that's basically represented succinctly using this notation now why am I interested in representing this sentence in a succinct notation it'll become very clear as I'm going to make this more elaborate this notation and use be able to specify more complex properties more complex sentences using this particular notation so here is an example where this particular property would hold that x equals a comma plus comma c loop name and so this is my let's say loop it has this name by the way how do I name a loop I name typically name a loop using the program point or the program counter value or add the header of the loop recall that a loop is uniquely identified by the header of a loop we have already talked about how we identify inner inner loops outer loops and things like that merge loops with the same header so every loop you know two different loops would always have different headers and so if I just name the header of a loop that's as good as naming the loop itself and so typically we just name the header of the loop to name the loop and so let's say this is my loop in the let's say in the intermediate representation I'm just showing the high level c like representation and in this you have x equals a and then inside the loop I have x equals x plus c and so this my induction variable analysis is going to identify this property from this loop right so that's that's very helpful I should also point out that let's say I had instead of this if I had x equals a plus c into i it would have still used the same representation so my induction variable analysis would have got the same answer irrespective of what syntax I use and that's what I mean by canonicalization this kind of representation of how this variable x is evolving as with the iteration count of the loop is basically you know a way to canonicalize the operation of the loop and with the canonicalization we can now do pattern matching on the canonical representation of the loop to basically do transformations or analyses or things like that so here is another example so let's say in my in my loop I also had y equals read and then I had x2 some other variable equals a plus i into c plus y now in this case it's no longer an induction variable but it says you know because y is completely unknown it's it's being read from some ios source let's say the keyboard and so this this value x2 is unknown it's not an induction variable yet it is expressible in this in this format by just saying that oh x is a plus c loop name so this part is the induction variable part which says that it's incrementing by site stride c on every iteration and plus y and where y is kind of you know some variable which is available at the point where x2 is being used so at this particular point I mean the canonical representation for this statement here is this so this is the canonical so this is the canonicalization of this statement okay I could have for example in this example had a different code for example I could have said x2 equals a and then instead of this I could have just said x2 equals x2 plus equals c and then I could say x2 equals yeah it would be a little complicated but even if I had a different representation for this it would have still given me the same canonical answer I couldn't really construct an example where it would have given me a different representation but the point is that you know I have hidden what the actual syntax was to obtain this you know to obtain this canonical form of how x2 is value can be computed for an ith iteration of a loop right so this is a canonical representation this forms part of my symbolic analysis one part of the symbolic analysis to canonically represent every variable in the loop in terms of the loop iteration index here is another example so let's say I have z equals something which is a loop invariant it's not going to change in the loop then I have x equals a and then inside the loop I have x equals x plus c and then I had x2 equals x plus z now once again this is you know there's some way that the values x and x2 are evolving we've already seen how x is evolving x would be just a comma plus comma c with loop name but x2 would be a plus z comma plus comma c because it's not just the initial value but it's basically represents what's the constant value that can be so this is basically the constant part and this is the stride and so this basically is now this representation here is basically saying that in the ith iteration of this loop I have x2 equals x2 equals a plus z plus i into c and my symbolic analysis should be able to figure this out on its own and this is a representation of how I'm going to represent it we have already discussed how the symbolic analysis proceeds in the previous module the induction variable analysis but here I'm really discussing how to represent such inferences in a canonical form so that they become easy to read manipulate and use for transformations so a plus z comma plus comma c loop name and this basically stands for x at the ith iteration x2 is a plus z plus i into c because this is just the constant part and this is the stripe all right so that's what this representation means now here's another example let's say I have a character array a of two-dimensional array where there are 100 rows and 200 columns and then I say for i equals 1 to 100 for j equals 1 to 200 p equals ampersand a i j all right now in this case I want to represent p as functions as functions of loop indices j and i right so we have discussed how induction variable analysis works for a single loop and I want to generalize it to multiple groups I'm not discussing how that generalization will happen it's actually very similar there's nothing you know there's nothing different for doing it for multiple loops but I'm going to discuss how am I going to represent the result in this notation because that itself is going to give you a sense of how this the analysis may have proceeded to basically get this representation and so basically if you look at how p is evolving p is basically the address of a i j i is the loop iteration index of l0 j is the loop iteration index of l1 and what is p if I look at if I assume that p is stored in a row major order then this is basically p plus 200 into i plus j all right sorry a plus so p is nothing but a plus 200 into i plus j and I expect my symbolic induction variable analysis to identify this that for the ith iteration in l0 and the jth iteration of l1 p is a plus 200 into i plus j and I will represent it in this form in my notation and this becomes even more interesting because I'm saying that p equals now I have two parts to this representation one is related to the outer loop so a plus 200 l0 so this is a is that if I am in the ith iteration of l0 then it is a plus 200 into i this is exactly what we had comma plus one of l1 so plus if I'm in the jth iteration of l1 then plus one into j and so this representation here represents the fact that a plus at the ith iteration of l0 and the jth iteration of l1 p is a plus 200 into i and plus two plus one into j now notice that I have kind of simplified here that I'm using i equals one two hundred and j equals one two two hundred and so I'm kind of assuming that the loop indices of l0 and l1 are i and j respectively but you know in general that may not be true for example I may have used a while loop maybe the loop in iteration index is not relate not directly equal to one of the program variables like in this case in this case I'm assuming that the program variable i is equal to the loop iteration index of l0 and which case also this representation holds so notice that neither i nor j find mention in this representation it's just mentions that whatever is the loop l0 whatever is the loop iteration index of l0 with respect to that this is what the value is one part of the value for p plus one into whatever is the loop iteration index of l1 so exactly a plus 200 into i plus one into j okay that's interesting one more idea here is non-linear recurrences so I could for example let's say have a program it says j equals zero then for i equals zero i is less than n i plus plus now here notice that I can first represent i what is i i is nothing but zero that's the initial value plus because it gets incremented by one every time plus one and whatever is the loop right so let's say this loop is l then I just say l here now that's i but what about j can I say something about j and the answer is yes we can say something about j because j gets incremented by i on every iteration and we know that i is already zero plus one l and so j becomes zero plus and whatever is the value of i that we know in terms of the loop iteration index so that's called zero plus one l and notice now I am going beyond the fine expressions now I can say that it's possible to increment the value of j by a non-constant value as long as that non-constant value is itself in a fine expression of the loop iteration index or not necessarily in a fine expression something that fits in this framework all right and so this becomes zero plus and then this becomes an increment and notice that the increment now is variable however even though it's not completely unknown it's actually representable in terms of the loop iteration index so and that is zero plus one l and so this becomes the value of j this becomes a representation of j zero plus and then this whole thing becomes zero plus one l and then this whole thing also is annotated with it so in both cases we are basically just saying that this note so this whole thing just represents a value as it evolves so this represents an evolving value for the for some variable at some program point with respect to the loop iteration index of the loop and this a shorthand for this is just have because they have the same loop loop annotation the shorthand is in scalar evolution zero plus zero plus one l and this basically means that j is at the ith iteration of the loop it is basically zero plus and then what is this becomes zero plus one into i into into i right because this is basically the so no not into i and this becomes summation over i from zero to whatever the current loop iteration index is right so which is the i prime and this should be i prime and from zero to i so if the current loop iteration index is i then j is zero plus summation of zero plus one into i prime from i prime going from zero to i and that's exactly what this loop is doing right it's just computing the sum of the first i numbers and that's exactly what this expression represents zero plus summation of one into i prime where i prime goes from zero to i and that's just going to evaluate to basically i into i plus one by two all right so we can now do non-linear represent recurrences using such a representation and such an analysis and such analyses are actually present in modern compilers here is another example let's say instead of the previous case where it was very explicit that on every iteration i'm adding i where i is getting incremented by also by one by you know every iteration here i just say j equals i into i all right so i say for i equals zero i is less than n i plus plus j equals i into i now i again expect my symbolic analysis to be able to identify that actually this is also a non-linear recurrence with respect to the iteration count of l and the new non-linear recurrence is going to be zero plus and the increment the stride is going to be one comma plus comma two for l and then all right so notice that this is non-trivial to identify i'm not discussing the algorithm to identify such relationships but i'm just trying to tell you what the representation of such evolving values is could be and is in you know modern compilers which have such known patterns with respect to the loop iteration index and once again the shorthand for this is zero plus one plus two l what this means is that the value of j is basically zero plus summation over i prime equals zero to i one plus two into i prime okay and if you just evaluate this expression you would see that this is going to be valued to just i square and that's exactly what we have in the loop and so this is again an example of canonicalization irrespective of what i had in the loop the way i had it i have canonicalized that i have a canonical representation which is this and then now i can understand what this canonical representation is and use it for further analysis and transformation
