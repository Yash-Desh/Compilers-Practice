Title: Compiler Design Module 31 : LR0 Algorithm
Video ID: 3c2Zw7QuVMo
Detected language: en
------------------------------------------------------------

So last time we discussed the construction of non-deterministic finite automaton NFA for recognizing whether a string is a viable prefix of a context-free grammar or not. Now, while we discussed the construction of the automaton as an NFA, we had to basically figure out whether a string is accepted or not by tracking all the possible states that that string could be in at any point of time. So we would basically track a subset of the states of an NFA at any time. Another formulation of the same algorithm is through a DFA and one way to construct a DFA is by just taking that NFA and using a standard subset of states construction to construct a DFA. Now, notice that it is somewhat easy to figure out what the states of the DFA would be. So the NFA states, if you recall, were basically items and these items had to be related in some way for them to have transitions among them and the DFA states would typically have groups of items such that the different items are connected through epsilon moves in the NFA. So for example, if there was an item like x produces dot y, then all the items that had y produces, let's say, alpha and y produces beta and so on, in the original NFA there were epsilon moves to these different items. In the DFA we are going to consider this as one single state. This would be the epsilon closure of the first state. So this would be the epsilon closure and that would form a DFA state. So it helps to also think of this automaton as a DFA and as we're going to see some examples and actually implement this algorithm using pseudocode, it would actually help to also think about this as a DFA. Once again, there is nothing complicated about it. In an NFA we are basically tracking some subset of states. In a DFA we just make a state for every possible subset of states and then we do the standard subset of states constructions for transitions as well. So with that, now we have a machine, whether it be a DFA or an NFA, an automaton that can tell us whether some string is a viable prefix or not. And so based on that we can define our first LR parsing algorithm and that's the LR0 parsing algorithm and this is how it works. Let's say at any point in my shift reduce parsing I am at this state where we have alpha on the stack, then there's a cursor and then I have omega which is the unseen input so far. Now I want to figure out whether I am on the right path or not. So what I can do is I can run alpha on, run the viable prefix automaton whether it be the DFA or the NFA on alpha as the input. And if I say, if I basically figure out that the automaton actually returned an error which basically means that it got stuck somewhere because there was no possible transition and I haven't yet consumed the entire alpha, then that basically means that I have a parser. So I have come on a path which is not a viable prefix and because it's not a viable prefix I have no hope of actually reducing this state of the shift reduce parser back to the original structure. And so if I'm stuck then it's a parser. What's the other option? The other option is that this particular string alpha reaches an item x produces beta dot in the NFA. Now notice that I'm talking about it as though it's an NFA. So when I say that it reaches x produces beta dot in an NFA it basically means it can reach through some path, through some non-deterministic path it can reach x produces beta dot when run on alpha. Now if it can reach x produces beta dot then that basically means that I should reduce using this production. So that's what the automaton is doing. Automaton is not only figuring out what are the viable prefixes but it's also telling us at what step I am in my viable prefix. So if this is what it is, if this is the item which is reached after running the automaton on alpha then I need to reduce because dot is at the very last position of the RHS which means that here there's nothing more to consume and so the only option here is to kind of reduce beta to x. Notice that if I have reached beta then basically I should have beta in my in my stack on the top of my stack also. That's just by the construction of the automaton. Also I would like to point out that if I'm using a DFA instead of an NFA then I would just change this statement to saying that if the state that the DFA state contains x produces beta dot as one of the items in that state. Recall that a DFA state is a subset of NFA state so if it contains x produces beta dot then I'm going to reduce. Okay similarly if after running the automaton on alpha if I reach a state which looks like the following x produces some string beta which contains the terminals and non-terminals dot t small t which is represents the terminal followed by some gamma then I should shift okay and I should shift and by consuming the next token and hopefully the next token is going to be at small t. If I have a look ahead then I can also check whether the next token is a small t and only then shift. Okay so the idea is that if I reach a state which is x produces beta dot where dot is on the rightmost side then I'm going to reduce. If dot is somewhere in the middle or in the beginning then I'm going to shift. Now the obvious question is if it can reach both these states then what do I do well that's the case of a shift reduce conflict. So first of all it is possible that there are two you reach two states one of them is x1 produces beta 1 dot and the other is x2 produces beta 2 dot and both of them indicate a reduction but they indicate different reductions so I could either reduce beta 2 to x2 or beta 1 to x1 and which one should I do well that's you know it's a choice in our shift reduce parser parsing state and if we reach such a choice then we have a reduce reduce conflict in our parser and in a predictive parser we won't have any choice we will just have to say that I couldn't you know I don't know how to resolve this choice or I'm going to just pick one arbitrary choice or something but that's not going to be satisfactory. So any grammar which does not produce such a choice it would be called a grammar that can be parsed using this particular algorithm LR 0 algorithm so it will be called an LR 0 grammar. Notice that LR 0 has no look ahead okay. Similarly if you reach two states one of them is x1 produces beta 1 dot which indicates a reduce and the other one produces x2 produces beta 2 dot t gamma which indicates a shift then I have a shift reduce conflict so I don't know whether to shift the next token and hope that it's a team recall that there's no look ahead or whether to reduce beta 1 to x1 and I don't really know which one to do okay so the algorithm is the following that at every point I'm going to run the entire stack from bottom to top or left to right on the automaton if I get stuck you know I'm not being able to parse this there's a parser if I reach this particular state a state that looks like x produces beta dot then I reduce if I reach a state that looks like x produces beta dot followed by some token t and followed by some other string gamma then I am going to shift and if I have more multiple options then I call it a conflict all right okay so this is the LR 0 algorithm LR 0 parsing algorithm we already knew what is shift reduce parsing algorithm with there we were basically saying initial exposition of the shift reduce parsing algorithm was that some oracle is telling us whether to shift or reduce now here we have an automaton that's going to help us make this decision sometimes it's going to say shift sometimes it's going to say reduce sometimes it's going to say neither which means it's a parser and sometimes it's going to throw up multiple options in which case it's a conflict right so if it throws a conflict then it's not an LR 0 graph or if it could throw a conflict on any particular string while parsing any particular string then it's not an LR 0 graph so one way to improve the precision of this algorithm is to increase the look ahead right so maybe I start looking at the next token t and based on that I can rule out certain options so for example if I have a shift reduce conflict so let's say I reach these two states one is a reduced state x produces beta dot and the other is a shift state x produces beta dot t gamma but then I have a look ahead of one so I can look at the next token and the next token is let's say not t if the next token is not t then I can rule out the shift possibility because in the shift I would have expected the next token to be t and that's what my NFA would have done it would have consumed small t and moved to a new state but now there is no I mean if there's some other terminal let's say t prime then there is no the NFA has gotten stuck which basically means that I have reached a situation which is not a viable prefix which means I basically reached a problem and so the shift step would in that case lead us to a wrong path and so the only path that is left is going to be x produces beta dot now you may say okay how do I know whether the reduced path is going to lead us to the right path or not either or maybe you know it doesn't even solve the reduce reduce conflict maybe there are two reduces or something like that so that's the next improvement over the LR0 algorithm so once again we have already learned about the LR0 algorithm that's our first LR parsing algorithm and now we're going to learn about the next improvement over LR0 algorithm which is the simple LR algorithm or SLR algorithm these algorithms only differ in how precisely do the rule out moves that are not fruitful to reduce conflicts just like we saw in the previous example maybe with the look ahead of one I was able to rule out more things all right so the more things they can rule out the more precise they have they become and just some heuristics to rule out more and more stuff is makes it more and more precise so what is the heuristic that simple LR uses to rule out some of these possibilities so the simple LR improves the LR0 algorithm by the following idea first of all it uses the look ahead to see whether it should shift or not just like we discussed in the previous slide so if the next token is a small t and the the NFA basically expects some other t then it's you know then that shift is not an option all right similarly reduce by x producing beta only if the next look ahead small t belongs to follow of x all right so the first thing to note here is that SLR uses a look ahead of one we already said that especially in the case of shift it's very useful if the next token is something other than what the NFA expects then we can just say that shift is not an option now I want to also rule out some of the reduce options if possible whenever possible and so here is the idea that if I have an option of reducing by x produce by this particular production x producing beta then I check what is my next token in my input stream if that next token does not belong to follow of x then I don't reduce okay so what's the rationale behind doing this well notice that if I have alpha if I have this particular state here of my shift reduce parser right alpha cursor t omega then if I reduce by beta then I'm going to end up with some state which looks like alpha prime x cursor and t omega because I would have reduced a part of alpha which would be beta with capital X and I would have left the cursor unchanged and the next terminal is still small t and now for this to be a valid parse this needs to get reduced all the way to the start symbol which means that this is actually a valid rightmost derivation starting from the start symbol and if this is a valid rightmost derivation this the terminal small t follows capital X all right and so this reduction would only make sense if small t belongs to follow of x and recall that we learned about follow how to compute follow sets when we were discussing ll1 parsing recursive descent ll1 parsing and so we can use the same algorithm to identify the follow sets of all non-terminals and then we can use this pruning heuristic which is that if my lookahead terminal is does not belong to follow x then that particular reduction is ruled out otherwise that potential reduction is active and I'll have to consider it so simple lr is an improvement over lr0 because it rules out some more reductions and anything that rules out some more reductions or more shifts then basically reduces the number of potential conflicts and anything that reduces the number of potential conflicts means that it can now accept or parse more grammars okay so that's why slr is a an improvement over lr0 because it can now parse more grammars some grammars that were not possible using lr0 are now possible using slr now lr1 is actually an even better improvement over slr and the idea here is that you build the lookahead into the item states itself so recall that in the nfa we were basically saying that each state is the item which basically represents what kind of prefix I have already seen in my in my stack all right but now I also say that what kind of prefix I have seen on my stack and what is my lookahead token and therefore one look ahead so I'm just going to say this is a stream that I've seen in the stack and this is my next lookahead token and now if I consume a token where do I go and so on so in this particular scheme I just my items now look like a tuple one is the original lr items which I'm now also now going to call lr0 items which is just the production the dot plays anywhere but now it also has another component which is all the any terminal derived from the terminal set of the language so any token in the grammar now lr1 so using this so notice that lr1 is strictly more powerful than lr0 because the items now are more fine-grained now for the for a single item in lr0 I have n items in lr1 one for each token and so I have more fine-grained disambiguation of which what state I am in and based on that more fine-grained disambiguation I can make better decisions or in other words I'll have fewer conflicts however one of the problems with this lr1 strategy is that the nfa can become really large because the number of tokens can be pretty large for a large language and large grammar and so one of them one of the kind of simplifications over lr1 is la lr1 which so lr1 tracks the lookahead token in a fine-grained manner by incorporating it into the nfa states and la lr1 is an improvement or is I should say the simplification over lr1 and it's a simplification in the sense that it reduces the size of the nfa but it is not an improvement it's actually it actually also you know trades there's a trade-off because it also now accepts fewer grammars it's able to parse fewer grammars as we saw in the Venn diagram in the previous module and so in la lr1 mod automaton how does it simplify it merges two states in the nfa if they differ only in the lookahead token so in the nfa if the two states differ only in the lookahead token and nothing else which means you know all the outgoing edges are identical all the outgoing transitions are identical or the incoming transitions are identical the only difference is basically in the lookahead token contained in that particular nfa state then you just merge all of them together and and doing this basically you lose some precision but more but importantly you shrink the size of your nfa or the dfa significantly and that's a significant improvement and so most languages are actually la lr1 and as I have said before popular parser generator tools like python use la lr1 by default
