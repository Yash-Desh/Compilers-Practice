Title: Compiler Design Module 124 2: Running time of flow insensitive analysis vs  flow sensitive analysis
Video ID: _6sKeWRlB5Q
Detected language: en
------------------------------------------------------------

In this module, I'm going to discuss the running time of a flow sensitive DFA vis-a-vis a flow insensitive DFA. Now it is common understanding and knowledge that a flow insensitive DFA is faster than a flow sensitive DFA and we're going to try and dig this further in more detail on why this is so. So in both cases, whether it's a flow insensitive DFA or a flow sensitive DFA, we use Kildal's work list algorithm to figure out which edges to consider for relaxation of the DFA value. Recall that even for the flow insensitive DFA, where we maintain a global value, we typically proceed the DFA using the same work list algorithm so that we have a better shot at identifying which edges to consider for the relaxation. Now in both cases and when I, the other trade-off between a flow sensitive DFA and a flow insensitive DFA is of course the fact that a flow insensitive DFA is less precise than a flow sensitive DFA. However, we have also discussed that once we convert the program into SSA form, then a flow insensitive DFA has equal precision to the flow sensitive DFA, assuming all the quantities of the interest in the DFA have been converted to SSA. For example, most IRs would convert all the scalars to SSA and if the DFA is tracking scalars, then a flow insensitive DFA will be as precise as a flow sensitive DFA. Now, the maximum number of updates that a fixed point algorithm, data flow analysis algorithm makes or could make is basically number of program points into the height of the semi-lattice and the argument here is that every update is going to bring the value, some value at some program point, one step down in the semi-lattice and because the semi-lattice is a bounded finite height semi-lattice, eventually all values will come down to the bottom value in that semi-lattice and that would be the termination, that's the termination condition of the fixed point algorithm and that has to be the termination condition and so the maximum number of updates that can happen in a flow sensitive DFA is the number of program points into the semi-lattice height. Now when I talk about the number, maximum number of updates in a flow insensitive DFA, in that case there's only a single global value and that global value can only go, for any update it can go one step down in the semi-lattice height and so you may be tempted to think that the maximum number of updates in a flow insensitive DFA is only H which is the semi-lattice height. However because we have converted the program into SSA form, the height, the semi-lattice height has actually become bigger because the number may have become bigger for most DFA's because the number of variables has become bigger and recall for many of the DFA's the height is proportional to the number of statements or the number of variables and both of them increase when we convert the program to an SSA height and in general you know the number of updates that is still proportional to the number of program points into the semi-lattice height for the original semi-lattice on the flow sensitive DFA. Even if you convert it to SSA and run a flow insensitive DFA, the semi-lattice structure doesn't really change and so the height remains similar and so the maximum number of updates is the same in both a flow sensitive DFA and a flow insensitive DFA and that's given by N into H which is where N is the number of program points and H is the semi-lattice height of the original program. Okay now we have already said that the maximum number of updates is order N into H. Now the question is what's the time taken between two updates in the Kildal's work list algorithm. Now recall that when we run the Kildal's work list algorithm in a flow sensitive mode then every time a node value a value at a node changes we add all the incident edges to the work list so for if it's a forward DFA then we add all the outgoing edges to the work list and then we check whether the values at the destination of those forward edges or the two node of those forward edges does that need to be relaxed based on the change at the from node of that forward edge and if that node does not need to be relaxed then we just remove it from the work list. If it needs to be relaxed then we relax it and we add its forward edges to the work list and so on and we keep doing this. That's recall the Kildal's work list algorithm. So the maximum number of steps that you need to take in a flow sensitive algorithm between two updates is basically the typical degree or the number of incident edges to that to that node for example if the number of outgoing edges in a forward DFA from a node was four then at most I would take four I would do four different explorations before I hit one update. If none of those four had an update then I would just actually I would declare that I've reached a fixed point I'll terminate the algorithm but if there was there was supposed to be an update then that update must be among those one of the four outgoing edges and the maximum gap between the maximum amount of work that you would do between the original update which is at the from node and the new update which is at one of the two nodes is four because you may have to look at you know four different edges before you actually get something that you want to update. If you are lucky maybe the first edge that you looked at was needed to be updated in which case it could be order one. So in the flow insensitive case the order the number of the time taken between two updates two value updates is between order one and order D where D could be the out degree or the in degree I've written out degree and written this as DO because I'm just using a forward DFA as an example without loss of generality. If it's a flow insensitive analysis however then when we relax the value and there's only one global value then we basically add all the nodes all the program points as part of all the edges in the program as part of my work list and this is done because we don't know which other instruction in the program may be using the value which has been changed in this particular step of the DFA and so in the flow insensitive case the maximum distance between two updates can be again order one if we are lucky or it could be as high as order n which is the size of the program because when something changes in a flow insensitive algorithm we add all the edges or all the instructions as part of the work list all of them need to be checked to be sure that we have reached a fixed point or not and if we have not reached a fixed point it's possible in the worst case that I have to look through n minus one instructions before I look at that that word particular instruction that was using the result of the newly updated value and in which case the maximum amount of distance or time taken between two updates can be as high as order n in flow insensitive so it seems like the amount of work that can in the worst case that it has to be done between two updates is higher in flow insensitive than in flow sensitive. However recall that we use we typically use the reverse post order for forward DFAs or the post order for a backward DFA to quickly identify the nodes the likely nodes where we need or the likely instructions where we need to relax the DFA value and that actually is very very effective typically and typically the amount of work that you have to do between updating a value and then identifying another value that needs to be updated as a result of the previous update is typically order one and that's what that's what I'm going to assume in the rest of this analysis so although the worst case values are ordered out degree or order n the time taken between two updates the typical value is actually just order one okay finally we have we have already characterized the maximum number of updates we have also characterized the number of the time taken between two updates the third thing we need to characterize to be able to characterize the overall running time is the time taken by a single update so recall that when we do an update then we have you know let's say we are doing an update across an instruction S then we have some value input value at the beginning of S once again without loss of generality I'm assuming a forward DFA so we have an input value at the beginning of S and we transfer it across S to obtain a transferred value in in transferred or I'm calling it in expert or in transferred I'm going to call it so I'm transfer in to in transfer and then using in transfer I update out which is which is the original value at the out of S and this this update can involve a meet operation between in a transfer and out now the time taken by an update basically involves changing the out value from whatever it was originally to its new value and that involves first transferring from in to in expert and then applying it to out and if delta and let delta be the magnitude of change between out and in and this is the this is the amount of work I claim that has to be done from in this transfer because after all whatever was the in you know whatever is the distance between in and out that much amount of changes need to be made at in out and at a minimum because after all you know some changes have to be made and whatever is the what the time taken to make the modification is at least proportional to the amount that the size of the change that that has that has resulted from this transfer okay so let's try to understand what the size of a change could be so for example let's say the transfer function for a statement S involves you know using the kill and gen sets the X minus kill union gen then the size of a change is basically less than the size of these sets kill and gen for that instruction S and this delta S is the distance between in transferred and in it's not the distance between in and out I'm going to come to that later so I'm going to characterize the distance between in and out and divide it into two parts one is the distance between in and in transferred and another is the distance between in transferred and out so if I look at the distance between in transferred and in then that distance is characterized by you know by delta S which is less than equal to in this case the sizes of kill and gen and typically these sizes are much smaller than the program size for example for many BFAs this size is just a constant for in three address code for example if I talk about liveness then kill is just all the values that are defined in that statement and gen is all the values that are used and because it's three address code these these sets have to be small maybe one or two size because it's a three address code representation similarly constant propagation would just require a constant number of kill and gen sets for every instruction however there it's not necessary that these are constant for example for available expressions we may kill all the values that contain a particular variable in which case it can be non-constant but even when they are non-constant they are actually much smaller than the size of the program or the size of the overall set of possible values that this program that this analysis could produce okay so we are going to go ahead with the assumption that delta S which is the distance the size of the change between a previous value of out and the new value of out between the value of in and value of out is actually sorry delta S which is the distance between the in transferred and the in is small okay now the distance between in and out the change between in and out is upper bounded by the change between in and in transferred and the change between in transferred and out there's a standard triangle inequality and we already have characterized the change between in and in transfer which depends on S and we call it delta S and we have already said that delta S is typically much smaller for most DFA's than the size of the overall program the next question that remains to be answered is what's the delta between in transferred and out and that would tell us what is the time taken by a single update typically so let's look at let's study this quantity in trans the delta between in transferred and out and we're going to discuss this separately for a flow sensitive analysis and the flow insensitive analysis so let's first look at the flow sensitive analysis in a flow sensitive DFA the in transfer so you know if there was an original value out it's basically flowing through the entire program and after flowing through the entire program it may have flowed into in because of let's say some cycle or something like that and that may have created in transfer so the path between out and in could be pretty long and because of that all the deltas could have pretty much accumulated across this path and so the gap between in transfer and out can be pretty large because it may include changes due to many statements and this is this is true for a flow sensitive DFA because in a flow sensitive DFA we are maintaining different values at different program points so for example out and out next would have be different by delta s and then out next and out next next would be different by another delta s and so on and all this can pretty much accumulate into a big value and a big difference and that could show up in the delta between in transfer and out and this could be as big as order n or it could be as you know depending on if the value size is small depending on the DFA if the value size which is the size of the maximum value of course you know the change set cannot be bigger than the size of the maximum value so you can say that this order of minimum of n comma value size okay so why is it once again why is it n because after all there could be up to n different changes and but if the value size is smaller than n then the maximum difference the distance between any two values whether they are in transfer and out cannot be bigger than value size so we also use a min to denote that okay but if I look at a flow insensitive analysis recall that in a flow insensitive analysis the out value and the in value are the same there's just one single global value and so the distance between in transfer and out is actually also upper bounded by delta s because after all out and inverse is the same and if the distance between in and in transfer is delta s then the distance between in transfer and out is also delta s in other words even though there could have been a big path that that the DFA transfers the DFA traverses before transferring to in transfer that the updates have been applied are being applied incrementally across every statement as the DFA progresses so we don't have you know across a single update we don't have a very big distance between in transfer and out in a flow insensitive analysis unlike a flow sensitive analysis so delta in and out is actually on the order of delta s because we know that n equals out in a flow sensitive insensitive analysis so in a flow insensitive analysis the distance between the gap between the value update is small it's local it depends on s but in a flow sensitive analysis it could be much bigger it could be global it could be as big as order n or order value size whichever is smaller and that that actually is a big difference between a flow sensitive and a flow insensitive analysis running time that comes up as a big difference in the running time of the two analysis okay so to summarize the maximum number of updates in both a flow sensitive and a flow insensitive analysis is similar it's both n into h where h is the size height of the semi lattice of the original non-ssa program the time taken between updates in the worst case is only you know out degree or in degree depending on whether it's a forward analysis or backward analysis in flow sensitive and it's order n in flow insensitive but typically because of the partial order the post order or the reverse post order this is order one but the time taken by a single update can be as high as minimum of n comma value size because it's a summation of all the delta s's whereas in the case of flow insensitive this is only order one to say whatever is the maximum delta s for a given statement and so the typical total time is basically obtained by multiplying n into h with the time taken with the single by single update because if you assume that the time taken between two updates is just order one then the time overall time taken by an algorithm is the number of updates into time taken by a single update which in this case is n into h into minimum of n comma value size and if i take the value size to be as big as n then it can be as bad as order n squared h whereas here it's just order n into h into delta s and if i assume that delta s is constant size with respect to the size of the program then this actually is order n h and this is the reason why a flow insensitive analysis with ssa is typically much faster to do than a flow sensitive analysis without ssa and that's one of the reasons why compilers or compiler developers prefer to use an ssa ir like the llvm ir because it allows faster analyses without you losing precision of those data flow analysis
