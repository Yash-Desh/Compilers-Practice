Title: Compiler Design Module 44 : Static vs  Dynamic Types
Video ID: vCsm3I4Hep8
Detected language: en
------------------------------------------------------------

Okay, so we've discussed typing and typing rules and how to do static type checking. So one of the things that was kind of implicit in our discussion so far was that we're doing static type checking because we used to check the AST statically to see whether it is well typed or not. Actually type systems can be thought of as two different types of type systems. One is a static type system and another is a dynamic type checking system. So languages can either have static type checking or dynamic type checking. We've already seen how static type checking works by doing bottom up pass over the AST. How does dynamic type checking work? Well dynamic type checking is nothing but just putting runtime checks to check whether the values are of the expected types or not. So in fact I think we have used the example of array out of bounds in the past where we may want to say that this is an array of size 100 and then let's say we index into the array using some variable i and then there would be dynamic type checker that would check whether i lies between 0 and 100 or not and if not then it would be flagged as a type error otherwise it would be a successful type check. Now array out of bounds was an example of a property that is better checked dynamically than statically because statically it's very difficult to say whether something belongs to an array's array bounds or not. However there are many properties that are very easy to check statically. For example whether something is of type integer or not. If it has been constructed through an expression tree that produces a value of type integer for sure we can say that that expression has type integer. For example if it is formed by adding two integers then it would be an integer and so on. But languages that have such form of static type checking also kind of have notionally the idea of dynamic types as well. So for example if I say 1 plus 2 the dynamic type of 1 will also be integer. This dynamic type of 2 will also be integer and then when I add the 2 the result would be 3 and the dynamic type of 3 would also be an integer. Now this is dynamic types that are getting propagated in the system and the runtime may or may not want to maintain these dynamic types. Even languages with static type checking have the notion of dynamic types for the kind of types that are being checked. I'm not talking about array out of bounds but even things like whether something is an integer, whether something is a pointer to a character and things like that. While we have looked at it from a static type checking point of view you can even consider that the runtime may be maintaining types all along the way and it may want to check them. So just that they may or may not implement dynamic checks and typically they will not implement dynamic checks because what is the point of implementing dynamic checks if you have already statically checked that all possible computations can only produce an integer or a pointer to a character. So if you have checked that statically you don't need dynamic checks and that's why static type checking is so useful because you don't really pay any runtime cost. Okay so we do not need the dynamic checks for properties that have been checked statically for example we don't need to check dynamically that 1 plus 2 produces an int we can check these things statically. Now that actually the notion of dynamic type allows us to state our soundness theorem differently. Recall that when we talked about a type system we said a type system is sound if all the values that an expression can produce are always of the same type as the type inferred by our static type system rules. So that's another there's another way to state the same property soundness property and we can also call it a soundness theorem is that for all expressions E the dynamic type is a subtype of the static type so a type system is sound if the dynamic type that is given by the execution semantics of the language is will always be a subtype of the static type so notice that I'm not saying that the dynamic type needs to be equal to the static type that would be a very strong condition and that's not what we are aiming for that would make it very restrictive and that would result as we going to see with examples that would result in many programs getting rejected by the type static type system we are okay as long as the dynamic type is a subtype of static type okay all right so in other words the static type checker will not accept any program that would have failed the So what do I mean by of equal power? basically whatever is the whatever properties that the dynamic type checker is checking is exactly the same properties that the static type checker is checking that's what I mean by equal power for example the dynamic type checker is not checking array out of bounds or anything like that it's only checking those properties of the static type checker is also checking and soundness basically means that the static type checker will not accept any program that would have failed the dynamic type checker of equal power. So notice that the the key phrases will not accept any program. So it says that anything that it accepts must always pass the dynamic type checker and anything you know so if the static type checker accepts a program then it should have also passed then that program would always pass the dynamic type checker's checks also assuming that the dynamic type checker is checking for exactly the same properties. Another way to say that is that all dynamically type incorrect programs would always be rejected by the static type checker. So anything that is dynamically type incorrect would definitely get rejected by the static type checker because the static type checker is basically the soundness theorem says that the static type checker checks whether for all possible executions the type system is being followed or not and so if there was a program that could be dynamically type incorrect means there is some execution under which the type check the dynamic type check would have failed then the static type checker would also have failed on that particular program because that's the definition of soundness. Okay so but notice that it is not saying much because even if the static type checker simply just rejects all programs that would be sound okay so because that still holds our static type our soundness theorem because it just says that anything that is accepted by the static type checker will always be resulting dynamically type correct executions but if the static type checker doesn't accept any program that's vacuously going to obey the soundness theorem. Also notice that if the static type checker just assigns void to all the you know just assigns void the void type for every expression or for every operator then what will happen is that only very few programs would be able to pass the static type checker for example I've used this example before so let's say I have 1 plus 2 plus 3 now if 1 plus let's say this is the association so if this 1 plus 2 is computed first and if my static type checker just says that the return type of 1 plus 2 is going to be a void then 1 plus 2 plus 3 would not be accepted by the static type checker right so the static type checker will only accept for example programs that have a single plus in them because now the resulting value can never be added again to something else to another integer so it's very easy to build a static type checker that is sound because you can just reject everything or you can accept only a very small fraction of the correct programs and notice that is why we are when we were talking about typing rules our focus was not only on soundness but also on precision we wanted it to be as general as possible and if you recall we were using the subtyping relation to improve the generality of our rules okay so the the flip property of soundness is called completeness and a complete a type system would be complete if all dynamically correct programs are accepted by the static type checker all right now this is a different property from soundness in soundness we were saying all dynamically type incorrect programs are rejected by the static type checker so that's soundness and the type system we have seen is sound completeness is the flip I just replace the word incorrect with correct and rejected with accepted and so completeness properties all dynamically type correct programs are accepted by the static type checker now is the type system that we discussed sound well actually in sound the answer is yes is it complete the answer is actually no and in general completeness is much harder property than soundness all right I mean actually I should say both soundness and completeness together is much harder to achieve than achieving only soundness right I can also achieve only completeness example I could accept all programs and that would be still complete but that won't be sound so soundness is very important I can't you know without soundness the type system doesn't we make sense but achieving soundness is easy but we want to be as general as possible but achieving both soundness and completeness may not always be possible and here is an example so let's say there are two classes C and D and let's say D is a derived class of class C in other words D is a subtype of C all right now let's say I declare two objects of type C small C and after type D small D and let's say I have a program which is which says another object D2 of type D is depending on some condition using the ternary operator either C or D now notice that the type of this particular expression here on the right of the assignment operator would be the least upper bound of C and D which would be C right based on our typing rules because the type of C is C the type of D is D and so least upper bound of C and D is C based on the type hierarchy so this would be C but the the type of the LHS of my assignment is D and notice for an assignment I wanted that the LHS should be a subtype of the LHS and that's not true here C is not a subtype of D it's the other way around D is a subtype of C but C is not a subtype of D and so this would fail the type check this would fail the static type check so let me just write it down this fails static type check all right but let's say let's look at the dynamic type checker let's say that dynamically I am hundred percent sure somehow that this condition is always false now if this condition is always false then I would always assign D2 to D all right so let's say you know dynamically it so turns out that for all possible inputs that I will ever execute on this program this condition is always false but that's very difficult to figure out at compile time and and so this is always false and this the dynamic type of D2 will always be D and so dynamically this will never fail the type check and so this would be a dynamically type correct program in that case but it has been rejected by a static type system and thus our type system as we discussed is not a complete type system and general it is almost impossible to unless it's a very simple language to have both a sound and complete static type checker in general here is a Venn diagram that explains this relationship between dynamically type correct programs and statically type correct programs if this is a space of all possible programs that could be put together that are kind of parsable so I should say maybe all parsable programs then and scopeable programs then a subset of those programs is is dynamically type correct and another yet another subset of the dynamically type correct programs is the statically type correct all right so so so this is something we should keep in mind and we already saw some example of a program that would be rejected by the static type checker so that program that we just saw in the previous slide would lie in this space because it's dynamically type correct but statically type incorrect soundness basically says that this set of statically type programs is a subset of dynamically type correct programs so that's what soundness is that means that any type correct it cannot be that a statically type correct program is dynamically type incorrect so that's what soundness says so this needs to be subset but you know soundness is easy to achieve because I could just shrink this shrink the brown line really small and that would still be sound completeness says that actually there this there is no space between statically types checker it and dynamically type correct which means they are exactly equal and that's not sound that would be sound and complete but that's usually very difficult to achieve as we just discussed so here's an example of you know why static type system may be often too restrictive in practice so in fact this is an example from the C++ language and it's a you know these kind of problems actually appear practically because the static type system is too restrictive and I thought I should discuss this as well so let's say there is this class called club count and I declare let's say a member variable I and and then I have a method a class member function called ink which increments I and then returns the makes returns a copy of the current object so return start this recall that C++ implements return by value so it's going to create a copy of the current object and return it and that's I mean because the current object is of type count the returned object will also be of type count now let's say there is a derived class from count and let's say that's the derived class is called stock and let's say you know it derives from count which means it has I a state member variable but it also has some extra extra field that's a name field which says that what is the name of this particular stock okay so so this would be a derived class or sometimes stock is a subtype of count now let's say I wanted to write such a program where it says stock a stock B equals a dot increment and then you know I want to use B's name somewhere because I know that B is of type stock now is this program type correct the answer is no this program is that type incorrect as far as the static type system goes why because if you look at this particular function call here a dot ink this is going to return of an expression of type what will be the type of a dot ink well we are just going to use the method typing rule and it's going to tell us that the return type is count whereas I'm trying to assign it to a variable which is stock and that would fail the type check of the assignment operator because the assignment operator needs that the RHS of the assignment operator needs to be a subtype of the LHS whereas here it's not true because the RHS type is count and the LHS type is stock and count is not a subtype of stock and so this is type incorrect more specifically this is type correct statically so if I statically it's static type incorrect but if I look at the dynamic type then actually I there is no way that this program can have a type error because I'm doing stock A and A is of type stock and I'm calling ink on stock not on count it just so happens that ink is defined inside the class count and so so dynamically speaking this is always okay because B would just get a copy of A and A is a stock so we are okay so this is you know can be argued that dynamically this is okay all right and now if I want to do this it's it's a little difficult to do it just because the static type system doesn't allow us to do it right so this is a practical example this is a practical programming pattern that makes it difficult to write useful programs because the static type system is too restrictive now how would you solve this problem well either one option is that I basically you know if I basically used the type count for B that would have sort of solved the problem right because that way this statement would have passed the static type check but then I won't be able to use name inside B because now B is of type count and although the dynamic type is stock the static type is count and so I can't just refer to the field name because name is only specific to stock all right so so this is also not a satisfactory solution it's not even a solution actually all right so so this would so while something something like this would have passed the dynamic type check it fails the static type check so one potential workaround of this kind of thing and this is something that C++ is the idea of a dynamic cast operator all right so what I can do is I can say stock star s equals dynamic cast stock star ampersand B the idea is you know the statically I'm basically saying that B is a count of is type count but then I can create an alias to it I can create a pointer to it of type stock by using this operator called dynamic cast and this may or may not succeed at runtime depending on whether B was actually an object of type stock or was it only an object of type count if it was only an object of type count then this dynamic cast operator would return null pointer otherwise you know it would be able it would return a non null pointer and I would have a pointer to an object of type stock and now I can reference the name of that stock later on in the program and I can you know maybe assert that s is never null pointer so in the example that we had the programmer knows that it's not possible that B is not a type stock and so I expect that this particular dynamic cast would always succeed on runtime at runtime now there are a couple of problems with there's a couple of while this approach works and actually practically used in a lot of C++ code because it's a workaround to the problem that I just described you know first of all it has some amount of runtime overhead it is not a very satisfactory solution in that sense because it's kind of saying that you know it's a way of kind of getting bridging this gap between dynamically type correct and statically type correct programs but but this is this you know so this is a trade-off that that language designers have to make and indeed the current kind of state of practice is something like the dynamic cast operator is is often used in real code another potential workaround which is slightly more complicated but would have also kind of I mean you know some people may think of it as more principle because you are letting the static type checker do more with what you're doing is to basically define the count class as a template and allow it to take a type T that you know that is basically the return type of the increment method so the idea is that I am going to you know return not a type of I'm going to return a copy but I'm not going to return a copy of type count but a copy of type T so or which basically means that it's must be true that T is a subtype or derived class of count so this is a code I basically again do increment of I and then I say return star this except that before star I do a static cast of this with a T star now why is this code correct well first of all what I need to do for the derived type is now stock doesn't derive from count but count is no longer a class but a template so I'm going to say it's stock derives from count with the of stock right so this is slightly stylized usage which basically means that it is the count but the no the type T which is supposed to be the right type of count is actually stock and so anytime you do an ink you should return a stock and not a count also notice that it has a static cast operator instead of a dynamic cast operator and the static cast is not evaluated at runtime but is evaluated at compile time and indeed in this case the compile time check would succeed because this would be of count star but because of count stock star and count and because the compiler would be able to check that you know this is actually always being called with from the stock or from whatever it's being called it is actually a valid type so at compile time you can check whether for all possible paths to this particular point is it true that this cast can be done and if so then then that's fine otherwise it would otherwise this would have thrown a compiler so unlike dynamic cast which which gives you returns a null pointer at runtime a static cast either gives you a compiler or it succeeds your compilation
