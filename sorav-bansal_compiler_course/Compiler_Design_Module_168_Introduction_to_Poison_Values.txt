Title: Compiler Design Module 168 : Introduction to Poison Values
Video ID: cIJ9pea8gSk
Detected language: en
------------------------------------------------------------

So last time we talked about how undefined behavior semantics are both a boon and a bane for optimization. Undefined semantics in the source language help optimization, but undefined behavior in the target language or the target program actually thwarts transformations and thus optimizations. And many optimizations in our compiler pipeline are IR to IR transformations, which means whatever undefined behavior is present in the source is also present in the target. And so the IR designer needs to walk a tight rope between optimization opportunity in the source and avoiding the thwarting of optimization opportunity due to UB in the target while designing the IR. And so we are going to discuss one such example of undefined behavior semantics in an IR like LLVM which are called Poisson values. So Poisson values are also undefined behavior semantics but they are a weaker form of undefined behavior semantics and it will become clear in the discussion and the rest of this module what I mean by weaker. So recall that when we talk about UB or undefined behavior, we say that if there is an error condition that occurs the error condition could be integer overflow for a signed integer arithmetic operation or it could be out of bounds memory access or it could be divided by zero or anything like that. Then the error condition, if that happens then that basically means that the program is wrong which means that the compiler can do anything, the runtime can do anything because what the programmer is basically given as a program is actually an incorrect program because this program can have an illegal behavior because this program actually executes an error condition. Poisson value semantics or I'm gonna call it Poisson value or Poisson value semantics interchangeably says that if there's an error condition for example, because of signed integer overflow then it's not that the program is wrong, the program is still be correct just that an error value is generated into the target. So if I'm saying X equals Y plus Z and if Y plus Z is an arithmetic operation that triggers an error condition for example, due to an overflow then it's not like the program is incorrect we just return an error value into the destination variable which in this example is X. So this is a weaker form of undefined behavior we basically saying that the program is correct program is incorrect means anything can happen out of that anything can happen we pick one particular behavior, the particular behavior is that this particular value is an error value, in other words that value should not be used or should not be printed out or should not be sent, should not be used to do some critical stuff or anything like that. And so that particular error value I'm going to call the Poisson value. Why is it called a Poisson value? We're going to discuss later in this module but just bear with me and basically let's call it a Poisson value from now on. So if there is an error condition that figure that occurs in the program then a Poisson value is returned into the destination register. And this I'm talking in the context of an intermediate representation and in fact the Poisson value concept was introduced in the LLVM IR. Okay, so let's say I have a source language and I have a source program written in the IR and I'm going to use a C-like syntax for my IR just like I was doing in my previous module. Similarly I have a target program again written in a C-like syntax but it's actually an IR program. And in this we basically have a while loop and say y equals x plus one and then I'm using y and the transformation is that I do loop invariant code motion and I have watched this entire computation y equals x plus one above the loop and bring it to y equals x plus one before the while loop. And this is the standard loop invariant code motion algorithm. However, we saw last time that under UB semantics, undefined D-way semantics, this transformation is not valid because if you consider the situation where x equals int max which means int max plus one will definitely overflow and the loop was actually never entered in the original program then in the new program you are basically computing x plus one anyway and you're basically triggering the error condition and that's basically causing the program to be incorrect. So the original program was correct but the transform program is incorrect for a particular input and that is not a valid transformation. If for any input, if the original program was correct then the transfer program should also be correct and if you have the same behavior but that's not true in this case and this is not a valid transformation and this is actually a big disappointment because these are the kind of transformations that we've been discussing all through the course. So how do we manage to still do these transformations? And the answer is Poisson value semantics. Something that we've already discussed. In the Poisson value semantics if x plus one causes an overflow that means there's an error condition then it's not like the program is incorrect. It just means that y is assigned an error value and so if I look at my target program then if x plus one overflows if I look at this particular instance when x was int max and the loop is never entered then x plus one will overflow but that doesn't mean the program is incorrect. It just returns an error value into this variable y and that's okay because now the error value has been returned into y but the loop will still not be entered assuming we have maintained the semantics of the loop and so the loop will not be entered and y will never be used so it's as good as a know-how. So in this case if I use the Poisson value semantics these two programs are actually equivalent because the target generates an equivalent value. The target generates an error value that is into y that is never used and so that's okay even though I'm generating an extra error value in the target that I was not going to generate in the source that error value is never used and we are doing okay and so the target program is actually equivalent and so I can say this is okay. All right so this transformation is actually okay and this transformation becomes available and becomes possible using the Poisson value semantics but was not possible using the undefined behavior semantics. So what is typically happening in programming language design is that a source language like C would use undefined behavior semantics because compiler pipelines don't typically transform C to C but an intermediate representation like LLVM would typically use semantics similar to the Poisson value semantics where it allows such transformations like loop invariant code motion or in general code hoisting transformations to become possible. Let's look at another example that we also saw in our last module which is let's say there's a variable called x and I have if C y equals x plus one and then I have you know because this is an IR representation of the program I have a phi node recall that my IR is SSA form and so in the phi node I say that if I come through this path then I get some value I'm calling it dot dot dot and if I come through the if path then I get the y value which was assigned on that particular branch which is y equals x plus one. Now let's say I want you to transform it so I will transform it as such I'll say y one equals x plus one unconditionally I will do the transform I'll do the arithmetic x plus one and then I would say if not C then revert that I mean kind of you know revert that computation and say y two equals y one minus one and now in this case again if I say z equals phi on this path which is the path of C I'm going to say that it is taking the value y one and on the path of not C I'm going to take this dot dot dot value okay and are these two programs equivalent and we have already seen that these programs are not equivalent under the undefined behavior semantics that C language has because in this case y equals x plus one is executed only if condition was true and so if x plus one was causing an undefined behavior it would only cause an undefined behavior if C was true so if you consider the case when x was int max which means x plus one would have caused ub and C was false that means this would never have executed then the source program basically never causes ub it actually does nothing whereas the target program actually causes ub unconditionally because it's doing x plus one irrespective of whether C is true or not so but when I look at the poison value semantics these this transformation becomes okay because if you consider this case this problematic case that we had discussed earlier which is x equals int max and not C then the target does have an error condition but the error condition just generates an error value or a poison value into y one and in this case and it will never be used because if C was false then I'm going to use this particular path and I'm going to use this dot dot dot value right so it's okay to do this transformation because even though I did an exerisimative operation that caused an error condition which was overflow that didn't mean that the program is wrong it just kind of returned an error value into y one and because you know I'm assuming that not C is true because that's why x plus was never executed in the source program I'm going to end up with a dot dot dot value into z that means y one is never going to use the error value is never going to get used in z so that's basically the idea the basic idea is that for some kind of operations the error conditions basically cause a poison value to get returned into the destination register instead of just saying that the program is wrong because if you just say that the program is wrong then it becomes very difficult to hoist competition above because if you hoist competition above you have to worry about all those error conditions and if those error conditions can cause the program to be incorrect then many of those hoistings become illegal but on the other hand if those hoistings just cause an extra error value then it's okay because in just in case that error value was not getting used or generated in the source program hopefully your transformation is ensuring that the error value is not going to get used or in this target program also even though it may get generated all right so that is the that's basically my introduction on what are poison values and how do they model undefined behavior in a weaker form because here we are not saying that the program is wrong we are just saying that an error value is generated now the question is this poison value is a special value it is not one of your integer values 0 to 2 to the power 32 minus 1 or anything like that it is actually a special value that is part of the operational semantics of my intermediate representation language the IR language the IR itself is a syntax and it has it is associated with its own operational semantics and so just like a source language like C would be associated with its own operational semantics and similarly the assembly language would be associated with its own operational semantics so now that we know that the IR contains an extra value called the poison value which is distinct from all other values in the program then what should be the operational semantics of the poison value for example what should happen if I add one to a poison or I divide poison by two or multiply two to a poison or any other number do any other kind of arithmetic operation or poison or what should happen if I divide something by poison so if I say two divide by poison as opposed to poison divide by two so these are the kind of questions that immediately arise as soon as we introduce a new value into my language in this case the IR language so let's look at an example to understand and motivate what should the semantics be and so in this case let's say I have int x and then I have while dot dot dot and y equals x plus one and z equals y by two now we've already seen that x plus one can potentially generate a poison value if x was int max and so in that case y would basically go get the poison value and now z is equal to y by two so basically that would basically mean that z would get poison by two and the question is what should I want what would I want the abstract machine of the IR language to do in this case should I return zero should I return a poison value into z should I consider it as undefined behaviour and so I'm going to discuss this by looking at a potential transformation of this program and the transformation is again the loop invariant code motion transformation that we find to be very profitable and that we've been using as our motivating example all through while discussing undefined behaviour and poison values so let's say y equals x plus one was loop invariant and so was z equals y by two which means it's possible to hoist the entire computation which is loop invariant above the loop now I want this transformation to be possible because this is a very profitable transformation if there is a block of code which is entirely loop invariant in this case the block is made by these two statements then I should be able to hoist the entire block of code above the loop but let's look at different options for what should be the operational semantics of the poison value option one is any operation be it addition, subtraction, multiplication on a poison value should trigger ub which means if you do try to add something to a poison value then that should just mean that the program is wrong which means you should never add or subtract or do any other operation on a poison value but if you do that then what happens to this transformation is it legal the answer is no this transformation is no longer legal because once again if I consider the case when x equals int max and let's say the loop was never entered in the source program then the source program would have done nothing it wouldn't have done nothing because the loop was never entered so it's almost like the source program does nothing but if I look at the target program then it computes x plus one so int max plus one that's an error and so y would contain the poison value and then I would do z equals poison by two and if poison by two triggers ub then my target program is triggering ub and this is similar to how we had earlier said that you can't hoist computation if it triggers ub and so in this case z equals y by two if I use this particular option for my poison value operational semantics then I cannot hoist this computation z equals y by two above the loop so the option two is not very good because this is a transformation that we would ideally like to support in IR this is a very profitable and meaningful transformation option two is that any operation on a poison value results in a poison value and this is the second option in this case when I do y equals x plus one and x was actually int max then I may generate a poison value into y and then when I do z equals y by two that's just poison by two and poison by two should just return poison into z but because the loop was never entered in the source and in the target the z will never get used neither y nor z will ever get used and that's okay it's just similar to our previous example where a poison value or multiple poison values are getting generated but they're never getting used and so in this case the programs would be okay the transformation would be okay because even if I consider this case when x equals int max and loop never returns the target just returns poison in z which is never used and we can show that the source and target programs are actually still equivalent under the poison value semantics under these semantics that any operation on a poison value returns a poison value and does not trigger ub so what are the value operational semantics of my poison value based on that motivating example that I showed on a previous slide and these are actually the semantics in the LLVM IR as well which is z equals x operation y for any such operation on in my three address code if either x or y is poison then z also gets poison all right so that it doesn't trigger ub but it just returns a poison value into z and now I come to the reason why this is called the poison value because it's almost like if one value gets poisoned then whatever derives from that particular poison value also gets poisoned so basically any operation it could be arithmetic operation logical operation or any other operation that the IR supports any operation on a poison value just returns a poison value on itself okay we're going to study more semantics of the poison value in our next module there are many more subtleties that we have not covered in this module but I'm going to introduce them briefly in this module and so to look at that let's look at this particular example similar to the example that we have previously seen which is y equals x plus one and z equals two by y this time instead of y by two I just kind of switch the order of the division operator operands and z two by y and so once again let's assume that y equals x plus one and z equals two by y are loop invariant which means they can be hoisted up and the question is should we allow this kind of hoisting which is y equals x plus one and z equals two by y and the answer is these are the kind of transformations we typically do not want because many architectures would basically just throw an error and completely abort the program on a division by a zero value and so unlike for example sign integer overflow where the machines typical hardware machines would just ignore the overflow and just maybe wrap around semantics or maybe act saturating arithmetic or something like that they won't really terminate the program some operations like division by zero often just terminate the program and this is just a fact of how typical hardware is implemented and based on that this kind of transformation actually is very dangerous because if on the original source program I never entered the loop then even if y was zero it doesn't matter but in my target program if y was evaluated to zero then a two divided by zero can basically cause a divide by zero exception and the program would terminate and so while the source program would have executed correctly which means it would have executed till the end maybe it would have done nothing in the program or maybe it would have done something because of these dot dot dot the target program would definitely throw an error if y evaluated to zero and so we don't want this transformation to be valid we don't want the compiler to be able to do these transformations these transformations are called unsound transformations because the behavior of the target program can be different from the behavior of the source program in this case the source program would do the right thing but the target program would actually potentially depending on the hardware machine actually terminate the program in other words we want that two by y or divide by zero should still remain undefined behavior we don't want divide by zero to actually turn a poison value because divide by zero is actually a more sinister type of undefined behavior it is actually causing the program I mean it is likely to cause the program to actually terminate and as opposed to things like signed integer overflow so basically what we are trying to do is we are trying to disambiguate between different types of error conditions there are some error conditions that are more dangerous than others divide by zero for example is more dangerous than signed integer overflow and so for these dangerous conditions we don't want to basically be able to execute them speculatively in this case because when I execute them speculatively if I actually do a division by zero in that case I am going to end up with a different behavior from my source program and so what happens to model this basically because we don't want that particular transformation we basically say that some operations on the poison value actually trigger UV so because the poison value is an error value and that error value when I go all the way down to the hardware can actually be implemented using a zero value because for example if I say int max plus one with wraparound semantics int max plus one it may give you actually zero and the IR is giving you the poison value and so X divided by poison should we should conservatively assume that X divided by poison can evaluate to X divided by zero which should trigger UV because we don't want that X divided by zero should execute speculatively because that's going to produce a different output than in the source program on the other hand we have already seen that poison divided by something which is non-poison X let's assume here that X is non-poison just returns poison into Y so here's then some brief introduction to some of the issues that we are going to elaborate on in the next module about what are the semantics of the poison value and operational semantics of the poison value in the IR some operations which can trigger this more dangerous form of UB is basically should trigger UB immediately whereas there are some types of operations like addition or poison being in the numerator of a division in which case we just return the poison into Y which means it's okay to hoist these kinds of transformations and these kinds of competitions but it's not okay to hoist these kinds of computations and so using those semantics which say that if you divide by poison which means poison is in the numerator then it triggers UB this transformation becomes invalid because if I look at X equals int max and loop never entered in the source program then the source program does nothing as we've already discussed but the target program actually triggers UB and so this transformation becomes incorrect in the IR to IR rate so what I'm doing here is or what the IR designer is really doing here is the IR designer is picking and choosing carefully the semantics of the IR operations and also sometimes introducing some extra values like the poison values so that some transformations remain correct and some transformations remain incorrect and we've already seen transformations that just involve sign integer overflow and things like that hoisting of those computations like sign integer overflow above should be okay but hoisting of divide by zero for example should not be okay and that's what poison value semantics that we discussed so far are providing us.
