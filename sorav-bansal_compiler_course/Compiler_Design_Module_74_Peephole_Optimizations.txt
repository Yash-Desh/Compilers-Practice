Title: Compiler Design Module 74 : Peephole Optimizations
Video ID: lQ1tG9b8Kdk
Detected language: en
------------------------------------------------------------

So, now I'll discuss another type of local optimization which is called peephole optimization. What is a peephole optimization? A peephole optimization is a pattern matching rule that has a pattern and a replacement. So for example, a pattern could be a set of instruction templates i1, i2, i3, i n. It's a sequence of instruction templates and then a replacement is also another sequence of instruction templates j1, j2, ...jn. The idea is that using this pattern comma replacement tuple, we can scan the code and see if this pattern matches with any existing code in the program being optimized and if it does then we can replace it with the replacement code. And this makes sense if the replacement pattern code is faster or in some sense better in terms of resource usage than the original pattern code. Traditionally, this kind of peephole optimization has been very successfully used on assembly code because at the assembly level there are all kinds of opcodes that are very esoteric in nature, so very corner case kind of opcodes and when you generate code from your IR then maybe you have missed out on those opcodes and so these peephole optimizations can scan windows of your instruction program screen inside the program and see if any of these windows matches the pattern and if so then it replaces it with the appropriate replacement. 

So for example, here is an example of a peephole optimization says if you see a move of r2 to r1 followed by another move of r1 to r2 then you can just replace it with a single move from r2 to r1 because the second move in this case becomes a no op because at this point both r1 and r2 have the same value. Now this kind of instruction sequence can actually occur very frequently when you are generating code from IR to assembly because when you are doing register allocation and then you are assigning IR registers to assembly registers then you have to emit code that is going to do this register naming and transfer of values from one register to another or transfer of values from register to stack slot and things like that and so often you will find that very closely located instructions which look like they are just moving registers back and forth and in this case you can just run peephole optimizer that's going to just, it's called a peephole optimizer because it just looks at a peephole of instructions, a small window of instructions and keeps sliding down the instruction stream and checks for any instruction sequence that matches the pattern and if it does then it just replaces it with the replacement pattern. Now notice that this is a pattern because r1 and r2 are not actual registers they are actually placeholders for any register names. So for example this particular pattern move r1 r2 and followed by move r2 r1 will also match something like move r3 r4 and move r4 r3. So even this would match this particular pattern because it's not the particular register names r1 and r2 that is important it's just that there's some register r1 and there's some distinct register r2 and this is the form of computation that's happening. So it's a pattern of instructions that it represents and the same pattern can match many different types of instruction sequences. Here's another example of a peephole optimization rule it says add, immediate and update dollar r1 dollar r1 i so recall that this means that r1 is updated with r1 plus i so i is added to r1 and i here is supposed to be an immediate value or constant value and similarly second instruction is add iu r1 r1 j that adds another constant j. Now both r1 i and j these are just placeholders in this pattern and these can match any register or any constant with them i can match in some constant and j can match some other constant I'm not really specifying what constant it is and I can replace it with the replacement which says add iu dollar r1 dollar r1 i plus j all right where i plus j is also an immediate value a constant value that you know that can be just formed by summing up the constants i and j with which they were pattern match. So for example if I have a code which looks like add iu r3 r3 r3 once again add iu r3 r3 and 3 and then I have add iu r3 it has to be the same register because you know it's the same r1 that's happening on both sides and let's say I have 4 then this can be replaced with add iu r3 r3 7 okay so so notice that this particular transformation can be implemented by this particular people optimization rule so a people optimizer is basically implemented as a database of people optimization rules like the ones that we have just shown you here and then an optimizer that basically is going to scan the instruction stream to find some sequence of instructions that matches this pattern and if so it executes the corresponding replacement as shown in these examples. 

It turns out that many local optimizations can be cast as people optimizations for example you can cast algebraic simplification as a people optimization you say if you see a multiplication by a constant and the constant is a power of 2 then you can change it with a shift or you can cast constant folding as a people optimization you can say if these are the constants that are occurring so for example even this here is an example of a constant folding optimization you can think of this particular optimization as an optimization of dead code elimination where the second instruction is actually dead code because it doesn't really do much although when we define dead code it was different a little differently you can think of this as a second instruction is a no op that gets eliminated through this people optimization rule but not all local optimizations can be cast as people optimization rules so for example let's look at copy propagation so when we say copy propagation then you know the copy the the statement which says w equals x which is a copy can be a lot of distance away from the actual use of w and so trying to cast it as a people optimization rule would require us to have a really large window of pattern matching rules that should match with that particular instruction stream and that would be a little baseball right because let's say I have w equals x then I have lots of instructions in the middle and then I have something which is a use of x so the x plus 1 right some v equals x plus v equals w plus 1 then here I would want that the w here in v equals w plus 1 is replaced with x but then to do this as a people optimization is really difficult because now I need to also have a pattern that not just matches w equals x and v equals w plus 1 but also matches all these instructions in the middle and there can be a lot of different possibilities permutations and combinations of what what can appear in the middle and so doing this as a people optimization may not make sense so some optimizations lend themselves very well to be represented as people optimizations and some other optimizations are better done algorithmically like copy propagation is an example or maybe even common sub-expression elimination notice that people optimizations like all other optimizations that we have discussed so far is also another type of local optimization so we are really talking about optimizations within a basic block although there has been this was the original notion of people optimizations although there have been subsequent approaches that have done people optimization across basic blocks as well although in in limited scenarios 

all right so how do you apply people optimizations well like local optimizations people optimizations are typically applied iteratively or repeatedly so you would just go through the instruction stream once and try to find a you know instruction stream window of instructions that matches one of the patterns in your database and if something matches you do the replacement and you keep doing this iteratively because what can happen is that let's say there was an instruction stream and there was some window which matched right now because of this match there was a replacement and because of the replacement there there was another new window that got created that can now get matched again or maybe that window can be somewhere here right so they could be overlapping windows that start getting matched because of the previous replacement and this can work both ways sometimes people optimizations can trigger more people optimizations in the next pass in the next iteration or sometimes they can preclude other people optimizations because of one people optimization the other people optimization got disabled and this is very similar to the kind of thing that we have seen also while we were discussing local optimization so but you know people have worked on optimal application of people optimizations and things like that also but the state of the practice is basically that you just apply people optimizations opportunistically wherever you find them and and you keep doing this either till you reach a fixed point just like our local optimization algorithm or maybe for a fixed number of iterations or something or any other metric that you want to use for your convergence criterion okay so here are some more examples of people optimizations so add iu r1 r2 0 can be just replaced with move of r2 to r1 so instead of doing an arithmetic which is just going to do arithmetic with zero and so unnecessarily turning on your arithmetic circuit you just want to implement a move instruction or similarly if you have a move r1 r1 you can just replace it with the empty sequence which means just remove this instruction because it's a no-op 

there have been a lot of prior work on automatic generation of people optimizations and that makes the whole idea of people optimizations even more attractive because sometimes they can be automatically generated and so they don't have to be human manually codified and the idea is to use enumerative or stochastic search techniques to automatically learn people optimization okay sometimes these techniques are also called AI techniques or things like that in fact some of my own research has been in this space 

okay so to summarize program optimization is actually a term that is used to improve the program using some algorithms maybe local optimizations or people optimizations but the word optimization actually I argue is a misnomer it's not there's no attempt to actually try to find the optimal solution so the word optimization is actually a misnomer here but the word has caught on and so we use it because of historical reasons a better term would probably be co-program improver because these algorithms all they are doing is trying to improve the code based on certain patterns or algorithms and and they just stop when you know certain criterion holds or try for some amount of time and then you give up or things like that so there's no notion of optimization here although it's called optimization it's this what we're really doing is just trying to improve the code using some well-known patterns
