Title: Compiler Design Module 4 : Outline of a Compiler
Video ID: HzCAHoQyWpA
Detected language: en
------------------------------------------------------------

okay so now today we are going to discuss the outline of a compiler or what are the different steps or stages of compilation so you know broadly there are five different stages of compilation the first is called lexical analysis the second is called parsing the third is called semantic analysis which includes identifying types scopes and checking them the fourth stage is optimization and the fifth stage is code generation or translation and we're going to discuss each of them in more detail so so i'm going to explain these different stages by using an analogy to understanding a natural language now lexical analysis is kind of similar to recognizing words or tokens in compiler lingo so just think about it let's say there's this in sentence called computing requires energy now when we read the sentence you know the sentence has lots of characters in it co and so on and then there's a space then req and so on the moment we look at it the first thing we do is we separate these characters into words right and and so for example if i just write the sentence a little bit differently it would be much harder to read isn't it so for example you know in this red ink i have written the same sentence but put the delimiters word delimiters differently it makes much less sense so a compiler is kind of similar it takes an input a stream of characters the first thing it does is it recognizes words or tokens in compiler technical language so it so in other words a lexical analyzer is supposed to take in a stream of characters and it outputs a stream of tokens where each token is formed by one or more characters right so it basically groups characters all right so so let's take an example of programming language so here is a programming sentence or a statement if foo is less than equal to two then bar brackets so it's a function call else x equals five semicolon now this string of characters looks like you know looks like a program and we can kind of identify in this the words that make sense right so if then and else these are words or tokens and these are the tokens which are kind of special tokens of the class keywords all right so even within tokens we will basically categorize token and say that these tokens are meant to represent keywords other tokens are meant to represent identifiers and so on and so if then and is our programming language keywords which means they have a special meaning inside the programming language okay then we identify identifiers all right so the identifiers are basically tokens that represent variable names function names etc in this case foo bar x look like identifiers constants so constants in this case are formed by strings of digits so two and five or they could have been large longer strings of digits like 20 or 53 or something like that and then operators so less than equal to the function call operator left bracket parenthesis and right parenthesis the assignment operator right so we can see three different operators notice that some operators are formed by a single character while other operators would be formed by more than one characters all right and this is actually true for all different types of token classes and then there is the semicolon or which in this case forms a delimiter right so delimiters are basically telling you separating one statement one programming state from the other all right so what is lexical lexical analysis or lexing for short it takes as input a stream of characters and generates as output a stream of tokens so in essentially it generates tokens from characters all right now let's look at parsing well parsing can be thought of as diagramming sentences as trees so for example if there's a sentence like fast computers are more energy efficient so recall that the first step in trying to pass the sentence would be to lex it all right which means identifying tokens from the stream of characters but once we are done with that we would want to understand the structure of the sentence right and in this case in the english language what we are going to do is we are going to say that okay where is the verb so in this case r is the verb and so we can see that this verb is describing what the noun on the left does right a characteristic of the noun on the left so we can we can we can think of it as though it it's a tree it's you know we can represent the structure of the sentence as a tree and on the left hand side of the tree so the root of the tree is the verb on which the entire sentence hinges and on the left hand side of this node r we have the noun and the noun itself is divided into an adjective and a noun right so fast computers and and then the right hand side we have also more energy efficient right so also is an adverb more is an adverb and energy efficient is yet another adjective all right so so you know we learn these kind of rules in english language grammar and we kind of use these rules to identify and parse sentences it is not very different in computers or programming languages so here is a sentence a programming sentence or a statement in in a language like c if foo is less than equal to two then bar else x equals five now after we have identified the tokens which we have done in the last step we would like to understand the structure of the sentence and in this case the structure looks something like this the top level here is the if then else construct right so so the whole statement is basically an if then a statement right and and and the other things are kind of filling in parts of the statement so the root node of the statement is if then else in the diagram and this root node has three different parts to it the first part is the condition which goes with if the second part is the statement that goes with then and the third part is the statement that goes with the else clause okay so now the condition itself is supposed to be an expression that evaluates to a boolean value in this case that is represented by this sub tree foo is less than equal to two right so so less than equal to two is the operator that generates the boolean value and it compares two parts of two expressions the left part and the right part and in this case both those left and right expressions happen to be leaf nodes one of them is an identifier and the other is a constant similarly in the then clause the the next level is a function call and the function call basically has its own operands in this case it only has one operand which is the name of the function if the function call was making a call using more than one zero operands then it would have had you know maybe one or two so if the function call was had one argument then the function call operator would have had two children right and so on similarly the else clause involves a statement and in this case it's an assignment statement so the root node is an assignment operator equals and the left and right nodes represent what is assigned to what all right so this process of converting this stream this linear stream of tokens into a structured tree or diagram that represents the structure of the program is the process of parsing all right the third step of compilation is semantic analysis and here we are more interested in understanding the meaning of the sentence we've already kind of identified the structure of the sentence and but now we want to understand the meaning of the sentence now what is the meaning of a sentence i mean that's a very fuzzy statement actually the meaning of sentence would mean what is the program actually doing and that is a very difficult problem and you know theoretically speaking it's an undecidable problem and similar to saying whether you know this program is equivalent to something else so can you can you see can the compiler look at a program and say that oh it is actually going to produce a sorted output or it is a quick sort function or something like that well something like that is pretty difficult to do instead what semantic analysis does is it does it just does some basic checking right for example it would check inconsistencies like a variable is defined before it is used or that things are not used outside the scope or it's going to provide bindings for things within the scope or it's going to check whether things are being used things are used consistently with the types of those objects so so so these are called catch inconsistencies and these this kind of catching inconsistencies is faster than than actually doing a full understanding of what is the meaning of the sentence which compilers would not do not tend to so there's no effort by the compiler to understand the actual meaning but at least there are some inconsistency checks that can be done by the compiler and this part is called the semantic analysis so here's an example of semantic analysis for the natural language once again taking an analogy from the natural language so let's say there's a sentence called the compiler generates an executable and it prints nothing when it runs now here when i look at it there are two parts of it so first of all this sentence looks like a linear sequence of characters or string of characters which can which after the little analysis pass is going to give us a linear stream of tokens which after the parsing pass is going to give us a sentence tree structure right so for example it's going to say you know the the maybe the top node of the sentence structure is going to be the and node which is the conjunction conjunctive operator and the left part of it is saying something and the right part of it is doing some saying something and so on right so we're not going to look at the parsing part but now once the parsing has been done we want to understand whether you know we want to check whether there's some some things which are consistent here so for example here we say it prints nothing when it runs what is it is it the compiler or is it the executable also there are two it's are the two it's referring to the same entity maybe both of them are referring to the compiler or are they referring to different entities for example the first it is referring to the executable and maybe the second it is referring to the compiler so all these things are basically under the purview of the semantic analysis now in this case actually it is a bit ambiguous frankly what does it refer to and this is this can be called an ambiguity in the in the natural language of english and similar ambiguities may be present in a programming language but because the programming language is more technical we should always have rules to resolve ambiguities and in the end there should not be any ambiguities all right so okay so this is similar i mean this example where i'm saying you know what does it refer to this this example is kind of similar to scoping rules in programming language so consider consider an object oriented language where let's say there's a class foo and within the class foo there's another class bar encapsulated within class foo and then i use this keyword called this now assume this is something like c plus plus for example so now where does what does this mean does this mean bar or does this mean foo now you know c plus plus would have very clear rules that this would basically bind to the closest context in which it is being defined so in this case this would mean bar right so so so this kind of analysis and checks is basically being done by the semantic analysis phase notice notice that apart from doing checking it is also basically you know identifying the meaning so for example it's identifying the meaning of this in this case this refers to an object of type bar okay the other part is checking right so here is another example of an activity that a semantic analysis phase is supposed to do it's supposed to check types so the compiler generates an executable and it print nothing when they runs now in english this is an illegal sentence and the parser won't be able to catch it because it's still the same structure but notice that it is a singular you know singular it is singular whereas print print should have and so the print should have had an s here right and that's missing and that looks like a type error all right so print can't be used with it and similarly when they runs so they and runs don't go well together and you know what does it mean like it print when nothing when they run so sometimes we're talking about it and sometimes we're talking about they now is it is it actually i mean is it actually consistent does it make sense this is what a semantic analysis phase is supposed to check now in a programming language you know taking in a taking i mean coming back to the programming language world let's say i have char star p and then i say q equals p multiplied by five now this is a type error because a pointer cannot be multiplied with anything else right and so this is similar to a type error and something a type error like this is also identified during the semantic analysis phase now is it a coincidence that there is so much similarity between the natural language processor and programming language processor a compiler well i don't think so in fact programming languages are sort of in many ways modeled on natural languages because you know the whole point of doing a programming language is language is to make it easy for humans and humans are good already good at doing natural languages all right the next phase of a compiler is optimization where a compiler modifies the program without changing the meaning right so that's important without changing the meaning okay because if it changes the meaning that would be an incorrect compilation and why does it modify the program well there could be you know either because the modified program runs faster or it uses less memory or it reduces the power consumption of the program or reduce the network messages reduces risk access and so on but there could be depending on the environment different metrics and a compiler may want to modify the program to optimize for that particular metric now this is a major part of a compiler as we discussed last time and and and probably this doesn't have too much of a counterpart in natural language processing as well you know you can think of optimization as editing except that you know optimization is much more heavyweight process than what editing is for a natural language in the context of natural languages all right also it is important to know that optimization needs to be very careful about preserving meanings and and preserving meanings typically requires careful semantic modeling so think consider this expression two into i by two can i replace this expression by i in a language like c assuming i was an integer a c integer well the answer is actually no all right and why is it a no because imagine if i was a a quantity that was larger than half of the maximum value that can be stored in an c integer in which case two into i would overflow and depending on the semantics it may happen that you know the last the topmost bit is lost because of two into i and so the value of in that case of two into i by two would be different from the original value of i all right it would probably be something that has you know the last the topmost bit uh removed turned to zero okay so so so you can't do these kind of things and we're basically pointing out that optimization requires precise semantic modeling here's another example let's say i have a variable of type float and let's say i'm you know initializing y which is a float variable in different ways and then i have this expression called z equals y into zero now because of the peculiarities of a floating point arithmetic it is not okay to convert y into zero to zero because y into zero could be something other than zero all right and also there is the notion of negative and positive zero and floating point so one needs to be careful about making these transformations in fact something like this is illegal all right here's another example let's say i have a loop so let's say in this case i have you know in this first fall in this example i'm considering the c language and let's say in the c language i have a variable i of type unsigned and then i have this loop for i equals zero i is less than n plus one i plus plus and then i do something is it okay to convert i is less than n plus one to i is less than equal to n all right in fact it turns out that this is not okay in this program all right this is sorry this is not okay in this program so this is not okay why well because you know it is possible that n was maybe you know the largest possible unsigned integer in which case n plus one would wrap around and overflow and in which case this loop should never execute right because it will become i equals zero and i is less than wrapped around value and typically with the largest value when it wraps around an unsigned value will wrap around to let's say zero and so it would become i is equal to zero i is less than zero and so this becomes false and the loop will never get and will never enter on the other hand the program on the right if n was the largest unsigned value would execute for forever actually because it's going to be always less than equal to that because i is actually an unsigned integer and and because it's an unsigned integer it will always be less than equal to the largest integer ever right so this transformation is wrong all right so this transformation is wrong but it turns out that if i replace the type unsigned with the signed integer right so if i instead have a signed integer so in c an int is supposed to be a signed integer if you do this then this becomes okay all right this is okay now now why is this okay and why is the previous transformation not okay how does it make a difference whether i was an unsigned or an signed integer now for this we need to be understand the deep semantics of the c language that i'm not going through in this lecture but we'll go we will probably get a chance to discuss this later in the course all right and then the final phase of a compiler compiler is code generation or translation now this is the instruction set architecture of where phase so you need to know about the isa and then you basically translate patterns of instructions in the uh optimized program to the low level instructions that the machine can actually implement and in fact this is not usually the only phase that is isa aware some optimization phases also need to be isa aware so that you know we basically come to this stage where the best possible instructions are chosen to be executed on on the hardware and so code generation phase usually also includes optimization and optimizers also usually are need to be isa aware so these things actually while i'm kind of showing them as separate phases they usually depend on each other for good quality for generation of good quality code all right here's a rough division of compiler complexity so if i look at a modern compiler like gcc or llvm how much of the complexity is due to which part of the compiler well it turns out that things like lexing and parsing are relatively well understood and you know they don't they're relatively pretty simple so for example consider gcc or llvm which are into the millions of lines of code complexity you know a lexer and a parser would only be you know less than maybe a hundred or two hundred thousand lines of code semantic analysis is also relatively well understood it may be slightly more complicated than parsing but uh but but but a small fraction of the overall complexity optimization and code generation are by far the biggest contributors to a compiler complexity and of which optimization takes the major share
