Title: Compiler Design Module 26 : Shift Reduce Parsing
Video ID: U1o5mpsNuWs
Detected language: en
------------------------------------------------------------

So now we're going to discuss shift reduce passing which is a strategy for bottom-up parsing Just to review we learned one important fact about Bottom-up parsing which is that bottom-up passing traces the rightmost derivation in reverse Now I'm going to call it important fact number one. We're going to see some more important facts So it's easy to refer to these facts as you know fact number one fact number two And so I'm going to just call this important fact number one All right, so what is the consequence of this important fact number one well consider any step of the reduction So you know at any step in my bottom-up parsing. I am reducing from the You know some sub string to another string and when I'm doing that I'm applying some derivation So or some production so for example I could be reducing from a b w To a x w in which case you know and here a and b and w are just strings of Non-terminals and terminals right so I don't know what they what they represent But here capital X is a non-terminal right capital X is a non-terminal everything else could be a non-terminal or a terminal So if I if I look at any step in this reductions for example I go from a b w to a x w using this production X produces b Which b could be a string of terminals and non-terminals it must be true that W is a string of terminals Okay Why do I say that? Well if w was not a string of terminals made that means w has some non-terminal Then I claim that this is not you know the reverse of it would not be a rightmost derivation right so if w contains some non-terminal Notice that in this step. I am basically reducing b to x so in the reverse direction. I am producing b from x Now this would make sense because it's supposed to trace the rightmost derivation. This would make sense if x was the rightmost non-terminal But if w has a non-terminal then x is not the rightmost non-terminal, but that's so that pilots at assumption So for this to be a trace of the rightmost derivation in reverse it must be true that w does not contain any non-terminal Because if w contains a non-terminal, this is not a reverse of a rightmost derivation So it must be true that w is a string of terminals because it must be true that AXW Producing ABW which is the reverse of this reduction is a rightmost derivation Okay so with that We have you know we have we have something to work with so we have an idea and the idea is the following that You know we're going to Advance our cursor the cursor that we studied in our last module as well But I mean here. I'm just showing that cursor as a red dividing line here and This cursor basically represents what we have already seen but Basically the cursor keeps moving right and on the left of the cursor We have a string of terminals and non-terminals because you've seen something some stuff and we may have reduced some of it But on the right of the cursor, it's only a string of terminals that has not yet been examined Moreover, it must be true that if there is any reduction Then it must happen at the top of the cursor or just just to the left of the cursor the reduction can't happen deep inside the left part of the string right because as we just saw that everything on the right is terminals and so The you know the reduction must happen at the very So I mean if you are doing things correctly I should say then if the cursor is moving right and we are we know when to do the reduction Then the reduction should happen ideally exactly at the top of Exactly to the immediate left of the cursor not deep left into the cursor Okay, even if this is not clear. I'm going to look at this in more detail later on so So so there are two types of moves in this Shift reduce parser. So so recall that I'm calling this a shift reduce parser Which is a strategy of bottom-up parsing and in this case the first move the first type of move is a shift move In this case, I basically decide to move the cursor one step to the right Which means that I kind of read one more input. So we move the The cursor right by one character which signifies that the parser has seen that character and this would be called a shift And then the other type of move will be called a reduce move and what's the reduce move? Well, let's say the cursor is at some point here and on the left of the cursor We have some string C A B X Y which which may contain terminals and non-terminals and on the right we have W Now W must be a string of terminals as we just said Assuming we are doing this correctly and we're not ever kind of skipping points where we have to reduce then We can you know, we could reduce through some production. Let's say A Produces X Y. So if we use this production A produces X Y I could convert this string from C A B X Y and Reduce it to C A B A. So I'm using the reduction X Y reduces to A and I'm doing this right at the immediate left Of the cursor now, why do I stress on the immediate left and not deep left? I'm just assuming here that if there was a reduction to be performed It will be performed when the cursor is there, you know if you if you move right then you have lost the chance to make that reduction and this is just you know a property or the of the algorithm that I want to implement which is the shift reduce parsing algorithm and How do I know whether to shift or the or reduce is something that we Going to figure out later in the lectures But but if there needs to be reduction then we better do it right now When we when the cursor is produced the position right next to the RHS of that production So in this case the cursor is positioned right next to X Y where X Y is the RHS of some production A Producing X Y and we want to reduce so we're going to say C A B X Y W Reduces to C A B A W where the cursor remaining unmoved So so there are two types of moves in the shift reduce parser. There's a shift move and there's a reduce move Okay, so let's look at an example and see you know What will what moves are we using at watch point and I'm going to use the same example that we have used earlier And we are using the same grammar of expressions that we have used earlier in this example so int star int plus int and the cursor starts from the leftmost position and The first thing and I should I could have decided to reduce here But I can only reduce recall that I can I'm going to only reduce at on the immediate left of the cursor And the immediate left of the cursor is the empty string and I don't really have a production in my grammar that That produces the empty string so I'm not going to know there's no choice of reduction here So the only choice I have is to shift the cursor, right? So I shift the cursor and I'm going to write what move I'm making whether I'm doing a reduction or a shift So whether I'm going to bring a shift or reduce on the right. So in this case, I did the shift so I move the cursor one step to the right and so that's a shift and Once again, and so, you know, I'm not going to discuss how I'm deciding whether to shift or to reduce Let's say some oracle is telling us whether to shift or reduce I'm just going to show you what happens when we are going to try to parse this successfully and We're going to later see how to decide whether to shift or reduce So in this case, the cursor just moved right so it's a shift move The next move is also a shift move because I again decided that I want to move the cursor right notice that here I actually had a choice. I could have reduced the int to a T We call that T produces int and I could have reduced int to a T But I decided to shift and you know, it was it was an oracle's decision that no, it's better to shift and not to reduce In this case now again int star int and I again shift At this point I decide to reduce and I reduce using the production T reduces int because I'm reducing int to a T Then I reduce int star T to T and I'm doing this using the reduction T produces int star T All right, so so far I've done shift shift shift reduce reduce All right, and I've ended up with this state of the parser T cursor plus int now I decide to shift again and now the cursor moves one step to the right and so I've now read plus and Again, I decide to shift and now I now now the state of my parser is T plus int and the cursor is positioned to the Rightmost part of the string and so now actually I really don't have a choice to shift anymore The only possibilities are reduced and so now I'm going to do the first reduction and that's going to reduce int to a T I'm going to do the second reduction that's going to reduce the T to an E So the first one was basically Using the production T produces int and the second one is using the production E produces T and Finally, I'm going to reduce this to E which is using the production E produces T plus E and using the sequence of steps which were kind of given to us by the oracle Which is a sequence of shifts and reduce so notice that there were three reduces three shifts two reduces two shifts and three Reduces in that sequence and I was able to reduce it to the start symbol and that's the that's the Objective of a parser to be able to reduce the string to the start symbol if such a reduction exists Okay so So so that's how a shift reducer parser works We are once again we haven't really discussed how to decide whether to shift or reduce But let's look at some operational details of how something like this may be implemented The first thing is that the left string the string to the left of the cursor can be implemented as a stack Right because what are we doing? We are basically just you know either shifting which is like pushing to the stack or we are reducing which is like Looking at the top few elements of the stack and doing something with it. All right, so The shift move pushes a terminal on the stack So if you want to shift the cursor to the right that's akin to saying That I just take the next symbol and push it on the stack All right, and if I want to look at the string I need to read it from bottom to top or you know, so left to right reading of the Left sub string would be the bottom of to top traversal of the stack and shift just pushes the terminal to the top of the stack Okay, and reduce What does the reduce the operation do it pops off some number of terminals at the top of the stack and pushes the non terminal To the top of the stack. So if if there was a reduction like X to AB Then it's going to pop off two Symbols A and B from the top of the stack and it's going to push The symbol X on the top of the stack So the the stuff that is popped off is the production RHS and the stuff that is pushed To the stack which which is a single non terminal is the production LHS Okay All right So so we have learned about shift reduce parsing and what are the what are the structures that are used to implement the shift reduce parsing, but No, there are some unknown Things about unknown steps of the algorithm in particular The it is the steps are related to shift reduce and reduce reduce conflicts So at some places in our shift reduce parsing It was unclear whether to shift or whether to reduce all right some places It was very clear that shift is the only choice some places. It was very clear Reduces the only choice, but some places it was not clear I could have either shifted or reduced right and that's called a shift reduce conflict So it is unclear whether to shift and whether to or whether to reduce So so this is something that we're going to learn in the next few lectures It's also Possible that there are multiple possibilities of a reduction so, you know, I could maybe there are two productions a produces a B and B produces a B and You know, I don't know which one to pick Typically this should not happen if your grammar has been written correctly So this is actually a rare case of reduced reduced conflict The much more common case that we're going to study more detail is the case of a shift reduce part conflict Reduce reduce conflict can typically be written by it can be removed by Rewriting the grammar such that these kind of ambiguities do not appear in the grammar All right So so what are some solutions? How do I resolve these conflicts? So solution number one is backtracking recall that even in the original recursive descent parsing algorithm We said that you know, there was a there's a conflict There was a choice and we didn't know which production to use right similarly in the bottom up parser Algorithm we have a choice whether to shift or whether to reduce or which production to reduce by and one option Of course here is backtracking So you make one choice arbitrarily you keep going and if you are able to reduce all the way to the start symbol You are done If not, then you backtrack and you try another choice and you keep doing this until you exhaust the whole search space And so this is the backtracking approach As we discussed while we were discussing top-down passing backtracking approach while While feasible is actually not very efficient and so it's not the it's not usually used in practice the more common approach is a predictive parser where we look try to look at the next key tokens and predict whether based on next key tokens whether to shift or whether to reduce and If you have to reduce what production to reduce using if I'm look I'm using a look ahead of key tokens It's called an LRK parser. So L here stands for left to right Because we are scanning the input left to right the cursor is moving left to right In fact, all our parsers are scanning the string left to right and as we discussed it's just a matter of convention R basically says we are tracing a rightmost derivation Although we are tracing it in the reverse direction unlike the recursive descent parsing We will be tracing the leftmost derivation in the bottom parsing scheme We are tracing the rightmost derivation in the reverse direction and K stands for the amount of look ahead The typical useful value of K is 1 so so so the typical types of Bottom-up parsers are usually LR1 parsers. I'm going to learn more about them in the next modules
