Title: Compiler Design Module 125 : Need for Locality
Video ID: KiGHVWI2QfA
Detected language: en
------------------------------------------------------------

So now we're going to talk about the need for locality in high performance computing and so to understand what do I mean by locality basically I'm saying that it's important that we have either spatial locality or temporal locality in the program to get better performance. So what is spatial locality? Spatial locality is a property that if you access a particular memory location then you also likely access a memory location that is closed in space and address space to that particular memory location. What is temporal locality? That if you have addressed a memory location then very soon you are likely to access the same memory location again. So you know we're going to discuss the need for locality through some examples in today's module but before we do that let's understand the characteristics of typical hardware that we have today. So we have registers which are on the CPU chip they take one cycle to access and they can be of size ranging from 256 bytes to 8 kilobytes. I'm talking about the entire space entire address space of the registers by address space I mean what is the total size of the register file including all the registers depending on the architecture it could be as small as 256 bytes or up to 8 kilobytes and this is the amount of circuitry that can fit on the chip. I should also point out so registers are kind of the fastest storage available to a computer program on a modern hardware processor general purpose processor and actually the larger the register file the more power hungry the CPU becomes because there's a lot of circuitry that needs to go into in basically demultiplexing address into data and then also having the interconnect to route the data to the CPU in the fastest possible time. So but the good thing is that you know you can have you can afford some amount of such space on the chip within and you can access it within a single cycle. Then you have caches multi-level caches L1, L2, L3. L1 is typically almost as fast as register access. L2 is you know maybe 5 to 10x slower. L3 is another you know 4 to 5x slower than L2 and then so this is basically from you know one to tens of cycles and the sizes here range from 256 bytes for L1 to let's say one megabyte kind of sizes for L3. Then you have the main memory which can go which is the actual memory that you basically plug into your computer which is outside the chip the CPU chip itself and so that's going from 20 to 100 cycles to access and this can range from megabytes to terabytes today. And then you have magnetic disk which takes you know half a million to five million cycles so notice that there's a huge jump from main memory to magnetic disk but the good thing is that you can have bigger capacity gigabytes to petabytes. I should also mention that recently there's been advances in non-volatile memory which also gives you persistence just like magnetic disk across power failures which has you know order of access you know 3 to 4x slower than what the main memory does different as technology progresses we expect these numbers to become better but it takes long times to basically become better and so you know even if they become better this kind of hierarchy would always remain and we would want that as many accesses as possible are satisfied by the upper layers of this hierarchy than by the lower layers of the hierarchy and it is the job of both the hardware and the software to ensure this in collaboration. Some of these parts of the hierarchy are managed by the hardware for example typically caches are hardware managed but then some part of this is also managed by software because even though the caches are soft hardware managed maybe the compiler can reorder the computation such that the caches become more effective and we're going to see some examples in today's module. So consider this particular loop this is a two loop nest outer loop is iterating j from 1 to 10 and the inner loop is iterating i from 1 to let's say million or 10 million and then I'm just accessing elements i ai and bi notice that I'm not actually the memory accesses do not really depend on j here it's just depend on i ai and bi and they don't really depend on j. Now let's look at what the access pattern for something like this is going to be the first thing I want to point out that this value 10 million is bigger than the size of any cache and so these accesses will have to go all the way to the main memory which are you know 100 cycles or you know up to 100 cycles slow it would have been better if you know when we are accessing it I could have been more sure that this this value would be in this cache and so in this particular thing by the time the second iteration of the outer loop starts the cache has no reusable iterations locations so for j equals one I just for j equals one so this should have been one so for j equals one and two for j equals one I'm going to do a linear scan on the array from beginning to end and and then at j equals two I'm going to again start from element number zero so I'm pretty sure that by the time I come to element number zero at j equals two element number zero is not in the cache because everything else would the hardware would have brought in the other elements into the cache and the cache would have been would would contain the these elements the ones at the end by the time I access zero it's going to be a cache miss and if it's a cache miss then I'm going to have to do 100 cycles to get the data into the cache and so every time I access so I'm accessing a zero let's say 10 times then every time I access a zero I'm going all the way to the main memory question is can the compiler reorder the computation such that at least I can save some of the trips to the main memory and the answer is actually here is yes and we can basically the problem here is that the reuse distance of any array location is very high the reuse distance basically refers to the distance between two uses of the same location reuse distance and so the distance between two uses of the same location in this particular code is 10 million right so the number of accesses that happen between two uses of the same location in this particular code is let's say 10 million pretty large number and I want to reduce this can the compiler do some transformation so that this becomes less and the answer is yes it is possible to reorder and the answer is really simple if I just make the j loop the inner loop and the i loop the outer loop so my loop now nest now looks like for i equals 1 to 10 million i plus plus for j equals 1 to 10 j plus plus ai into star equals bi now I claim that the logic of the program hasn't changed it's going to do the same thing but the performance of the program has increased dramatically because of this transformation okay so let me just first fix this this is one and two and now what I have done is that I have reordered the loop iterations to decrease the reuse distance because now for i equals one j is going to go from one to ten but it's the same location i that's going to be accessed ten times for different values of j and so this is now the new picture uh 10 times access a0 then 10 times and b0 I'm not drawing b0 here then access a1 10 times then access b a2 10 times and so on and so after the first access the next nine accesses are guaranteed to be in the cache maybe even the l1 cache in which case I have saved almost a 100 x access latency because memory access was 100 cycles l1 access is close to one cycle and so for that particular memory access for nine of those 10 accesses I've been able to satisfy the request from the l1 cache overall this loop in on a modern architecture would typically give you at least 10x improvement compared to the previous loop while computing the same answer this kind of transformation is also called loop interchange where we interchange the loops the inner and outer loops without changing the meaning of the program and we're going to see techniques on how the compiler may be able to do such things automatically here is another example this is the common you know mathematical operation matrix vector multiplication here basically we have for i equals i equals zero to n you basically are going to compute the result in y so you say y i equals zero and then you say for j equals zero to n y i plus equals aij in star xj so I'm trying to multiply a matrix a with vector x and get my result in y and so if I draw this diagrammatically let's say this is my matrix a and this is my vector x then I for a given row i I just scan j in this direction and also I scan j in this direction keep multiplying and accumulating the result into yi so my question here is what would be a good is this an efficient way of doing computation is it going to give me good locality right and the answer is it depends what depending on what is the memory layout or the 2d array a if the 2d array a is row major then ai comma j and ai comma j plus one would be adjacent locations in memory and because of spatial locality because cache sizes are not bigger than single words they are typically let's say eight words or 16 words because of spatial locality I would have the value of ai comma j plus one in cache if I have previously accessed ai comma j with a high probability and if I have that then that's a good idea so if it's a row major then it would be a good performance whereas if it was column major if a was stored in a column major form then it would give me a very bad performance because then you know ai comma j and ai comma j plus one would be far apart because there will be a full row in the middle of ai comma j plus one and ai comma j and so there will be very little spatial locality and so the caches will not work well because recall that the caches are hardware managed and the hardware is just going to not bring ai comma j plus one if it has seen ai comma j because ai comma j and ai comma j plus one seem like they are very far apart and so this is going to be bad for performance but row major is going to be good for performance now I should also point out that this is just an artifact of the way the code has been written if I had written the code in a different way for example if I had hydrated over the columns in the outer loop and over the rows in the inner loop I'll have to figure out how to do that but it is possible to do it in this particular example then maybe the column major would have been a better solution so either I should change my program or I should change my data representation to basically give me better locality and we are going to we are interested in compiler algorithms that given a data layout give me the best way of computing the code such that it gives me best locality or given a competition give me the best memory layout that gives me the best locality
