Title: Compiler Design Module 95 : Introduction to Semilattices
Video ID: evK-aPeYA74
Detected language: en
------------------------------------------------------------

So we are now ready to discuss the foundations of dataflow analysis. We have already looked at several dataflow analysis examples and we have also looked at alternate representations of dataflow analysis where in one representation we had we were looking at a single variable in another representation we are looking at sets of variables or sets of values and things like that. But you know whenever we talk about dataflow analysis we talk about the properties of termination, we talk about the properties of precision or optimality of the solution and we need to have developed some theory to be able to reason about these things and so that's what we're going to do in the next few lectures and these set of lectures are going to be under the title of foundations of dataflow analysis. So in this we will develop a common framework for dataflow analysis and the advantages are that we can prove properties of an entire family of problems or algorithms once and for all in this framework. So we prove properties about the framework we have basically proven properties of all the different problems together and even more importantly actually or I mean equally importantly it aids in software engineering. So if you have a common framework you can also kind of when you write the logic for this when you write the code for this for implementing dataflow analysis you can implement a common logic let's say you know in object-oriented language have a base class and then have all the different problems or different algorithms just derived from this base class and that way you don't have to repeat the same logic over and over again and also you can reason about all the properties of that logic once and for all even in the implementation or the code. Alright so let's get started dataflow problems are defined using three symbols capital F capital V and the carrot symbol. The carrot symbol is called the meet operator capital V is a set of values that this dataflow analysis computes at every program point so at every program point the dataflow analysis computes a single value and that value is drawn from this set of values capital V and capital F is a family of transfer functions which is a transfer function is defined as a function that takes in a value from a value from the set capital V and returns another value from the same set capital V and capital F represents the family of all such possible functions right but they need to have some properties some theoretical properties for them to be admissible in a dataflow analysis. So there are these three components to dataflow analysis capital F capital V and meet operator right and I'm going to divide this three properties of dataflow analysis into two parts one of them I'm going to call a semi-lattice which is formed by capital V and the meet operator and so that basically represents the domain of values V and the meet operator represented by the carrot symbol the meet operator and then the second part is just going to the family of transfer functions capital F right and so I'm going to just divide this dataflow analysis into two parts one is a semi-lattice and the other is the family of transfer functions F. So let's talk about the semi-lattice first a semi-lattice S is basically represented by the dupal V comma meet operator right a set of values V and a meet operator represented by the carrot symbol where the meet operator must have the following property so it's not just a meet operator is basically an operator that takes a takes a set of values as input and returns another value right but the meet operator needs to have certain properties and the properties are that it needs to be idempotent so x meet x should return x right so first of all meet operator is basically you know a function that operates on two values and then returns a value two or more values and then returns a value but this meet operator cannot be just any operator it needs to have some properties and the first property I want of it is that it needs to be idempotent so x meet x should be x that's what idempotence means the second property is that it needs to be commutative which means that x meet y should be the same as y meet x all right and the third property is that it needs to be associative which means x meet y meet z so if you do the meet on y and z and then meet with x that's the same as doing the meet of x and y first and then meeting with z so the order in which you apply the meet operator should not matter that's what associativity means so what are some examples of operators that would satisfy these properties so here are some examples so set union so if x if the domain of values are sets then meet operator can be set union and you can see that set union operator set satisfies idempotence because a union of an L set with itself is just the set itself right it's commutative and it's associative similarly set intersection is actually satisfies all these three properties idempotence commutativity and associativity if if x and y are some kind of bit vectors or some kind of Boolean values then and or or are also valid meet operators because they also satisfy idempotence commutativity and associativity or if there were integers or any other kind of values that are comparable using you know using some kind of arithmetic operator then min and max are also valid meet operators because min for example is also idempotent minimum of x comma x is just x and also the min is commutative and associative similarly max and so all these are valid examples of the meet operator so what are some examples of non- what are the non-examples of the meet operator which is some examples of operators that would not satisfy these three properties and here are some non-examples add for example is not a meet operator so why is it not a meet operator which of these three properties is it violating well it is commutative add is commutative add is associative but add is not idempotent so x plus x is not x right it's something else similarly subtract is not a meet operator subtract is neither commutative nor associative nor idempotent multiply again just like add it is commuted in an associative but it is not idempotent but for a meet operator it needs to be it needs to satisfy all the three properties all right and so all these are not meet operators add, subtract, multiply, divide and so on are not examples of meet operators okay so let's take an example and let's assume that our capital V is the set of all x such that x is a subset of d1 d2 d3 so what is x the set of values so x is some set which is a subset of d1 d2 d3 so that's that will be my set domain of value a set of values from which I'm going to draw out a value for my data flow analysis and let's assume that my meet operator is the set union operator we've already seen that set union satisfies all the three properties so it is qualified for to be a meet operator so one from the meet operator itself we can kind of identify what would be the partial order and things like that so for example if the meet operator is set union recall what is the meet operator a meet operator basically returns a value that is less than equal to both the values so x meet y is going to be z if z is less than equal to x and z is less than equal to y now if the meet operator is set union that means z equals x union y x and y are set then what do you think less than equal to should be well it would be the superset operator because the union operator basically gives me a value such that it is some superset of both x and y right and we want the we want the maximum such value also so that's another another thing so yeah so from from the meet operator we also kind of can infer what is the ordering and this is actually true in the general case I'm just using an example to show that okay if the meet operator is set union then the ordering operator which is less than equal to operator is a superset operator but you know in general also if the meet operator has been specified completely then it's possible to get the ordering operator from the meet operator itself and vice versa so so we're going to see that also so here's another way to represent the semi lattice these are all the values in v right what are all because what is v v is the set of all subsets of d1 d2 d3 so what are all the subsets of d1 d2 d3 well empty set is a empty set is a subset of d1 d2 d3 singleton sets d1 d2 d3 each of them is a subset of d1 d2 d3 similarly sets that contain two any two of the three elements d1 d2 d3 example d1 d2 or d2 d3 or d3 d1 and finally d1 d2 d3 itself is a subset of itself right so so d1 d2 d3 now this these are I mean in this particular case this is a finite set of values in v which is formed by the all the subsets of d1 d2 d3 and I can also represent the ordering relation which is coming from the superset operator or also coming from the meat operator set union and that to do that I can just use these arrows these directed edges as though it is a graph where each of the values basically represents a node and there's an edge from one node to another if the destination node is less than equal to the source node of that particular edge so for example d1 is a superset of empty set and so I will draw a edge from the empty set to d1 and so on right also I want to draw the minimum such edges right so for example d1 d2 is also a superset of the empty set but I but that is that relationship is kind of implied because d1 is a superset of empty set and d1 d2 is a superset of d1 and so for by transitivity I know that d1 d2 is a superset of the empty set so I'm not drawing all the edges I'm drawing the minimum number of edges that basically transitively imply all the relationships okay so with that I have now I have this diagram which is also called the semi-lattice diagram which basically represents all the ordering relationships the partial ordering relationships between all the elements notice that not all elements of this set v are comparable for example if I ask is d2 d3 less than equal to d3 d1 there's no answer they're really not comparable because you know but I can say that d3 is less than equal to d2 d3 right similarly I cannot compare d1 with d2 d3 right because d1 is not a subset of d2 d3 so I cannot say d1 is less than equal to d2 d3 that's not that's not what it is and that's why it's called a partial order because a partial order basically only orders a some only gives some ordering doesn't totally order all the elements in the set it just gives you some ordering relationship between some of the elements in the in the set or in other words not every pair of elements in the set v is related by the ordering relationship and that's why it's called a partial order okay so so now this is this is one way to represent the partial order and this is one way to represent the semi-lattice because it contains all the values and it contains the ordering relationships in fact from this diagram itself I can basically define what is the meet operator well the meet operator is the first descendant so meet of x comma y is the first element that is that that basically is a common descendant of both x and y so for example if I say oh what is meet of d2 comma d3 I'm going to go down from d3 and I'm going to go down from d2 and look at the first node or first element in the set v that is reachable from both d2 and d3 so in other words the first common descendant of d2 and d3 is basically what gives me the union of the two sets or in other words that gives me the meet of the meet result of the two values all right and that would be my general in general also not just specific to set the set union operator but in general also the semi-lattice diagram is going to be such that if I want to compute x meet y all I need to do is find the least common the greatest common descendant from both x and y so I just traverse paths from x and y and find out the first element that is that is reachable from both x and y and that would be the meet the result of the meet of x and y here is another example if for example my meet operator was set intersection recall that in my previous picture the meet operator was set union but let's say the meet operator was set intersection then my semi-lattice diagram would look completely different for example here now I'm going to draw edges from bigger sets to the sets that are contained in so d1 and d2 is less than equal d1 comma d2 is less than equal to d1 comma d2 comma d3 and and so on right so basically what has happened is the whole structure of the semi-lattice has gotten inverted and now d1 d2 d3 now becomes the value that is less than equal to every other value in the in the set of values so so in other words what is the ordering relation in this in this case so if the meet operator is set intersection you can check whether the meet operator is intersection because what is if I want to find what is the what is meet of d2 comma d3 and d3 comma d1 well it is going to be the least common descendant so what is the least common descendant of d2 comma d3 and d3 comma d1 well it is nothing but d3 all right and that's exactly the intersection of the two sets all right similarly you can you can do other kind of pairs and so this lattice diagram you can check for yourself basically satisfies the properties that the meet operator is set intersection by using the fact that the meet operator is computed by computing the common descendant the first common descendant of the of any two elements okay so so let's look at some properties of semi-lattice so we have some idea of what is a semi-lattice a semi-lattice is a set of values and a meet operator all right this can also be represented as a diagram a semi-lattice diagram and and and in this semi-lattice diagram x meet y is the first common descendant of x comma y we have already seen this with a couple of examples but the diagram itself by construction is constructed such that x meet y is the first common descendant of both x and y all right also we define a special value called the top value so this is called the top value such that x meet top is equal to x all right so for all x so for any value x in set v x meet top is going to be x so what is such a value in this particular set such that x meet top is equal to x well it's easy first of all basically you know if i just look at the diagram it has to be the value that that is basically reaching everybody else right because then what happens is that if i do a meet if i say meet of d1 d2 d3 with d3 d1 that's just going to be d3 d1 because the least common descendant the first common descendant that is common to both d1 d2 d3 and d1 d3 d1 is just d3 d1 because everything is descending from this value d1 d2 d3 right so the value that's at the top of the whole semi-lattice diagram is basically the value that that's going to satisfy this property that x meet top is going to be x another way to look at it is because the meet operator's intersection i am interested the top value will be a value that contains all the elements of the of you know of the of the full set d1 d2 d3 and so that's going to be d1 d2 d3 so we define the top value such that x meet top equals x and in this particular example where the meet operator is set intersection the top value happens to be the full set d1 d2 d3 also we define a special value called the bottom value so this is the bottom value such that x meet bottom equals bottom so find such a value such that for all x x meet bottom should be equal to bottom so this has to be for all x so what is such a value well it is a value such that it is it is reachable by all other elements because then what happens is that if i take a meet of them then i look at the first common descender then descendant then it's going to be bottom itself because it is reachable from everything else and so the only element that basically looks to fit the bill here is this particular element which is the empty set which is which also happens to be the bottom of the semi-lattice diagram and so you can see you can check for yourself so if i say what is meet of d1 d2 d3 and the empty set well it is an element that is reachable that descends from both of them and that that is actually the only option here is the empty set itself okay so yeah so basically that this is our bottom element and also if we look at the definition of meet is the set intersection then x meet x intersection of empty gives me empty so anything intersection with empty set give me empty and it's the only the empty set that satisfies this particular property that x meet bottom gives me bottom for all x finally this is this this whole picture is called a semi-lattice diagram and and this is you know you can also call it a picture of the partial order because it is not just giving you information about how to compute the meet but it is also telling you what is less than equal to what so for example if something is reachable from something else if x is reachable from y in this diagram then x is less than equal to y so the transitive closure of these edges basically implements the less than equal to relation in this diagram so so we so here is here's one way to represent the semi-lattice it is through this diagram or this graph of nodes and edges where nodes are the values in v and edges represent the partial order ring the partial order ring relationships in the among the values
