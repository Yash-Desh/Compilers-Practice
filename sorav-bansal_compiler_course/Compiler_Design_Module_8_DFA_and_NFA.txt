Title: Compiler Design Module 8 : DFA and NFA
Video ID: uKoPV5DoRYc
Detected language: en
------------------------------------------------------------

Okay, so today we are going to discuss about finite autometons or for the finite automator meta and There are two types of automatons deterministic and non deterministic. So here is here's an example of a automaton An automaton is basically a machine or a program that can be run on a given input data to check whether That string that input belongs to a regular language or not. All right So, so let's understand this automaton. This is just a review lecture. I hope hopefully you have already studied finite automatons In some previous course, so this particular Automaton has three states s 0 s 1 and s 2 The state s 0 is special because it is the start state and this is indicated by this incoming arrow That's drawn here So this basically means that the program or the machine starts in state s 0 Also notice that s 0 is drawn with a concentric circle, which means that it is an accepting state Which means if the end of the input data and at state s 0 then that particular input is Accepted by the regular language. Otherwise, it is not accepted. All right so and a string is accepted by a regular language if and only if it at the end of After the entire string has been consumed the automaton is on an accepting state All right, if I'm at state s 0 and the next string is 0 then I am going to stay in state s 0 That's what this transition indicates If if I'm at state s 0 and I get 1 then I'm going to transition to state s 1 If I'm at state s 1 and I get 0 then there is no outgoing transition Which basically means that it's going to transition to some special error state. All right, which means that This string is not accepted by the regular language on the other hand if I'm at s 1 and I get 1 as my next character Then I'm going to transition to s 0 Similarly if I'm at s 2 So actually this is is not what I originally intended So let me just redraw this automaton slightly So let's assume that this is a 0, okay So so now if I'm at s 1 and I get a 0 Then it's going to come back to s 0 if I'm at s 1 and it's a 1 then I'm going to go to s 2 If I'm at s 2 and I get a 0 then I'm going to come back to s 1 and if I get a 1 on s 2 Then I'm going to stay on s 2 Now if now this is an automaton and we can you know run this on some input string to see if that input string Belongs to the regular language or not. Let's take a couple of examples. So let's say there is the input string 0 0 0 0 is going to start on s 0 the first Character 0 so it's going to stay in s 0. The next character is also 0 So it's going to still stay in s 0 and indeed s We have reached the end of the input and we are at s 0 and so s 0 is an accepting state And so 0 0 is belongs to the regular language represented by this automaton Now let's take another string. Let's say it's 0 1 So once again, I start at s 0 the first character is 0 So I remain at s 0 the next character is a 1 and so I transition to s 1 Now this is the end of my input and I am at s 1 and s 1 is not an accepting state and so 0 1 is not Accepted by the regular language represented by this automaton Now also notice that this automaton is a deterministic automaton. All right Why is this a deterministic automaton because at any state for any input character There is only one possibility Right example if I'm in s 1 and I get a 0 there's only one possibility. I'll just move to s 0, right? There is another there is a larger category of automatons which are Which can have non-determinism so they would they could either move to s 0 or s 1 and we'll see an example Moreover, there are no epsilon moves and we'll see what an epsilon move is All right. So here the second example here is so let me just fix this automaton also The second example here is a non deterministic automaton Okay, why is this a non deterministic automaton? Well consider this state s 1 if it gets an input 1 then it's going to It could either stay in s 1 or it could transition to s 2 Now this is the non determinism machine could move and make any of those moves and any of those moves would be a legal move And so such an automaton is called a non deterministic automaton Moreover, there are some special epsilon characters here, right? What is an epsilon character? It basically means that I move without actually consuming any character in the string recall that the epsilon represents the empty string And so in this case there there are epsilon moves. So which means that if I'm at s 2 I can Decide the automaton can decide to move from s 2 to s 0 without consuming any character. All right, so that's called an epsilon move As opposed to the other types of moves which always consume a character and epsilon move does not consume a character of the string And so this is also a non determinism the non deterministic machine can choose to move between You know to traverse epsilon edges and these are called epsilon moves Okay, so this is an example of a non deterministic automaton Okay, so this is a result in theoretical computer science that the language accepted by a deterministic finite automaton so DFA deterministic finite automaton and An NFA which is the non deterministic finite automaton Are actually the same. So in other words If it's not like the whatever language can be accepted by a deterministic finite automaton and non deterministic finite automaton can also be accepted by another Maybe a different deterministic finite automaton so it's not like NFAs will have will accept a wider category of languages or anything like that So that's a that's a significant result in theoretical computer science moreover the languages that can be accepted through these finite automatons deterministic or non deterministic are exactly the category of regular languages and Recall what are regular languages regular languages are languages that can be Represented through regular expressions, right? So we call that a regular expression its meaning of a regular expression is a set of strings which is the language it represents and And it turns out that a DFA or an NFA Language that is accepted by a DFA or NFA can also be represented using a regular expression and vice versa And this is this is a major result this is an important kind of significant result isn't it and All right, and just to give you an intuition why why this is true well both DFA and NFA are basically Examples of machines that have finite amount of memory and this memory is basically how many states You know how many different states that does the machine have that determines how much memory it maintains at any given point of execution similarly a regular expression also has a finite amount of Memory You know so basically a regular language can be represented Can be a string in a regular language can be Identified using a machine with a finite amount of memory and so this correspondence holds of course a more rigorous Explanation of this this result is a subject of theory of computation All right So here's a quick review of how you could you know given it in an NFA if you would like to Identify the DFA the corresponding DFA for that NFA there exists in algorithm recall that I just told you in the previous slide that The category of language is accepted by an NFA is the equivalent to the category of language is accepted by DFA's which means that if they if if you if you give me any NFA and That NFA accepts some language then I Can give you a DFA a corresponding DFA which will accept Exactly the same language right and so in fact there is an algorithm that we use to convert any given NFA to a corresponding DFA So there are two steps to the algorithm the algorithm is rather rather simple First is you find the epsilon closure of every state so for any given state in the NFA you identify All the states that can be reached from that state With through epsilon moves alone all right and the idea is that you know in the DFA there are no epsilon moves So what we're going to do is we're going to club all such states that can be reached With epsilon moves alone into one big state and we're going to put that state in our DFA All right, and the other idea here is that In an NFA recall that the machine can move non-deterministically between any two moves across any two transitions and And so in the DFA what we're going to track is what are the possibilities For the NFA at the current point right for example if in if the NFA could have moved on the one character it Consuming a character one if it could have moved to two states S0 and S1 S0 or S1 Non-deterministically then in the DFA we would like to record the fact that The NFA could be either in S0 or in S1 and to record this fact I'm going to create a new state called S0 comma S1 That's going to record the fact that the NFA could have been either in S0 or in S1 All right And once I do that then it becomes easy to identify how to draw the edges I'm going to I'm just going to show you an example of how to convert an NFA to a DFA Running through this example is only intended to kind of show you how this algorithm works Also the point of this exercise is only to show that any NFA can be converted to a DFA because they are equivalent Power in terms of what they can recognize of course a more detailed Discussion of this is a subject of theory of computation So here's an NFA it starts at state S0 If it gets if it consumes zero or one then it's remain item then it could either remain in S0 or transition to S1 if it is at S1 and it Consumes a one then it transitions to S2 which is also an accepting state Further from S2 it could move have an epsilon transition to S1 Notice that I haven't drawn some missing edges. For example, if there was a Zero transition from S1 then I would just go to a special state called the error state Right, which which is not an accepting state And so I will remain the error state from there on and so the error state You know, you just remain in the error state for all the further inputs and basically means that this string is not Not accepted, but I'm not drawing such edges just for simplicity All right Well, so if I want to convert this NFA to a DFA, my first job is to identify the epsilon closure So what is the epsilon closure? Well, what is the epsilon closure of S0? It's just S0 because there are no outgoing epsilon edges from S0 What is the epsilon closure of S1? It's just S1. What's the epsilon closure of S2? Well, it's S2 and S1 Right, because it could know if it's an S2 it could non-thermistically just choose to go to S1 whenever it likes All right, so so I'm going to do that. So we're going to have states S0, S1 and S2, S1 But let's start from the beginning So so this is S0. So in the At the very beginning if I have not consumed any character then the NFA is going to be In state S0 and there's no other possibility right because there's only one start state in the NFA So even in the DFA we're going to be only in state S0 Now if I get a 0 on While I'm in S0 then I could either move Then there's only one possibility actually and so I'll remain in S0 and so in the DFA I'm just going to draw this edge from S0 to S0 on input 0 Now if I get a 1 on state S0, then I have two possibilities I could either remain in S0 or I could transition to S1 So to represent this fact recall that I'm going to have a state called S0, S1 Which represents that the NFA could be either in S0 or in S1 and I will say that if I get a Z1 While I was in state S0, then I'm going to transition to the state S0, S1 Okay Now let's continue if I'm in state S0 or S1 and I get a 0 Then what happens? Well, if I am in state S1 and I get a 0 I don't go anywhere which basically means I go to the error state which I'm not interested in drawing But if I am in state S0 and I get a 0 then I stay in state S0 So if I'm in state either S0 or S1 and I get a 0 Then the only possibility after this is actually in the NFA is going back to S0 because I couldn't remain in S1 Alright now if I'm in also Now let's look at what happens if I'm in either if I'm in S0 or S1 why I got a 1 So if I get a 1 while I'm in S0, S1 I will move to S2 But recall that S2 has an epsilon closure, which is S1. So I'm going to draw S2, S1 Because S1 is in the epsilon closure So if I'm in S2, I could also be in S1, right by default but because of the epsilon move Further because S2 is an accepting state S2, S1 will also be an accepting state So if any of the states in the is an accepting state, then we're going to consider that an accepting state all right and Now if I'm either in S0 or in S1 Then if I get a 1 Then if I'm in S0 and I get a 1 then I would stay in S1 All right So so from S1 if I get a 1 if I if I was in S0 and I get a 1 I would have stayed in S1 if I am in S1 and I get a 1 then I could have transitioned to S2 and That's almost like saying that I could have been either in S1 or in S2 because there's because the epsilon So overall if I'm either in S0 or in S1 and I get a 1 Then I would be either in S1 or in S2 Okay, and that's it. So this is the DFA and so this is the DFA for the NFA that we just saw
