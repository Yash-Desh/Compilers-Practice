Title: Compiler Design Module 129 : Affine Transform Theory
Video ID: uA_JiuwQdWM
Detected language: en
------------------------------------------------------------

Now that we have been introduced to the idea of the polyhedral model, we're going to try and develop a theory for affine transforms and when we are going to talk about affine transforms, recall that in the polyhedral model we are basically talking about affine loops where the iteration space has some constraints, the accesses to memory through arrays have some constraints and then also you know there is the angle of parallelism and so to be able to capture this more formally we're going to define three spaces. The first space here is the iteration space and this basically represents the set of dynamic execution instances of a computation. So if I have a loop which says for i equals 0 to n something i plus plus and then something in the body then there are n different dynamic instances one for each possible value of i from 0 to n minus 1 and so these form the set of dynamic execution instances. If it's a depth two loop, it's a loop nest of depth two then maybe the first iterator variable is i going from 0 to n and maybe the second iterator variable is j going from some other bounds maybe 0 to m in which case the number of dynamic execution instances are m into n where i can range between 0 to m and j can range between 0 to n and also it's not necessary that this is a rectangular region for example I could say that for j equals i to m or something like that and that would also be a reasonable affine space iteration space that can be captured using the polyhedral model. In other words this is the iteration space which is the set of dynamic execution instances is basically the set of combinations of values taken on by the loop indices. So if I have a loop indices a i and j what are all the combinations of i and j that are possible in this loop nest. So in my example it would be 0 0 0 1 0 2 all the way till 0 m and now 1 1 1 2 1 3 and so on okay. So that's the iteration space and we discussed last time how the iteration space can be expressed as a convex polyhedron in a d dimensional space for a d depth loop nest. In other words for d iteration variables we're going to see that in a little bit more detail today. Then there's a second space which is the data space and this is the set of array elements accessed. Recall that we are confining ourselves to loops where those memory regions that are accessed can be expressed as affine expressions of the loop iteration indices surrounding loop iteration indices and so you know we can assume that these are arrays because arrays also have a very regular memory layout and so these are the set of array elements accessed and that's the data space and typically we would express the data space as a function as in a fine function of the iteration space because that is our that is our constraint for the polyhedral model that the set of memory locations that are accessed can be expressed as a fine expressions on the surrounding loop variables. In other words the data space can be expressed as an affine expression over the iteration space. So if I just morph the iteration space using some affine matrix I would get the data space. Once again we're going to look at this in more detail in the coming modules. And finally there is the processor space and what do I mean by the processor space? So we're going to be interested one of the things you're going to be interested in parallelization. We're going to want to map different points of the iteration space to different processors. For example affine partitioning that we discussed in the last module. When we do that we're going to create a map from points in the iteration space to points in the processor space. We're going to say that okay this particular iteration let's say zero comma zero is going to execute on processor two and zero comma one is going to execute on processor one and so on and so we need some way of being able to represent the processor space and so this is this we're going to have a third space to be able to do this and that's going to represent the set of processors in the system where each processor has been assigned a unique id. Now here's an interesting fact that even the processor space we would be basically you know in the polyhedral model be representing as a affine expression or an affine function of the iteration space. So given an iteration space we're going to have an affine function on the iteration space to identify what is the mapping. So the mapping from iteration space to the processor space is going to be in a fine function. Recall what is in a fine function something that can be written as you know summation ci xi plus c0 where xi's in this case would be the loop iteration variables. Okay once again we're going to see this in more detail in the coming modules. So for a D depth loop nest each variable each iteration variable has a lower bound and an upper bound. This is a constraint that we're going to work with in the polyhedral model but notice that these lower and upper bounds can potentially be a function of the iteration variables at lower depth. So for example if I have a two nest loop depth the first iteration variable is i for i equals 0 to n and the second iteration variable is j then i is the lower depth variable because it's at depth 0 and then j is the higher depth variable it has depth 1 and so the bounds of j can be expressed as functions of iteration variables at lower depth. In other words the upper bound and the lower bound for j can be functions of i in this particular example and that's why these iteration space need not be perfect cuboids or rectangles or anything like that they can be any potential convex polyhedron and so this can be represented as a convex polyhedron as we also saw in our last module and recall what is the definition of a convex polyhedron if two points are in the polyhedron that's you know this is the definition of convexity if a polyhedron is convex if and only if two points are if two points are in the polyhedron then all points are the line connecting those two points are also in the polyhedron and we saw an example last time. So let me take an example of a non-convex polyhedron to show the counter case. So here's an example let's say the first iteration once again I'm going to use a 2d loop nest and let's say the first iteration variable is x1 which goes from 1 to 6 now this looks very convex because if I take any point between x1 and between 1 and 6 and draw a line between them it's also going to be between 1 and 6 but now x2 goes from minimum of 3 comma 5 minus x1 comma 1 to maximum of 10 minus 3 x1 and x1 plus 6 by 2 now this basically I mean I'm not going to show how this is this you know how I'm going to draw in this picture but just take it from me that if I was to draw the iteration space for all possible values of x1 and x2 all possible integer values of x1 and x2 that satisfy these constraints I'm going to get a figure something like this right so these different lines are basically representing all the linear combinations that I have for example x2 is greater than equal to 1 that's coming from the fact that the 1 present here or x2 is less than equal to x plus 6 by 2 that's coming by x1 plus 6 by 2 and that's coming from this constraint here and so on but notice that this particular polyhedron that I get or polygon that I get in a 2d space is actually non-convex because if I draw a line from let's say this point to this point then there are some points in the middle that are not part of this polyhedron so how did we end up with such a non-convex polyhedron well notice that x2 goes from minimum of some values to maximum of some values and so the constraints are no longer specifiable only as linear inequalities because now I have to say that x2 is greater than equal to 3 depending on what is minimum among these x2 is less than greater than equal to the minimum of these things and that's not specifiable as only a set of inequalities on the other hand if I was going for from maximum of some things to minimum of some things it would have still been possible to represent as a convex polyhedron in any case here is an example of a convex polyhedron and this is an example that we have seen before for i equals 0 to 5 for j equals i to 7 some loop body and this is the iteration space of this particular loop nest i is going from 0 to 5 j is going from i to 7 and and notice that this particular polygon is a convex polygon now one quick check to figure out whether something is a convex polygon or not is that can I write the space the polygon space that is covered by the iteration variables as a conjunction of in equations or inequalities so in this case I can indeed do this because notice that i is greater than equal to 0 comes from the lower bound that i has a lower bound zero so i is greater than equal to zero i is less than equal to five so i has an upper bound of five j equals i so j equals i to seven so j's lower bound is i and j's upper bound is seven and so if I just use these inequalities and conjunct them you take the logical and of them then what I will get is a convex polygon so in fact any conjunction of inequalities a fine inequalities will always result in a convex polygon and or convex polyhedron even for a general D depth loop nest and one way to express these inequalities is to bring them in a kind of standard form where we try to bring it into the form summation Cixi plus C0 is greater than equal to zero and and so in this case this becomes i is greater than equal to zero this becomes minus i plus five is greater than equal to zero minus i plus j is greater than equal to zero and minus j plus seven is greater than equal zero notice that I have in my constraint I'm mandating that it should be a greater than equal to inequality and I can just rearrange my elements so that I always get a greater than equal to zero on the right hand side and I'm doing this so that I have an easier representation of these inequalities and here is a matrix method of representing these inequalities. I just basically, this matrix basically forms a set of coefficient matrices and these are the loop, this is the loop iteration space augmented with a one at the end. So i, j represents the loop iteration space and one is basically the constant element for which we, which represents, for which capture the C0 part of our affine expression and so if you read this, this becomes 1 into i plus 0 into j plus 0 into 1 is greater than equal to 0 and that's exactly our first in equation and the second one is minus i minus 1 into i plus 0 into j plus 5 into 1 is greater than equal to 0 and that's exactly our second in equation and similarly I have two more in equations corresponding to the two in equations that I have in the yellow color on the left. Now this is a standard form of writing the, an affine space, a conjunction of inequalities and such a representation also guarantees that what we get as a result is going to be a convex polyhedral. So all these things are interrelated and this may also makes the analysis of a program that behaves in this way easier and more higher level and so we can do transformations related to locality and parallelism. So I'm going to define, I'm going to use a standard notation to be able to represent such spaces, some such convex polyhedral spaces and here is the notation. I say i with the underline, this represents the i vector belongs to a d-dimensional integer space, so z here represents the set of integers to the part d. So i belongs to a d-dimensional integer space such that b i plus b, so b is a matrix which which looks something like this, this is the coefficient matrix, capital B is the coefficient matrix and small b is the C0 part of my affine expression. So this part, this column basically becomes small b and this part here becomes capital B alright and so this is another way of writing the same thing and basically say b i plus b greater than equal to 0, capital B becomes the multiplier of the iteration space variables i represented by the vector i vector which in this example the vector i is ij, the coefficient matrix is you know this first two columns 1 0 minus 1 0 minus 1 1 and 0 minus 1 and small b is the third column 0 5 0 7 which forms the multiplier for the unit elements 1 which basically represents the C0 of our affine expression and so this representation here i belonging to z to the part d b i plus b greater than equal to 0 is guaranteed to represent a convex polyhedron and because and you can represent any convex polyhedron using this this representation a d-depth convex polyhedron and so we're going to use this notation for representing our iteration spaces. Okay it is not enough however to just represent the set of iterations we also need to encode their execution order so it's not just enough to say that 0 0 is going to get executed and 1 1 is going to get executed but you also want to know what is going to get executed first so maybe 0 0 gets executed before 0 1 and 0 1 gets executed before 0 2 and so on and we need to have some way of encoding that execution order. Now notice that there are multiple ways of arranging my i comma j i could have you know it's just a convex polyhedron i could have switched i comma j to j comma i or something else and and and still i would have been able to represent it using i mean just change my capital B and small b matrices appropriately to be able to represent the same convex polyhedron but we just don't want any order we want that particular order that is also going to reflect the execution order and so we are going to encode an order in the i vector i with the underline and so the and we and this is going to be encoded in this way and it's just a convention that we will follow is that the execution order is the ascending lexicographic order on i okay so for example I could have said that you know so let's say 0 comma 0 is the first thing and then 1 comma 0 is the second iteration and then it is 0 comma 1 and and then finally 1 comma 1 so let's say my iteration space is 0 0 and this is for you know and let me just say that this is i comma j I'm representing i comma j and let's say this is my iteration space 0 0 1 0 0 1 1 1 now it is not okay to basically use i j as the is this the lexicographic order so this is the execution order by the way right so this is the execution order and my question is is this also the lexicographic order of i comma j and the answer is no the lexicographic order of i comma j is actually 0 0 0 1 1 0 1 1 because we first increment the the second element the the least significant bit before incrementing the more significant bit and so i comma j is not encoding the execution order in fact I should probably make this j comma i and then this would you know j comma i would encode this particular execution order because now if I do j comma i then I'm going to get 0 0 0 1 1 0 and 1 1 and this is going to be my ascending lexicographic order and this is also the execution order that I desire and so I'm going to use j comma i instead of i comma j in this particular example in general I'm going to encode the order by encoding the order of variables or choosing the variables appropriately such that the order is encoded in the set of iteration space and that would make my future analysis simpler and because we also need this information of ordering because if there are data dependencies that we want to respect that particular order so once again we are going to encode the order in the vector of iteration variables i such that the execution order is equal to the ascending lexicographic order of the iteration variables in i now just for completeness sake let me define what is an ascending lexicographic order I think you should already know this let's say there are two vectors i and i prime i is i1 i2 i3 dot dot in and i prime is i1 i1 prime i2 prime dot dot dot i n prime prime okay so notice first of all that n and n prime can be different okay and this is also similar to our lexicographic order in a dictionary we can say that the word zebra is lexicographically later comes later than the word let's say Abra Kadabra right even though Abra Kadabra is a bigger word so Abra Kadabra the n in the Abra Kadabra is maybe 10 or something like that number of the length of the string whereas zebra is the n prime is 5 yet we can say that Abra Kadabra is lexicographically less than zebra right and so the definition of lexicographically ascending is identical i is lexicographically less than i prime if and only if there exists some index m so some index m here let's say m such that m is less than n and m is less than n prime such that all the elements till n are equal so these are all equal and the m plus 1th element is less okay and this is exactly what the dictionary order says right so if I have to compare let's say Abra Kadabra with ABBA then I say a there it's equal then I look at B it's equal and then I look at a third character or the third variable and that is B in one case and R in the other case and so we say ABBA is less than equal to is lexicographically less than Abra Kadabra and so that's exactly what's going on here we basically look for the first different element and then we check which one is less among them and that that one particular string is going to be less than the other one and that's exactly the ascending lexicographic order in the iteration space as well now one last thing how about loop invariant variable so so far my capital D and small d I have said that they need to be constants but what about i equals zero i is less than n i plus plus here n is neither a loop iteration index variable nor is it a constant how do I capture that does it fit in our framework or our representation of i belonging to z to the power d here d is just one so i belonging to the set of integers said that bi plus b is greater than equal to zero well it does the we just going to extend our our small b such that it can also have loop invariant variables in it so for example this would be in the value in this representation these would be the matrices B capital B and small b and this would and I would be just a single dimensional vector capital B would be 1 and minus 1 so that basically and small b would be 0 and n minus 1 so this would basically say so the first row is going to say i is greater than equal to zero and the second row is going to say minus i is greater than equal to n minus 1 which basically translates to i is less than equal to n minus 1 probably I should have said minus of n plus 1 and that would basically translate to i is less than equal to n minus 1 so even for loop invariant variables also this representation would work we just need to basically allow the addition of loop invariant variables I find some expression on loop invariant variables in the vector B
