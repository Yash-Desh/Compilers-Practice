Title: Compiler Design Module 87 : Limitations of Graph Colouring
Video ID: WcYgn1H47Hc
Detected language: en
------------------------------------------------------------

So, we've been discussing register allocation and one of the assumptions that we've been making is that it's a many-to-one mapping from temporaries in your IR to machine registers or you know think of it as a limited number of temporaries which so, but this many-to-one mapping we've been assuming it's a static mapping, it's a mapping that does not consider program locations, it's the same mapping that's used throughout the program and that really simplifies the problem both for identifying this mapping and for actually doing the code generation. But there are limitations and a primary limitation of course is that because registers are allocated statically, it has an all or nothing problem. So, what's the all or nothing problem? Either a temporary is allocated to a register at all the program points or it is not allocated to a register at all the program points or it's not at none of the program points it is allocated to a register, it's actually spilt to memory at all program points 

and you know here is a pattern that where it would be a bad idea to have this all or nothing problem because let's say there's this T1 temporary and here's a loop and so T1 is being used heavily inside the loop body but then T1 is being used very sparingly here on the exit path maybe this exit path is taken only you know once very rarely maybe you know once in a million executions this exit path will be taken but if it's taken then T1 will be used there. Now, it doesn't make sense to allocate T1 to a register here but it makes a lot of sense to allocate T1 to a in the middle of the loop body. Now, if I draw the register interference graph for T1 I will include all the temporaries that are occurring in the loop body and I will include all the temporaries that are occurring on the exit path and now for some reason if you know the graph is not colorable the dense graphs is formed by adding all these edges both on the rear path and on the hot path then we are going to probably decide that let's fill T1 and if we decide that that would be that would be a tragedy right because you know you could have just retained T1 here and not retained T1 in the register at the exit path so the point is that you want to divide this reasoning between at different regions of the program all right you want to say that okay let's draw a different rig different register interference graph for this region and this region could be a loop body and then let's have a different register interference graph for this region which is this exit path and then at the intersection or at the join points of the two regions the point where the two regions meet that those program points which are kind of in the interface between the two regions we can have appropriate translation code that is going to you know translate between the two solutions the between the two many-to-one mappings right so each region can have its own many-to-one mapping but at the boundary of the two regions if you ever go from one region to another region you're going to have to translate based on the two mappings that are that could be potentially different in the two regions all right so that's the idea 

and so this is called a region-based register allocator and the idea is we partition the program into regions and solve for each region separately for example using our previous strategy which is the rig-based strategy which uses graph coloring and reconcile at region boundaries all right so that's basically the idea the question is how do you decide what should be the regions of your program and here are some you know example schemes to decide the regions and once again these are heuristic schemes and we basically could say that okay we have a region for each loop body so loops are you know i did possible it's possible to identify loops inside a control flow graph and we are going to look at more you know more formal definitions of what are what is a loop how to identify it later on but you know think of any cycle and any node all the kind of basic logs that participate in that cycle as the basic blocks that are part of that loop and we can say that okay this is the body of the loop which is consisting of all these basic blocks that's part of the cycle and we can say that this this set of basic blocks is one region and then everything else is another region and so on so we can have you know we can have different cycles in my control flow graph in which case i could have different regions for each different loop we could have nested loops in which case the inner loop could be a different region from the outer loop and the outer loop could be a different region from the main program the you know the outer region of the program which doesn't have any loop whatsoever right and so you can have a region for each loop body and you'd say okay i'm going to do my register allocation for each loop body separately and then i'm going to do the reconciliation at region boundaries or you could also do regions based on register pressure for example how many are live registers are simultaneously present in this region so here the register pressure is very high so even if it's within the loop body or within a straight line piece of sequence of code where there are no loops there could be some regions where there's a lot of register pressure lots of registers are being used and then there are some regions where you know you can see that only a few registers are live and so you can kind of divide the program in the straight line program for example into two regions here there's a lot of register pressure so i'm going to have to spill some things and here there's none so i can kind of bring everything back into machine registers and that way you can you know at least the place where there is less register pressure we can kind of have full utilization of our machine registers as opposed to if we had taken the whole program as a single region in which case some registers would have to be spilled and those would have been spilled even when there was low register pressure in parts of the program so some key things here how to decide a region the region shouldn't be so small that you know because there's some overhead at the region boundary so the region boundary you need to kind of translate from one map to another and there will be some overhead of that so loops are great because you know you go from an inner loop to an outer loop hopefully that's going to be a rare event compared to executing within the loop because loop should at least create a few iterations before it goes out and so at those boundaries it is acceptable to pay the overhead of reconciliation but maybe you know when i am doing based on register pressure then i don't want to kind of make my region so fine grained that the overhead of doing the reconciliation outweighs the advantages of doing region-based register allocation 

okay there are some more limitations to this way of doing register allocation the way we are discussing register allocation is that you know we have done the optimizations in the instruction in the intermediate representation IR and then we are basically doing register allocation in the IR itself so we haven't really translated to assembly and after we have done the register allocation in the IR we are sure that the number of registers in the IR that are being used is less than equal to the number of machine registers and that's when we actually translate it all the way to the assembly code right so we basically look at every single IR instruction and then find the corresponding assembly instruction for that IR instruction or maybe a sequence of IR instructions is translated to a single assembly instruction or maybe one IR instruction is translated to a sequence of assembly instructions so all these three possibilities are possible but the way we are discussing it is that we first do register allocation and then do instruction selection but there is a problem because on some architectures for example x86 some instructions operate only on specific registers so for example you know there will be an instruction which is going to operate on the flags for example or the instruction that's going to do multiplication or division on x86 so maybe it requires that the operand needs to be in a specific register or at least it will require that the operand needs to be in a register all right and in which case if you have register if you have not allocated it to that register or if you have allocated it to memory because of spilling then you may have an issue all right and so this is you know of course you know there are ways different ways to solve this problem for example you could as I discussed last time we could have some reserved registers that are just used for this temporary business but then you know you pay the cost by just keeping some reserved registers so if you have 16 registers and you reserve let's say two registers then you just you know have to work with 14 registers and that's going to slow down your program your implementation and so in general so we would really like to have a solution that is both good hopefully optimal but at least as close to optimal as possible and still kind of you know it's you know we can kind of design an algorithm that will work for all scenarios all right and notice that our algorithm has this pass by pass architecture and that's the problem that and this this problem is basically just a symptom of the bigger problem that we have organized our compiler into a pass by pass architecture so what's the pass by pass architecture well we have passes right so we first created IR then we had optimization passes over the IR then we got an optimized IR then we had a register allocation pass over the IR and now we have an IR that is using no more than machine register number of registers and and then we have an instruction selection pass that's actually converting it to assembly the problem is this pass by pass that the register allocator doesn't know what the instruction selector needs the instruction selector doesn't know what the register allocator needs and so they are just working in their individual silos and that's why it's called a pass by pass architecture ideally it should have been true that you know the both of these things have been done together or even for for that matter all the compiler is basically all the logic in the compiler is kind of working cooperatively with each other that would have been the ideal scenario but that has software engineering issues right i mean the complexity of logic that software developers will have to tackle to be able to do something like that is just so big that it's it's not practical at least today to do something like that so there have been some fixes which are more like hacky fixes to this problem for example the reloading pass and gcc's register allocator so after the register allocation has happened and while the gcc is doing instruction selection it would see whether you know the opcodes the register allocator has chosen the correct registers or not or whether it has you know chosen even a register or has spilled into memory and if the wrong choice has been made then it has some rules that would basically for you know architecture specific rules for example it's x86 x86 specific rules on how to transform that code so that you know it can be you can generate instructions for that sequence of instructions so in other words reload the instructions just change the instructions so that they are different so these kind of hacks have been present in compilers for years now or decades in fact and all this is not a satisfactory solution but it is an engineering solution to problems like this 

there have been alternative approaches that have been more principled for example you can do register allocation and instruction selection simultaneously all right and there have been some dynamic programming approaches that would try to figure out what are all the possible instructions that can be used for this point part of the ir code and then they will also try to figure out what are all the possibilities for the register allocation for example if i spill this then what happens if i spill that then what happens you collect all those possibilities and then you have an algorithm that's based on you know maybe dynamic programming to figure out what's the best combination of choices that's going to give you the optimal solution for a given program so this is generally more expensive during compilation so compiler takes more time to do these kind of things but it can produce significantly better results and some of my own research has been in this space and i will point you to a paper in the comment section that where we kind of show the improvements that can happen because of doing register allocation and instruction selection simultaneously
