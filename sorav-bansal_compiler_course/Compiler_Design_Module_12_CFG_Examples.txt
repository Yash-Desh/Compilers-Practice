Title: Compiler Design Module 12 : CFG Examples
Video ID: WQjBMjjY46Q
Detected language: en
------------------------------------------------------------

So today we are going to look at some examples of context free grammars. So we will start with the example of arithmetic expressions. So as you all know there is you know you can write arithmetic expressions for example say x into y plus z plus a into y maybe I want to say plus g brackets and so on right. So you know I could arbitrarily construct an arithmetic expression and it could have an arbitrary amount of parent nesting in the sense of I could say here I show g plus x but I could say for example g plus x into a plus b plus c right. So I mean these are all examples of arithmetic well formed arithmetic expressions and we would like to ideally try and support all of these okay. So here is a context free grammar for this language of arithmetic expressions. It supports two operators plus and star for multiplication. It also supports parenthesis parentheses so I could say you know open parentheses capital E close parentheses and then it supports a terminal token ID which could be your variable identifier x, y, z, a, b, c and so on. So this form of grammar basically first of all it has one terminal which is ID. So the terminals are basically just the ID. The non-terminals in this case are only capital E and the start symbol is E alright and notice that it is a very intuitive way of specifying what is accepted and what is not. You could add two expressions where let's say E represents an expression so you could add two expressions, you could multiply two expressions, you could put an expression into a bracket or even a variable identifier standalone identifier will also be called an expression and we can see that the sequence of tokens the expressions that we wrote in the previous slide are also accepted by this language because there exists some derivation starting from E that accepts those those strings those expressions right. So for example let's look at this example of let's take a simple example. So let's say a plus b into c so if I want to check whether this expression belongs to this context free language or not I want to know whether there exists a derivation that leads to this particular expression. So well it turns out that there does exist a derivation. We are currently not discussing how we are going to find such a derivation that would be the topic of parsing but let's at least understand that there exists a derivation right. So I could start with E which is my start symbol and then I could apply this production E star E because that seems to be the top level operator here so I would do E star E and then I could choose non-terministically to expand the first E so I would say okay the first E is and then I can use this parenthetic production that involves parentheses so I would say parentheses E into E and then I could use the second the first production let's say E plus E into E and at that point I could use the last production E2 identifier to change all of them to identify as A, B and C all right so indeed this particular expression is derivable from capital E and so this particular expression is a valid is a valid expression in this language is an accepted expression in this language all right here is a real life example of how a context free grammar is used in practice for a real life programming language like C so I took some took a snippet of the grammar from the C parser and you can see that you know they are these these are productions and the right arrow of these productions are basically represented by colon so you know you just read this colon as a right arrow this is in the syntax of the tool that that contains these productions and then it has on the right hand side it has the RHS of the productions also notice that these productions there are these vertical bars which are delimiters so if I say I could write so a goes to y1 y2 or y3 y4 this is equivalent to saying that I have two productions A goes to y1 y2 and A goes to y3 y4 all right so this is a shorthand for saying that A can go to either of these things A can produce either of these things so in this case the selection statement can produce and if which is in so in this syntax that capital identifiers are terminals so if is a terminal and unsurprisingly because if is a keyword then the character parenthesis character is also a terminal expression which is a non terminal so you know there will be some grammar for what an expression is which is not shown on this slide statement else statement right so so what is the selection statement it is it could be if expression statement else statement so the first statement is the then clause and the second statement is the else clause and then there are some other rules for example the second rule does not have the else keyword in it and the third rule basically says that it could also be a switch statement similarly there are rules for an iteration statement while for do and so on and there are different flavors of these loops in C also you could convert an expression to a statement so you could you know you could just put a semicolon after an expression and it would become an expression statement or just a semicolon by itself is also called a statement in C and what are all the statements we're not showing all the statements here but notice that the selection statement which we just saw is one of the statements so a statement could expand either to a selection statement or to some other types of statements including an expression statement but and so on right so these are some this is a selection of productions and we can also kind of say what are all the terminals and what are all the non terminals by looking at this snippet of productions in C maybe not all but at least the ones that are occurring in this snippet
