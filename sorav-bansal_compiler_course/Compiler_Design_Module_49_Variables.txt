Title: Compiler Design Module 49 : Variables
Video ID: IGN8pWzcIc0
Detected language: en
------------------------------------------------------------

So far we have learned about activation records and how activation records can be maintained as a stack of the activations themselves or corresponding to the activations themselves. Now one of the common feature in programming languages is variables and variables are a way to allocate memory and there are two common types of variables one is global variable and other is local variables. So let's talk about global variables. A global variable's lifetime transcends all procedure activation lifetimes and so it has you can think of it as a global lifetime so typically global variables you know the lifetime of a global variable starts when the program starts and ends only when the program completely finishes and so global variable is basically live as long as the program is live and so that's pretty much for our purposes from a perspective of compiler a global variable is always live or we can call it a global lifetime. So to be able to you know so because a global variable has a global lifetime clearly it cannot be maintained as part of some activation because activations do not have global lifetime activations are created and die and they have much shorter time lifetimes as dictated by the duration of individual procedure invocations. So for this reason globals are actually typically assigned a fixed address once in at the very beginning and so that we also mean typically this is done by the compiler so the compiler basically assigns an address to the global variable or variables and and that's it and the operating system when it's loading the program does not only load the code but also loads these global variables into memory at initialization time of the program or this is also called the load time of the program and the other commonly used term for this type of allocation of memory is also called static allocation or statically allocated. So just like code there is a region in memory which is called data and just like code is static in the sense that we assume that code is not going to grow or shrink over time as the program executes similarly data is assumed to be static so this static data which contains information about the global variables can be considered to be static in the address. This is in contrast to for example the stack which grows and shrinks as procedures are called as the activation record stack grows and shrinks. Okay once again the data addresses and size is basically determined by the compiler and typically this can be done at compile time because just like you are allocating space for code you may want to allocate space for your global variables. Now another type of memory allocation are through the heap and we call these heap objects they are distinct from global variables because these are not created at program initialization time in fact they can be created anytime during the program using programming interfaces like malloc or new or other types of implicit allocation and other languages. So such heap objects are created anytime and can be deleted anytime during the program execution. In fact they have nothing to do with the global lifetime of the program it can be any part in they can be any they can live in they can be created anytime in the program and destroyed anytime so of course they're contained within the program's lifetime but they don't have any other requirement. Also what about their relation with procedure activations an object on heap typically outlives the procedure that creates it in fact that is that's how we define heap objects so unlike global objects that have global lifetime and local variables or local objects that have lifetimes associated with individual procedure invocations or activations we're going to discuss that later heap objects is everything in the middle so everything that's remaining and so a heap object is an object that outlives the procedure that creates it and so it cannot be stored in the activation record. So here is an example from C++ I could just say return new int so the activation that created it is this activation of foo but this object is going to live much longer than the lifetime of the activation foo. So unlike the data region which is for global variables which is static the heap is dynamic because it can grow and shrink depending on how many times the new operator has been called or how many times the delete operator has been called or for language like C, malloc and free are called and so the heap is a dynamic structure and just like the stack the heap is a dynamic structure too and this is unlike the data and the code and so typically what is the organization that is used is that he the stack is growing in one direction so the stack is positioned at one end of the address space and it grows in the other direction and the heap is placed in the other end of the address space and it grows in the opposite direction and so that way both the stack and the heap do not interfere with each other when they are growing they only interfere when we actually run out of the address space completely and at that point we anyway have a problem at our hands because we run out of memory so that's a typical way of organizing things in this particular figure I'm showing code and data somewhere in the middle but you can imagine that maybe the code and data have been pushed on one end of this address space and then heaps start from there or even if code and data is somewhere in the middle the heap can basically allocate some things here and then maybe allocate things in other reasons so heap is actually free to anyway which way it likes but typically we would want it to be as contiguous as possible because it gives better locality it also reduces fragmentation and things like that okay let's look at local variables so a local variable's lifetime is contained within the lifetime of the procedure activation that created it in fact that is the definition of a local variable and so where should we store the local variables well actually there are two options on where we can store local variables notice one first of all that local variable is also a dynamic quantity in the sense it doesn't live for the entire duration of the program it gets created and destroyed depending on how procedures are being called and returned and so a local variable is the place where the local variable needs to be a dynamic place because it cannot be for example from the data region because data region is static the moment you run out of data space you won't be able to allocate any more local variables so either the local variables need to be allocated in the stack of activation records or they need to be allocated from the heap and so what do compilers and programming languages typically do well there are both options that are used for by different programming languages for example languages like c c plus plus and rust would typically allocate local variables as part of the activation record so recall that when we discussed activation records we said it has these four fields result arguments control link and return address but maybe there is a fifth variable set of fields which is what are the local variables in the current activation so if you have in c you declare local variables inside a procedure body then when that procedure gets called the the activation record that is created would also allocate some space for the local variables in that particular procedure call and so that could be part of the activation record and in this case because the activation records themselves are living as part of the stack the local variables space is also being allocated as part of the same stack another option is to allocate and deallocate within the heap and so in this case whenever an activation is invoked so whenever a procedure gets called if there was a local variable inside that procedure then maybe that that local variable is just allocated on the heap and so the compiler responsibility to generate code to allocate that local variable and maybe when the from the heap and then when the procedure returns then the compiler would have generated code on the return path to deallocate that particular variable in this particular case the local variables are being allocated and deallocated on the heap which one is better is it better to allocate on stack or whether it is better to allocate on heap well typically it is much cheaper to allocate on the stack because allocating on the stack typically just means because a stack is organized in array it just means decreasing you know just changing the stack pointer the top pointer of the stack so that you have allocated more space and so it's typically much cheaper to allocate space and so and so that makes sense on the other hand some languages like especially the garbage collected languages like java python o camel and so on they do not really you know make a distinction between local variables and heap allocated objects for them all of these are basically dynamically created objects and let's just allocate them on the heap because references to them can get passed around and that's legal and in and so in such cases it it does not make sense to allocate these local variables in activation records because their lifetimes can actually transcend or go beyond the lifetimes of the activation that created it so you know you can say that then they are not local variables sure so some languages like these wouldn't you know would have variables for example in java if you wanted to create a local variable you just say new and you would have to call new and so it would basically get allocated on the heap similarly languages like python if you just declare a local variable whenever the you can kind of use the reference for that local variable and pass it to your caller and so that kind of a thing can only be legal if that local variable was allocated on the heap and not on the stack because the stack would get deallocated as soon as the function returns the procedure returns and so these kind of languages which do these do this type of allocation deallocation implicitly based on compiler support typically also use garbage collection to identify identify garbage or identify memory that was allocated but was not properly deallocated the reason for this is that typically it is pretty expensive to first allocate and then deallocate on every function call so let's say you have a small function and then you call it and there was a local variable that was created and then you deallocate it when you return that would be a lot of a lot of runtime cost to pay to both allocate and deallocate on the other hand maybe you just want to pay the cost of allocation which is maybe cheap and just defer deallocation to later on and so how do you defer the deallocation to later on well you just say that okay periodically we are going to do garbage collection i'm going to have some way of identifying what is memory that is not currently getting used so all such local variables that have been allocated but have not been deallocated would appear as garbage to such a garbage collection algorithm and and so the the the runtime would be able to collect it so all these are options on how to implement local variables where it can be argued that some of these are not local variables and they should just be called heap allocated objects but but so so local variables are usually if the local variables lifetime is typically within the lifetime of an activation record or activation then they're typically implemented as part of the stack
