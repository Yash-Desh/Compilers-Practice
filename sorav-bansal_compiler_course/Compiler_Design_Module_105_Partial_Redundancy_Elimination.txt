Title: Compiler Design Module 105 : Partial Redundancy Elimination
Video ID: bC83QnRcoeM
Detected language: en
------------------------------------------------------------

So now we have discussed the data flow analysis framework in a lot of detail and also talked about its various properties. Now let's look at some examples of DFA based transformations, some of which we are already familiar with. So for example, we have looked at constant propagation, copy propagation, dead code elimination, which is based on the liveness DFA analysis, which is actually a backward analysis. The other two that I mentioned, copy propagation and constant propagation are forward data flow analysis, common sub-expression elimination, and we're also going to discuss loop invariant code motion and partial redundancy elimination in the next few lecture modules. So here's an example. Let's say I have a program which says for i equals 0 to n, i plus plus ai equals m plus n, an minus 1 plus equals ai plus 1. Now in this particular program, there are a couple of things that are loop invariant, which means they don't really change across different iterations of the same loop. What are those computations? Well, one of them is m plus n. Notice that this loop is not actually changing m, neither it's changing m nor it's changing n. And so m plus n is actually a constant across the iterations of the loop. In other words, m plus n is loop invariant. Also notice that this particular computation here, which is the address of element i in array a is not loop invariant because it changes with every iteration because every iteration has a different value of i and so ai would be different value for the address of ai would be different value and every iteration. Similarly, ai plus 1, both the address and the value of ai plus 1 are going to be different in every iteration. But let's look at this one an minus 1. So n is loop invariant and so the address of an minus 1 is also loop invariant. And so there are two computations in this particular loop which are invariant across iterations of the loop. And so it is possible to implement loop invariant code motion. So that's the name of this particular transformation where you move the loop, move the computation which is loop invariant outside the loop. Typically before the loop. So for example, here you do m plus n in the beginning of the loop. So t1 equals m plus n and similarly you do t2 equals the address of an minus 1. So in the C syntax that's using the ampersand operator. So t2 is just the address of an minus 1 computed once in the beginning of the starting of the loop. Now this is likely to be a beneficial optimization if the loop is going to execute at least a few number of times potentially a large number of times maybe tens of times or hundreds of times or even thousands or millions of times. In which case this is a very useful optimization because without this optimization we would have, now using this optimization we are saving potentially millions of operations if the loop had million iterations. Alright, so now the loop just becomes ai equals t1 and star t2 plus equals ai plus 1. So instead of m plus 1 I just replace it with t1 and instead of an minus 1 which involves arithmetic of adding n minus 1 to a all that has been done a priori into t2 and I just need to dereference t2 and then add ai plus 1 to it. So these are examples of loop invariant code motion transformations and we want to know how we are going to implement such transformations. First of all once again I want to point out that these are very consequential transformations because the loop may execute a large number of times. I also want to point out that it is not necessary that this transformation actually reduces your program time because if for example it was true that the loop was n was 0 for example in this example. So if n was 0 this loop would have never executed and if this loop would have never executed I would have never had to compute m plus n or I would have never had to compute the address of an minus 1 and this transformation is actually always going to, I mean it's going to cause that computation to always happen. So in that rare scenario where n is going to be 0 we may end up doing more work in the optimized or in the transformed program than in the original program but typically we expect that if the programmer has written a loop it's likely to execute at least a few number of times typically large number of times and so a compiler can make this approximation. Once again compiler is making a trade-off here between the probability that the transformation is actually going to cause a slowdown is small and the probability that the transformation is going to cause a speed up is large and the associated speed up itself is also large and the associated slowdown is also small because if the loop was not going to execute even once then all we are doing is adding two extra computations on that path. On the other hand if the loop was going to execute you know at least let's say a hundred times then we have saved 200 computations so you know what the compiler needs to make these trade-offs when it is doing these transformations and typically loop invariant core motion is considered a very profitable transformation as in this example. Alright so here is we are going to talk about one transformation which is called you know we're going to talk about a category of transformations which is called partial redundancy elimination and we're also going to call it lazy code motion in subsequent modules. So the good thing about partial redundancy elimination I'm going to describe what it is in the next few slides is that it subsumes a few different transformations for example it subsumes common subexpression elimination. We have already learned about common subexpression elimination earlier and partial redundancy elimination subsumes CSE why what do you what do I mean I mean that if you implement partial redundancy elimination all common subexpression would automatically get eliminated in the same algorithm. Similarly partial redundancy elimination subsumes loop invariant core motion so that seems pretty powerful a single transformation can subsume both loop invariant core motion and common subexpression elimination and it gives you a few extra capabilities alright so it is a it is a very powerful transformation it is also based on data flow analysis it is one of the very interesting ones because it involves multiple data flow analyses to be fit together and we're going to use this as a case study in the next to and study it in more detail in the next few lecture modules. I should also mention that partial redundancy elimination is a practical transformation that is typically implemented in all modern optimizing compilers. Alright so what is partial redundancy elimination and to understand that let's look at this particular example so in this example I am computing B plus C on one branch and I am updating B on the other branch and then on the midpoint again I am computing B plus C. Now B plus C is partially redundant the computation B plus C is partially redundant why do I say it's partially redundant because it's redundant only on some of the program paths so it's only redundant on this program path it is not redundant on this program path in other words in on this program path I don't need to do it again I can I could have used common sub-expression elimination to eliminate the second one and kind of reuse the previous previously computed value but on the other path I still need to compute it and so so this computation is only partially redundant this is in contrast with common sub-expression elimination where we eliminate a second expression only if it is totally redundant so here you know because in common sub-expression elimination recall that the meet operator is intersection and so if I run the common sub-expression elimination algorithm on this particular example I won't be able to eliminate B plus C on any of the paths all right but partial redundancy elimination is a more sophisticated idea and in this we are going to say okay so for example I may want to transform it on the right side and so the right is program here is the optimized transform program which does not have partial redundancy so how does it work well on the left hand side we just basically have the same computation just that we store it in a temporary variable T and then just do A equals T recall that creating these extra copies is not a problem as long as we're going to do copy propagation later on hopefully that's going to solve these problems for us and on the other branch I basically when after I do B equals seven I recompute B plus C only on this particular branch and then at the meet point I just do D equals T so what have I done I've eliminated the partial redundancy if you look at it on both the paths B plus C is not redundant right so B plus C is not being recomputed redundantly on any of the paths and that's why it's called elimination of partial redundancy in the original program B plus C was redundant on some of the paths that means it was partially redundant in the transform programs B plus C is not redundant on any of the paths so the partial redundancy has been eliminated on this particular path B plus C is only computed once and on the other path also B plus C is only computed once okay so that's what partial redundancy elimination means and so you know one way of kind of asking this question in a more general way so right now I just showed you a single example but if I wanted to ask this question of partial redundancy elimination in a more general way I want to ask the question can we place computations of B plus C such that no path recomputes the same expression all right so I basically no program path so recall that there are there could be potentially an infinite number of program paths in the program because there are loops and other things but can we place computations of any computation so in this example I'm using B plus C as an example or any given computation such that no path recomputes the same expression more than once right so if I if on that path it has already been computed and it has not been killed for example if I'm computing B plus C then B hasn't been set to 7 or something like that or no other value or C hasn't been set to some other value something like that so as long as that expression is available on that path I'm not going to recompute that expression on on that path all right so this is very similar to the available expressions analysis that we've seen earlier just that in the available expressions we had the mandate that it should be available on all paths here we are saying that and we would say that there should not be redundancy if there is if it is available on all paths here we are saying that look at each path in isolation and even on every single path there should not be any redundancy so that's a much more difficult statement or analysis to kind of imagine so first of all I want to point out that partial redundancy elimination subsumes loop invariant code motion and that's easy to see because if I have a loop and let's say this m plus n is a loop invariant computation then m plus n is is redundant on many paths for example if I just take the path which comes which goes around the loop once and then goes goes out here m plus n is being redundantly computed once more right so it's total in total is being computed twice but randomly computed once right so once it's the first time you need it but the second time it was redundantly computed or even if I take a path which has which goes around the loop three times or ten times then m plus n being is being redundantly computed you know twice or nine times or things like that so so here there is partial redundancy right so involves redundant computation on loop path so there are some paths where there is redundancy and so this expression this program has partial redundancy in it what would be a way to solve this problem well one way to solve this problem is to basically eliminate the partial redundancy by doing loop invariant code motion you recompute m plus n ahead of the loop and then you just do a equals t inside the loop and now I have eliminated partial redundancy on this particular program because in this program m plus n is computed only once on all the program paths right irrespective of whether so let's say let's take a program path that goes around the loop ten times even there m plus n is only computed once all right so partial redundancy elimination or pre subsumes loop invariant code motion or licm all right now here is an example that shows that pre may require adding basic blocks so here is an example so for example you have c equals a plus b and then you know some from somewhere above in the program there is another basic block and there's some computation here and then there's a meet point where I say d equals a plus b so now once again a plus b is being partially computed on some of the program paths for example on this particular program path a plus b is being partially computed but if I look at this particular program path a plus b is not partially is not is not redundant so here it is redundant on this particular program path it's redundant on this program path it is not redundant so a plus b is redundant on some path there is partial redundancy with respect to a plus b the question is how should I remove this partial redundancy now notice that if I bring this a plus b here so if I bring this a plus b here then that's not a good idea because then I have introduced redundancy so I have eliminated redundancy on this path so with this if I bring a plus b here let's say so let me just draw an arrow so let's say I bring this here then I have eliminated redundancy on this path but I have introduced a redundancy on this path because on this path a plus b was not getting computed probably it was not even required and still I have put a plus b computation there so one of the other things about redundancy is that only those expressions should be computed which are actually required on that particular program path right and so here I mean if in the original so I shouldn't introduce any extra computations on any of the program paths in the program but if I move a plus b here then I have added an extra computation of a plus b on this particular program path and that would be that would that is not an acceptable solution for partial redundancy elimination because to eliminate redundancy on one of the paths we're kind of adding redundancy on another path we are adding extra computations on another path where they were not even required all right so that is not you know that is not something that we would like to have in our solution and so it is not okay to compute a plus b here all right so this is this is a bad solution so what is a better solution well first of all I want to point out that why is this problem even occurring this problem is occurring because because there is a an edge here which basically has a basic block which has two successors and so the from basic block of this particular edge has two successors and the two basic block of this particular edge has two predecessors and so if I want to move a plus b into this here then I'm adding extra redundancy on some of the program paths for example I'm adding redundancy on this particular program path so what is the solution the solution is to add a new basic block which basically breaks this particular edge into two edges all right so and that way I'm being able to remove redundancy on this particular path without adding redundancy on this particular path so on this particular path a plus b is not getting computed on this particular path a plus b is on this particular path a plus b is only getting computed once and on this particular path it is also getting computed before it is getting used so it's it's all good in this situation so in this case we had to eliminate the partial redundancy we had to add an extra basic block and why did we have to add an extra basic block because I wanted to break this particular edge into two edges so that I can place a computation in the middle of that edge I didn't want to place the computation at the source of that edge I wanted to place the computation in the middle of the edge so what is special about this edge that I had to break it into two and place a computation in the middle of it well it's because this edge is special because it as I just said before it's from node has two successors and it's two node has two predecessors and that's why it becomes difficult to move a computation or hoist a computation when we move a computation above earlier in the program then we call it hoisting of a computation so it becomes harder for to hoist a computation because across a critical edge because now you may be adding redundancy on some other parts of the program and so for in so I want to act so I need to break the critical edge into two edges and insert a basic block in the middle of them so what what is a critical edge a critical edge is an edge where the source basic block has multiple successors and the destination basic block has multiple predecessors so here is an example of a critical base edge this particular basic block has two successors and this particular basic block has two predecessors two or more predecessors two or more successes all right critical edges make it harder to remove redundancy because if I have a plus b here and let's say a plus b was also being computed on some other path in the at the at some of the predecessors of the destination node of the critical edge then I don't know where to put a plus b because if I put it here then I'm adding redundancy on this particular path and that's not a good idea so where to hoist a plus b if it is redundant on the other incoming edge of the destination well the answer is that you need to add a new basic block for that particular critical edge so so this is going to be a feature of our algorithm we are going to typically have we may have to break critical edges into two edges and put a basic block in the middle of them to be able to eliminate partial redundancy now I want to point out that partial redundancy may still not be eliminatable even after adding basic blocks and here is an example here basically I have a equals b plus c and d equals b plus c and I have this diamond shaped two diamonds I have a program structure which is made of two diamond shapes there is a meat point first and then again there is branch and then there is meat and now it is compute b plus c is computed on one of the branches in the first diamond and then it is recomputed on one of the branches in the other diamond now in this particular example if you look at this particular path that has been marked in green then the expression b plus c is computed redundantly twice in this particular path on the other hand if I look at any other path there are four paths in total in this other program and on the other four paths there is no problem of redundancy but on one of these paths there is a problem of redundancy the question is how to remove this redundancy and and the the thing is that here we it's not enough to just add a basic block to remove this partial redundancy in fact here if you want to do this you may have to duplicate the code by duplicating this part of the second diamond of the program right so this program is made up of two diamonds the first diamond structure involves branching and then meeting and the second diamond structure again involves branching and meeting and one way to eliminate partial redundancy in this particular program is to copy the code of the second diamond for each branch separately so specialize the code for each branch and then in one of the branches eliminate the redundancy and on the other branch there is no redundancy anyway and so that way we can achieve partial redundancy elimination so to see that you know what I can do is in this particular example I have first kind of eliminated this b2 to specialize the code of the second diamond on the left hand side path and so this is what I get and on this particular path I've already computed b plus c so I can reuse that and so instead of recomputing b plus c I can just say d equals t where b plus c is already computed into t and for the other branch I copy the original code as it is now you can imagine then when I copy the code then the code size increases and that can also have a dramatic effect on the runtime because of caching effects and things like that moreover when we duplicate code like this example then what happens is that the code size increases by a multiplicative factor and when there is a multiplicative factor involved things can quickly become exponential right so if you start duplicating code like this then then the program can really become really big and that can be detrimental to performance and so but nonetheless even if I'm doing that I can be a little smart about this so maybe I just duplicate b3 but then from b3 the outgoing edges are kind of shared so for example if b3 goes to b4 then both b3 and b3 prime which is the copy of b3 can go to the same b4 right whereas for d equals b plus c one of them has a specialized version d equals t and the other one has the original version which is d equals b plus c and both of them can now go to b5 and so this would be our transform program notice that in the transform program I have duplicated b3 b3 and b3 prime and that is and so to be able to remove partial redundancy I had to duplicate you can check that in this particular program once again there are four possible paths and on all those far four paths there is no redundancy so we have successfully implemented partial redundancy elimination but unfortunately we had to duplicate code to do this we are not interested in our particular algorithm we are not interested in duplicating code because as I said duplication can quickly become exponential in nature and can have detrimental effects both to code size and to performance and in our work we are interested in eliminating only those redundancies which can be eliminated without code duplication right we are not interested in code duplication but without changing the control flow graph structure by much there are some things that we will still allow and that is the addition of basic blocks is still allowed on critical edges but apart from that we don't want to modify the control flow graph structure in any other way yet we want to eliminate partial redundancy wherever possible in such a restricted setting so that's what we're going to be discussing in the next few lecture modules.
