Title: Compiler Design Module 37.1 : Subtyping Relation
Video ID: osCx0cahxeY
Detected language: en
------------------------------------------------------------

I'll now introduce the subtype relation. S is a subtype of T is read, S is a subtype of T or alternatively T is a supertype of S. So this less than operator basically represents is a subtype of less than into the colon in this notation and so we read this as S of T. So what does S being a subtype of T mean? Type S is a subtype of type T if any term of type S can be safely used in a context where a term of type T was expected. So notice that I'm saying that a term of type S can be used in a context where a term of type T was expected. And any term and if this is true if this statement is true then we say that S is a subtype of T. Okay so I'm going to show some examples to explain this but the key words here are safely used and context and they are not precise in this definition and they are actually very programming language specific and so when we look at examples this will become clearer. So precise definition of S is a subtype of T is very programming language specific and here are some examples of subtyping relations. So let's say my language has two types int and bool and let's say I decide that int is a subtype of int which means if any term of type int can be used in a context where a term of type int was expected. Now this is pretty straightforward in fact as we're going to see later every type is a subtype of itself. Similarly bool is a subtype of itself but notice that there is no subtyping relation across the two types so neither is bool a subtype of int and neither is int a subtype of bool. What does this mean? Well let's say that we had an operation plus that was associated with int which was the addition operator and let's say we had an operator logical and which was associated with the bool type. Now if and let's say the term true is of type bool similarly a term false is of type bool and because the logical and operator is something that is acceptable on the type bool so true and false is a valid expression. However true plus false is not a valid expression because plus is not expected with bool types plus is only expected with int types and true is of type bool true is a term of type bool and the bool and a type bool has nothing to do with the type int there is no subtyping relation between bool and int now these these relations basically don't say anything about what is the relation between bool and int and so in this language let's say true plus false is invalid and similarly if I you know try to logical and two integers let's say one and two then that's also invalid. Now let's say I mean this is one example of a programming language now let's take another example of another programming language where they decide to have different relations between int and bool and the different subtyping relations so once again int is a subtype of itself bool is a subtype of itself but additionally the language says that bool is a subtype of int which means that any term of type bool can be safely used in a context where int was expected. So for example if I look at the operator plus then one plus two is legal but also because bool is a subtype of because bool is a subtype of int if I replace one with let's say true and say true plus two then in this language that would also be type correct so true plus three is type correct similarly true plus false is type correct because a term of bool can be safely used wherever a term of int a term false of type bool is can be safely used at any place where a term of type int was expected here a term of type int was expected because of the plus operator and because bool is a subtype of int I can use false here and it may have some meanings for example the plus operator may treat true as one and false as zero it you know it depends on the language but notice that int is not a subtype of bool which means that anything anywhere where bool is expected it's not true that I can use an int so for example one logical and with false is incorrect you know because here this one is incorrect because I expected a term of type bool maybe I wanted to use a true here similarly one logical and with two is incorrect because you know we expected a bool and but we're getting an int and int is not a subtype of bool it's the other way around bool is a subtype of int but int is not a subtype of bool and so one and two is wrong but because bool is a subtype of int true plus false is correct now let's take another example of an other language in this language once again int is a subtype of int bool is a subtype of int but let's say int is a subtype of bool and in this case these expressions here one and false one and two become correct written in green but true plus three and true plus false become incorrect right so it's a subtyping relation is used to disambiguate the correct expressions the type correct expressions from the type incorrect expressions and depending on how you choose the subtyping relation you mandate what is correct and you you specify what is correct and what is incorrect so notice that subtyping is a form of type polyformorphism for example in the previous examples if bool was a subtype of int then true can be used both as a bool with the logical and operator and true can be used as an int with the plus operator in other words a single term true seems to belong to more than one type because it can be either used as an int or used as a bool and it will have a meaning with depending on in which context it's been used for example if it's used in with plus then maybe it has a meaning one and when it's used with logical and then it has the meaning true so subtyping is a form of type polymorphism there are other types of there are other forms of type polymorphism and subtyping is one of them in object-oriented languages subtype is the same as derived type so for example if you have a if you define a new type using this keyword class let's say in c plus plus then you have class a and you have some specification of class a and then you can say class b is a derived type of class a in other words class b is a subtype of class a so b is a subtype of a and a is a supertype or also called a base type in object-oriented languages of b which basically means that a term of type b in this example this represents a term of type b can be used safely where a term of type a was expected so what is expected here was a term of type a because it's being assigned or more precisely a term of type a star because it's being assigned to a variable of type a star but because b is a subtype of a it's safe to use a term of type b in a context where a term of type a was expected now notice that once again what do i mean by safely used and what do i mean by context is very programming language dependent for example in c plus plus i can do this for pointers but perhaps i cannot do this for you know i cannot for example in c plus plus just do a a equals b all right so subtyping relation is kind of has two two keywords here which is what is the what does what do i mean by context and what do i mean by safely used and and the programming language will specify what what it means for example here in c plus plus it means that you know it needs to be a pointer context and and it can be safely used in these ways etc so informally speaking if s is a subtype of t then a term small t of type s can be used in all operations of type t notice that small t is of type s yet it can be used in all operations of type t because s is capital s is a subtype of capital t but it may additionally be used in more operations specific to s because it is of type s so it can be used in more operations which are specific to type s in addition to all operations that are acceptable for type t so here are some properties of the subtype relation a subtype relation is reflexive must be reflexive i mean these are requirements of what a subtype a valid subtype relation the language can be it needs to be reflexive recall that in all the examples we saw we said bool is a subtype of itself and int is a subtype of itself and that just follows for from definition for all types s s is a subtype of s it basically means a term of type s can be safely used in a context where a term of type s was expected transitive if s is a subtype of t and t is a subtype of u that basically implies s is a subtype of u for all possible s t types once again this follows from definition if a term of type s can be used anywhere where a term of type t was expected and the term of type t can be used anywhere where a term of type u was expected then transitively a term of type s can be used anywhere where a term of type u was expected and finally a subtype relation is anti-symmetric which basically means that well if s is a subtype of t and t is a subtype of s then they should essentially be the same type so this is basically ensuring that you don't have two types that behave identically to each other and if they do behave identically to each other they're basically the same type they're just different names for the same type and so if and so this is the anti-symmetric property if s is a subtype of t and t is a subtype of s then s must be equal to t for all the types s and t in fact because of these properties you can define you can you can specify the subtyping relation through a drawing as a tree so for example if these were your subtyping relations this is the corresponding tree every node in the tree represents a type and the arrow represents the subtyping relation so for example the arrow here is kind of has this shape and and so you know this basically means that bool is a subtype of int notice that the arrow has the same orientation that or direction as a subtyping relation operator in this statement and notice that reflexivity just comes for free in this diagram representation because every node is a subtype of itself transitivity it can be basically seen so for example if some other type t was a subtype of if well if if bool was a subtype of t then by transitivity int would also be a subtype of t now i want to emphasize that subtyping and typecasting are two different things so for example these are examples of valid syntax and c and c plus plus so you could say int i equals character c now it may appear that because character c is a term of type care in c so maybe and because it's appearing on the right hand side here it may appear that character is a subtype of int but in fact in c and c plus plus character is not a subtype of int similarly i could you know you can write code like this you can write float f equals three and it may it may kind of you know make it appear that int because three is an integer int is a subtype of float but but int is not a subtype of float okay neither is float a subtype of int and so on then how how come these these are valid syntaxes in c and c plus plus if you know char is not a subtype of int in c float is not a subtype of int in c then why is this why are these valid syntaxes well well it turns out that c and c plus plus also support what is called implicit or explicit type conversion also called typecasting and so subtyping and typecasting are two different things so when you write code like this when you say int i equals character c the compiler implicitly inserts a type conversion operator here int and you could have done that yourself too and this is a this is something that the compiler provides to the programmer as a matter of convenience different compilers may choose to do different things here for example some compilers may say i'm not going to do implicit type conversion these implicit type conversions are implemented by compilers or convenience and they should not be mistaken for the subtyping relation because the subtyping relation is actually a much more a much deeper requirement which is which has which pertains to language design itself where this whereas this implicit type conversion is just a matter of convenience for the programmer all right notice that subtyping relation is designed by the language designer you know depending on what the designer wants from his or her language they will design the subtyping relation accordingly and this is a very you know this is an art and you know various considerations need to be taken into when you are designing a language one of the considerations that i show here which is kind of subtle is function polymorphism or overloading for example languages like c and c plus plus allow simultaneous iterations of the same function identifier foo but which take different types of arguments for example one of the arguments could be int comma int another signature could take the first argument is character and the second argument is integer and the third could say oh the first argument is integer and the third second argument is character now notice that if character was a subtype of int if char was a subtype of int then disambiguation becomes much harder for example if the user types foo with two characters let's say character a and character b now it's you know if character was a subtype of int it becomes much harder to say which one of these should should be called should should the first one be called should the second one be called or should the third one be called because well i mean this is if character was a subtype of int then it appears that either of them can can pattern match right because you know character is being used in a context where character is expected and a character is being used in a context where int is expected and because character is a subtype of integer it may appear that the second function can get called but the same logic also applies for the first and the third functions as well so when you when the language designer is making these choices of the subtyping relation there are all these considerations they have to consider
