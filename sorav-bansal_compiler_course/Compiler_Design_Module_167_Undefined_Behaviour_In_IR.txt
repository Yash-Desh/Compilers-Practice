Title: Compiler Design Module 167 : Undefined Behaviour In IR
Video ID: Zh_mtwKEz0c
Detected language: en
------------------------------------------------------------

We've seen before that undefined behavior or UB in the source programming language actually enables optimization and to recall this let's look at this example that we have seen before. So let's say there is a variable n and I have I'm doing some computation which involves computing n plus one anywhere in the program. In this particular program I'm computing n plus one inside the check of the for loop. Now n plus one for an assigned integer in C can potentially overflow and so the question is what should be the semantics if n plus one overflows and one possible answer could have been that n plus one actually discards the most significant bit or in other words this is also called wraps around to zero in that case but that actually disables some optimizations and we've seen before one of the kind of transformation that would be a common transformation is you want to change i is less than n plus one to let's say i is less than equal to n and this transformation which looks really simple transformation and something that we take for granted is actually not valid if we use the wrap around semantics for integer overflows operations and so what programming languages like C which are very conscious of optimization and performance do is that they basically say that n plus one is undefined behavior or n plus one should never happen because n plus one triggers you be and if the program triggers you be the abstract machine can do anything it's basically means that the program was actually wrong and so because of those semantics we can now do these transformations because the compiler can assume that you know the compiler only needs to preserve semantics for correct programs if the program is illegal then the compiler does not need to worry about and for all legal programs which means there is no overflow this transformation i is less than n plus one can to i is less than equal to n is actually a legal transformation and so we have seen that non-determinism caused due to undefined behavior in the source language is actually good thing because it increases the choices of a compiler because there is non-determinism because there's undefined behavior semantics in the source language the compiler can have has bigger space to play around with for example it can do this transformation from i is less than n plus one to i is less than equal to n and so undefined behavior is actually very critical for compiler optimization but what if we want to introduce non-deterministic operators in the target that would that seems like that was going to cause problems because if there are non-deterministic operators in the target which means there are operators that could cause undefined behavior in the target then you know it becomes a little bit tricky because now you want to make sure that if there is non-determinism the target that agrees with whatever the semantics of the source program is you cannot introduce more non-determinism than was already present in the source program so i'm going to use examples to explain this further so let's say i have a program which is which looks like this and so most transformations in our compiler toolchain basically are ir to ir transformation so recall that a compiler pipeline basically involves first starting with the source language then the abstract syntax free then the intermediate representation and there could be multiple intermediate representation for different levels of abstraction and optimization and then finally you generate code for the machine language maybe assembly language or something like that now because most transformations are ir to ir this scenario where target language can also have non-determinism is most relevant when i do ir to ir transformations in other words i should represent my source program also in the ir and the target program also in the ir so i'm going to represent the program that we just saw which was this for loop in my three address code like syntax with ssa form with five nodes and things like that this syntax is actually pretty similar to the llbm ir syntax which is also a three address code syntax so in this syntax this is just the same you know there's the same program that we saw in the previous slide which is this program in latency but here i write it in you know ir syntax the first basic block which i'm calling the entry basic block or e just initializes i to zero because it's ssa form i also version the i variable and say i1 equals zero then i have the loop head which where i say i2 equals and i use a phi node because there are two values of phi that are flowing in one is flowing from e and the value that's flowing from e is i1 and the other is flowing from the back edge of the loop and the value that's flowing in from the back edge of the loop which i call l is i3 we're going to see what i3 is going to be then we have a check which says if i2 is greater than equal to n plus one we go to n so this is our loop condition it says i is less than n plus n so if i is less than n plus i1 then we strain the loop otherwise we go to the end of the loop then the rest of the body and this is the kind of increment operator of the loop which is the step of the loop the step of the for loop which is i equals i plus one and once again because it is ssa i do i3 equals i2 plus one and then i do go to h now and on the target side basically what i'm doing going to do ir to ir transformation the kind of transformation that i just showed before i'm going to say because i want to change i2 is less than n plus one to i2 is less than equal to n so that basically you know roughly translates to a conditional branch which says if i2 is greater than n go to end basically saying if i2 is less than equal to n then strain the loop now i mean i'm just using a very rough representation of the ir also it is not the most optimized representation of the program but it just gives you an idea of what the ir representation is going to look like for the programs that i showed you in the previous slide which was written in the c syntax these if statements here with go to are basically representing conditional branches that are typically a part of any three address code intermediate representation syntax the most relevant ir feature that i want you to remember from this example is ssa all right notice that this is an ssa representation which means you know every variable is assigned at most once and statically speaking in the program so for example if i is being assigned twice or thrice in the program so in this case i was actually being assigned twice one was the initialization value zero and the other was the increment i equals i plus one and to basically model this i had to version i1 and i3 for the two updates and for the meet point i use the final and so i end up with three versions of i i1 i2 and i3 and this is going to come in handy when i'm going to discuss the semantics of undefined behavior in at the ir level later on so i just wanted to kind of you know point out that irs are typically ssa and we're going to use that in our undefined behavior semantics we're going to talk about in the next few modules okay so let me come back to my original syntax and you know i just wanted to show that ir syntax to motivate the fact that you know the most the ir that we're going to consider is going to be an ssa form but it's a little bit hard to read ir so i'm going to go back to my c syntax just that i'm going to ensure that every variable is assigned only once so that we maintain this ssa property now let's look at another program just like we saw one example where non-determinism caused due to undefined behavior actually increases transformation opportunities let's look at another example where we may find the opposite which means non-determinism in the language semantics actually decreases optimization opportunity and i'm going to take the scenario where i'm doing ir to ir transformations because as i already said this is the most common form of transformation where you transform one program a program written in the ir to another program written in the same ir and so once again even though i'm using the c syntax it's basically you know an abstracted form of the ir syntax and i'm just using the c syntax for easy readability really we are actually operating in the ssa ir syntax so let's say this program we have a variable x of type int and then somewhere in the program i have if some condition c a boolean valued condition c y equals x plus one now recall that my ir is ssa so basically on this path y should have some value and on this path y should have another value and because this is ssa we'll introduce a new variable z at the meet point so you know it's like a diamond structure i come here there is an if condition so this is an if condition i assign y to something and then i join here and at this join point i want if i want to if i want to use the value of y at this point i need to define the new variable z which says z equals phi node and on this value the value the value that's flowing in some is something and on this path the value is that is flowing is y and so that's what i represent using using this phi node here which is that you know this if i come from this path this blue line that shows one path then i use some value that i'm not really shown i'm using dot dot and if i come from the if path then i use the value y because y is only available on because there's only one assignment to y in this ssa form and that is within if so y can y cannot be defined here and so if i want to use the value of y then i want to i'll need to have a phi just that okay now let's say i want to transform this program into something like the following i basically version y into two variables y1 and y2 equals x plus one and say y2 equals y1 minus one so in other words you know if i execute y1 equals x plus one and say y2 equals y1 minus one what i'm really getting hopefully is y2 equals x and also i do y2 equals y1 minus one only when condition is not true so i also negate c and then i have the same z at the meet point and basically say z equals phi and if i'm coming from the if condition then i use the other value which is the dot dot dot value and if i'm coming from the you know without the if condition then i use y1 and i claim that for for regular values for values that don't cause any undefined behavior or any over so on anything like that the value of y1 at this point that z will get would be identical to the value of y in the original program that z would get you can see that clearly because if c was true then y would have been x plus one and in this case if c was true then also y1 would have been x plus one and that's you know that's basically what it is if c was false then this would have got some other value which i'm calling dot dot and even here even here if c was false then it's getting some other value dot dot so i'm saying that in both cases z gets the same value and i really want i mean this transformation seems like an okay transformation but i put a question mark here because i'm going to show that this transformation is actually not an okay transformation in the presence of undefined behavior semantics so notice that in the language semantics as we have already studied for let's say language like c x plus one has ub for some values of x and that values for example would be when x equals int max then x plus one will overflow okay and yet we are hoisting its computation so for example in this case x plus one was only going to be computed if c was true but in the target x plus one is always computed irrespective of whether c is true or not and so that's called hoisting of the computation computation is going to occur in more scenarios than it would have occurred in the original program and so it will be computed in the target even if it would not have been computed in the source and so that is wrong that would be incorrect in the case when x was equal to int max and c was equal to false in the source program if c was false x plus one one won't having even been computed and so you know it would basically the program would behave as a no-opt in the source program because c is false you know you don't even execute x plus one and you basically come to z and z takes the other value which is dot dot but in the target i compute y one equals x plus one irrespective of whether c is true or false and so right here i'm going to trigger ub and the program is incorrect and so for these values even though the z is going to still get the correct value but even before that the program has triggered ub and because the program has triggered ub the abstract machine can now do anything it can crash the program it can remove your files whatever right so because it's an illegal program and so so this is a this is actually an incorrect transformation because there is some computation that could trigger ub that was for some particular input that ub gets triggered in the target but it does not get triggered in the source and so this transformation becomes an illegal transformation here's another example so let's say i have you know i have an integer variable x and i have a y loop and inside the y loop i define the new variable y and y equals x plus one and then i use y once again i'm using you know i'm using an abstract representation of the ir program i'm basically obeying the ssa property and every variable is i'm assuming is only assigned at most once okay now can i hoist this y equals x plus one assuming x was loop invariant can i kind of do loop invariant code motion a transformation that we've actually studied in the past to above the loop and say y equals x plus one and then i have my loop body and the loop body may be using y now this looks like a perfectly valid transformation in fact this is a transformation that we have discussed in detail when we discuss loop invariant code motion and this is a very profitable transformation however this becomes incorrect in the presence of ub semantics because once again x plus one can cause ub and in the source program x plus one is executed only if the loop is entered at least once if the loop is not even entered once then x plus one is not even executed and source doesn't do anything but in the target x plus one is always executed irrespective of whether the loop is entered even once or not and so if x was equal to int max then target would exhibit ub whereas the source would conditionally execute ub depending on whether the loop was entered even once or not and so consider the case when x equals int max and loop is never entered then source is basically behaving as though it's a no-op but the target triggers ub and that's that doesn't seem like an incorrect transformation a transformation is correct if the target program behaves identically or i mean the behaviors of the target program are a subset of the behaviors of the source program for all possible inputs but here is an input which is x equals int max and you know some condition such that loop is never entered in this case the source is behaving as a no-op but the target is triggering ub which means anything can happen target is showing a non-deterministic behavior where the source is showing a deterministic behavior and that is not acceptable so basically what we have what we are finding is that if there is ub semantics in the source program or in the source language then that enables optimizations because that kind of gives more space for the compiler to do transformations because the semantics are non-deterministic the compiler can choose any possible implementation for that non-deterministic semantics because it increases transformation space for the compiler on the other hand ub semantics in the target program disable optimization because because they're because of this kind of non-determinism the target you have to be sure that the non-determinism the target can only happen if the equivalent non-determinism was also present in the source and so that requirement actually causes many of our very common transformations that we have been reading about and studying about and invalid and so ub semantics in target actually disable optimization because that decrease the transformation space for example they disallow hoisting of computation so given this fact i mean ideally one would have lied that source is some high level ub like c high level programming language like c which has a high amount of ub so c is called a ub heavy language so because the source would have a high amount of ub the compiler will have a lot of space and the target should be some low level assembly language which does not have any non-determinism or does not have any ub and that would be the ideal scenario for a compiler but what is actually happening in a compiler pipeline for software engineering reasons is that source is also an intermediate representation and the target is also intermediate representation so that as i said most transformations are ir to ir and so the source and the target are actually in the same representation and so whatever ub semantics are present in the source are also present in the target and if that is the case then i have to design the ub semantics very carefully the way i chose the ub semantics for a programming language like c where i just said that the program is illegal if this happens is not a good enough answer for an intermediate representation semantics because you know compiler is not typically transforming c to c but the compiler is transforming transforming ir to ir and so while c can be more kind of non-deterministic in its outlook and say okay if this if this error condition happens then the program is illegal ir cannot be so non-deterministic because as we have seen through examples that level of non-determinism actually causes some transformations to become incorrect some very common and profitable transformations to become incorrect and so that makes the problem interesting for the ir designer because the ir designer now needs to walk a tight rope while deciding what semantics to use for ub for undefined kind of error conditions in the in the intermediate representation if the designer basically disables all ub then we then it also it loses transformations it is if it uses very aggressive ub semantics then also it loses many transformations because now the ub can also show up in the target side and so it means the designer needs to use such a semantics that transformations are still available on source the common ones and and even if the ub is present in target yet some transformations like hoisting are still available and so that's going to be our topic of discussion in our next few lecture modules about what should be the typical semantics of what are the typical semantics of modern irs with respect to undefined behavior
