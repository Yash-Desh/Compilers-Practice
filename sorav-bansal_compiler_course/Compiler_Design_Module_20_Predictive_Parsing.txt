Title: Compiler Design Module 20 : Predictive Parsing
Video ID: oebpjSLx1jo
Detected language: en
------------------------------------------------------------

So far we have learned about the recursive descent passing algorithm and one of the features of the algorithm is that it involves backtracking and backtracking in the worst case can be exponential time. So apart from the fact that recursive descent passing suffers from the problem of left recursion, the other problem that passing algorithm suffers from is the potentially exponential running time of the algorithm. Now ideally we would like to have a passing algorithm that is not exponential but more like linear in the size of the input. So if the program is you know so the time taken by the passing algorithm should be linear in the size of the program and one way to achieve this would be if we could potentially eliminate backtracking in the algorithm. So recall that backtracking comes into play when we have to choose between multiple productions. For example if we are choosing between S1, S2, S3 dot dot dot SN we will try each of them. Now what if we could predict which one of these we should pick maybe we have to pick S10 out of all these n different productions and to be able to predict we are allowed to look ahead in the input. So far we basically do not look ahead in the input we just try different possibilities but what if we are able to kind of we start looking at the current token under the next pointer and based on that we are able to uniquely identify which production is going to be activated because of this rule. Now not all grammars are amenable to this but for the grammars that can be passed using such prediction based linear time parsing algorithms we should be able to do better than what we are doing right now. So the idea is to be able to predict based on some look ahead right and these class of algorithms for parsing are also called predictive parsing algorithms. These algorithms avoid backtracking and they do so by predicting the correct production whenever there is a choice and they can do this they can do this prediction potentially by looking at the next few input tokens. So you can you can say that your algorithm is predictive and it requires the look ahead of k tokens and so which means that the algorithm is allowed to look ahead for another k tokens and based on that it's able to predict which production to use. So before we talk about predictive parsing let's make some observations on recursive descent parsing. Recall that recursive descent parsing reads tokens left to right the next pointer is incrementing left to right and it attempts to create a leftmost derivation. Why do I say that it attempts to create a leftmost derivation? Well at any point of time for example we start with s and then we you know maybe we derive e dollar and then we derive t plus e dollar and so on and and then we derive int plus e dollar and so on. Notice that in this derivation we are at every point trying to first derive the leftmost non-terminal and once we are completely done with the leftmost non-terminal then we move forward to the next leftmost non-terminal and so on and thus we are trying to derive a leftmost we are attempting to create a leftmost derivation. This is also the nature of the algorithm if you recall we are basically moving left to right in trying to scan the input. Even though we can create function objects and chain them in the reverse order from right to left the actual execution proceeds left to right and so we first try to resolve the first leftmost token and after we have done that leftmost non-terminal and after we've done that we move to the next leftmost token and this is exactly what a leftmost derivation would look like. Now predictive parsing for recursive descent you know takes us a step further and it says we are going from left to right and we are trying to always identify what is the derivation for the leftmost non-terminal and if I have a choice for which derivation to use for the leftmost non-terminal I'm going to use the one that I'm going to predict which one to use for that all right and so and I'm going to predict based on the next k tokens and such an algorithm such a so such an algorithm that involves recursive descent parsing using a look ahead of k tokens without that tracking is called an llk parsing algorithm okay typically we are interested in k equals one one you know that is that is the more most interesting case and most useful case but you know it is easy to also extend and generalize this algorithm to k greater than one as well. So not all grammars can be parsed using an llk algorithm because it may be possible that using by just looking at the next k tokens it is not possible to decisively disambiguate between which production should be used to for the next derivation and in that case the grammar would not be parsable using an llk algorithm this is unlike the original recursive descent algorithm which would be able to parse any grammar which didn't have left recursion because it involved backtracking here we are using predictive parsing and we want to be able to save just by looking at the next k tokens which rule to use and this may be possible for some grammar and may not be possible for some other grammars so for example if i say what is the grammar requirement for an ll1 parser which means i'm k equals one which means i'm just looking at the token pointed to by the current next pointer and based on that i should be able to clearly make a decision which production to use so looking ahead at the next one token at each step there should be at most one choice for a possible production so let's say at any step of my derivation i've already derived some sequence of terminals and non-terminals in this case this is wab wabeta and w in this case because it's a leftmost derivation is only terminals and beta could be both non-terminals and terminals and thus a is the leftmost non-terminal and we are interested in figuring out which derivation to use if based on if the next input token is let's say t in the input stream this is the terminal t if the next input token is t then if i can clearly say that oh there's only one choice for a and that is a derives alpha and thus wabeta derives w alpha beta then or or maybe there is none so it's possible that you know if i am in this state and the next input token after w is t and but there is no possibility for a to derive anything for t in which case it basically means that the the input stream of tokens is not parsable using this grammar does not belong to the language represented by this grammar so in either of these cases if if if these are the only two choices either there's a unique production that can match based on the look ahead t a look ahead of one which shows t and or there's none that matches then the grammar for all possible strings then the grammar is ll1 parsable grammar or for short just short ll1 grammar okay there are some grammars which may have multiple choices so if if i'm in the state wabeta and my next token input token under the next pointer is t then there are multiple productions that may be used in which case the grammar is not ll1 okay so let's consider our example that we have been using to discuss parsing this is the grammar of expressions and let's try to understand if this is an ll1 grammar or not so e produces either t plus e or t t produces int int star t or parenthesize now notice that t can produce either int or int star t and if i have to you know if i'm ever in the state where i have w t beta such that the next token is an integer then i don't know which production to use because i could have either used w t to int or t to int star t these are both possibilities okay and i don't know which one to use because both are possibilities and and so this grammar is actually not ll1 parsable all right or just this grammar is not ll1 so cannot choose between the first two productions of t based on one look ahead because they both have the same token in fact there's another problem with e because both productions of e start with the same non-terminal t and the same non-terminal t can produce the same first token and so it's not clear it can't be clear by if if i'm trying to produce from e which one i should use just by looking at one look ahead so not only is there a problem with productions of t there's also issues with the production of t so we won't be able to clearly disambiguate which production to use just by one look ahead in this example either
