Title: Compiler Design Module 128 : Introduction to the Polyhedral Framework
Video ID: BKcFoP6B4Rw
Detected language: en
------------------------------------------------------------

We have seen matrix multiplication and how its performance can be improved by improving the locality and also we talked about parallelism a little bit. In the next few modules, we are going to focus on affine loop transformations. So what do I mean? Well, we will focus on numerical applications that use arrays as data structures and which have simple regular access patterns on those arrays. So what do I mean by simple regular access patterns? Well, we will assume affine array accesses with respect to surrounding loop indices. So if the loop is iterating from let's say 1 to n, then the access pattern on the array is an affine expression on i which is the iteration variable. And there could be multiple, there could be, this is just one loop, there could be a loop nest of depth 2 or a loop nest of depth 3 and so on and we are going to see some examples. So notice that the key operating word here is affine array accesses and before we talk about this any further, we need to define what's an affine function and affine function on variables x1, x2, xn can be expressed as summation over i ci xi where i goes from 1 to n plus c0, where ci's and c0 are constants, numeric constants. So for example, 2 into x plus 1 is an affine expression on x and when I say affine array accesses with respect to surrounding loop indices, the array accesses can be expressed as affine functions that means this particular form ci xi summation plus c0 of the surrounding loop indices. What are surrounding loop indices? The indices of the loops, the iteration variables of the loops that surround that particular statement. So for example if it was a loop of, a single loop then it's just one variable, one surrounding loop index variable i. If there was a loop nest of depth 2 then there are two index variables, one let's say i and another is let's say j and so on. So here's a simple example, here is a single loop, here's an array a of size let's say 200 and then I have a loop which says for int i equals 0 to 100 i plus plus a of 2 into i equals 4. Now this is an affine array access because the access, the address of the array a of 2 into i is basically I can write this as the address of a which is let's say plus 2 into i into size of whatever is the int. So this whole expression a has a constant address because it's a global variable and size of int is let's say 4 on your machine architecture and so this becomes constant plus 8 into i and that's an affine expression. So overall the array address a of a 2 into i is an affine function of the loop iteration variable, the surrounding loop iteration variable i. Now let's look at the advantage that we have by using such specialized analyses for affine array accesses. First of all I would like to point out that for several numerical applications such as in the domains of image processing, graphics, also recently machine learning it is common to have such patterns where the accesses are affine addresses of the surrounding loop iteration variables or these are also called affine loops and also the loops the iteration variables themselves have a very regular pattern and have a lower and upper bound. And because these are common it makes sense to optimize, have special optimizations for such patterns and what are some optimizations that such patterns enable? Well if you look at this particular loop we can see that all these accesses a of 2 into a the 2 into i-th element of a are all independent so the first element would be a of 0 that will be accessed in the first iteration. In the second iteration it will be a of 2, in the third iteration it will be a of 4 and so on and so one easy analysis that we can do just by looking at this code is that different iterations access different regions of memory or different elements of the memory and so it is possible to perform each iteration in parallel with other iterations because they do not interfere, they do not have any data dependency, they do not touch any common piece of memory. So however let us say if I also had another variable j in this and let us say j was not a loop iteration variable, not a regular loop iteration variable, j was just some other variable and I had this particular statement in my loop aj equals 0. Now suddenly we cannot perform each iteration in parallel with other iterations because we do not know whether j matches with any of the iterations 2 into i and so it is no longer possible to compute each iteration, parallelize each iteration of this loop separately because we do not know if j may be equal to 2 into i for some i and so notice that this is no longer an affine loop nest because j is no longer, I mean aj the address access to aj is no longer an affine expression on the surrounding loop variable because j is not a surrounding loop iteration variable. So the other point here is that if for such affine loop nests the types of transformations that are possible are much richer such as parallelization of the loop iterations. Here is another example, let us say I have a of 2 into i equals a of 2 into i minus 1 star 3 percentage 31, now this is just some made up example but I want to point out that it is not necessary that if all accesses are affine accesses it is always possible to parallelize the iterations of a loop. Notice that in this particular loop all array accesses are actually affine expressions, the addresses are affine expressions of the surrounding loop variables whether you take a of 2 into i or a 2 into i minus 1 these are all affine expressions on i which is where i is the surrounding loop iteration variable. However there is a data dependency across iterations so the element 2 into i, index 2 into i minus 1 will be computed in a previous iteration let us say and or maybe just to make this more clear let us say this was 2 into i minus 2. So in a previous iteration I have computed a of 2 into i minus 2 and this here I am computing 2 into i and there is a data dependency and now it is no longer possible to execute these iterations in parallel. So even though all accesses here are affine accesses inside the loop it is still not possible to execute these iterations in parallel and we would like to develop an analysis that is able to identify when is it possible to execute the iterations in parallel and by how much amount and when it is not and so that is the kind of analysis that we want to develop a because such affine loopness are common and b the types of transformations that such affine loopness enable are actually very rich and very consequential. Okay so to be able to do this we are going to develop a set of algorithms that is commonly called the polyhedral framework. The basic idea here is that we use linear algebra and integer linear programming techniques to reason about these things about inter iteration dependencies and interactions and we try to optimize for parallelism and locality similar to how we try to optimize for parallelism and locality when we were discussing the matrix multiplication example. The assumptions we will make while we are in this polyhedral framework of reasoning and this will this is basically defining the scope of program that we would be trying to optimize based on this polyhedral framework is that all loop iteration variables have a lower and upper bound so all loop iteration variables iterate from some lower bound to some upper bound so you know and we know what those bounds are and the other interesting assumption that we make is that a loop iteration variable increments by one in each iteration so for example it goes from 10 to 20 and increments by one in the every iteration now this may seem like a limiting assumption why do I want the loop iteration variable to increment by one why can't it increment by two or three or four the first the important the important implication here is that it must increment by a constant step okay and if it increments by constant step then it fits in this framework and if it increments by a constant step then it is always possible so if the variable increments by a value which is constant but not equal to one then we can introduce another iteration variable that increments by one and express the original iteration variable in terms of the new variable so let's say there was an iteration variable that was incrementing by two on every iteration let's say this variable was I then what I can do is I can introduce a new variable I prime that only increments by one on every iteration and then just say I equals two into I prime right or express I as in a fine expression of I prime so it's this is called normalization of the iteration variables and I'm going to bring my loops in this normalized form in this canonical form where every loop iteration variable increments by exactly one in each iteration because that just makes the rest of the analysis easier and this can be thought of just as a pre-processing step if it does not increment by one but increments by a constant value we can always you know introduce a new variable so that that variable increments by one and the original variable is written in terms of the new variable here is an example of an affine loop nest affine loop code with the loop nest depth of two so far we have looked at single loops but let's look at this loop for I equals zero I is less than equal to five I plus plus for J equals I now notice that the lower bound need not be a constant it can be a fine function of the surrounding loop variable so here it J is starting from I J is less than 7 once again less than equal to seven and once again the upper bound need not be a constant in this example it is a constant but it could have been some affine function of I J plus plus and then I say Z of J comma I equals zero now there are two things here first of all this expression this array access Z of J comma I is actually an affine address with respect to the surrounding loop variables what are the surrounding loop variables I and J now let's look at the iteration space let's look at what is the iteration space I iterates from zero to five and J iterates from one to seven so I can now represent this iteration space of the loop iteration variables as a graph and here I represent I on the y-axis and J on the x-axis and these are all the points so starting from zero so this is zero and this is five this is seven and this is five all right and so if I draw this shaded region represents all the loop iteration values all the values of the loop iteration indices that this loop is going to go through so for example I iterates from zero to five so this is zero one two three four five and then for every value of I J iterates from I to seven so for I equals zero J iterates from zero to seven so these are all the points zero one two three four five six all the way to seven for I equals zero similarly I equals one it goes from one to seven so one two three four five six seven two three four five six seven three four five six seven and so on and so this this space of I this space are formed by the iteration and by the way it stops at I equals five because that's the upper bound of I now this space of iterations that is shown with this shaded region is is formed by the all is basically representing all the possible values that the tuple I comma J can take right for example I comma J can take the value two comma two but it cannot take the value for example three comma two right because that's outside this space the other interesting thing to note here is that this particular space is actually bounded by straight lines and whatever is the region that is demarcated by these by the by these straight lines by some direction of the straight lines is basically this region and in fact it can be shown that this is always going to be a convex region what do I mean by a convex region the convex region is a region where if you draw a line between any two points in that region then all points in those that line belong to that region so for example this red line here between this point and this point all these points basically belong to this shaded region right that's what a convex region is for example this is not a convex region okay because between this point and this point if I draw a line then there are some points that lie outside the convex region but this particular region is a convex region because irrespective of what points I choose if I draw a line between those two points those points are guaranteed to lie within the shaded region between the iteration space region and so this is actually going to be a property just by the scope of the program that we are looking at which is that everything has an upper bound and a lower bound and increments by one we are going to always get a convex polyhedron and in this case it's a convex polygon in 2d space or convex polyhedron in 2d space a convex polygon an interval in a 1d space is also a convex polygon and in general the space of loop iteration variables for the scope of programs that we are going to study in the polyhedral framework can be represented always as a convex polyhedron in a d-dimensional space alright so we just saw an example of a two-dimensional space but I can now generalize it to a deep dimensional space for example for a 3d dimensional space there will be a 3d pi-polyhedron for a four-dimensional space will be a 4d polyhedron and so on where d is the loopness depth and the number of loop iteration variables so notice that in the previous slide the loopness depth was 2 and so we got a two-dimensional convex polyhedron if it's 3 then we get a three-dimensional polyhedron in general if we have a d-dimensional depth loop nest then we get a d-dimensional space of iterations and where each iteration which each dimension represents one iteration variable corresponding to that particular line or loop in the entire loop nest so in this representation of the loop iteration space every iteration of the loop nest is basically represented by a point in the polyhedral space alright so in other words there's a one-to-one mapping between loop iterations and points in the polyhedral space so we are going to represent the space of loop iterations as these convex polyhedra because that just makes it easier for us to reason about all the kind of dependencies and parallelism and locality opportunities that may be possible and this is also the reason why this is called the polyhedral framework there are two categories of transformations that we are going to study in the polyhedral framework one involves a fine partitioning which means you split up the polyhedron which is in other words I split up the iteration space into components that can be executed in parallel or in general reordered with each other so now that I have basically been able to represent the iterations of my loop as points in a polyhedron now I would want to do some kind of analysis to figure out which of these points can be reordered with which of the other points in other words I may want to basically partition these points into different sets such that it is okay to execute each set in parallel with the other set so maybe within the set I need to follow a certain order but every set can be executed in parallel with any other set more generally any set can be reordered with respect to any other set if it can be reordered then it can also be executed in parallel because the relative order of execution would not matter so for example if this was my loop iteration poly if this was my loop iteration space which is this let's say this rectangle this was representing my convex polyhedron then maybe my partition is going to be these diagonal lines and notice that each of these lines basically has two may have one or more points for example this line has one two three one two three four five points let's say and so this these lines are basically representing partitions so in other words these iterations can be executed in parallel with the iterations on the second line and similarly with the third line and fourth line and so I can divide these iteration space into partitions I can partition this iteration space such that each partition can be execute needs to be executed serially but across partitions I can execute the code in parallel and so this kind of approach is called a fine partitioning and we would want to know do some kind of data dependence analysis to figure out how we can partition an iteration space such that all the data dependencies are still preserved recall that we saw some examples where the loop could be parallelized and some other examples where the loop could not be parallelized and the reason the difference between the two was the nature of data dependencies and sometimes the loop may be parallelized only up to a certain degree for example it was a two loop nest depth but maybe you can only parallelize one of the loop bodies and even there we need to so here the loop may be from for i equals zero to ten for j equals zero to five and that gives you this rectangle as a convex polyhedron but the data dependency the body of the loop basically determines some kind of data dependencies which says oh you cannot you can neither parallelize on i you can nor you can parallelize on j but it is maybe possible to parallelize on i minus j so along this this plane of path it is possible to parallelize it all right we're going to see this in more detail but basically the partition of this iteration space such that within a partition there may be data dependencies but across partition there are no data dependencies and if we partition it in such a way then each partition can be executed in parallel with every other partition and we can achieve better parallelism the other category of transformation that we're going to study is blocking and this is something that we've already been introduced to with the example of matrix multiplication but matrix multiplication was a very special example and we were just studying that in isolation but in general for any loop nest that has a fine accesses recall that matrix multiplication in an example of a loop nest that has only a fine accesses and so it is in a fine loop nest and fits in our polyhedral framework but we would like to generalize that analysis to blocking which basically is able to block any loop nest that has a potential of improving locality due to blocking and so what is blocking blocking creates a hierarchy of iterations recall that we basically from a three loop nest we went to a six loop nest where the upper hierarchy was basically you know iterating with steps of d and the inner hierarchy of the inner three loop nest was doing a block multiply where the iteration step was only one so it creates a hierarchy of iterations and in general given any loop nest it may or may not be a matrix multiplication loop nest how do i figure out whether it is possible to create such a hierarchy of iteration space or iterations such that such that we can exploit the cache locality in a better way and so that both affine partitioning and blocking are categories of transformations that the polyhedral framework will enable basic idea behind polyhedral transformations well the basic idea is that once we have represented the iteration space as convex polyhedrons or convex polyhedra then we're going to use linear algebra techniques to help determine the best affine partition and best blocking strategy okay so for example here is a loop for i equals zero to n i plus plus z i equals x i into two and here our analysis should be able to partition the address space so firstly what is the iteration space the iteration space is it just let's say this was i now the iteration space is zero one dot dot dot n okay and how do how can i partition the address space such that each of the partitions can be executed in parallel well in this case it turns out that these are these have no data dependencies across iterations because they are all accessing different different elements and so the partition is just each partition is basically a singleton element zero zero is a single partition one is another partition two is another partition and so each of these iterations can be executed in parallel in general this is a this is a way to partition this and execute this code in parallel let's say i have m processors then i'm going to define b equals n by m ceiling and so the idea is that i'm going to iterate for you know i'm going to have b iterations in every processor right i have m processors i have n jobs to be done so how many how many jobs do i do on every processor i do n by m jobs and then maybe i take a ceiling because i need an integral number of jobs so for i equals b into p so b p is basically the processor number which ranges between zero and m so i said b into p i is less than minimum i is less than let's say b into p plus one so i want to go from b into p to b into p plus one with the added quirk that the last processor should only it should not go beyond n if n is not a multiple of m so just to take care of that i'm saying minimum of n comma b into p plus one i plus plus and the body remains the same z i equals x i into two so notice that the code is pretty much the same it's just the upper bound and the lower bound that have changed i am now iterating from b into p to b into p plus one and barring the last iteration last processor on every processor and every processor is basically so i partition the space into p partition i partition the iteration space into n partitions and each partition has a label called p and depending on what label it is i execute the corresponding code this kind of a parallelism is also called spmd parallelism single process single processor single process multiple data so basically you have the same logic that is executing on different regions of data for example the first process or the first cpu or the first processor is going to execute the same logic for elements zero to m zero to b right and the second one is going to execute from b to 2b the third one is going to execute from 2b to 3b and so on depending on what what the value of parameter p is i'm going to do the same logic for different regions of data and so in this case there's the same code that is parameterized by p and depending on p i basically use different data and that's why it's called an spmd program also when we do this kind of parallelism after all the processors are done we need barrier synchronization so what is barrier synchronization after everybody is done we wait for everybody to complete before we move on to the next part of the program because we want i mean if the next part of the program depends on all the values in z to be filled in then i need a barrier synchronization and recall that a barrier synchronization basically involves waiting for all the processors to finish before moving before any of the processor moves
