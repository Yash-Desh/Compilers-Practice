Title: Compiler Design Module 102 : DFA Iteration Algorithm Properties
Video ID: yf0n_ZJpmbQ
Detected language: en
------------------------------------------------------------

So we have talked about a DFA framework and its properties that need to be true for the fixed point iteration algorithm to converge. So given a data flow framework which is specified with F, V and the meat operator which is monotone and the semi-lattice formed by V and the meat operator has finite height, then the fixed point iteration is guaranteed to converge. So we have been stating this result or this theorem but today we are going to discuss this in more detail and also share a proof sketch for this particular theorem. Okay and moreover we say that it's not just true that it converges but if we initialize all the interior points to top, so all the interior points are points that are not part of the boundary condition, then the algorithm is the maximum fixed point solution of the DFA equation. So I'm using this term maximum fixed point solution of the DFA equations. Recall that data flow analysis is basically specified as a system of equations. We are interested in finding a solution that satisfies that system of equations and the fixed point algorithm is one way of identifying such a solution and we are not just interested in any solution, we are interested in the most precise solution and one way to say that is that we are interested in the maximum such solution where maximum is defined with respect to the ordering operator less than equal to of the semi-lattice. In other words we are interested in the maximum fixed point. So what is a maximum fixed point? Let's try to understand that in a little bit more detail. Recall that any solution will have a value associated with each program point. So for example the value at the loop head is going to be something, the value at the loop tail is going to be something and at every basic block in the beginning and the end there'll be some value. An MFP solution, the maximum fixed point solution or MFP solution has the maximum value at each program point such that these values satisfy the equations. So if I look at all the possible solutions that satisfy these equations, there will be one solution where all the values are maximum across at all the program points compared to all the other solutions and we can argue that such a solution will exist and that solution is called the maximum fixed point solution. And where maximum here is based on the less than equal to operator of the semi-lattice. So let's look at an example to understand MFP in more detail. So let's look at the constant propagation example and once again the lattice is formed by top constant value which could be anything and bottom. And then let's look at this particular program. Let's say I initialize x to 3 and then I say y equals z plus w some random stuff and then there's a loop here. Now this solution here which is associating empty set at the beginning of the program x equals 3 at and all the other points of this program is the MFP solution I claim. Here is another solution that would also satisfy the dataflow equations and that solution is basically giving me x equals 3 at this particular point but it's giving me the empty set at all the other locations. Now I'm claiming that both these solutions are satisfying the dataflow equations. So maybe we want to check and so we can see that across in and out this is phi and this is x equals 3 and if I use the gen and kill form of constant propagation transfer functions then the value at the end of this trans statement includes this extra generated mapping from x to 3 and that's true for both the solutions. Now let's look at this particular point at the loop head. Here it needs to be the meat of the values coming from the initialization x equals 3 and the loop tail and in the first case the loop tail is x equals 3 so the loop head is still x equals 3 so everything is good and so the first solution satisfies the equations. Let's look at the second solution which is empty set and empty set. So the first at the loop head I say that it should be a meat of x equals 3 and the loop tail value which is the empty set. So what is the meat of empty set with x equals 3? It's the empty set and so even the second solution actually satisfies the dataflow equation. So both these solutions actually satisfy the dataflow equations but the first solution is the maximum such solution and that's why it's called the MFP. The second solution is a fixed point because it satisfies all the equations so we just call it the FP solution but not the MFP solution. It's a fixed point solution but not MFP. So now what is the explanation that the fixed point iteration algorithm is guaranteed to converge? So here is the rough explanation that value at any program point can only decrease because meat operator just decreases that value so if there's some value somewhere then it can only go down and from there and because we start with all the unknown values at the interior points with top we should hopefully get to the maximum fixed point because we are decreasing only by the amount that is required. We're going to discuss this in more detail very soon. So why are values only decreasing? Because when we meet we are only assuming that the incoming values have decreased the meat value will also decrease and because of monotonicity if the value at the beginning of a basic block for a forward DFA has decreased then the beginning the value at the end of the basic block will also decrease and that's coming from the monotonicity property of the transfer function and so these properties will provide convergence and we're going to discuss this in a little bit more detail. The other question is how do we say that this is the MFP and so for that we basically use this argument that values do not come down unless some constraints from the equations drive them down all right and so the values are brought down by the minimum possible amount such that they start satisfying the constraints and that is our rough argument for the maximum fixed point solution why is it maximum okay so let's try to prove this and I'm going to give show you a proof sketch based on induction and so first we want to prove that the values at the k plus 1th iteration of the fixed point solution are no greater than the values at the kth iteration by no greater I mean they're less than or equal to so the values at the k plus 1th iteration are less than or equal to the values at the kth iteration this is what we want to prove and I'm going to use mathematical induction to try and prove this so here's the base case the base case we want to prove that the values after the first iteration are no greater than the initial values now this is easy because the initial values are just the top value and so this is trivial actually because all initial non-boundary values are top and all the boundary values are anyways fixed so anything any other set of values will always be less than equal to the initial values because that's how we are initializing it so our base case is trivial and that's just coming from our initial values which are set to top for all interior points now here is our induction hypothesis and induction step for mathematical induction so induction hypothesis let's assume that after the kth iteration the values are no greater than those after the k minus 1th iteration so if the values at kth iteration are less than equal to the values at k minus 1th iteration then we want to prove that the values at k plus 1th iteration are also less than equal to the values at the kth iteration in other words if this is the k minus 1 values and if I am able to say that v k minus 1 is less than equal sorry I want to say let's say I've been able to say that vk is less than equal to vk minus 1 and this forms our induction hypothesis right so this forms our induction hypothesis and what is our induction step our induction step is vk plus 1 is less than equal to vk so assuming the induction hypothesis we are interested in proving the induction step okay so how are we going to do this so let's assume forward DFA for this discussion we could have done the same thing with the backward DFA without loss of generality so in the forward DFA we have this let's say we have some basic block and we have multiple incoming predecessors and the value being computed is at the beginning of this basic block that's called in of s right where s stands for some statement or basic block and then we say that in of s equals meet over out of p where p belongs to the set of predecessors of s so we look look we meet the out values of all the predecessors to obtain in s now we know this data flow equation and in our fixed point iteration what do we do we basically keep iterating so at the ith iteration we take the values at the i the out values of the i minus 1th iteration to arrive at the values of the in values of the ith iteration so let so to capture that let in s superscript i and out s i denote the values of in s and out s after the ith iteration for any s including p and everything else right so let in s i and out s i denote the values of in s and out s after the ith iteration just after the ith iteration then we can write this particular equation as the following in s k plus one equals meet of out of p of k so the values at the end of the kth iteration are you know met with each other to produce the value of in s at the k plus one iteration now you know you may say oh why can't i just update so i'm assuming here that you know at one step we just taking an original snapshot and using that to update get the next snapshot in our fixed point iteration algorithm of course when we use the work list algorithm and things like that you know we need to have more nuanced arguments on why this is true but let's just take the simple algorithm where you know we just in one snapshot we just propagate the old values into and meet them into the new value and so this is what our algorithm is going to look like in that case now from here we can show that in of s k plus one is going to be less than equal to in of s of k so how i'm going to use mathematical induction we know that out of p of k is less than equal to out of p at k minus one and this is coming from our induction hypothesis because we said that values at iteration k are less than equal to the values at iteration k minus one so that also means that the out value at any predecessor p at the iteration number k is also less than equal to the same value at the iteration k minus one thus if i meet over all the out p all the meet over all the p's for out p at iteration k that's going to be less than equal to meet over all the p's at iteration k minus one or for out p and that and notice that this is just in of s k plus one and this is just in of s k and so from there we get in of s k plus one is less than equal to in of s k so we have been able to use the induction hypothesis to show that the in values for any basic log at the k plus one iteration will always be less than equal to the in values of the same basic log at the kth iteration now we need to show the same thing for the out values right though that's the only remaining part of the proof and that's also easy notice that out of s k plus one is nothing but transfer function applied to in of s k plus one and we know that in of s k plus one is less than equal to in of s k we just proved it on the previous slide so from the monotonicity property of the transfer function we know that because in of s k plus one is less than equal to in of s k we will know that we know that out s k plus one should also be less than equal to out of s k because f s is monotonic it maps smaller inputs to smaller outputs okay so that's our proof sketch and based on that we have been able to show that the values in the dataflow analysis can only decrease assuming that the framework is monotonic and assuming that the framework is monotonic all right now assume if the values can only decrease then if the semi lattice has finite height then after all at some point they will stop decreasing because at some and in the worst case you're going to all the values are going to reach the minimum value which is the bottom value and so from that we basically can prove that the algorithm will definitely converge or terminate hence we have been able to prove that you know that the algorithm terminates now the second part of the claim which we want to prove is that the iterative algorithm yields the maximum fixed point now this it converges all right but what is the proof that it yields the maximum fixed point now there are two parts to this first of all we need to show that it actually yields a fixed point and that will be easy to show because you know after all it you know we only stop when all the equations are satisfied that means it is a fixed point and the other part is that it is the maximum such fixed point all right and and so here is the proof sketch the algorithm terminates only if all the equations are satisfied and that basically tells us that whatever solution we get at the end of the algorithm when the algorithm has actually terminated it's got it has to be a fixed point because that's the only condition under which the algorithm terminates and second if an equation is not satisfied at any step the value is driven down by only that amount such that so that the equations now becomes satisfied so we basically take the smallest possible step so that the equation becomes satisfied and so from that we know that you know we're going to stop only when you know only after we have we would have taken the smallest amount of number of steps and amount of step gone down in the solution by the smallest amount at every step and so even in the whole we're going to have we would have gotten down by the smallest amount and so that's the argument for the maximum fixed point now this is just a proof sketch more formal proof you can will always will also have more formal details and sketch but that's basically than the sketch but that's that's that's beyond the scope of our discussion
