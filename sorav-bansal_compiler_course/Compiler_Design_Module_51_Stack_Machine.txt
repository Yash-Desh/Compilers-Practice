Title: Compiler Design Module 51 : Stack Machine
Video ID: eYk0EennUrA
Detected language: en
------------------------------------------------------------

So we are now going to talk about an example of a machine architecture and see how we could potentially generate code for that particular machine architecture. So we are going to look at a very simple machine and we also call it a stack machine and this particular machine the only storage is a stack. Now this is different from you know the typical machine that we are familiar with which has resistors and memory and things. In this particular machine let's assume that there's only one way of doing storage and that's a stack. And the instructions are of the form result equals some operation f applied to some number of arguments a1 dot dot an. For example the operation could be plus and the arguments could be you know any integers four byte integers and the result is also supposed to be a four byte integer. Now what such an operation is going to do for a stack machine so this is the instruction set of this particular machine it's going to pop the top n words of the stack apply f on these popped words and then push the result on the stack right. So we're just assuming that this there is such a machine that has stack as the only storage we're also assuming that it has an instruction set and the instruction any instruction in this instruction set looks has this particular form and has this particular semantics or meaning. So here are some example instructions for example an instruction like push i. So let's say this is a stack and by convention we're going to assume that the stack grows downwards so when you have an instruction like push i is just going to push the argument i which is a constant which is supposed to be a constant here and it's just going to push that constant to the top of the stack. Now similarly there could be an output called add and so add takes two operands so if there were two operands at the top of the stack let's say x and y then it's going to pop off the top two operands so it's going to remove x and y from the stack and then instead push x plus y so it's going to compute the addition and then push the result on the stack. So this is the operation of this machine. Here is an example computation so let's say I wanted to compute three plus four on this particular stack machine then I'm going to first push three by dollar I mean it's a constant three so I'm going to push the immediate value constant value three I'm going to push the constant value four and then I'm going to execute the add output that output is going to pop off the top two words in which case which in this case are going to be three and four and is going to push the sum of these two quantities which in this case would be seven and the I mean assuming here that the programmer should expect the result of this program at the top of the stack and that makes sense because the only storage is a stack so you know we at the end of a program we expect the result to be at the top of the stack now before we go further let's just compare such a stack machine to a typical register machine a register machine has explicit operands for example if I want to say add I just don't say add I say add what so I say add r1 r2 r3 maybe this means that you're supposed to add r2 and r3 and put the sum in r1 I could use different registers name for example I could say r1 r5 r7 and then that's going to do you know name different registers on the other hand on a stack machine we just have this opcode called add and the operands are implicit because the operands are just the top two words of the stack and so we don't have to explicitly name them now they're both advantages and disadvantages of a stack machine over a register machine one of the big advantages is that the instruction opcodes are much smaller or the instructions themselves take smaller amount of space as you can see we just need to have the opcode and because the operands are implicit we don't really need to know the instruction size itself is going to be typically smaller well you could argue although the instruction size is smaller you may need more number of instructions to perform the same number of operations but overall you know it's for typical type of programs it is usually considered that it is possible to implement them using shorter code size on stack machines than on register machines in fact this was one of the reasons why the java virtual machine bite code so the java virtual machine is also a machine abstraction and that was designed as a stack machine because originally java was supposed to be a language that allows code mobility and so code was supposed to be shipped over the network and network bandwidths were low and so a lot of stress was given on the size of the code and so one of the reasons that they chose a stack machine for the java virtual machine was because of this reason the disadvantage of using a stack machine is sometimes the number of instructions that you require to do the same amount of computation can be more and which means the program can be slower for example but then you know it also depends on what kind of program it is what the application that we are interested in developing and it's not necessary that one is going to be faster or slower or one is going to be bigger or shorter than the other it is the the answer is more nuanced and depends on the application now there's a generalization of a stack machine it's called an n register stack machine conceptually the idea is you let's keep the top n stack locations in registers and you know the thought behind using an n register generalizing the stack machine to an n register stack machine is that there's some values that access very frequently and so we want to avoid memory accesses and and so let's keep some values in registers and and you know maybe we want to keep the top n values in stack locations in registers because that's going to make our you know code generation strategy simpler as we're going to see with some examples and a special case of an n register stack machine is a one register stack machine where there's only one register and a stack and this unique register is also called an accumulator in such a machine okay so what is the advantage of one register stack machine we can look we can understand this using this example so in a pure stack machine if i wanted to do an addition then i would use the add-op code and it's going to do three operations it's going to pop the top word then pop the second top word so it's going to do two pops so it's going to say t1 equals pop and t2 equals t2 is also assigned pop and then it's going to push t1 plus t2 right so you this has three memory accesses two because of the pops and one because of the push on the other hand in a one register stack machine the ad would have different semantics notice that we're assuming that the top one location of the stack is already in the register or which we also call in the accumulator and i'm using the acc abbreviation to represent the accumulator and there are semantics of the add-op code are accumulator is assigned accumulator plus stack top all right so it has a very different semantics first of all it is not it is adding value it is storing the result in the accumulator itself it's not actually storing the result on the top of the stack secondly it's reading one of the operands from the accumulator itself so it doesn't need to make a memory access for that and third it's actually not changing the size of the stack right so it's just that this particular machine has different semantics for its opcode and in this particular case i'm assuming that this is the semantics of the add-op code the accumulator is assigned accumulator plus stack top notice that it has only one memory access and you can see that because of adding one register i've saved two memory accesses for this simple operation and so one can imagine that you know adding registers is going to improve performance typically because memory accesses are more expensive than register accesses typically speaking all right so how would you uh you know operate a one register stack machine so let's say you wanted to compute an expression op applied to an argument even dot dot en you know for example op the operation could be plus and then even dot en could be integer constants now here is one strategy right so this is just one of the potential strategies but it's it's a it's one of the most it's simplest and probably the easiest to understand and also you know the natural strategy for this kind of architecture for each ei even dot en so but for i from one to n for each ei computed in the accumulator so first we compute let's say even and we compute it in the accumulator then we compute e2 computed in the accumulator but then by the time we compute e2 we don't want we want to also preserve the value that was computed for e1 and so before we compute e2 we push the value of e1 right so the other the other part of the strategy is you push even dot dot en minus one to stack as they are computed okay so i computed even in the accumulator but then before i start computing e2 i push even to the stack pushing into the stack basically means i sort of saved that value because i may need it later to actually compute my operation right after i computed all the operands and then even you know e2 is going to do override clobber my accumulator but hopefully it's not going to override the top of the stack if it needs some storage space on the stack maybe it's going to push on the stack but never override the value that i save for even and then i eventually evaluate the operation and store the result in the resistor the accumulator and finally when i am done i want to pop off the top n minus one words from the stack so that the stack remain stack looks exactly as it was before this computation started okay so here's an example let's say i wanted to compute seven plus five then i would say push seven that's going to push the constant seven put five into the accumulator add and that's going to execute this particular operation accumulator will get seven plus five and one thing that's missing is that i may want to pop all right so basically clear the extra space that i had used on the stack for saving seven all right so here's the invariant that i want to follow after evaluating an expression e the accumulator holds the value of e right so that's the invariant that i'm using in my code generation strategy for example when i compute even i assume that after even has been computed the value is the computed value is stored in the accumulator the accumulator holds the value of e and the other invariant that i'm following is that the stack is unchanged so the stack is unchanged so if i start computing e whatever was the value before the starting of computation of e is also the same contents of the stack at the end of computation of e and that's something that i'm going to maintain as my invariant during this code generation strategy so in other words expression evaluation will preserve the stack in our strategy so for example if i wanted to compute three plus seven plus five then i would first compute three and put put it in the accumulator then i would save this value from the accumulator to the stack and then i would go on and try to compute seven plus five now notice that seven plus five is going to also maybe push and pop from the stack and it's going to clobber the accumulator but because i have the invariant that's the evaluation of seven plus five will not change the stack as it existed before the start of this evaluation the value three would still remain intact in the stack so here are the steps that would be involved in kind of generating the code or this is what the generated code would look like it would first evaluate three in the accumulator save this value three to the stack evaluate seven in the accumulator save the value seven to the stack by pushing nine not overwriting evaluate five and then now we are kind of ending our you know now we are ready to do the first addition which is the seven plus five so i'm going to say add that's going to put 12 in the accumulator and then i'm going to pop stack that's going to remove seven from the stack and then finally i'm going to compute three plus whatever was the result of seven plus five so i'm going to say the result of seven plus five is already in the accumulator three is on the stack so i'm going to execute add and that's going to put three plus twelve seven plus five into the accumulator and then i want to pop the stack to basically get rid of the save three value which i no longer need okay notice that steps three to six are basically computing seven plus five and steps one two and seven are basically computing the top level expression top level operation of addition so this just represents a code generation strategy and there could be different code generation strategies here in this strategy the evaluation is being done left to right and that's just a matter of choice for example we decided that we were going to first compute three then look at the next expression which is seven plus five we could have done the other way around we could have first computed seven plus five and then computed three the reason we could have done the other way around is because addition is a commutative operator some operators are commutative others are not depending on what the operator is we could choose what strategy we want if the operator is not commutative then the order is specified by the operator if the operator is commutative then you know the order is specified by the code generation strategy in this case our strategy decides to do the evaluation left to right okay what about optimization is this an optimized code or is it just some code right so actually this machine is rather narrow in its interface it only has stack as a storage and there's a very limited instruction set that we can do and maybe there are opportunities to optimize here but the way we generated this code we really didn't worry too much about optimization perhaps the only things that we cared about while you know in terms of optimization was where to store the result so we basically the choice of storing the result in the accumulator was sort of based on this assumption that accessing reading and writing the cumulative is going to be cheaper than reading and writing to memory and so it makes more sense to keep the most recent values in the accumulator because they are also going to be the most likely used for values that are going to be used in future in the near future other than that we haven't really done much optimization here also i the scope for optimization is not that much the whole premise of optimization is that there should be a lot of redundancy there should be many different ways of doing the same thing and only then it makes sense to choose the best way of doing that thing right so if there are for example in a real machine there will be lots of ways to compute the same function and so optimization is basically about finding the you know a better way to do the same function in this particular machine though that space of potential solutions is not that large and so that is also a reason why optimization is is is not such a big issue and we haven't even discussed optimization in this context yet here's what the generated code would look like it's just the pseudo code for the steps that i had in english earlier accumulator gets three push accumulator accumulator gets seven push accumulator accumulator gets five add plus pop which is add execute add and then execute pop and then again execute add and execute pop i'm just putting add plus pop in a single row actually there are different instructions but but just putting it in a single row for easy readability here's what the code generation strategy for something like this would look like this is i'm assuming that my input programming language the higher level language is the language of expressions that is just computing express some operations on some constant integers example examples like we saw three plus five or three plus seven plus five and things like that and so the code generation strategy that i just described informally would look something like the following there would be a function called cgen that's going to look take as input an expression and output the set of instructions that are going to implement or compute that expression for example if the expression is just a single constant const for example three then code gen for three is just going to have the instruction which just assigns that constant to the accumulator all right because in a code generation strategy the only that the invariance that we're following is that the result is in the accumulator and we are not clobbering the stack in any way and this this this this particular rule of code generation is following toward those invariants now let's look at another rule just using examples we're going to see this in more detail code gen for a plus b where a is an expression and b is an expression involves first code genning a then after code gen for a is complete then i would i can assume that the result of a is an accumulator then i do push accumulator so push it on the stack save it on the stack now i can do code gen b and i can be sure that code gen b is not going to clobber my pushed value it can clobber the accumulator but will not clobber the push value because of my invariant and then i after i've done code gen of b i'm going to execute the add instruction because at this point i know that the accumulator contains the value computed for b and the top of the stack contains the value computed for a and so i can execute add to get the addition result in the accumulator and finally i want to clean up the stack by using the pop instruction
