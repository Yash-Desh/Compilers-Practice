Title: Compiler Design Module 14 : Ambiguity in CFG
Video ID: 42zrWMQQHtM
Detected language: en
------------------------------------------------------------

So today we are going to discuss ambiguity in context free grammars in a little bit more detail than what we did last time. So recall that if you have a grammar that accepts arithmetic expressions with plus and star and then if you have a string like id star id plus id then there are two associations that are possible. We could either consider it as the association on the left which is you associate the left two ids on a star and a star first or you associate the right two ids and the plus first and they result in different parse trees or derivation trees. A more formal definition, a grammar is ambiguous if it has more than one parse tree for some strings. Even if it has more than one parse tree for one string it would be an ambiguous grammar. So a grammar is unambiguous if it has a unique parse tree for every string. Alright, so let's consider the grammar that we were considering last time and from which that first string that we saw in the first slide was derived. So E can produce E plus E or E star E or E within parentheses or an identifier which is the leaf node or the terminal. Now one way to remove the ambiguity in this grammar, so this grammar is ambiguous right because there exists a string which is id star id plus id and there exist many more strings actually which have more than one parse tree. So id star id has more than one parse tree and hence this grammar is ambiguous. Now one solution to this situation is to rewrite the grammar so that it becomes unambiguous and this type of rewriting of the grammar is kind of also can be also thought of as stratification of the grammar. So what do we want? Maybe we want that whenever there is a choice the star should be grouped before the plus. So that's what the board mass rule for example says that the star should have higher priority which basically means that you should first associate all the stars and then associate all the pluses. We also in high school mathematics call these terms and expressions. So the multiplicative expression is called a term which doesn't have any plus in it, no addition in it. So just a star b star c this is called a single term and then you guys say a star b star c plus d so this whole thing would be an expression where plus would be at the highest level of the parse tree and the association would be first with the star and then with the plus. So to represent this in a grammar we could take the same intuition and define a new non-terminal t which stands for term and we say e can produce either e plus e or a term t and a term could be either a leaf node which is a terminal or t star t so you can multiply two terms and still get a term or if you put an e which could have a plus in it which may be a non-term but if you put it in a parenthesis then it becomes a term and this is exactly what we also do when we are talking about high school mathematics. So this becomes a term whereas that is an expression e. That's the intuitive way of doing it. So we call this stratifying the grammar because we have stratified it and we said that you can only notice that you can only go from e to t but you cannot go back from t to e unless you use a parenthesis. If you use a parenthesis then of course you can go back and it becomes a term but once you are in e so once it's an expression it's not so you can only go one way. So I mean depending on which side you are looking at you can convert a term to an expression but you cannot convert an expression to a term because expression is more general it can have addition in it without parenthesis. So to see why this grammar is unambiguous I claim that this grammar is unambiguous so let me just write it down and we are not going to prove that it is unambiguous that is beyond the scope of what we want to discuss but let's understand what happens when we try to parse the string id star id plus id in this in this grammar. So before I do that intuitively you know the start symbol is e so this is the start symbol and so I'm going to start with that and then notice that I could either you know use the first rule which is e plus e or I could use t. Now if I use e plus e that's exactly what we wanted and that's you know we're going to get one parse tree but if I use t then there's no way to derive this and this this string from t right because this string has plus whereas t cannot allow a plus until there is a parenthesis and so we'll be stuck so the only derivation has to go through the first derivation which is e plus e and so that kind of ensures that there is no ambiguity and the association is also clear from the derivation because the star will be associated first and then the plus. So let's look at this so this is our new grammar stratified grammar e produces e plus e or t, t produces either id or t star e or parenthesis e within parenthesis. So let's look at the first possible derivation so I say e gets so let's I'm trying to derive here id into id plus id. So let's see what are all the possible derivations. So one option is that I first derive e plus e and then and then I say okay this is e star e plus e and then I can say id star id plus id. So good I've been able to derive id star id plus id through this parse tree and notice that here we are basically you know basically the parse tree that gets constructed is basically akin to first associating id star id and then adding the third id to it. Now let's try the other one which is to first produce star and then produce plus which was the case with ambiguous grammar and I'm going to show that we're going to fail. So in this case I tried to produce t in the first go and now I'm going to try to produce maybe t star t because I want to the star operator that's only option and now I would like this particular t so and then maybe I want to say t star id but now this particular t I want to convert it into id plus id but can I convert this t to id plus id? Well there is no way actually because unless I use a parenthesis which I don't want to use because the string doesn't have a parenthesis so there is no way I can reach id plus id and so there is no way I can get to id into id plus id and so this grammar is actually removing the ambiguity in the original grammar. So stratifying the grammar is one way to remove the ambiguity of that grammar and we're going to see one more example. Now here's another example if e then e so let's say you know this is the if then else statement e can either produce if e then e or it can produce if e then e else e so either there could be an else clause or there may not be an else clause and this is a common type of language syntax in many languages including C so you could either have an else clause or you can not have an else clause and this would be the way to represent it in a context free grammar so you say e can either produce if e then e or it can produce if e then e else e but it turns out that this grammar is ambiguous. Why is this grammar ambiguous? Well let's look at an example if e1 then if e2 then e3 else e4 so in this case I you know nested if then else into an if then but now there are two ways I can interpret this right I so notice that there are there are two if thens but only one else the question is what does the else group with associated does it associate with the second if then or does it associate with the first if then so in other words there are two ways to interpret this one is if e1 then and then the whole thing is a then clause and then you say if e2 then e3 else e4 so that would be the then clause or the other way to interpret it is if e1 then if e2 then e3 else e4 in this case else e4 is associating with the first if then in the first case the else e4 is associating with the second if then and you know I am just using brackets to represent the different parse trees but you could also express the same thing using derivation trees or parse trees so this grammar is ambiguous and we would like to make it unambiguous and once again you would like to stratify it to make it unambiguous how do we stratify such a grammar so that it becomes unambiguous well the idea is the following first of all what is the what what is the correct derivation tree here alright so usually in languages the rule is that else binds to the closest if then the closest unmatched if then so in this case this would be the the correct interpretation and this would be the incorrect interpretation because in the absence of any brackets or braces we just assume that else binds to the closest if then closest unmatched if then alright so in this case it is this if then so so we need to write a grammar and unfortunately this particular grammar that we have in the slide accepts both these derivation trees but we want this unique derivation tree that only accepts the first one and rejects the second one so to do that basically we want to express this property that an else would always match the closest if then by the following which basically means that if there is an else then it must be true that in the then clause there is no you know so this this else here let me use a different pen so this else e4 can associate with the top level only if this thing is completely matched is matched what do I mean matched which means it does not have any unmatched if then so if there were for example if the sentence was if e1 then e2 then oh sorry if e1 then if e then e else e else e yes let me just write let me just write it here okay so take another sentence let's say if e then e else e oh sorry if e then if e then e else e else e looks a little bit difficult but it's actually pretty easy once you once I draw the brackets so notice that this is if e then e else e now this is very clear that there are two ifs two thens and two else's and so there is no there's no problem here and I could easily group this and then this particular else will get grouped with this particular if then now this is happening because the closest unmatched if then is basically the first one okay in other words the else e can match the top level if then the first if then only if the then clause has all its if thens matched with the corresponding else otherwise the else would match the then clause so that's what that's what no that's how we would capture this stratification and so this is the typical rule else matches the closest unmatched if and so this is correct and this is wrong all right and this is the stratified grammar this is a little bit complicated but I'll try to do this and let's see so e can go either to a matched if so matched if is an if which has all its else's matched or it can go to an unmatched if in which case there is some if that does not yet have an else all right okay so what is a matched if a matched if is if e now this e could be anything then mif so else mif so all the ifs here should be matched which means you know there should be else's for everything for all the if then else and similarly here should be matched and this is because if any of them is unmatched then we say that it's not part of the mif grammar or mif language so mif language basically is something that has else for every if right so it has an else for every if then whereas an unmatched if is basically has grammars where there is at least one if which does not have a corresponding else so here's an example if e then mif else uif so so here okay so this is you know here the then clause has a matched if but the else clause still has an unmatched if so this is kind of uif in terms of uif but let's see another example uif produces if e then mif notice that it does not have an else no else and thus it is part of a uif unmatched if because there is no else and then or it could have if e then uif so it's possible that the then clause itself is not a match does not have a matched if state now I claim that this stratified grammar resolves the ambiguity and the key observation here is that it has it allows mif in the it allows this sentence to match only if the then clause is an mif all right so this implicitly saying that the else will match with the if then only if this thing has all everything is matched right this is exactly what we said in the previous slide and this is being captured by this grammar because mif everything is matched and hence else uif can get matched with the top level now you can you can you can see why you know you would be able to see that in the previous sentence the correct grouping association would be accepted and the incorrect one would be rejected in the stratified grammar now it is clear that I mean while the original grammar of if then else was relatively simple the stratified grammar has become slightly complicated and and thus is prone to bugs so here is an here is working so if so if e1 then if e2 then e3 else e4 so e first of all this cannot be matched through mif because there is an unmatched if here you know there are there's only one else and two ifs so of course there's an unmatched if so I will you know I'll first produce uif and then the top level there is an else so I would say if e then mif else uif but will this work well it turns out that this will not work because if I use this rule then this thing should have been an mif but if I use this rule then what remains inside here because I use if then else so what remains here would be if e2 then e3 but which is not an mif because it doesn't have an else that matches the if so it's actually a uif and so this rule is not going to be able to produce this particular sentence and so this is the wrong thing to do and so I cross it out and the right thing to do here is I use this particular production if e then mif and then this mif expands to if e then e else e and this is exactly what we wanted because now the else is by associating with the closest if then and not the first top level if then okay in practice ambiguity is removed either through stratifying the grammar as the examples we just saw but often because stratifying the grammars can become complicated and especially for large grammars often in practice what people do is they allow ambiguity in grammar so they allow the grammar to remain ambiguous but then they allow the user to specify some disambiguation rules some particular rules and typical disambiguation rules say that this particular operator has more precedence over that particular operator just like for arithmetic expressions we could say that star has higher precedence over plus all right or we could say that else binds to the closest if then and things like that so some types of rules are allowed to be specified and here is an example for example you could write a grammar which accepts all arithmetic expressions e plus e e star e parenthesis e or id now this is an ambiguous grammar as we saw but bison for example allows you to represent have rules of this form which say what what is the precedence so it can you can say that star has a precedence or you can say that plus has a precedence also you can specify the associativity you can say that if there's a choice so for example if I have a plus b plus c or plus is an associative operator but let's say you have a divided by b divided by c so should I first divide b by c and then divide by a divide a by b by c or should I you know should I first divide a by b and then divide the result by c so this is called associativity and you can also specify associativity of the operator so in this case it says that star is left associative so in practice tools allow you to specify ambiguous grammars as long as all ambiguity is resolved through such custom rules which are of certain types for example you can specify precedence and you can specify left versus right associativity
