Title: Compiler Design Module 154 : Affine Transform #5 - Loop Reversal
Video ID: cgGVpFQ3qKQ
Detected language: en
------------------------------------------------------------

So we're going to discuss our fifth primitive of I'm transformed today, which is loop reversal So let's look at this example for i equals 0 to n y n minus i equals z i So notice that it's you know y is being traversed in the reverse order because the coefficient of i is minus 1 whereas z is being traversed in the forward order just like all the other examples and now we have for j equals 0 to n x j equals y j so both x and y in this case are being traversed in the forward order Now once again as we have been using in our examples There is a read up to write dependency between elements by accesses of y with accesses to y n minus i and accesses to y j and if I was to draw a pictorial representation the crosses represent the instances of x 1 and the circle represent the Instances of s 2 then the first instance of s 1 is going to access y n and that is what is going to be accessed by the last instance of s 2 because the last instance of s 2 is going to Access y n and so there's a read up to write dependency from the first instance of s 1 to the last instance of s 2 similarly, there's a read up to write dependency from Second instance of s 1 to the n minus 1th instance of s 2 and so on and so this is what our dependency graph looks like And the question is what would be the resulting space partitioning constraints? What would be the resulting? transformation of the program for synchronization free parallelism and also locality as a result and so and and whatever is the result is actually going to form our fifth primitive affine transform which is loop reversal Okay, so my first step is going to be to represent this program in the polyatrial representation There are two statements s 1 and s 2 the iteration space of s 1 is from 0 to n and that's for i and Then the transition space for s 2 is also 0 to n both are single dimensional iteration spaces Now I'm going to create my space partitioning constraints. What are my space partitioning constraints? Whenever there is a dependence a data dependence from s 1 to s 2 Then there should be they should have those instances which have data dependence must be mapped to the same processor ID and so this basically translates to saying that whenever n minus i equals j because that's when there will be a data dependence between an Instance of s 1 and an instance of s 2. So whenever n minus 1 equals j This is just the you know lowering of f i plus small f equals capital F prime small i prime plus small f prime then This should be then Pi equals pj in other words the iteration i and iteration j should map to the same processor And so based on these space partitioning constraints the the solution that I will get The maximum rank solution that I'll get is p equals n minus i and p equals j So for the i-th iteration of s 1 it is mapped to the n minus i-th Processor in the processor space for example the 0th iteration of s 1 is going to get mapped to the nth processor And on the other hand for s 2 it is p equals j which means the nth iteration of s 2 is going to get The nth processor so 0th iteration of s 1 and the nth iteration of s 2 are going to get mapped to the same processor Which is processor ID n and that's exactly what our space partitioning constraints require and and so on for other Processor IDs as well and now the next job is to generate the code for this particular maximum rank solution The naive code generation strategy is to have an outer loop on the processor space so for p equals 0 to n Notice that the lower bound is 0 because both s 1 and s 2 mappings based given that i-th maximum value is capital N So the p's minimum value will be a capital N minus capital N that's 0 and p equals j is just because j's minimum value is 0 So p's minimum value will be 0 so the lower bound is 0 similarly you can check for yourself that the upper bound for p Is going to be capital N so the outer loop is just for p equals 0 to n p plus plus And then I just paste the original program as it is except that I predicate the statements s 1 and s 2 with their respective Mapping functions the solutions because we only want those iterations to execute on the current processor ID Which are mapped to that particular processor ID so I'm going to say if p equals n minus i y i n minus i equals z i if p equals j x j equals y j And now I'm going to tighten my loop bounds or eliminate empty iterations It is very clear that this inner loop on i is only going to Have one iteration which is meaningful that all the other iterations are going to be empty for this inner loop on i Similarly for the inner loop on j only one iteration is going to be non empty all other iterations are going to be empty Which is the iteration of the i-th loop on i which is non empty It is the iteration where i equals n minus p Right because that's the only iteration for which this predicate is going to evaluate to true similarly the iteration for which this is going to be non empty for j is when j equals p and So I can basically simplify the loop and in fact remove the inner loops and just replace i with N minus p and so that gives me N minus i becomes n minus n minus p which becomes just p right n minus Parenthesis n minus p that just becomes p and i there is replaced by n minus p so this becomes n minus p and that's s1 And similarly for s2 it becomes x p equals y p because j is equal to p and so I get x p equals y p And this is my new transform program Because we basically respected space partitioning constraints while finding our mapping function You can be assured that this new program transform program is equivalent to the original program And if I was to draw the same thing pictorially our original loop data dependencies look something like the following You know there were edges from the first iteration of s1 to the last iteration of s2 And the last iteration of s1 to the first iteration of s2 But now I have been able to transform the loop while respecting their dependencies such that The data dependencies that actually have a very small resistance the data dependent iterations actually occurred in the same iteration of the outer loop P Essentially what I have been able to do is just rearrange s1 iterations so that You know so that these edges become the shortest possible and this has been the common theme in all our Primitive of fine transform examples that we have been looking so far which have involved the use of space partitioning constraints
