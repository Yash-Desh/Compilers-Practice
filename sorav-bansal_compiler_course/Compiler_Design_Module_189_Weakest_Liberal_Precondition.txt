Title: Compiler Design Module 189 : Weakest Liberal Precondition
Video ID: oI1n1cBVhao
Detected language: en
------------------------------------------------------------

we've been looking at the weakest precondition predicate transformer and let's step back a little bit to before we discuss the weakest precondition rules for other constructs such as loops and other things, let's step back a little bit and try to visit again what are the semantics of this weakest precondition or precondition. So given a program or statement S and a post condition R, we are interested in finding some predicate such that if this predicate is true and S executes then R holds but more precisely the precondition is the condition or predicate just prior to the execution of S such that when S executes then R holds but this the statement is a little bit ambiguous so we need to define it more carefully and that's what we do in the next slide. So one option is if when S executes and it terminates such that the control reaches just after S so if S terminates where terminates basically means it S completes execution and control reaches the program counter value just after S then R holds so that one option for precondition could be the condition just prior to the execution of S such that if S terminates then R holds this is option one. What is option two? Option two is the condition just prior to the execution of S such that S terminates and R holds so option one is saying if S terminates then R holds option two is saying the condition under which S definitely terminates and R holds so the option two is also kind of constraining the conditions to be such that S definitely terminates which means S completes execution and reaches the PC just after S and so these are the two different ways of defining preconditions and both are valid ways they actually define different correctness criteria the first one is basically the whole triple for partial correctness and this is something that we have discussed earlier as well when we define partial correctness if the execution terminates then this holds and that's exactly the partial correctness definition or meaning and so the option one corresponds exactly to the condition for partial correctness option two says S terminates and R holds so that's actually the condition for total correctness which means that this statement S or this program S actually terminates for sure terminates which means it completes execution reaches the PC just after S and to distinguish partial correctness from total correctness we kind of have a different notation for the corresponding whole triple in the partial correctness whole triple we were using these braces these curly braces for the precondition and the post condition for total correctness we're going to use these square brackets for both for the precondition and the post condition all right so to summarize basically option one is if S terminates then R holds and this is the partial correctness condition and option two is S terminates and R holds and this is the total correctness condition and the total correctness condition is also represented as using a whole triple with square brackets something like this all right so this condition this precondition that is for partial correctness which means option one is going to be different from the precondition for option two and so we need to distinguish it when we talk about weakest preconditions and things like that and for this reason we actually now define two different values one is the weakest precondition which corresponds to total correctness and the other is weakest liberal precondition which corresponds to partial correctness and we're going to see some examples of where weakest liberal precondition would be different from weakest precondition the first point i want to point out is that weakest precondition is going to be a stronger condition than weakest liberal precondition and that's easy to see because weakest precondition requires a stronger post condition weakest precondition requires that S terminates and R holds whereas weakest linear precondition requires a weaker post condition which is if S terminates then R should hold if S does not terminate then I don't really care and so weakest linear precondition correspondingly is going to be weaker than weakest precondition the other way to kind of say this is that weakest precondition for any statement and post condition S and R respectively WP will imply WLP if I write it in the Venn diagram set notation then WP represents a smaller space and WLP represents a larger space and this represents the fact that WP implies WLP all right so how do I what should be my rules I mean so far I've been talking about weakest precondition all the way but now I have introduced two different preconditions one is WP and the other is WLP for total correctness and partial correctness respectively and so how do my rules that have discussed so far change the good news is that the rules that I've discussed so far do not change so for example for the skip instruction it's the same thing WP skip R equals R WLP skip R equals R assignment WP X colon equals E comma R is just R with X replaced by E and similarly WLP is identical when I come to sequence so WP S1 semicolon S2 comma R equals WP S1 comma WP S2 comma R similarly WLP S1 semicolon S2 comma R is WLP S1 comma WLP S2 comma R so really nothing has changed but notice that there is a subtle thing here WP on the right hand side I mean for W to define WP on the sequence I also use WP on the individual statements on the right hand side similarly when I define WLP then I use WLP on the individual statements on the right hand side so far WP and WP are identical but later on we're going to discuss some constructs such as loops where WP and WLP will be different in which case a sequence which involves a loop would involve would give a different result for WLP and a different result for WP and similarly the final rule is for if then else or conditional and these two rules are also identical and it's not hard to see that all these kind of constructs that we've discussed with skip assignment sequence and conditional they are terminating constructs in the sense that if S1 for example if I look at sequence firstly assignment is terminating because you know I'm guaranteed that this execution of this atomic statement assignment statement will terminate in the sense that it will complete execution and the control will reach the end of the statement for sure because just an assignment similarly conditional is a terminating statement sequence assuming S1 and S2 are terminating statements then the sequence of them would also be terminating and so it's not hard to see that you know the rules will be identical because whether you talk about total correctness where we say oh it definitely terminates and our holds or you talk about partial correctness which says if it terminates then our holds you know at least because of this particular construct that we have we have looked at for example sequence or if then else or assignment or skip it doesn't because it always terminates the two definitions are identical assuming the kind of sub statements also terminate and you know we are basically relying if the substatements didn't terminate then that's what's captured by the fact then for WP we are using WP of the substatements like S1 and S2 for WP and similarly for WLP we are using WLP for the substatements all right so now we're going to look at the first example where WP and WLP diverge and that's the abort statement so let's say in my program I have just like I had a skip instruction the skip skip statement I have the abort statement the abort statement basically just aborts the program and that means that the control doesn't reach the end of the statement because the program is just aborted and so abort can be is basically by our definition of termination a non-terminating instruction and so by that definition WP abort R for any post condition R is just false because no matter what pre-condition you use abort is never going to terminate and so the strong I mean so the only answer for the pre-condition the weakest pre-condition for total correctness is false on the other hand W the weakest liberal pre-condition WLP for abort for any post condition is true because recall the WLP says that if it terminates then R holds if it does not terminate then I don't care and so in this case by irrespective of what R is it will never terminate and and if it doesn't terminate then this WLP is vacuously true because if it doesn't terminate I don't need to have any condition on it and so you can use any pre-condition and it you know it will be true that it will be partially correct this is the same thing that we discussed while we were discussing partial correctness so let's look at an example to see this in action so let's say I have my assignment statement called x colon equals y by z notice that I'm using the division operator on my RHS and the post condition that I'm interested in checking is that y equals x star z after this execution and you can see that for well-behaved values of x y z this post condition should hold and so I mean it's the same statement which is the assignment to a division of y by z to x and the same post condition is y equals x star z but what is the weakest pre-condition and what's the weakest linear pre-condition in this syntax I'm assuming that divide by zero causes the program to abort so if you divide by zero it causes the program to abort I mean this is different from for example the C language where divide by zero is undefined behavior which means anything can happen but anything can happen includes abort but it also includes other things which says okay maybe terminates and then it goes to the next thing in which case WP and WLP would have behaved differently but in this example that I'm going to show to motivate you know why WP and WLP will be different for this case I'm assuming that divide by zero actually you know for sure causes program abort which means it you know this if z was zero then the statement does not terminate so what is the answer for this thing now of course I can basically I should be applying my assignment rule and things like that but now for weakest pre-condition I also need to check that the program does not abort because if it aborts then it's going to not terminate on the other hand for the weakest linear pre-condition I don't need to do any such check so that's the difference between partial correctness and total correctness in WP I need to additionally check that s terminates and so although I haven't really discussed how I am going to check whether s terminates or not intuitively basically what happens is that for the weakest linear pre-condition any the weakest linear liberal pre-condition here is true whereas the weakest pre-condition is that z is not equal to zero because for weakest pre-condition I also need to ensure that the program actually completes successfully or terminates on the other hand for weakest linear pre-condition I don't need to do that and so for this particular statement where we assume that divide by zero causes abort WP and WLP are different WP is needs to ensure that z is not equal to zero whereas WLP has no such constraint and you can see that WP is a stronger condition than WLP.
