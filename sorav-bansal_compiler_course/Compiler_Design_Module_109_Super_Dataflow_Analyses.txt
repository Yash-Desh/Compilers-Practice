Title: Compiler Design Module 109 : Super Dataflow Analyses
Video ID: WV7D1ZzA_FA
Detected language: en
------------------------------------------------------------

We've been talking about the interactions of data flow analysis and here's one example that shows you the problem in more detail so It's similar to the example that we discussed in the last module X equals 10, X is greater than 0 but now on one of the branches there's a loop which also updates X So recall that in the previous example we were updating a different variable Y But here we are updating X and on the other branch There's no loop and we just set X to 20 and then we set Z equals X now Let's look at a sample execution of this program We're going to say X equals 10 then X is greater than 0 is going to evaluate to true So let me just actually also say that this is the true branch and this is the false branch All right, so X is greater than 0 is going to evaluate to true And so the true branch will be taken X will be equal to 20 and then Z equals X and that's it so actually the program can be this entire loop can be eliminated and We can just constant propagate X equals 10 to X greater than 0 and X equals 20 to Z equals X So Z equals X can be replaced with Z equals 20 for example And we can also eliminate this particular condition check and the associated branch and the loop So this program can be made really small and we can see that with you know, just by looking at it It's very easy to see that this program can be made really small But let's see what our data flow analysis is going to do with this program So it's very clear that you know what we need is constant propagation and Data dead code elimination or some mixture of that So maybe let's try to do constant propagation first and then dead code elimination And so let's say I do constant propagation first. Then what happens is that when I do constant propagation we have Let me just add my annotations here So when we do constant propagation we say X equals 10 But at this particular point we are not able to say that X is definitely a constant because on one of the paths X is getting modified and so at this point I would say X is bottom. Alright, so here X is going to come out to be bottom and So I'm not going to be able to infer the fact that X is actually equal to 10 and so I'm not going to able be able to Do the replacement do the constant for them Everything else will remain the same here The X is not a constant here X is not a constant and now X is getting assigned to 20 and what about at this point? Once again on one of the branch X is a 20 but on the other branch X is not a constant And so once again, I'm not able to do constant propagation and once again X is bottom at this point as well So we can see that constant propagation will leave the program completely unchanged because it will be unable to infer any constant relationships And that's because of this incoming edge Here which basically causes X to be inferred as not a constant and similarly this particular incoming edge here Which is going to infer that X is not a constant at this location So, okay So if constant propagation leaves the program unchanged then dead code elimination will also not work on this program after that Because it will seem like this this path can be taken and you won't be able to infer that this path is actually dead code Right. So this particular loop is not going to be inferred to be dead code if I run Constant propagation first and dead code elimination second. Now, let's try to do the other thing let's try to run dead code elimination first and Then run constant propagation. Alright, and so once again if I run so let me just complete this example So if I run dead code elimination first, I don't see any dead code in this program because there's you know Or it seems like all branches can be taken unless I do constant propagation that information is not available to the algorithm And so dead code elimination will leave the program unchanged and even if I do constant propagation after that It's not going to make any difference So even though we can see that some mixture of constant propagation and dead code elimination can Kind of make this program much smaller Irrespective of the order in which I execute these analyses. I'm not going to be able to get my desired program and And so irrespective of the order of DFA transformation passes Constant propagation and dead code elimination this program will remain un-simplified And the problem is because we are trying to do one thing at a time So when we are doing constant propagation, we don't even think about dead code elimination and when we try to do dead code elimination We don't even think about constant propagation. We try to do these in separate phases So first phase constant propagation second phase dead code elimination or vice versa But we don't know one when we are executing one phase We are completely blind about the reasoning that is involved in the other phase And so that is the problem and that is the reason we are not able to simplify this program any further So what is the solution to this problem? Well one solution which is which is a potential solution to this problem is that we use a super DFA or super analysis That does both constant propagation and dead code elimination simultaneously So we just augment our analysis so that it you know Let's say I augment my constant propagation analysis to say that oh, you know as you are Identifying the constants also check keep checking for dead code simultaneously. And as you see dead code by you Identify constants just mark it as such so that later on I mean I can get an optimistic result. So for example In constant propagation, I'm going to figure out that this is 10 is greater than 0 and then I'm going to say Okay, if this is actually if this remains a constant at 10, then this branch is unreachable So maybe I need I have any a value which says that this run this program location is unreachable Right, so I just keep extra value. So my values are now a tuple which is one is whether something is a constant or not So for example X comma 3 or X or comma 10 or whatever and the other value is whether this code is this program point is Reachable based on the current Constant information that I know and with that I would be probably able to figure out that oh this is not reachable in the first iteration of the DFA and then in fact, even if I kind of try to Identify a fixed point because this is not reachable. I'm going to use that information here and say oh, this is not reachable So I don't need to even consider this branch to do my constant propagation And if I do both these simultaneously and so when I say simultaneously, it's almost like within each iteration of the dataflow analysis I am Propagating both the constant information and the reachability information for dead code elimination And if I do that then yes, then it is possible to do this in a single DFA In a single DFA fixed point iteration loop, I will be able to identify and simplify this particular program So that's a potential solution But that is you know, that is not a very practical solution because that basically means I Just taken one example of one DFA Algorithm like constant propagation and another is dead code elimination But then I can take any pair of such DFA's and so that will make my potential number of DFA's quadratic You know if I have n different possible Analyses then I need to also kind of make super analysis of every pair and why stop at pairs? Maybe you know, even if I do pairs at once then I want there may be some programs where I Still get suboptimal solutions and if I did triples, you know I make super analysis out of all possible triples then that's the best possible solution and so on and a this kind of a solution makes the code really complex the compiler logic very complex and we already know that compilers are already very complex and we don't You know and something like this is just going to make things even more complex and be You know, so actually this is this is the main problem that it's just too complex and be even if you know I do it then what is the order in with these pairs should run and things like that So if I combine constant propagation dead code elimination and then there is another super analysis It combines common sub expression elimination with dead code elimination And so now there are too many analysis to worry about and then that also slows down the compiler Also, we have a bigger phase ordering problem now and so on so So this is actually not typically considered a practical solution, but it's good to know that Something like this can be solved using super analysis Where we combine two or more analysis into a single analysis and kind of do that in a single DFA fixed point loop What is done in practice? What are the common responses that compilers take in practice and this is something that we have discussed in the past as well One response is carefully choose the order of the transformations and allow the same analysis to be performed multiple Times in that order and that that carefully chosen order is basically based on experience heuristics and things like that And and basically the idea is let's try to solve for the common case. Let's look at the common case So the example that I gave you You know some of these things we just have to leave on the floor We are not going to solve each and every program in the world But let's solve at least 95% of the programs in the world and maybe this program that I discussed in this particular module It does not belong to the 95% that current compilers can solve All right, so we try to solve for the common case and you know, the good news is that by and large Doing analysis separately solves for the common case in most cases and so we don't really need To construct super analysis for most programs, although there exists program We do end up with suboptimal solutions and of course if we end up with suboptimal solutions we also have to worry about you know, how much sub optimality we do we have to live with because of Because of this trade-off that we are making between complexity and the quality of the code that is getting generated The other common response and this is also something that we have discussed in the past is to use a meta fixed-point loop that applies all the transformations in In a carefully chosen sequence and keeps repeating it until there is no change and so this meta fixed-point loop will converge if we are ensuring that all transformations are only going to Take the program in a certain direction. For example in the direction of reducing time running time with for some Notion of what is the running time computed statically on the program? So let's say I have some cost function if my transformations are only take decreasing the cost of the program With respect to that cost function Then we can be sure that this meta fixed-point loop will eventually converge because after all the cost will eventually become 0 if it just keeps decreasing The problem with this is that even though this is guaranteed to converge if it is set up like this It can take very long and the second problem is that defining such a cost function and making sure that All my transformation passes in the presence of lots of updates by developers in parallel continuously Every day such that they still follow that That rule that you know, they only decrease They only transform a program in the decreasing direction of the cost function is also a very difficult Invariant to maintain in practice when there are lots of people working on the same code base
