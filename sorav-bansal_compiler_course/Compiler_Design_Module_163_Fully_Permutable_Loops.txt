Title: Compiler Design Module 163 : Fully Permutable Loops
Video ID: vSp5rHBiXTk
Detected language: en
------------------------------------------------------------

Last time you were talking about an example which was not possible to pipeline and this was a successive over-relaxation example or the SOAR example and so the problem there was that if I pipeline using the iterations of the outer loop as tasks then between different stages of the task there were dependencies on in both directions. For example, the stages in the first task are dependent, the stages in the second task are dependent on the stages of the first task and vice versa. For pipelining what we require is that these dependencies are one way. For example, it's okay for stages in the second task to depend on the stages in the first task but it shouldn't be the other way around and so if that's true then what can happen is that I can keep executing the first task and then as the stages keep completing I can just keep signaling the second task in which can basically now execute in this waterfall or wavefront kind of manner which is also called the pipelining execution model. So how do we distinguish between a loop that is pipelinable and a loop that is not pipelinable and this concept of fully permutable loops is basically a very useful one when you're talking about pipelining. So what is a fully permutable loop? A loop is fully permutable if it can be permuted arbitrarily without changing the meaning of the program or if it's loop, if the different kind of iteration indices that form the different loops of the loop nest can be permuted. So to take an example let's say I have a loop for i equals something something for j equals something something for k equals dot dot dot s1 then this loop nest should be equivalent to if I permute let's say j and k or let's say I permute i and j so example here I permute i and j so I say for j equals dot dot dot for i equals dot dot dot for k equals dot dot dot s1 and these two programs should be equivalent which means the meaning of the program should be equivalent and it should be possible to do this for any two or three or four indices and repeat at least for example I could say for k I could make k the outer loop, j the middle loop and i the inner loop and still the program should retain its meaning. So now the question is what kind of programs or what kind of properties that and do I need on s1 or in general the whole loop nest such that that loop nest becomes fully permutable. Now of course if s1 was completely data independent then all this is okay to do but I mean data independence is kind of you know a sufficient condition but not a necessary condition I can basically have a more relaxed set of constraints on s1 such that these kind of permutations still preserve the meaning of the program. So what are this kind of what is the most relaxed meaning what is the most relaxed constraint that I need to put on the program such that it is okay to permute these loops and yet retain the meaning of the program. So here is what it is okay notice that let p be the permutations for example if my original loop nest was ijk let p be the permutation which converts ijk to let's say jik or maybe it permutes it to kji or whatever right so p represents some permutation and so there's p so p of i basically tells give me a new permutation. For example if it's you know i was equal to 1 j was equal to 2 then a permutation would be j equals 2 and i equals 1 okay and because j executes in the outer loop and i executes in the inner loop. So if there were two different iteration points in the iteration indices i1 and i2 then if i1 preceded i2 in the ascending lexicographic order in the execution order in the original program and in the new program in the permuted program i2 preceded i1 all right so basically i2 was going to get executed before i1 in the new series. So for example if it is 1, 0 and 0, 1 right so let's say i, j is 1, 0 and 0, 1 or let me just take an example 0, 2 so 1, 0 and 0, 2 so which one precedes which one in ascending lexicographic order well it's clear that 0, 2 precedes 1, 0 but now if I look at the permutation then permutation let's say this switches the order then 1, 0 precedes sorry if I if I switch the order then 1, 0 becomes 0, 1 so 0, 1 precedes 2, 0 in ascending lexicographic order and so whenever this is true whenever it is true that you know for any i1 and i2 and it is true for 1, 0 and 0, 2 that 0, 2 precedes 1, 0 and the permutation 0, 1 precedes 2, 0 whenever this is true it should be true that i1 and i2 don't have any data dependence in other words there should not be any data dependence between 0, 2 and 1, 0 if I was to draw this diagram diagrammatically 0, 2 so this is 0, 0 0, 1 0, 2 right and then this is 1, 0 so there should not be any data dependency between these two locations okay and if there is no data dependency then it is okay to basically do this permutation for all possible i1 and i2 okay so which basically so for all possible i1 and i2 whenever the order have relative orders of those i1 and i2 have changed it should be true that there was no data dependence and if that is if this condition is true then it basically means that the loop is fully permutable because even with respect to the permutation P and then we can also quantify over all possible permutations P but with with respect to the permutation P whenever this is true then there should be no data dependence that basically means that the permutation preserved the meaning of the program because for all the iterations which were reordered there was no data dependence and for iterations that had data dependence there was no reordering so to understand this using a picture let's say it's a 2d loop then this loop here which I've shown in you know with blue crosses and the inner loop is on the x-axis and the outer loop is on the y-axis then these green arrows represent the data dependencies and I claim that this particular loop is fully permutable you can see that you know whenever there is an iteration two iteration pairs such that the their relative order would change in case I switch the axis then either they would retain their relative ordering for example if I take 0 comma 0 and 1 comma 1 so 0 comma 0 and 1 comma 1 irrespective of which one I make the inner loop would always have the same relative order which means 0 comma 0 would always execute before 1 comma 1 irrespective of whether i is the inner loop or whether j is the inner loop however if I take 1 comma 0 and or let's say 1 comma 0 and 0 comma 2 just the example that we used in the previous slide then you can see that there is no data dependency if I look at this one and this one there is no data dependency between the two why do I say that there is no data dependency between this one and this one because there is no path from either one to the other okay so there is no path that basically I can take in this dependency chain that basically takes me from one to the other in either direction and so these two are data independent and and I claim that this is basically a fully permutable loop an easier way to think about this is that all the data dependency edges in this 2d diagram should have an angle between 0 and 90 degrees inclusive of 90 and 0 right so and this basically means that you know all my data dependencies are from lower values of i comma j strictly lower values of i comma j which means both i and j are lower to strictly higher values of i comma j and so that's that's exactly the condition that the angle of these data dependency edges is between 0 degrees and 90 degrees it should not be greater than 90 degrees and if it is between it if it is greater than 90 degrees then I would basically have edges of this type right and then I have edges of this type I basically have problem and the loop does not remain fully permutable anymore because between if I look at these two pairs then if I switch the axes then the relative order of these two pairs basically changes and because I have a data dependency edge that basically violates my fully permutable loop condition that I had on my previous life so this is an example of a loop that has that is not fully permutable where the red edges basically show data dependencies and for non permutable loops non fully permutable loops the theta angle can be greater than 90 in general it can be anywhere between 0 and 180 if the theta all the dependency edges are between 0 and 90 then the loop is fully permutable if some of the dependency edges have angle greater than 90 for a 2d loop then the loop is not fully permutable so we have this loop for i equals 0 to m j equals 0 to n xj plus 1 equals xj plus xj plus 1 plus xj plus 2 by 3 this is our successive over relaxation or sor program and in this program we saw in our previous module that the dependency edges are basically at an angle also at an angle of 135 degrees including angle 90 and 0 and stuff like that there are some dependency edges that kind of go beyond 90 degrees and we say that such a loop is not fully permutable because because if you permute the ing axis then you get you violate some of the data dependencies and so what we did was we just converted we just change the axis so that we basically make it fully permutable and recall that the changing of axis basically involved a new set of axes i prime and j prime which was basically formed by i prime equals i so i prime and i are the same and in fact i'm going to use just i and not i prime anymore because i prime is just equal to i and j prime equals i plus j right and so this is my axis transformation matrix and so with that the program that i get is for i equals 0 i is less than equal to m i plus plus for j prime equals i because you know j was from 0 to n so j prime is i plus j so j prime starts from i plus 0 to i plus n right so that's just j prime equals i to i plus n j prime plus plus now i'm not showing all the steps involved in figuring this out we'll use the Fourier mode skin method to identify the bounds and stuff like that but but this is what it's going to be and finally we need to write j in terms of j prime and i prime or just i and so j prime minus i plus 1 equals x of j prime minus i plus 1 equals the right hand side where once again j has been replaced with j prime minus i so this is my new program. This is a program in which I have modified the axis change the axis in fact what I've done is I just kind of used the 135 degree angle line which is i plus j as my new y-axis right so I just kind of squished and rotated the the whole kind of iteration space such that the 135 degree angle now becomes a 90 degree angle and to see this if I draw the iteration space now with this transformed program where i is on the y-axis and j prime to the inner loop is on the x-axis then i goes from zero to m and for every i j prime goes from i to i plus n so for example when i equals zero then j prime goes from zero to n when i equals 1 then j prime goes to 1 to n plus 1 and i equals 2 then j prime goes from 2 to j plus 2 and so on and so I get this parallelogram style figure of my iteration space and that's because I have rotated my y-axis right from 135 to 90 originally I had kind of a rectangular shape of my iteration space but once I change my axis I kind of change my 135 degree angle to 90 degree then I basically get this parallelogram shaped iteration space and now when I draw the dependency edges on this new iteration space transformed iteration space all the dependency edges have also rotated because of this transformation axis transformation and all the 135 degree angle edges in my previous figure that I showed in the previous module have now become 90 degree angles in my new figure and all my 90 degree angles in the previous figure have become a 45 degree angle in the new figure and the 0 degree angles remain the same and so this is my new dependency edge figure and notice here that because all dependency edges are either 90 degrees angle or less you know this 45 this is 90 and this is zero so all dependency edges have theta less than equal to 90 this particular transform loop now becomes a fully permutable loop so this is a nice trick that you can play for some programs not all programs I'm going to discuss which program we cannot play this trick with later on but for some programs you can play this trick by just rotating the axis and basically ensuring that we have a fully pipeline a fully permutable loop and recall that if you have a fully permutable loop then it is possible I claim to do the pipelining for that particular loop so how will pipelining work well in this case basically what we do is we use tasks as you know every iteration of the outer loop as one task so for example for i equals 0 I have this first task which is you know which is I know which is basically labeled as the i equals 0 task then for i equals 1 I have another task i equals 2 I have another task and so on so I have m different tasks so capital N number of different tasks and within each task I have stages so for example this is j equals 0 stage I'm just using j and cross j prime just to make the discussion simpler so j equals 0 is the first stage of the first task j equals 1 is the second stage of the first task j equals 2 is the second third stage of the first task and so on right so these stages are now labeled with the j values stage j equals 0 stage j equals 1 stage j equals 2 and these are all the stages of the different tasks and recall what is the idea of pipelining the idea of pipelining is that I specialize a processor for a particular stage and so the idea is that you know CPU 0 will basically specialize on stage j equals 0 CPU 1 will specialize on stage j equals 1 CPU 2 will specialize on stage j equals 2 and see for example CPU 2 will first execute stage j equals 2 for the first task then stage j equals 2 for the second task then stage j equals 2 for the third task so it's going to execute all these points in the iteration space that's formed by the vertical line and similarly so you know I just showed the stages for task i equals 0 these are the stages for task i equals 1 for task i equals 1 I start with stage j equals 1 I don't start with stage j equals 0 I start with stage j equals 1 because that's what my iteration space looks like and then stage j equals 2 and so on but I end at stage j equals n plus 1 instead no task i equals 0 was ending at j equals n but task i equals 1 is going to end at task j plus task n plus 1 and so on right so I mean so there are all these different tasks task i equals 0 task i equals 1 task i equals 2 and so on all the way till task i equals m and then each of these tasks has multiple stages and the processor is supposed to specialize on the stages so for example CPU 0 is going to execute stage j equals 0 and actually at stage j equals 0 there's only one task that executes so that's okay CPU 0 is not going to have to do much work so you know CPU 0 only has one thing to do one iteration point to execute state CPU 1 will execute two points two iteration points see CPU 2 will execute three iteration points and so on right CPU 3 and onwards will execute four iteration points each in this particular figure until you know we reach this point where at CPU n where you know it's going to be again we going to have fewer and fewer points as we go along in the CPU but these are all the CPUs that I can execute in a pipeline fashion and so for example CPU 1 would first execute stage 1 of task i equals 0 so stage j equals 1 of task i equals 0 but it cannot just executed straight away it has to wait for stage j equals 0 of task i equals 0 to tell it that it is done that's my synchronization only then the CPU 1 can execute stage 1 i j equals 1 of task i equals 0 and then similarly you know let's so in general basically if I look at any particular point so let's say I look at this particular point which is stage j equals 1 of task i equals 0 so stage j this particular point that are circled in red before the CPU 1 executes this point it should wait for CPU 0 to say that it has already executed stage j equals 0 of this particular task right so CPU j equals 1 needs to wait for CPU j equals 0 to say I'm done in general CPU j waits for CPU j minus 1 to signal before executing what it needs to do all right so in this case j equals 1 so CPU 1 needs to wait for CPU 0 to signal before it can execute this particular iteration point so similarly CPU you know if I just generalize it for example if I look at this particular point here so let's say I look at this particular point here let me just you know circle it in red then this particular before it can execute this particular point it has to wait for this point to execute before it can execute this right so it has to execute wait for this point before it can execute this so in general any CPU j so in this case j is 4 so any CPU 4 needs to wait for CPU 3 to signal and this has to happen for each task so for example it has to wait for CPU 3 to signal before it executes this then it has to wait for CPU 3 to signal again for task i equals 2 before it executes this point and so on right so every CPU just keeps executing you know just keeps climbing this vertical chain but before it executes any point in this vertical chain it waits for the CPU on its left to signal before it can execute and that's the whole idea of pipelining where we basically do order and synchronization on every CPU because you know CPU 4 basically waits for synchronization at every point before it executes so CPU j waits for CPU j minus 1 to signal before exec and this is for every task and there are m different tasks in this particular example and that's why we say this is order m synchronization how do i wait well how so basically there needs to be some signal wait mechanism and if this if this particular CPU is waiting for the CPU on its left to signal then it's the job of the CPU on the left to actually do a signal so there needs to be a message that CPU 3 saying saying okay I'm done and you know you whenever you are ready you can also execute now right but unless CPU 3 is done CPU 4 cannot exit execute and so there needs to be some way of doing the signal so for each task I so first of all I want to point out that this this wait and signal has to happen for each task I have been saying this but also written it here and moreover after the execution CPU j signals CPU j plus 1 all right so CPU j signals CPU j plus 1 so after I have executed this I should signal CPU 2 that okay I'm done with you know stage 1 of task 0 now you can execute stage 2 of the same task 0 and so that's what's going to be you know done for every point I'm going to wait for the CPU on my left to signal after I receive that signal I'm going to actually execute that particular point and then I'm going to signal the CPU on my right and I'm going to do this for every horizontal row in other words every task which is the every iteration of the outer loop there are only there are some exceptions some places I don't need to wait for the CPU on the left and those are the boundary points for example on the left boundary I don't need to wait for the CPU on the left like example at CPU 1 when I'm at this point that this is j equals 1 and i equals 1 then there is no left point here and so I don't need to wait for CPU 0 to basically signal me similarly at this point here I don't need to wait for CPU 1 to signal me I can just start executing if I have reached this point right so in general every CPU is going to execute these points sequentially climb up this chain sequentially but when it reaches this point it no longer for example when it's here then it needs to wait for CPU 1 to signal once again when it's here then it needs to again wait for CPU 1 to signal but when it's here at the boundary point at the left boundary point then it does not need to wait for the CPU 1 to signal because it is a boundary point and so when we are going to do our code generation we need to be careful about these boundary conditions as you are going to see in the next module similarly at the right boundary there is no need to signal just like on the left boundary there's no need to wait similarly on the right boundary there is no need to signal I don't need to send any message because I know that there's nobody on my right that's going to wait for a message for this particular task similarly this is the right boundary point and this is the right boundary point and this is the right boundary point for all the internal points which are non-boundary I need to do both wait and signal for the left boundary points in this figure I don't need to do a wait for the right boundary points in this figure I don't need to do a signal
