Title: Compiler Design Module 71 : Phi Nodes
Video ID: m_LT1jv0ozQ
Detected language: en
------------------------------------------------------------

So last time we discussed how we can convert a program written in any IR to an SSA IR but then we saw there's a problem if there's a there's a join point where there are multiple paths that are coming together and the different versions of the same variable that are flowing on the two incoming paths and so the solution to that was that we're going to introduce a notion of a phi node or a phi function and phi functions we said can only be added to the beginning of a basic block and they will be added usually to the basic block that has multiple incoming edges because if there's a single incoming edge then it doesn't make sense but question is how do you add phi nodes where do you add them how do you know where to add them all right and so here's one option which is not such a particular good option which is insert a phi node for every variable at each join point all right so anywhere you see a join point by join point i mean there are greater than one incoming edges to the basic block so anywhere there are two or more join incoming edges i'm calling it a join point and every join point have a phi node for every variable right every variable that is present in the program so so that's one option but that as you can imagine is a little bit wasteful because sometimes i really don't need a phi node and then why am i adding a phi node in that case 

so to see that with an example let's say i have this program b equals three then check whether c equals zero or not if true then do a equals four if false then do a equals five and then here we have a join point right so this is a join point right so now the question is do i need a phi node for a and the answer is yes because the two values that are flowing for a or the two versions of a that are flowing are let's say this is a1 and this is a2 and these are different versions and so we need a phi node which is going to choose between the two versions depending on what was the incoming node so i need a phi node for a and the answer is yes okay do i need a phi node for b and the answer is actually no because it's the same version of b that is traveling on both the paths all right and so the answer here is no so i need an algorithm so i mean intuitively with this example i could figure out whether i need a phi node or not fit for which variables i need my previous algorithm that i just showed you the naive algorithm would have created phi nodes for both a and b and which would have been phase full and the question is how do i can i have an algorithm that basically identifies where i need a phi node or whether i don't need a file so i'm not going to discuss you know the most efficient algorithm to do so but at least i should define the criteria the which basically i should use to decide whether to use put a final there or not 

okay so this criterion is called the path convergence criterion and we say that we need a file node for variable a at basic block z or node z if and only if all right and so i'm going to list a bunch of conditions and if and only if all those conditions are true am i going to add a file node for variable a at basic block node z all right so the first condition is there should be a block x that contains the definition of a all right so by the way this here this is z and the question is do i need a file node or the form ak equals five ai comma aj where i'm assuming in this particular diagram that there are two incoming edges but in general there could be any k in any any greater than one number of incoming edges because it's supposed to be a joint point and the question is do i need a file node for a in this case and any final is going to look like ak equals phi of all the different versions and the number of arguments to the final will be the number of incoming edges in this case the number of incoming edges is two so the number of arguments to the final are a are also two ai and aj so i need a final of this form at z if a there is a block x containing a definition of a so let's say a equals something something 

similarly there is a block y which is different from x which also contains a definition of a so a equals something something and notice that i'm saying y should not be equal to x so y should be a distinct node a distinct block in my program 

and and there are non-empty paths pxz and pyz so these these squiggly lines here are representing the paths pxz from x to z and pyz from y to z so there are non-empty paths pxz and pyz from x to z and y to z respectively okay so in other words there should be a path there should be a reachability possible reachability from x to z and there should be a possible reachability from y to z notice that it is possible that these paths actually have cycles in them and things like that and that's perfectly okay all right so if there's any such path it may have cycles or not doesn't matter but if there is any such path we're going to consider that 

the fourth requirement here is that the paths from the node x where the first definition of a lives and the node y where another distinct definition of a lives to z which are pxz and pyz should not have any node in common except z so for example here is an example of path pxz and notice that it has this node let me call this node let's say w so it these nodes pxz and pyz have w in common and so if they have w in common then we don't need a phi node at z intuitively instead of z the phi node should have instead appeared at w and so we don't need a phi node at z so the condition for there to be a phi node at z is that the there should exist paths from x and z except from x to z and from y to z such that they have the only node they have in common is z notice that there may be other so for example you know maybe this path from x to z that i just showed which goes through w and this path from y to z also goes through w both of them have an extra node in common which is w which is a non-z node and so we don't need a phi node but let's say in the program there was yet another path that was going from x to z which didn't go through w and didn't go through any other nodes that pyz goes through in which case we would need a phi node at z for a 

so to summarize a phi node must exist at a node or at a basic block z for a variable a if there is a block x containing a definition of a there's a block y not equal to x that also contains a definition of a in other words there are two distinct basic blocks that contain a definition of a and there are non-empty paths pxz and pyz from x to z and y to z such that the only node they have in common is z if all these four conditions are met for any existings of x y pxz and pyz then a phi node must exist at z for a and if it does not exist then then the ssa representation would be ill-formed so now we will you know so from so you can think of this as a set of constraints that a well-formed ssa program should have these four constraints and if these four constraints are satisfied then the phi nodes have been correctly added at every place and that these if you know even if there exists x y and z such that these constraints are not satisfied then then that program is not a well-formed ssa so how or the phi nodes have not been properly added i mean there are more things to add to make it ssa for example we'll have to rename the different definitions of a but at least from the perspective of adding phi nodes these are the you know this is a requirement for a well-formed program that has the correctly correctly added phi nodes 

all right so from here we can derive an iterative fixed point algorithm that converts a given program into a well-formed program which has the right set of five nodes and the algorithm is very simple you just identify three nodes x y and z that satisfy all the four conditions that we had on the last slide which is that there are exist non-empty paths from distinct blocks x and y to z such that x and y contain definitions of a and the paths the non-empty paths from x to z and y to z do not have any node in common except z then z must contain a phi node and if z does not already contain a phi node for a then insert this this phi node for a in z right so insert this phi node for a in z which is and notice that the number of arguments the a arguments in the phi function is equal to the number of predecessors of the basic block z so if the basic block z has two predecessors then there will be two arguments if it has three predecessors it'll have three arguments to indicate the number to indicate the number of predecessors in the as arguments of the phi node so this is this is one algorithm this is a fixed point algorithm a fixed point algorithm is characterized by a set of constraints and an algorithm that keeps running until those constraints get satisfied such that at every step of the algorithm we make some change such that we move towards satisfying that constraint it is instructive to note that this algorithm actually suffices to construct a program that has a required set of phi nodes based on our requirements it also inserts a minimum such set of phi nodes this is another interesting property of this algorithm and one thing i would like to point out here is that when we insert a phi node for example we insert this assignment from a to phi a comma a comma a then this assignment also becomes becomes an extra definition in the program so for example this new node z which contains this phi node now can take on the role of x or y for some other set of triple of x y z right because this phi node is considered a new definition in the program and so you keep doing this so the insertion of one phi node can cause insertion of more phi nodes downstream in the program and and if you keep doing this you will you will eventually end up at a fixed point because there will be enough number of phi nodes in the program such that all the four constraints are satisfied and we'll have the required number of finals now once again this is not the most efficient algorithm later on in this in this lecture series you'll find we'll discuss a more efficient but also a more complicated algorithm to identify the set of phi nodes that need to be added to a program so that you know so that it becomes a well-formed
