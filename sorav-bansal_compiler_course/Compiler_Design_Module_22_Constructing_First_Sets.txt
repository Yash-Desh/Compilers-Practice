Title: Compiler Design Module 22 : Constructing First Sets
Video ID: CLQ-Lv2o-Cg
Detected language: en
------------------------------------------------------------

So last time we discussed how given an LL1 parsing table we could implement an LL1 parser. Now our next discussion is how to construct such an LL1 parsing table given a grammar. So to be able to construct an LL1 parsing table we basically need to identify two types of quantities for each non-terminal and terminal. One is first sets and the other is called follow sets. So what is first sets? First sets are what are the set of tokens that can be produced in the first position, in the leftmost position for any non-terminal or terminal or string of non-terminals and terminals. So for example first of so if I have E produces open parenthesis T close parenthesis then first of E equals the open parenthesis token because E when it produces something it may produce an open parenthesis as the first token and so open parenthesis would be a part of first E okay and there could be more rules so there could be maybe E also produces an integer in which case int would be a part of first E so if E produces int there's another production then first E would get expanded to open parenthesis and right so basically first is supposed to represent what are all the tokens that can appear in the first place in the leftmost place during any production of this non-terminal. This first is not defined not just for non-terminals but also for terminals and for terminals the definition is really simple for example a terminal may produce a terminal itself so for a terminal the set of tokens that may appear at the first position is that terminal itself so first T equals the set T and first is also defined for a string of terminals and non-terminals so for example I could say first of a1 a2 an maybe this is a string of terminals and non-terminals maybe a1 could be a terminal or a non-terminal a2 could be a terminal or a non-terminal and so on and this would be equal to you know whatever is the first character that a1 can produce would be the first character that a1 a2 a3 an can produce also if a1 could produce epsilon then whatever is the first character that a2 can produce is also the first character that this whole thing can produce so let me call a1 a2 a1 an and alpha alpha string which is of and a string of non-terminals and terminals then first of alpha is must be a superset of first of a1 also if a1 produces epsilon then first of alpha must be a superset of first of a2 and so on so if a1 and a2 both produce epsilon then first of alpha is a superset of first of a3 and so on if you're going to see this in more detail but it's but for now I want you to find understand that first is defined for a non-terminal a terminal or a string of non-terminals and terminals which would be the rhs of a production for example or part of just like first we also define a follow set and I'm going to define it in the next module today we are only going to discuss first set but it's good to know that we're going to also later on define follow sets okay so to provide some intuition of why we care about first set and follow set consider any non-terminal a in your grammar also consider a production a produces alpha which is part of your grammar and consider any terminal t now we are interested in constructing the ll1 parsing table recall that the 11 passing table has cells of the form a comma t where a is a non-terminal and t is a terminal token and we want to know what should we put at that particular cell in this table 11 passing table should we put alpha should we put some other production or should we leave it blank and so we say that we will put alpha in this cell so ta comma t equals alpha if either alpha can produce in one or more productions the terminal t followed by any beta beta could be a string of terminals and non-terminals but t is a terminal token so if alpha can produce t as the first token in any production then any sequence of productions then t would belong to first alpha and also we would say that t at should be equal to alpha um in other words because we know that alpha can produce t and uh you know and a producing alpha is a possibility uh and if the next lookahead token is a t then we know that we should put alpha there hopefully there are there's only going to be a single alpha if the grammar is 11 parsing parsable single such alpha if the grammar is not 11 possible there could be multiple such alphas in which case we would just say that the grammar is not 11 possible and so we will not be able to construct an 11 parsing table for such a grammar further if uh so in other words if alpha can produce t as the first character that is exactly like saying that t belongs to first of alpha as we have defined first and how to compute first we're going to discuss later but the definition of this first set for a string of terminal and non-terminal exactly this that alpha can produce t in the leftmost position of uh of a derivation also if alpha can produce epsilon which means alpha can produce an empty string in any sequence of productions or in any derivation and and a to a can produce alpha then if there exists a production such that it can produce s can produce some gamma which is a string of terminals and non-terminals some uh the non-terminal a which is the non-terminal that we're considering and then followed by a terminal t and then some delta which could be a string of terminals if there exists any such derivation notice that this is not a complete derivation there's some intermediate step of a derivation where there are some non-terminals and some terminals in the in the derived string but if we can have such a situation where there is an a that is followed by a t and it turns out that alpha can produce epsilon then we would set t a t to be alpha the idea the the the reasoning here is that if the next look ahead token is a t and capital a could have collapsed into the empty string through alpha and you know starting from the start symbol there was a derivation that could follow t with uh follow a with a t then if the next look ahead is a t then a can produce alpha is the right derivation to use in this case right because a can produce alpha alpha can produce epsilon and and there exists a derivation that can produce p after a so that this must be the option possibility and that in that case we would like to set t a t to alpha we're going to see more in more detail about how follow sets work and how to compute them but it's good to know why we are considering first and follow sets once again first sets are important because we want to know whether this terminal can be produced as the first leftmost token from by deriving a particular non-terminal or and follow is important because if that non-terminal can collapse into an epsilon we are interested in knowing whether a token can follow this particular non-terminal a in any derivation from the start symbol or not if so then we know that this is the right derivation for it because it collapses it into epsilon okay so that's the intuition behind computing first and follow sets so today we're going to discuss first sets and once again first is defined for some terminal or non-terminal so for let's just consider for simplicity x to be a non-terminal so first of x is the set of all tokens t such that t can be derived from x t alpha can be derived from x all right which means x can derive a string of terminal and non-terminal such that t is on the leftmost position in that string further if x can derive epsilon then we say that epsilon belongs to first of x all right so if x can derive the empty string then we say that epsilon is also part of first of x this is just by definition recall that when we defined first we were talking about all the tokens in possible tokens epsilon is not a token epsilon just represents an empty string and we say that epsilon belongs to first of f if and only if x can derive the empty string okay so this is just by definition so first of x is all tokens t such that x can produce t on the leftmost position union epsilon such that x can if x can produce epsilon or x can derive epsilon all right so how do i compute first of x for any given x well x could be a terminal or a non-terminal as we discussed so first of all we say that for any terminal t it's very easy first of t is just t because the terminal can only derive itself and that it would be present in the leftmost position for it is for any derivation for that particular terminal so first of t is just the set single ten set t further if x can derive epsilon or x can produce epsilon in a single production or x can produce a 1 a 2 a n such that epsilon belongs to the first of a 1 and a 2 and a 3 right so if x produces epsilon then it's easy epsilon belongs to the first of x right this is just by definition okay but notice that here the production is a single production whereas here we said there could be a potential derivation which could involve multiple steps so to capture that we say if x can produce you know if there's a production in the grammar of the form x produces a 1 a 2 a n where a i's can be either terminals or non-terminals and it is true that epsilon belongs to the first set of each of these a i's all right so i'm defining first sets recursively so i'm trying to define first of x based on the first set of assuming i already know the first sets of a i and then i can kind of keep doing this inductively and so if epsilon belongs to first of a i for all i then epsilon belongs to first of x because if epsilon belongs to first of a i for all i it basically means that each of the a i's can produce epsilon right that's the definition the epsilon will belong to the first set of a non-terminal if that non-terminal can produce an epsilon and so it basically so if epsilon belongs to the first sets of each of a i's for all i's belonging to one comma n then x can produce epsilon and thus epsilon is part of first of x right so this is another rule that we are going to use and the third rule we are going to use is the following that if x can produce a 1 a 2 dot dot an alpha so if there is a production of the type a 1 dot dot an alpha and epsilon belongs to first of all the a i's a 1 dot dot an which means this entire thing can produce epsilon okay this entire thing can produce epsilon then whatever is the first of alpha is also the first of x and assuming that first of alpha is not epsilon because if the first of alpha is also epsilon then that would be case number two but first of alpha is not epsilon then whatever is the part of first of alpha will be part of first of x as well right so that's what this means first of alpha is a subset of first of x okay which means that whatever is in first of alpha should be put into first of x if you see such a situation okay so let's look at the algorithm once more in full concrete sense the three rules to the algorithm the first rule says that the first set of a terminal token t is just the terminal singleton set t the second rule says if x produces a 1 a 2 dot dot dot an this such a production exists in the grammar and epsilon belongs to the first sets of a i okay so epsilon belongs to the first sets of a i let me just correct this for all i in 1 to n then epsilon also belongs to the first of x because now x can also produce epsilon right because each of the a i's can produce epsilon which also means that x can produce epsilon finally if there's a production in the grammar of the form x produces a 1 a 2 dot dot dot an and then some string alpha such that epsilon belongs to first of a i for all i between 1 and n which means that each of these a i's can produce epsilon then first of alpha is a subset of first of x which means everything that is in the first of alpha is also in the first of x because anything that x can produce alpha can produce in the first position x will also produce x can also produce in the first position right so that's the definition of first how does the algorithm work well i just explained to you some rules that we are going to use how does the algorithm work well we're just going to keep applying these rules until we reach a fixed point right so we're going to keep looking at each of the production so initially initialize the first sets to empty except that we will initialize the first set for all the terminals as explained in the first step and then we're going to look at each production and anytime we see a production we're going to check whether this is true or not you know whether this rule fires and if so whether first alpha is a subset of first x or not if it is not then everything that is in the first step alpha we just add it to first of x and we keep doing this until we reach a fixed point notice that when we reach the fixed point all three rules will be satisfied by all the productions in the grammar and at that point we have our fixed point solution for the first sets for each of the non-terminals and terminals okay so let's work on an example and figure out what are the first sets for this grammar this is the same grammar that we have been using so far for the grammar of expressions and terms we have also left factored this grammar so that it becomes LL1 parsable and we want to find the first sets of each of the non-terminals and terminals so recall that the terminals are plus star open and close parentheses int and dollar and the non-terminals are e x t y and s so let's first figure out what are the first sets of each of the terminals well that's really easy first of int is just the singleton set int first of plus is the singleton set plus and so on so the set the first sets of terminals are really easy now we want to figure out what are the first sets of each of the non-terminals so using the rules so we're going to look at the first production here which is e produces t x and from here we are going to get that whatever is the first set of t that should also be the first set of e right so that would be our first rule which says first of e is a superset of first of t so anything that's in first of t should also be in first of e because e can produce t in the first position also we had a rule that if t could produce epsilon then anything that's in first of x should also be in first of e so we check whether can t produce epsilon well so far we don't think that t can produce epsilon in first t produces either an int or a open bracket in the first position so t cannot produce an epsilon actually and so we we don't we so this is enough then we look at what is first of t all right so first of t is what is what can t produce well we look at the first derivation of first production and says int y so what is for the first of int y that's just int so we add int and then what's the first of open parentheses e close parenthesis that's just open parenthesis as we put the open parentheses in the first of t and that's it then we look at first of y the first of y firstly y can produce epsilon and recall that if y can produce epsilon then epsilon must be in first of y so we first put epsilon into the first set of y and then y can produce star t so we put star in the first set of y okay then we look at x and so x can produce plus in the first in the first position or it can produce epsilon and so epsilon is in the first set of x we are not done yet you know the when we first said first e is a superset of first t at that point t was just an empty set right so we first e is just empty there but so we haven't reached a fixed point yet so we're going to try this again and now again you're going to apply this rule and say first e should be a superset of first t so anything that's in first it should be put in first e and right now first is empty but first t has two elements intent open bracket open parentheses and so we're going to say first e equals int and open brackets open parentheses and at this point actually similarly if we try to find what is first s we are going to find that first s is basically should be a superset of first e and so we also get this relation that first s equals first e equals int and open brackets so these are all the tokens that can appear in the first position in a derivation of s or e and at this point this set of first sets are actually a fixed point solution so you can you can check that all the equations all the three rules that we had in a previous slide are satisfied by this solution of first sets for each of the non-terminals and terminals okay so we have learned how to compute the first sets we also have some intuition of why this is important because if something is in the first set of a particular rhs of a derivation then we know that then we can use that to construct our llv and ll1 parsing table such that that derivation is used if the next token is one of the first sets of that particular rhs
