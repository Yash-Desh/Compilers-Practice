Title: Compiler Design Module 166 : Undefined Behaviour Semantics
Video ID: yK5kU7uV6Qg
Detected language: en
------------------------------------------------------------

So, I am going to talk about undefined behavior semantics in programming languages and why do we need undefined behavior semantics in programming languages. So, let us look at this example and this is you know on the left hand side column I am using an expression in a programming language and you can also call it a high level programming language and an example of a programming language is the C programming language. So, I can say I can have an expression which says x equals y by z. Now what does the programming language say what should happen if z equals 0 right so that would be the divide by 0 error that you know divide division by 0 should be should have some kind of error semantics because it is not a well defined result and so you know the C programming language has many choices and designers of the C programming language had many choices on what to do when z or the denominator of a division operation evaluates to 0. One option was that you just say okay you are going to actually explicitly throw an error to the programmer saying oh you try to divide by 0 too bad and I am stopping your program. Another semantics could be that if it is a divide by 0 you just ignore it and as though it is a no-op so you just say oh x is not even modified or the third option is that I can just set x to 0 and these are all kind of different alternatives that a programming language can take because if something is not well defined the language designer can choose what semantics he or she wants that suits his or her particular environment. And so while some languages will give you a well defined semantics for such error conditions for example a high level language like may be Java or Python would say oh if you divide by 0 then I am going to always throw an illegal or divide by 0 exception. And so that is a choice that some languages made and then there are some other languages like C and C++ which said oh I am not going to explicitly say what will happen here. I will leave this choice to the runtime to the implementation to whatever you know is the environment under which this program is running. What does that mean? Well if this program is running on an x86 machine it can behave differently from if this program was running on a PowerPC machine from if that program was running on an ARM machine from if that program was running on an Arduino or etc etc. So I mean depending on the particular environment that you are running this program this particular error condition could behave in different ways and that is exactly what we call you know that is roughly what we call undefined behavior some people you know there is also something called implementation defined behavior etc. But I am going to you know kind of say what is undefined behavior very you know I am going to give you more examples to motivate the need for undefined behavior. So for example one implementation could be oh if z equals zero raise the divide by zero exception and if z is not equal to zero do the computation x equals y by z in the lower level assembly implementation for example in the assembly implementation the divide operation may have may be implemented by the div opcode or something like that. And here is another implementation that says oh just do x equals y by z in the assembly program as well and depending on what the assembly program semantics say for example the assembly program semantics may say that if you divide by zero I am going to actually generate a hardware exception and that is going to crash the machine. So in that case it is assembly semantics that basically win here and at the C programming language layer the programming language or the abstract machine says I don't really care what I mean if that is an error condition anything can happen I don't really know what will what may happen it depends on the particular implementation what it wants to do for example one particular implementation may want to explicitly raise the divide by zero exception and let the programmer know another implementation may just crash the entire machine all together not just the program with the entire machine yet another implementation could just ignore the error and so that these kind of semantics where you want to say that if the certain error occurs anything can happen these are called undefined behavior semantics all right look so anything can happen semantics all right also called catch fire semantics so if there's a fire somewhere which means you know some exceptional situation occurred then what should happen that's what undefined behavior says it is the catch fire semantics you know anything can happen here and it depends on what the implementation is. Here's another example and this is a very very consequential example let's say X equals Y I so I'm just indexing into the array Y using an index I and this kind of an operation which is you know just dereferencing memory at a certain offset is very common in our programs and different languages use different semantics for these kind of operations for example Java would say that if I was out of bounds or out of range of the array Y then it would explicitly throw the out of bounds exception or out of array exception or let's call it just the memory exception I'm just using Java an example there are many other high-level languages that would also do that but this this kind of explicit semantics of what should the programmer program do what should the machine do if the index was outside the range of the array being accessed comes as a cost the cost is the runtime cost of checking so each time a programmer writes an expression like this the machine or the compiler needs to basically check what is the value of I and how does it relate to the upper bound and the lower bound of Y and if it is out of bounds for of Y then it needs to raise an exception and so there is an extra two or three instructions that will get generated and that would cause an extra runtime cost and but this is the choice that a language like Java may on the other hand a language like C doesn't want to pay this extra cost because it just says most of the time the programmer is going to do the right thing it's only some of the time that the programmer is going to do the wrong thing and so it is performance inefficient to keep checking every time the programmers does Y I right and so so the C takes the other extreme view it says I'm not going to check whether I is in the range of Y or not if it is in the range I'm going to return the right result if it is out of range then it is undefined behavior and once again what is undefined behavior anything can happen all right anything can happen the either the program can crash or the machine can crash or the program or an exception can be generated or a segmentation for depending if it's a Linux operating system something else can happen if it's a Windows operating system something else can happen anything can happen I'm not going to specify what happens if you do this wrong thing which way you basically exceed the bounds of the array index for example if it's possible that nothing happens I mean no the programmer neither a segmentation fault happens nor the machine crashes it just returns some garbage random value from the memory space of the computer all right and this and this is actually happens because this is actually the basic basis of many security exploits which are you know grouped under the category of buffer overflow attacks and so a buffer overflow attack occurs because of these undefined behavior semantics in a language like C and language like C basically you chooses to use undefined behavior semantics because it doesn't want to pay the performance cost of checking whether I belongs to the range or not and maintaining that state all the time on the other hand a language does check all the time and so these kind of security problems will not arise in a language like Java but Java programs would consequently be slower than C programs typically so here's one implementation the first implementation actually checks whether I is out of bounds or not if I is less than zero or I is greater than equal to y dot size then you raise the memory error otherwise you basically return the value and another implementation doesn't care it just says y i and you know it just adds i to the address of y and returns the result and if i was belonging to the valid range it would return the correct result if i was belonging outside the correct range depending on what y plus i evaluates to it can either cause a segmentation fault or it can return some garbage random value so this again falls in the domain of undefined behavior here is another example x equals y plus z now this plus here is arithmetic addition and let's say in language like C and we know that y and z and let's assume y and z are signed integers so y and z are not infinite integers right I mean y and z are you know if they are maybe 32 bit integers or 64 bit integers so let's assume they are 32 bit integers so what happens if y plus z overflows the 32 bit value what should happen in that case now one option here is that the C abstract machine could have said whenever you do y plus z and it overflows and there's a 33rd bit which is non-zero then you just discard that 33rd bit and pretend as though that 33rd bit was zero these are called wrap around semantics and that's the typical kind of implementation that you would find on a two's complement machine and this kind of a wrap around semantics can be you know can be represented using this circled plus so y plus operator circle z which basically you know plus operator basically explicitly says that it's a two's complement addition with wrap around semantics where you discard a 33rd bit in case of an overflow but there are some machines which do not use two's complement maybe they use one's complement maybe they use some other representation and so the C designers didn't want to basically explicitly say what should y plus z evaluate for example there could be a machine which implements saturating arithmetic so it may say that if it overflows then the value that you get returned is not the wrap around value but actually the maximum value that 32 bits can hold right so if you basically say you know two to the power 32 minus one plus one then you don't expect the answer zero you actually get just two to the power 32 minus one because that's the saturating value that's the maximum value that you get and that's called saturating arithmetic and so the C designers the high level language designers once again left it as undefined behavior they said oh by the way if y plus z happen over happens to overflow then it's an error condition it's an undefined behavior and anything can happen including the fact that the machine can crash or it can return a wrap down value or it can return a saturating value or something else can happen that's undefined behavior basically means that this is an error condition it shouldn't happen but if it happens and all bets are off and I cannot make any guarantees on what your program will do in that case on this abstract machine of the C programming language so why do we need undefined behavior semantics I think I've already motivated this by comparing C and Java one language doesn't use undefined behavior semantics that use this well-defined error semantics and the other language like C uses undefined behavior semantics and that allows the C compiler to generate faster code for the C program and Java programs are typically slower in comparison so let's look at some examples what kind of transformations or what kind of compiler optimizations does undefined behavior or ub enable or facilitate so let's say I have int n some variable and I'm you know evaluating n somewhere here and then I have a loop which says for int i equals zero i is less than n plus one i plus plus now I'm already storing n let's say in a register and now for this particular loop I have to compute n plus one in a separate register and keep comparing on every iteration so one obvious kind of you know one kind of optimization that comes to mind here is that you can say oh just convert i is less than n plus one comparison to i is less than equal to n and you can imagine that this should be correct and question is is this legal and the answer is well imagine what will happen if n was equal to int max right if n was equal to int max so int max is let's say if on a 32-bit machine that would be two to the power 32 minus one so all ones right and so if n equals int max then the first program would be int max plus one and if I assume wraparound semantics that would be just zero because you know two to the power 32 minus one plus one would be two to the power 32 and that won't fit in a 32-bit number and that would make it to zero and so the for loop would look like i equals zero i is less than zero i plus plus which means the for loop never executes on the other hand look at this loop here it would be i equals zero i is less than equal to int max i plus plus and note and in this case the loop will go on forever it's actually an infinite loop because you know i goes from zero all and keeps incrementing till it becomes int max and then it increments again and then it becomes zero again so i would everything i would always be less than equal to int max and so this loop goes on forever so these two programs are completely different in what in their observable behavior in the behavior that they produce and so this transformation may will not be legal if i assume one particular semantics of the addition operation which is the wraparound semantics or the two's complement semantics but fortunately this transformation is very common it's widely used and it's widely used because it's a legal transformation in c because c says that signed integer overflow is undefined behavior it says that if n plus one overflow that would be undefined behavior and if it's undefined behavior anything can happen so if the programmer has written code where at the program actually can potentially do int max plus one then that program itself is illegal and so the compiler can do whatever it likes and in that case it can also kind of convert that into an infinite loop and so we are okay all right in other words it should never happen from the compiler's perspective that the signed integer overflow occurs so signed integer overflow should never occur from and if it occurs that would be illegal and this is exactly what the undefined behavior semantics or ub semantics state here's another example and this is an example for type based strict area semantics so let's say i have two arrays or two pointers one is an integer a pointer to an integer value or set an array of integer values another is a pointer to an array of float values and i have this loop it says for int i equals zero i is less than n i plus plus bi plus equals star a so in this case i am dereferencing a in this case i am dereferencing b at a certain at a given offset i and now the question is can i improve this loop can i make this faster because i have two memory accesses on every iteration of the loop i but one of those memory accesses is going to be the same memory access which is star a and so can i change this to now take star a into a register let's say r or some kind of you know easily accessible value location let's say r is a register in this case if i'm talking about assembly instructions on cpus then for int i equals zero i is less than an i plus plus bi plus equals r so this is just you know i just kind of did some loop invariant code motion and just kind of cached the value and then kept using it over and over again the question is is this legal and the answer is this is not legal if it is possible for bi to actually alias with a all right so if bi could kind of point to the same location as a then across different iterations it's possible that one of these bi's actually writes to a and so that means i should keep reading a over and over again because you know it's possible that i modify star a in one of these iterations and if i modify it then you know i need to refresh that value and so if i do this then i'm not going to be refreshing the value on every iteration because i and so that would be so this these two programs would be in equivalent because once again if bi can alias with a then these two programs are in equivalent because a's value can change star a's value can change across you know because of the right to bi however uh the c programming language says that if uh this code like this then because these are two different types this is a floating point type b is referring to a floating point type array and a is referring to an integer type array then it would be undefined behavior if b and a alias to the same location it shouldn't ever happen that i'm basically referencing an integer type value where a float type value could have been present and this kind of type-based strict area thing is basically meant to accommodate the fact that different compilers or machine environments may use different representations of different types for example a floating point type could be representing represented either through i triple e 754 standard or something else and i don't want to make assumptions on what they are and if the programmer is basically kind of mixing and matching the same region of memory i sometimes using it as a float and sometimes using an integer then that would be undefined behavior and in other words that should never happen in a legal program when i say that should never happen it doesn't mean that the compiler will flag it or compiler will generate an error because compiler cannot check everything so what the programming language designers basically did was they said it's a responsibility of the programmer to ensure that there is no undefined behavior in their program because the compiler cannot check everything right the compiler for example compiler cannot check whether i belongs to the range of d or not in general that's an undecidable problem and so what the designers in case of c decided was that we just leave that responsibility to the programmer in the case of java they decided that we're going to do runtime checks to make sure that you know a programmer is not doing the right thing so in this case this transformation is legal if you make the type-based strict aliasing assumptions which are based on the type-based strict aliasing undefined behavior semantics of the c abstract machine here's another example so let's say i have an array a which is you know size 256 and another you know small array b which has size 1 and i initialize a pointer p to a and then i have a loop from i equals 0 to n and i'm also incrementing p every time and i'm saying star p plus equals b0 it's very similar to the example that we've seen before but here instead of actually explicitly dereferencing ai i'm actually maintaining another pointer p and in this case the semantics are that because p is based on a and i'm incrementing p any number of times irrespective of how many times i increment p it should never happen that p actually points outside of a in other words what the c language says is that p is based on a p is based on a and because it's based on a it should never exceed either underflow or overflow the range of a and so because of that i can actually do this transformation which is i can basically change i can basically catch the value of b0 into a register r and instead of star p plus equals b0 i can do star p plus equals r why can i why is it legal because why is it legal to do so now because i know that p cannot alias with b0 because p should belong to the range of a and the range of a is basically this range from 0 to 255 of the a array and it can never go into get into b and if it gets into b that's undefined behavior so the compiler doesn't need to worry about that you can just assume the compiler can just assume that the programmer has written the correct program which does not have ub or it's a ub free program so this answer is that this is legal under the based on semantics nc finally for everything that we have discussed so far is basically at the programming language level but most of our transformations like all the optimization that i described in our previous slides are actually usually done at the intermediate representation level so how does one capture the pl level semantics at the ir level so first of all you know you basically add instructions to your ir which basically mimic the behavior of the instructions at the c language for example llvm also has a divide instruction or the divide opcode and that can also trigger ub so llvm also has its own ub semantics and llvm's code will also trigger ub if you do a divide by zero and so that then becomes because of these semantics in llvm it's easy to translate from c to llvm now you may say okay but that's just for c to llvm what if i want to translate a java program to llvm well if you want to translate a java program to llvm what will happen is that in the llvm in the generated llvm you would have explicit checks for that particular index variable before you actually do the or for that denominator being not zero before you actually do the division operation also when you know another example that i use for arithmetic overflow or sign arithmetic overflow i'd like to point out that in c it's only signed in integer overflow that is undefined behavior unsigned integer overflow is not undefined behavior and this is just to provide flexibility to the programmer so that you know if the programmer wants to use explicit wraparound semantics for his or her variable then he or she should use the unsigned variable and not the signed type for the variable okay in any case there are three flavors of the addition instruction or addition opcode in lvm one is just the plain addition y plus z which would have let's say the wraparound semantics so if it overflows then you just discard the 33rd bit or the most significant the carrying the overflow bit the other is the no signed overflow variant of the add instruction so nsw stands for no signed overflow add nsw y comma z can assume that y plus z will not overflow and if it overflows then it is ub so in this case just like c ub if signed overflow okay and the other kind of nice thing about lvm is that also gives an unsigned overflow variant of this instruction so this is nu w so no unsigned overflow and that is ub if unsigned overflow recall that i said that in c there is no notion of nu w or c only supports signed integer overflow ub unsigned integer overflow is actually wraparound in c but because llvm caters to more languages in c it actually treats signed and unsigned more uniformly and if somebody wants to implement unsigned integer overflow then they can use the nu w opcode in llvm so basically what's coming out is that whatever are the semantics in the programming language are also being reflected in the ir to be able to reason about the transformations kind of transformation that we discussed in the previous slides and finally you know another example that i used was based on relationships based aliasing reasoning in c and the same kind of reasoning can also be done at the ir level so when you're doing the transformations you already know which language that this llvm come from and based on that you can basically make assumptions on what is based on what and based on that you can make you can do the transformations overall then you know when you kind of move from one abstract machine to another abstract machine let's say you move from the c abstract machine to the llvm abstract machine you may lose some information and so in these all the three things i said described i'm basically saying that you know the programmers or the compiler developers had made have made good efforts to ensure that information is not lost for example you know it's possible to basically use these custom operators or to implement the based on relationship based reasoning but some information is lost in any case and i'm not going into details but not it's obvious that whenever we lower one piece of syntax to another piece of syntax to a lower level syntax then not all programming language ub semantics are captured and some programming language ub semantics are forgotten at the ir level what what are the implications of forgetting some ub semantics well it doesn't make the program any wrong it just makes optimization opportunity it just kind of kills some optimization opportunity so if the translation from a high level syntax to a lower level syntax forgot some ub in the program in other words for example you know when i was doing arithmetic maybe i just use add by comma z instead of using add nsw by comma z then what i'm i'm not making the program any wrong it's the correct still the correct program because you know i've just made one particular choice of a wraparound semantics in case the error condition occurs but but the problem here is that you may have lost some optimization opportunity
