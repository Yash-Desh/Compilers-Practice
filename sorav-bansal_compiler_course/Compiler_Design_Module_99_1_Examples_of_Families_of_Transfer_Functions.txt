Title: Compiler Design Module 99 1 : Examples of Families of Transfer Functions
Video ID: 5H2t_uol4nk
Detected language: en
------------------------------------------------------------

So, I'm going to discuss another example of a family of transfer functions to check whether this family of transfer functions is actually obeying the three rules that we laid out in the previous module and we're going to take the familiar example of constant propagation for a single variable x and recall that the semi lattice formed for this particular data flow analysis was consisting of these values top and bottom, top was basically representing the unreachable value or the program point hasn't been reached so far, then all these constant values which represent all the possible constants that this variable x can take assuming x is an integer variable then maybe it can take all these integer values and then bottom basically says that we cannot be sure that it is a constant and so it may not be a constant and that's what bottom means and now if we look at all the transfer functions that we discussed one of the transfer functions was that if the program involves a statement which assigns a constant to the variable x then the transfer function was defined as follows that given an input value in the output value should be less than equal to if we are basically modeling this as a set of constraints or equal to if we are modeling this as an algorithm then if in equals top which means if in is not reachable then even out is not reachable else it is just constant because we know that x is equal and equal to c is going to always produce a constant in the variable x and the transfer function says that if in is top then out is also top otherwise out is c now this is one transfer function similarly there was another transfer function which was for another statement where we are not assigning x to a constant c but some arbitrary expression which I am denoting with this dot dot dot and in this case the transfer function looks like the the one shown here which I'm labeling as the second transfer function the first one as I'm calling it the first transfer function so it says if in equals top then top just like the first transfer function else bottom because we are assigning some arbitrary expression to x we can't be sure that it is that arbitrary expression is going to be a constant or not so conservatively we just assign the value bottom and this forms our second transfer function in the family of transfer functions for this particular data flow analysis of constant propagation on a single variable finally there was this transfer function which did not involve x I did not write to x at all so for example if you know if there is a statement which just assigns to another variable y or if there was a brand statement or something like that then the out value is just you know a copy of the in value or the transfer function is just the identity function or Fn equals in and so this forms our third transfer function and you know we could potentially make the transfer functions more sophisticated but let's assume that these are the only three transfer functions available and these actually suffice for I mean although they may not give the most precise answer but they actually we have covered all the possible cases that a program statement may be in a three address code IR program and so these are the three transfer functions that we have discussed the first one says you know I'm using this ternary operation this ternary operator question mark in colon as it is used in the C language for example so if x equals top then top otherwise C another transfer function was if x equals top then top otherwise bottom and the third transfer function was the identity function which is FX equals X which was if we are not writing to X at all okay so these are the three transfer functions and I want to check whether it satisfies these three this set of functions satisfies the rules for it to be a family of functions usable in a data flow analysis and the first property was that for all the transfer functions belonging to this family which is this family of three functions is it true that all the functions map you know the same domain of values take a value from the domain of values and return a value again in the domain of values so it's easy to see that because I mean it you know the domain of values is either top bottom or one of the constants and the values that are being returned by each of these functions are also one of either top bottom or one of the constants so we it's very easy to see that all the three functions actually satisfy the first requirement if I look at the second requirement it says that the ID function the identity function should belong to the family of transfer functions and that's also true because the third function is indeed the identity function just returns the input value so that's also easy to check now let's look at the third one which is you know which is slightly more complex it says that for all pair for all F comma G belonging to capital F is it true that the composition of F and G also belongs to capital F now we have three different transfer functions in this family so the possible number of compositions that I can create are you know I can have three values for F so that's three and then I have can have three values three different possible values for G so three cross three nine so you know I need to check three nine different combinations to see whether the nine combinations each of them is belonging to my family of transfer functions or not so let's start from let's say the first combination so F is one and let's say G is two so one basically says that if X equals top then top LC so I'm writing one here and now it's getting composed with two so I first apply one and then I apply two so let's say the first you know if the input was X if I apply one then this is what I get this expression that's shown here and then whatever is the return value from this function I apply two on it which basically means that I apply two on it and so this whole value gets checked against top and if the answer is true then the return value is top otherwise it's bottom because that's the second transfer function that we have now this is the transfer function that we obtain by composing one and two and if I just simplify it slightly then I can see that this this thing in the brackets this expression in the brackets actually just evaluates to X equals top because this this purple expression will be equal to top if and only if X was equal to top otherwise it will be C and so this whole expression actually simplifies to X equals top and that actually is nothing but now it becomes X equals top then top otherwise bottom which actually looks exactly like this second transfer function and so indeed that is just the second transfer functions because just X equals top then give me top otherwise give me bottom and so one composed with two actually gives me two and so here this check passes and so we kind of check all the other possibilities so one composed with two is two similarly you can check for yourself that two composed with one is just one the same argument applies again also because three is just an identity function so anything composed with three is just going to be that so X composed with three is three similarly three composed with X is X where X could be one two or three so these two rules here actually represent six different rules because you know you can replace X with either one two or three and similarly here you could replace X with one two or three so so far we have covered six plus two eight cases there's one there are a few more cases left and those are basically these ones here so one composed with one what happens you know it's you can check it for yourself that's going to give me one and two composed with two is also going to give me two and so overall you know we have exhaustively checked that the composition of any pair of functions is going to give me a function which belongs to the family of functions and so we are we satisfy all the three requirements for it to be a family of functions for the data flow analysis framework
