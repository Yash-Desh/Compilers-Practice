Title: Compiler Design Module 152 : Affine Transform #3 - ReIndexing
Video ID: -m3MLxBwDLA
Detected language: en
------------------------------------------------------------

I'm going to discuss our third affine transform the third primitive affine transform and this is called re-indexing and here is an example to show the third primitive affine transform. So let's say I have for i equals 0 to n y i plus plus y i equals z i and this forms my statement s 1 and x i equals y i minus 1. So if I was to draw the dependency the iteration space and the dependency edges then basically I have this brown line that indicates the progression of the loop iterations and for each iteration point I have two statements by s 1 denoted by the circle and s 2 denoted by the cross and so I have multiple instances and multiple instances of s 2 and in this case there is a dependency from y 0 to y 0 in other words the 0th iteration of s 1 has a dependency on the first iteration of s 2 because that's when both of them access y 0. So I draw an edge from the 0th iteration of s 1 to the first iteration of s 2. Similarly I can draw an edge from the first iteration of s 1 to the second iteration of s 2 and so on and so this becomes my iteration space with dependency edges pictorially speaking. If I was to basically say the same thing using constraints space partitioning constraints I would have to say that whenever there is a data dependence so in other words whenever i 1 for s 1 equals i 2 for s 2 minus 1 then p of i 1 must be equal to p of i 2 in other words then both iterations i 1 and i 2 must map to the same processor id p and this basically when I solve these constraints these space partitioning constraints I get p equals i for s 1 and p equals i minus 1 for s 2 and so basically this circle and this cross must be allocated to the same processor and this is you know pictorially speaking this makes sense all these chains should be present in the same processor the rank the max the rank of the solution of the mapping function is basically one which is equal to the rank of the iteration space as well. So we are we are good on that part and we have found the maximum rank solution and now we are going to do the code generation. So this is another example small example of a space partitioning constraint problem that solves this kind of example and that constructs a fine transform that that which we are going to call indexing. So let's see how the code generated code would look like for this solution to the space partitioning constraints and here is the generated code my simple code generator I basically have my outer loop on p which is the processor space the lower bound of the processor space is minus one because that's the lowest value that p can take the lowest value that i can take is zero and because for s 2 i have p equals i minus one the lowest value for p that this outer loop can take is minus one the largest value that p can take is n because that's coming from p equals i because i can take n as the largest value so p can also take n as the largest value and then p plus plus and then i just paste the original program as the body of this loop except that i predicate each statement with if p equals the corresponding function so in this case it becomes if p this if p equals i only then execute x i equals y i equals z i if p equals i minus one only then execute x i equals y i minus one so this is my simple code generator and now i'm going to tighten the code so my first attempt at tightening the code is just that i basically tighten the loop iteration indices of i and so what i get is i equals maximum of zero comma p because you know p is already known so either i is going to be equal to p or i is going to be equal to p plus one and from that i say that i is greater than equal to p and i is less than equal to p plus one and so that i basically change my loop iteration bounds for the inner loop on i from zero to n to max of zero comma p to min of p plus one comma n and notice when i write it like this the inner loop can have at most two iterations start at p and end at p plus one and at min it can have one iteration and so this is my code generated code with tightened loops it is almost perfect except that we have this for loop and then we have these extra tests and we can eliminate these extra tests by specializing the code by potentially duplicating the statements code and so that's what we're going to do at the very end and i basically find the iterations i try to partition the iteration space of the outermost loop which is p and there are three different partitions that i create one is minus the singleton point minus one for which only s2 would execute then we have the points from zero to n minus one where both s1 and s2 would execute and then i have the singleton point n comma n which basically only s1 executes and then specializing the code for each of these three partitions for minus one comma minus one i only execute x s1 s2 and so this is the code that i get if n is greater than equal to zero x1 equals y0 how do i get n is greater than zero well if i just use you know if i just say that p should iterate from minus one to minus one and then when i try to identify the condition for the inner loop i basically get that the inner loop will execute either zero or one iterations and the condition for it to execute one iteration of s2 would be if n was greater than equal to zero all right and then i also notice that i substitute for p equals minus one in this statement of s2 xi equals yi minus one and that basically gives me x1 equals y0 okay you know because you know the i that we get would be equal to p plus one or something like that and so that if you work this out we are going to get x1 equals y0 okay now we're going to look at the then we are going to have the second partition from zero to n minus one and here we basically iterate from p equals zero to n minus one p plus plus and we just paste the original code because both the statements will execute we just paste the statements without actually doing the test predicate test and say yi yp equals zp because p equals i and yi equals zi so you can just say yp equals zp and then if p equals i minus one xi equals yi minus one and this translates to xp plus one equals yp okay because you know i becomes p plus one so i just substitute i for p plus one and i get x of p plus one equals yp and finally i add the third partition where i say if n is greater than equal to zero yn equals zn once again the if n is greater than equal to zero is the simplified form of the fact that p is equal to n p is equal to n and it's the s1 statement that executes in this case which just which is yi equals zi and because p was equal to n and i was equal to p i can just replace that with yn equals zn i want to make a correction it should be probably x0 equals y minus one one here right so this should have been x0 equals y minus one this would have been the right boundary condition at the entry of the loop okay let me also fix this here and if i look at it you know pictorially then this was my original iteration space let's say this is iteration i equals zero so i have x and z s2 and s1 but i have these diagonal lines of iterations so first i'm going to execute s1 then s2 of the first point then s1 and s2 or the second point then s1 and s2 the third point and so on but there is a the reuse distance between the access to the same element is actually larger here because i have to know i do lexicographically i do the s1 first then s2 then i do s1 and then finally i do the reused iteration with re-indexing i have basically changed this iteration space so that i first execute the zeroth iteration of s2 then i execute the zeroth iteration of s1 then i execute the first iteration of s2 then i execute the first iteration of s1 and so on and now the dependency edges are basically just straight lines straight vertical lines and that basically decreases my reuse distance between data dependencies and this transformation is exactly represented by this particular program and its iteration space is represented pictorially here and we also call this the affine primitive affine transform number three called re-indexing
