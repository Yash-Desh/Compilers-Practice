Title: Compiler Design Module 124 6: Placing phi nodes during SSA conversion
Video ID: E1afi4VsnzI
Detected language: en
------------------------------------------------------------

So, we've been talking about the translation of a program to its SSA form and the first step in this translation was the computation of dominance frontiers. The second step which I'm going to discuss in this module is the placement of trivial phi nodes. So, consider this assignment statement at location x to variable v, v colon equals something and let's say y is in the dominance frontier of x which means y is the first node which is not strictly dominated by x if I do a depth first search traversal starting at x and if y is in the dominance frontier of x that means that there must be another definition of variable v that is potentially reaching y because y is not strictly dominated by x. Now when I say another potential assignment to v, I also include the initial values of v which must be present at program entry. And so, either there's an assignment on this other path to y or the original value, the initial value of v is flowing on this other path. In either case, I need a phi node here. Now I'm going to stage this placement of phi nodes into two steps. The first step I'm going to place a trivial phi node which means I'm going to add an instruction which says v equals v is assigned colon equals phi v comma v comma v dot dot dot where the number of arguments to the phi function depend on the number of incoming edges at the node y. So this is also called a trivial phi node because it's a no op instruction effectively and later on we are going to rename the variable so that it becomes an SSA form, the program becomes an SSA form. Also notice that I'm only going to place the phi nodes at the dominance frontiers and this will satisfy our requirements for a minimal SSA which is the minimum number of phi nodes are placed for all the variables. So once again, if I see an assignment, for example, v colon equals something, then at a node x, then I look at the dominance frontier of x. If y belongs to the dominance frontier of x, for all such y, I add a trivial phi node which looks like as shown in the slide. Not just that, we need to do this transitively. So once we add a trivial phi node to y and let's say z was in the dominance frontier of y, then I need to similarly add a trivial phi node to z and this is because the phi node that I've added at y is basically treated just like another assignment and so just like we placed a phi node at the dominance frontier of x which was an assignment because now y contains an assignment, so we are going to place a phi node at the dominance frontier of y which in this example is z and so on and I'll keep doing this until I place, you know, until I have placed the phi nodes at all the requisite places where the phi node is required by this dominance frontier transitive relationship. The other important thing to note here is that once I place a dominant, a phi node at a particular node, then I do not need to place another one. So there's only for a single variable, there can be at most one phi node at any node because after all a phi node is basically just at the very beginning of the basic block and for every variable I need at most one phi node. So for example, if z was also in the dominance frontier of x, then there'll be only one phi node that will be placed in z. There won't be two phi nodes by virtue of being in the dominance frontier of both x and y. We will only place one phi node and not two phi nodes in this case. Finally, I've shown this placement of trivial phi nodes for a single variable v, but I'm going, I need to do it for each and every variable present in the program and the way I'm going to structure the algorithm is I'm going to do this separately for each variable. For example, I'll do it for v1 first, place all the trivial phi nodes for v1, then I want to place all the trivial phi nodes for v2 and so on and so this can be done separately and independently for each variable. Okay, so the high level algorithm for the placement of phi nodes looks like the following. The place phi function, which places phi nodes in the program, trivial phi nodes takes as input a program and for each variable v in the program, it changes the program by placing the phi nodes for the variable v and so it gives you a new program and this iteratively continues until we have a place phi node for all the variables in the program v. So now I'm going to look at more deeply into this function place phi for var, which takes as input the program and a particular variable and I'm supposed to place the minimal number of phi nodes for this particular variable. So we have already seen what the algorithm is going to look like. I'm going to basically look at all the assignments of v and look at the dominance frontiers of the nodes which contain those assignments and then place phi nodes in those nodes which belong to the dominance frontier. For this, I'm going to maintain a work list and this work list will be the node whose domf or the dominance frontier needs to be examined. For example, if a node contains an assignment to the variable v, then its domf needs to be examined and so the work list will contain that node x which contains an assignment to v and as the algorithm proceeds the work list, the more nodes may be added to work list because after all, we need to do this transitively. So if I add a phi node to a particular node say y, then all the dominance, so I need to consider that new phi node as a new assignment and now I need to look at the domf of y and so on and so I'm going to maintain this as a work list and when I pick something from the work list, I'm going to remove it from the work list but sometimes when I add phi nodes then I'm going to potentially add to the work list. Similarly, I'm going to maintain a set called considered already which will be the nodes that are ever been through the work list. So if I have already considered a particular node x and I've already examined the dominance frontier of node x, then I don't need to examine it again because after all, I only need to place phi nodes at most once for any dominance frontier node for that particular node and so once I have considered a node, I don't need to consider it again and I'm going to maintain what I've already considered in the considered already set. In one way, considered already are the nodes that have ever been through the work list. So anything that has been through the work list, maybe it has been removed, it still remains as part of the considered already. So as the algorithm proceeds, work list may shrink and grow depending on what are the dominance frontiers where the phi nodes have been added but the considered already set only grows because it's a cumulative set of all the nodes that have ever appeared in the work list so far. And finally, I'll maintain a set called has already which are nodes that already have a phi node for the variable in the program. Because these nodes already have a phi node for the variable in the program, I don't need to place another phi node in those nodes of the program and that's because of the property that I need at most one phi node for a given variable at a particular basic block. So this is my high level pseudo code of the algorithm. I initialize the work list which I'm going calling W with the nodes with assignments to V set to true. So initially all the nodes or the basic blocks which have assignments to V, they belong to the set W. So to represent the fact that these nodes belong to the set W, I just say that which set which nodes have their value set to true or it's a map from nodes to boolean values which and a true value indicates that it belongs to the set and a false value indicated it does not belong to the set. So W is basically nodes with initialized the nodes with assignments to V. Considered already recall is the cumulative set for the work list and it is initialized to everything that W has been initialized to. That's the initial value of considered already as the algorithm proceeds considered already will keep growing as we have already said but work list may shrink or grow depending on where what the dominance frontiers we see and has already are the nodes where a phi node already exists for this particular variable V and it has been initialized to empty. So far initially there is no phi node anywhere. The algorithm proceeds as a fixed point iteration where we say that while W is not equal to empty so while the work list doesn't become empty we pick one node from the work list so pick take x from where wx is true and set wx to false so that's the shrinking part of the work list and for each y belonging to the dominance frontier of x place a trivial phi node at y if it does not already exist at y so if a if a trivial phi node does not already exist at y for this variable V then place this trivial phi node at y set has already for y to true because now it there's already a node phi node in y and if considered already if y has not been considered already which means y has not already gone through the work list once then add y to the work list and also add y to considered already so this is this is the transitive step so if i added a phi node to y i also potentially need to add it to the work list if it has not already been added to the work list if it has not been considered already and so this is my high level algorithm to place five nodes it's a fixed point algorithm that maintains the work list maintains a cumulative considered already set for the work list and it has already set to basically ensure that we don't place duplicate five nodes and then it just looks at each node from the work list looks at the dominance frontier and places a phi node if does it does not already exist there and adds y back to the work list if it has not already been considered now i want to point out that this algorithm that i just mentioned is for a minimal ssa but recall that we also discussed another form of ssa which is a pruned ssa which is a stricter requirement which says that you place a phi node only if the variable V will be used in the downstream nodes or in other words if V is live at y and so just just a simple change to this algorithm will also can you know can adapt this algorithm of placing five nodes to a pruned ssa you just need to check whether V is live at y or not and you can run a liveness analysis a priori and recall that the liveness analysis only needs to be a conservative liveness analysis so it doesn't need to be fully precise or anything like that and depending on how precise your liveness analysis is you will you may be able to reduce some of the phi nodes in your program for the pruned ssa representation okay so i'm going to run through an example with our algorithm to see how this works so let's say this is my program it has these assignment statements to V and i'm looking at V and placing five nodes for this variable V and there are two assignment statements one at PCX one another at PCW and then this is this is the rest of the control flow graph i'm also showing the dominance frontiers of each of those nodes example dominance frontier of x is y because there's another input incoming edge to y dominance frontier of y is z because there's another path coming to z some from somewhere dominance frontier of z is u and x because there's a back edge going out from z so dominance frontier of z is x includes x and also includes u dominance frontier of u is empty because probably this is going to exit and dominance frontier of w is z now when i initialize my algorithm i initialize my sets my initial work list is basically the set of all assignments to V which is x and w so that's my initial work list my initial considered already set is anything that has gone through the work list which is also equal to w and has already is empty recall this was the initial state of our sets in our algorithm now i'm going to take one by one and a node from the work list and look at its dominance frontier and place five nodes in those dominance frontier and potentially add those nodes back to the work list and to the considered already set so let's say i pick x first so i pick x first and the dominance frontier of x is y so i place a phi node or phi function at y i add y to has already and this is the state of my sets at this point now the next thing i want to do is i want to check whether y is already part of considered already or not it is not in which case i should add y to the work list and so that's my next step i add y to the work list because i want to consider this assignment and also look at all the five nodes that need to be added by virtue of this phi function added at y so my new work list is y comma w and my considered already set also includes y now because of by virtue of y being added to the work list now once again i need to pick up some node from w so let's say i pick y and i look at the dominance frontier of y which is z and i'm going to add i'm going to check whether z is already part of has already it's not and in which case i'm going to add a phi node to z so that's my next step i pick y i look at the dominance frontier of y and that's z i add z to has already and i add a phi node to z now once again i check whether z is a part of considered already or not it is not in which case i should add z back to the work list so that's my next step i add z back to the work list and i also i add z to considered already now i once again i need to pick something from the work list and examine its dominance frontier this time let's say i pick z and i examine the dominance frontier of z the dominance frontier of z includes u and x u and x so i'm going to place phi nodes at u and x and add u and x to the has already set so let's see that so indeed i picked z and i added phi functions to u and x and i added u and x to has already now both u and x may be part uh so now i also need to transitively add u and x to the work list before before that i need to check whether u and x already belong to considered already or not so you does not belong to considered already but x already belongs to considered already because x already belongs to considered already i don't need to add x to the work list this is because i have already considered x and i I have already added its 5 nodes, its dominance frontiers, I have already added 5 nodes at x's dominance frontier and so I don't need to consider x again but I do need to consider u again. So I am going to add u to the work list and to consider it already and so I am going to add u here and I am going to add u to consider it already and I am going to continue my algorithm. So in this case I am going to let's say pick once again one of the nodes in w, let's say I pick u and when I pick u then I am going to look at the DOMF of u which is empty set and I am going to add 5 nodes there but the DOMF of u is empty so I don't need to do anything. So I am just going to remove u and I am going to add, so nothing needs to be done for u and so has already and everything remains the same just that u gets removed from the work list. And now I go back to the work list and pick the next node and in this case now I have only one option, let's say I pick w, I pick w, I look at the dominance frontier of w, it has z, z already has a 5 node, so z is in the DOMF of w but z already has a 5 node, so I don't need to do anything and that is the end of my algorithm and I don't need to and I have reached the empty work list so I have reached the fixed point and these are the minimum, minimal set of 5 nodes that need to be added in the program that I just showed you. So what is the running time of this algorithm and to be able to reason about the running time of this algorithm that places trivial 5 nodes in the function, I need to first define some quantities which depend on the, on which the running time depends, so one of the quantities is what is the number of assignments in the program and notice that as the algorithm proceeds it adds more assignments, it adds more 5 functions, trivial 5 functions in the program, so there is one quantity which is the number of original assignments in the program which is, which I call A-orig and if I sum the number of original assignments over all the basic blocks x, then A-orig is summation over x, A-orig x, so where x is a node in the CFG or a basic block in the CFG, so this is the number of original assignments but our algorithm actually has a runtime that's proportional to the number of final assignments in the program and the final assignments in the program include all the original assignments and the extra 5 functions that we have added and so the number of final assignments is represented as A-final which is again summation over all the basic blocks A-x-final, so for every basic block x, I can compute what is A-x-final and A-x-final is nothing but A-x-original plus all the 5 functions that have been added to x, so in other words A-x-final equals A-x-orig plus A-x-final. Let's look at this equation A-x-final equals A-x-orig plus A-x-final in more detail. A-x-orig is the original number of assignments in basic block x and A-x-final is the number of assignments added due to 5 nodes in basic block x. Now the number of assignments, the number of 5 nodes that may be added can be as big as the size of the entire program A-orig which is the summation of all the over all the basic blocks A-x-orig. To see this, I mean consider the number of 5 nodes that can be added to this basic block, the number of the 5 nodes that are added here may be due to all of the other basic blocks x, y, z and w and so the number of basic number of 5 nodes added in a single basic block can be as large as the size of the original program. In other words A-x-final can be between A-x-orig which is because of this if there were no 5 nodes added or and can be less than A-orig so it can be A-x-final will be either will be greater than the lower bound will be A-x-orig and the upper bound will be the total number of assignments in the original program because that's how big A-x-orig plus A-x-final can be because we already saw that A-x-final can be as big as A-orig and so you know if you sum it up that that comes out to be roughly A-orig and if I and this is just for a single basic block the number of final assignments in a single basic block if I sum up sum it up over all basic blocks then that becomes A-final and I sum it up over all basic blocks so that can be as high as roughly speaking A-orig square because the number of basic blocks is roughly proportional to A-orig and so you multiply A-orig with A-orig square that's the upper bound the lower bound is summation over all the basic blocks for A-x-orig which is just A-orig and so the total number of final assignments can be quadratic in the number of original assignments in the program and we're going to use this later when we talk about the running time of our algorithm so please keep this in mind so if I want to analyze the running time of my algorithm notice that this this statement here which is take x from wx equals true is executed for every node x where there exists an assignment to a variable v and the number of assignments to a variable v changes as the program executes so this statement actually executes as many times as there are final assignments in the program because after all I execute this also for I add to the work list also those assignments which I which are five functions which are added as consequence of the algorithm so this statement here is executed a final times where we call a final is basically summation of over all the basic blocks A-x the number of final assignments in that basic block so this statement is executed a final times and for each such time that this executes the number of times the inner loop which is this for each y equal belongs to DOMF is executed for the number size of the DOMF of that particular node so this statement this inner loop statement or the inner loop logic is executed A-x final into the size of DOMF x so for any basic block x the number of final assignments in x multiplied by the size of the dominance frontier of x that's the number of times the statement is executed for a particular basic block x and we sum it over all the basic blocks x and that's the number of times this inner statement is executed now what is the what is the typical size of A-x final well it could be as big as a program size what is the typical size of the dominance frontier of a basic block well depending on the program it may be constant size or it could be as big as a program size itself we're going to see how we are going know how we can reason about how big a dominance frontier can be but this this quantity here can be anything from linear to quadratic and we're going to characterize when it's linear and when it will be quadratic but the good news is that it will be mostly be linear sized like linear in the size of the program this quantity can also be written in this more succinct form as a final into the weighted average dominance frontier size where the weighted average is based on the average of the dominance frontier size for the different weights where the weights are dependent on the number of assignments in that node x for with whose dominance frontier we are looking at so this is executed a final times weighted average df size a final can be between linear and quadratic in the size of the original program as we saw earlier the weighted average df size can be constant sized for a given basic block or can be as high as the number of basic blocks in the program so it can be between constant and linear size in the size of the original program and so the product of both can be between linear if a final was linear and weighted average df size was constant to cubic if a final was quadratic and weighted average df size was linear so the overall running time can be between linear and cubic
