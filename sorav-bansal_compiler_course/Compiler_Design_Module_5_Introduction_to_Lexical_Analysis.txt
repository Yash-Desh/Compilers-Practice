Title: Compiler Design Module 5 : Introduction to Lexical Analysis
Video ID: VLf_wGD5bBU
Detected language: en
------------------------------------------------------------

Okay, so today we're going to discuss about lexical analysis We already got introduced to lexical analysis lexical analysis or lexing converts a stream of characters to a stream of tokens Now consider this example if X is greater than equal to Y Z then Z equals one semicolon else a equals Z now to a computer This program looks like a stream of characters So it looks something like what I what's shown in blue here, you know where backslash T represents the tab character and Backslash N represents the end line character and then there are spaces in the middle All right, and the job is to convert this into a Stream of tokens. So what is the stream of tokens? Well, basically we want to insert delimiters such that they distinguish one token from another so here for example if is One token and recall that tokens are of different types and in this case if is a keyword token similarly X is an Identifier token and so on Also notice that there are White space tokens. All right, so backslash and backslash T or space for that matter kind of come in White space tokens All right. So So before we understand how we get tokens, let's understand What's a token class a token class is basically, you know, for example, the keyword is a token class and identifies the token class So more formally a token class corresponds to a set of strings for example an identifier is a token class and Here it is a string of letters or digits that start with the letter All right, so I can say that any token or any Any token that has a string that contains letters and digits as it starts with a letter is an identifier Belongs to the identifier class similarly an integer token class could be a string of digits or a keyword token class could be Special strings like if then while else for and so on and then there could be a white space token class Which could say that if it's a sequence of blanks new lines or tabs or any other types of white space Then that would belong to the white those strings would belong to the white space token class So what's a lexical analyzer? Well You know, you can think of the lexical analyzer or lexer as a function that takes a string what which is just nothing but a sequence of characters and it turns a sequence of tokens All right And what is a token a token is basically a tuple Which I'd indicates the class of that token and the string that forms that token Alright, so let's take an example So here is a string foo equals 42 and there is white space between equals and 42 and equals in foo and We would You know the lexer would convert this What would the lexer convert this to well it would say it is a stream of tokens so the first token is going to be foo and Was the token class the token class is identified the second token operator equals Was the third token maybe constant? 42 right So depending on the language you may want to choose different to know the token classes may be different Also the strings that belong to each token class may be different In this case we have we have lexed foo equals 42 into these three tokens identifier operator and constant And let's take a more complicated example like the one that we showed on the first slide now here We are going to Say okay Backslash T is just white space So I'm going to omit the white space tokens. The first token here is a keyword. So it's a keyword if This is the first token the second token is The left parenthesis, so I'm going to say Let's say maybe the left parenthesis itself is the token class and say left parenthesis L parent and say this is a string that corresponds to this token then identifier X Would be the next token Now it's now the next token here is greater than equal to so, you know We could potentially say that the greater than sign is a separate token and the equal to sign is a separate token Because they both mean something And then leave it to the parser to figure out that they are occurring together And then we can say that this is a greater than equal to operator or we could resolve this at lexing time And so in this example, I'm going to assume that the lexer is able to figure this out And so it constructs a single string from greater than equal to and then says this is of token class operator All right, and then we have the right parentheses. Let's say okay. This is the right parenthesis After that we have some white space I'm going to ignore the white space but could potentially the lexer could have white space tokens as well Notice that a language like C Completely kind of ignores white space between different tokens But there are some languages for example like Python where indentation matters in the program semantics in which case you may want to preserve The white space tokens as well, but in this example, I'm going to omit the white space tokens The next token here is Z. So I'm going to say ID Z Then the operator the assignment operator. So I'm say operator equals Then constant one delimiter semicolon keyword else identifier a operator equals Identifier Z and finally the delimiter semicolon. So what have I done? Just converted the stream of characters to a stream or sequence of tokens All right. So so how what is the lexical analyzer and how do and how does it work? Well, it recognizes substrings Corresponding to tokens just like we did in the example before and these substrings are also called lexemes Alright, so in the previous example the string E L S E L would be called a lexeme or The string greater than equal to would be called a lexeme because they get grouped into a single string while identifying the identification of a token Similarly, we recognize a token class for each lexeme to obtain the tuple token class comma lexeme So after we have figured out what is the lexeme? We figure out what is the token class corresponding to lexeme now, I think I know you must be thinking how do I know? What is the lexeme for example? when I have greater than equal to should I consider greater than as Standalone lexeme or should I wait for to get equal to to get the next lexeme? Well to understand this Let's let me take a more difficult example actually from the Fortran language in Fortran It turns out that white space is insignificant. All right completely insignificant Unlike see where white space can be any amount of white space can be present between different tokens in Fortran white space can even be present within a token. All right, so for example an Identifier var1 can be written as va space r1 and it would still mean var1 Okay, and and and so once again, I mean it's similar to the problem that we had earlier How do I know when I am lexing the string? va r1 That should I say that va is a separate identifier and then r1 is a separate identifier So should I generate two tokens each of type identifier or should I consider this a single token var1? Now it turns out that doing this in general is a little bit hard And and so the language should be designed in such a way such that it becomes possible to understand whether I should you know include the following characters into the current lexeme or should I Close the current lexeme. Here's an example that motivates this further So in Fortran consider this statement do 5 I Equals 1 comma 25 now, you may not be familiar with Fortran syntax neither am I but it turns out that this Statement here basically is like a loop. It says do 5 do the following statement You know Execute the statements till statement 5 in a loop. All right, that's what it means on the other hand the second state String here is all completely identical except that comma in the first one is replaced by a dot in the second one and Here this is supposed to mean that the variable do 5 I so do 5 I is a single variable Is being just assigned a value 1.25. And so this is just a constant And this is just an identifier All right, so just a change of a single character that too pretty late in the string Completely changes the lexing output of this particular example. All right, so so, okay So let's understand what is lexing supposed to do It's supposed to partition the string using those red bars that I showed you in the previous thing or delimiters It's supposed to do this reading left to right. Well, this is not a rule But this is the general convention that you read just like we read Natural language left to right the compiler reads the program string left to right while deciding where to draw the partition delimiters and It recognizes one token at a time. All right, so Currently has one lexing that it's kind of Accumulating and it needs to know whether it needs to close the current lexing or it needs to keep accumulating All right, and to know whether to close the current lexing or whether to keep accumulating it requires look ahead So it needs to know a little bit in future what's coming. All right, so in the example that I showed earlier If I kind of if the lexer was looking a little bit in the future and seeing that there is a foot dot or Comma it would have been able to make a decision whether do is a separate Lexing and token or is it a part of a larger lexing do 5i similarly When I'm looking at the greater than equal to operator I can look ahead one character and see whether I have more Something else coming after greater than if not then greater than can be thought Emitted as a single lexing if it is if there is an equal to after greater than equal to then the whole greater than equal to Can be emitted as a single lexing because it is also a distinct operator distinct from greater than All right. So every lexer needs some look ahead and the question is how much look ahead do you need? Well, the amount of look ahead that you need depends on how easy is the language to lex. All right So typically the language should be designed so that it's easy to kind of Understand what it means and and so it should be possible for an automatic program to just look ahead a little bit to figure out What it needs to do, but every lexer would need some type of look ahead in any case as we saw in the current examples all right, so So to kind of show this with examples Some languages are harder to lex than others. So for example, take the example of C++ Now here I have foo is less than foo less than operator and bar now This would be interpreted as a relational expression where foo is being compared Against bar, but the moment I put an other greater than sign So let's you know a matching Angle brackets after bar this thing means completely different. This is a template expression Right which says that foo is Specialized with bar with foo is a template and bar is kind of the template argument to foo and this means completely something completely different similarly, I could potentially have You know foo bar baz This is a deeper template expression right here here bar is Bar has a template argument baz and foo has a template argument bar baz All right. So so this is a deeper But now think look at this particular sequence of characters greater than greater than two angle brackets, right Now C++ also has a stream operator, which is which is some which is looks exactly the same and for example You could use it to stream Things from so for example, if you want to read from standard input you would say C in Angle brackets two angle brackets greater than greater than var which would mean that read the variable var Read the standard input into the variable var Now, how do I know whether you know this angle brackets in the first case? I would basically want to generate different tokens for each of the angle brackets because they represent Different things right so in This particular angle bracket is matching with its counterpart while the outer one is matching with its counterpart Whereas in the second case, I would want to have a single leg zine for the two angle brackets together This is just to motivate that some languages are harder to legs than others and you know, this is this was actually a problem in C++ for a long time because C++ mandated that if you are using it in the first form in this templatized form and it if here you have a situation We have two angle brackets have to appear together There must be a space in the middle. Otherwise, it would kind of result in a syntax error This has been fixed in more recent versions of the compiler of C++ compiler Okay, here's another example of an old language that has similar problems Now PL1 was a language where the designers decided that There is no reason why identifiers cannot have the same names as the language keywords So it's possible to have so PL1 has identifiers like if then else but it also allowed the programmer to specify Identifiers that had the same name For example, you could have an identifier in your program called then or else All right and once you have that then you know, the lecture becomes a little bit harder to do because you need Look at this program if else then then equals else else else equals then now How do I know whether this else is an identifier or a keyword? Okay, and similarly for the other ones and once again the answer is we need some look ahead to figure out What is the context in which this spring is appearing? Notice that if the PL1 designers did not decide I mean did not allow Keywords to have the same name as identifiers this problem wouldn't have a reason. Okay you
