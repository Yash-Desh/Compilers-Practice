Title: Compiler Design Module 62 : Ways of Specifying Semantics
Video ID: CY3tTOt-b0s
Detected language: en
------------------------------------------------------------

So we'll now discuss what are the multiple ways to specify the semantics of a language and actually there happen to be multiple ways to specify the semantics of a language and they're all equally precise and powerful in their specification that is they do not either under specify or over specify but some of them are more suitable for various tasks than others and maybe some examples that we discuss in this lecture module will make this clear. So the first way of specifying semantics is called operational semantics. This describes the program evaluation via execution rules on an abstract machine. So there's the notion of an abstract machine which has some state and then there are some instructions and as the instructions get executed the state of the abstract machine is mutated all right and so this is what is meant by operational semantics and this is very useful for specifying implementation and this is what we are going to also use to specify the semantics of the toy language that we have discussed in this course so far and there are two types of operational semantics big step and small step. The small step semantics are also called reduction semantics. So what's the difference big steps operational semantics specify the value or effects of an expression or a statement in terms of its constituent expressions. So here's an example. So for example I could say that if under a given context it is true that E1 evaluates to value five and under the same context it is true that E2 evaluates to value seven then this is an inference rule under the same context it is true that E1 plus E2 evaluate to value 12. So basically we have specified what is the semantics of E1 plus E2 in terms of its constituent sub-expressions E1 and E2. So this is a form of operational semantics and in particular big step operational semantics and we're going to see them more when we discuss operational semantics of our toy language. Then there are small step operational semantics unlike the big step small operational semantics they give you more detail so they specify our single step a small step is taken in the evaluation of a program and these are thus also called reduction semantics. So it's almost like how is a program reducing to its effect or its value. So you know make a full detailed explanation of what are small step operational semantics are beyond the scope of this course but here's an example to discuss what an example rule in small step reduction semantics may look like. So for example under if under a context it is true that E1 evaluates to five then under the same context it is true that E1 plus E2 would evaluate to five plus E2. So it's just saying that look here is a program that's called that's E1 plus E2 and we know if E1 evaluates to five then E1 plus E2 would evaluate to five plus E2 in one single step of the evaluation. So it's specifying one small step as opposed to the previous example we were where it was just giving you the entire it was just evaluating the whole thing in one big step all right and that's why these names big step and small step in operational semantics. The third the second type of semantics are denotational semantics and in this type of semantics the meaning of the program is specified as a mathematical function and this function you know as what is a mathematical function a function maps input to an output so it takes some input arguments and then it gives you an output value all right and this is an elegant approach because it brings out the mathematical meaning of the program explicitly but is often unnecessarily complex and there's too much detail that's not really required. So for example here is an example of a denotational semantics once again you know the full treatment of denotational semantics would require a more detailed example of a language and how denotational semantics are being specified and used for that language but that's beyond the outside the scope of this course but here's an example just to give you a sense. So typically denotational semantics involve this meaning operator so this these double these double brackets basically and here you can specify your program and this basic whole expression is basically supposed to tell you the meaning and recall that what meaning means in denotational semantics is a function a mathematical function. So this particular inference rule says that if the meaning of x is that it is a function that takes a memory lambda m so this is a lambda this is you know the lambda is used to specify a function so lambda m so a function that takes m a memory m and returns adder x comma m so it returns the value which is which is adder x it returns some value some concrete value adder x and it returns the same memory m all right so just a different way of representing it take i'm just representing the meaning of a variable x as its address as a function that takes an input state which is the let's say the only state in the program in the machine is our memory so it takes an input state which is m and then it turns the same m that means it doesn't modify m at all and it returns the value of x which is supposed to mean the address of x all right and and in that if that is true then x equals zero here x equals zero is an assignment is a function that takes a memory m and returns zero as the value of this particular expression so the value of this particular expression is just zero all right and a memory which has been modified such that zero has been you know the the address the value at address x has been replaced with value zero all right so so maybe i can i can say that or the other way that i could have written this is adder x the same memory except that adder x is now mapping to zero all right so this is you know just to give you a flavor of what denotational semantics look like the third type of semantics are called axiomatic semantics and here the program behavior is described via logical formulae potentially temporal formulae all right so we maybe we don't know we don't really get into detail of what are all the different types of logical logics and their corresponding formulas but but we'll take an example to see what an axiomatic semantics look like and this is axiomatic semantics are the foundations of many program verification systems now once again i want to point out that anything that is possible to specify using operational semantics is possible to specify using denotation semantics and vice versa and similarly with axiomatic semantics so these are all different ways of saying the same thing these are different notations of saying the same things but some some ways of saying it is are better suited for some applications and axiomatic semantics are very well suited to do program verification and static analysis for example so what what is an example of axiomatic semantics for example i could say that you know something if something is true then after that if something is true before you execute the program then something will be true after you execute the program all right so this is one way of specifying axiomatic semantics for an imperative program for example you could say if before executing x equals zero so let's say x equals zero is a statement in the program if before executing x equals zero if this is true about your program so for example x plus y is equal to 10 i'm just this random stuff so if x plus y equals 10 and y plus z equals four then after x equals zero it will be true that y plus z would still remain four because neither y or nor z would have been modified and x would be equal to zero now this will be called a precondition so before executing the program this is a precondition and this is the post condition all right and this is just one way of specifying axiomatic semantics and this is this is just a very kind of crude example where we are just using some predicates that we assume to be true in the beginning and then what should be the predicates that are assumed to be that that must be true up at the end and one can generalize this notion to be able to specify the complete semantics of a programming language as well to summarize there are different ways of representing the same information about the semantics of a programming language and all are equally precise and general whether i take axiomatic semantics operational semantics big step or small step or i use denotational semantics and in our next few modules we'll use big step operational semantics to specify the semantics of our toy language
